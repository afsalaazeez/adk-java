/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test zbio-dec-16-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=findSubAgent_7b7e7c4829
ROOST_METHOD_SIG_HASH=findSubAgent_3a41365e17

Scenario 1: Find Direct Sub-Agent Successfully

Details:
  TestName: findDirectSubAgentByName
  Description: This test verifies that the findSubAgent method correctly returns a direct sub-agent when the provided name matches one of the immediate children in the subAgents list. It tests the basic functionality of finding a sub-agent at the first level of the hierarchy.
Execution:
  Arrange: Create a BaseAgent instance with a list of sub-agents. One of the sub-agents should have a specific name (e.g., "subAgent1").
  Act: Call findSubAgent("subAgent1") on the parent agent.
  Assert: Assert that the returned BaseAgent is not null and that its name() method returns "subAgent1".
Validation:
  The assertion verifies that the method correctly identifies and returns a direct child agent when its name matches the search parameter. This is the most common use case where the target agent is found in the first iteration of the loop. This test ensures the basic search functionality works correctly for immediate children.

Scenario 2: Find Nested Sub-Agent Successfully

Details:
  TestName: findNestedSubAgentByName
  Description: This test validates that the findSubAgent method can recursively search through multiple levels of the agent hierarchy to find a deeply nested sub-agent. It tests the recursive nature of the method.
Execution:
  Arrange: Create a hierarchical structure with a parent agent, a sub-agent, and a sub-sub-agent (grandchild). The grandchild should have a specific name (e.g., "grandchildAgent").
  Act: Call findSubAgent("grandchildAgent") on the root parent agent.
  Assert: Assert that the returned BaseAgent is not null and that its name() method returns "grandchildAgent".
Validation:
  This assertion confirms that the recursive call (subAgent.findSubAgent(name)) works correctly and can traverse multiple levels of the agent tree. This is crucial for hierarchical agent structures where agents can be nested several levels deep. The test validates that the depth-first search algorithm implemented in the method functions properly.

Scenario 3: Sub-Agent Not Found Returns Null

Details:
  TestName: subAgentNotFoundReturnsNull
  Description: This test ensures that the findSubAgent method returns null when no sub-agent with the specified name exists in the entire hierarchy. It tests the error handling for non-existent agents.
Execution:
  Arrange: Create a BaseAgent instance with a list of sub-agents, none of which have the name "nonExistentAgent".
  Act: Call findSubAgent("nonExistentAgent") on the parent agent.
  Assert: Assert that the returned value is null.
Validation:
  The assertion verifies that the method correctly handles the case where the search fails to find a matching agent. Returning null is the expected behavior as indicated by the @Nullable annotation on the method. This test is important for ensuring that calling code can properly handle cases where an agent doesn't exist in the hierarchy.

Scenario 4: Find Sub-Agent With Empty Sub-Agents List

Details:
  TestName: findSubAgentWithEmptySubAgentsList
  Description: This test verifies that the findSubAgent method correctly returns null when called on an agent that has no sub-agents (empty subAgents list). It tests the boundary condition of an empty collection.
Execution:
  Arrange: Create a BaseAgent instance with an empty list of sub-agents.
  Act: Call findSubAgent("anyName") on the agent.
  Assert: Assert that the returned value is null.
Validation:
  This assertion confirms that the method handles the edge case of an empty subAgents list gracefully. The for-loop should not execute any iterations, and the method should return null immediately. This is important for leaf nodes in the agent tree that have no children.

Scenario 5: Find Sub-Agent With Null Name Parameter

Details:
  TestName: findSubAgentWithNullNameParameter
  Description: This test checks the behavior of the findSubAgent method when a null value is passed as the name parameter. It tests how the method handles invalid input.
Execution:
  Arrange: Create a BaseAgent instance with a list of sub-agents.
  Act: Call findSubAgent(null) on the agent.
  Assert: Assert that either a NullPointerException is thrown or null is returned, depending on the expected behavior.
Validation:
  This test validates the method's behavior with invalid input. Since the method uses equals() comparison (subAgent.name().equals(name)), passing null would cause a NullPointerException when equals() is called on null. This test ensures that the method's error handling for null parameters is understood and documented.

Scenario 6: Find First Matching Sub-Agent When Multiple Exist

Details:
  TestName: findFirstMatchingSubAgentWhenMultipleExist
  Description: This test verifies that when multiple sub-agents at different levels have the same name, the findSubAgent method returns the first one encountered in the depth-first search order.
Execution:
  Arrange: Create a hierarchical structure where multiple agents have the same name (e.g., "duplicateName") at different levels - one as a direct child and one as a grandchild of a different branch.
  Act: Call findSubAgent("duplicateName") on the root agent.
  Assert: Assert that the returned BaseAgent is the first one encountered in the iteration order (the direct child, not the grandchild).
Validation:
  This assertion confirms that the method follows a depth-first search pattern and returns the first match it encounters. The method iterates through subAgents in order, checking each one before recursively searching its children. This test is important for understanding the search order when duplicate names exist in the tree.

Scenario 7: Find Sub-Agent In Middle Of Sub-Agents List

Details:
  TestName: findSubAgentInMiddleOfList
  Description: This test validates that the findSubAgent method correctly finds and returns a sub-agent that is positioned in the middle of the subAgents list, not at the beginning or end.
Execution:
  Arrange: Create a BaseAgent instance with multiple sub-agents (e.g., 5 sub-agents), where the target agent is the third one in the list.
  Act: Call findSubAgent with the name of the third sub-agent.
  Assert: Assert that the returned BaseAgent is not null and matches the third sub-agent.
Validation:
  This test ensures that the loop iteration works correctly for all positions in the subAgents list, not just the first or last elements. It validates that the method doesn't have any off-by-one errors or issues with iterating through the middle of the collection.

Scenario 8: Find Sub-Agent With Case-Sensitive Name Match

Details:
  TestName: findSubAgentWithCaseSensitiveNameMatch
  Description: This test verifies that the name comparison in findSubAgent is case-sensitive, meaning "Agent" and "agent" are treated as different names.
Execution:
  Arrange: Create a BaseAgent instance with a sub-agent named "TestAgent" (with capital T).
  Act: Call findSubAgent("testagent") (all lowercase) on the parent agent.
  Assert: Assert that the returned value is null.
Validation:
  This assertion confirms that the equals() method used for name comparison is case-sensitive. This is important for ensuring that agent names are matched exactly as specified, preventing accidental matches due to case differences. This test documents the expected behavior regarding case sensitivity.

Scenario 9: Find Sub-Agent In Deep Hierarchy

Details:
  TestName: findSubAgentInDeepHierarchy
  Description: This test validates that the findSubAgent method can successfully find an agent in a very deep hierarchy (e.g., 5+ levels deep) without stack overflow or performance issues.
Execution:
  Arrange: Create a deep hierarchical structure with at least 5 levels of nested agents, where the target agent is at the deepest level.
  Act: Call findSubAgent with the name of the deepest agent from the root agent.
  Assert: Assert that the returned BaseAgent is not null and matches the deepest agent.
Validation:
  This test ensures that the recursive implementation can handle deep hierarchies without issues. It validates that the method's recursive approach is suitable for realistic agent tree structures that may have multiple levels of nesting. This is important for ensuring the method's robustness in complex scenarios.

Scenario 10: Find Sub-Agent Returns Correct Instance

Details:
  TestName: findSubAgentReturnsCorrectInstance
  Description: This test verifies that the findSubAgent method returns the exact same instance (reference equality) of the sub-agent, not a copy or different object with the same name.
Execution:
  Arrange: Create a BaseAgent instance with a specific sub-agent instance. Keep a reference to this sub-agent.
  Act: Call findSubAgent with the name of the sub-agent.
  Assert: Assert that the returned BaseAgent is the same instance (using == or assertSame) as the original sub-agent reference.
Validation:
  This assertion confirms that the method returns the actual agent instance from the tree, not a copy. This is important for ensuring that any modifications or interactions with the returned agent affect the correct instance in the hierarchy. Reference equality is crucial for maintaining the integrity of the agent tree structure.

Scenario 11: Find Sub-Agent With Empty String Name

Details:
  TestName: findSubAgentWithEmptyStringName
  Description: This test checks the behavior of the findSubAgent method when an empty string ("") is passed as the name parameter.
Execution:
  Arrange: Create a BaseAgent instance with sub-agents that have non-empty names.
  Act: Call findSubAgent("") on the agent.
  Assert: Assert that the returned value is null (assuming no agent has an empty string as its name).
Validation:
  This test validates the method's behavior with an edge case input of an empty string. Since agent names should typically be meaningful identifiers, an empty string should not match any agent. This test ensures that the equals() comparison correctly handles empty strings and doesn't produce unexpected matches.

Scenario 12: Find Sub-Agent In Sibling Branch

Details:
  TestName: findSubAgentInSiblingBranch
  Description: This test verifies that the findSubAgent method can find an agent in a sibling branch of the tree, not just in the direct lineage.
Execution:
  Arrange: Create a parent agent with two sub-agents (siblings), where the second sibling has a child agent. Search for the child of the second sibling.
  Act: Call findSubAgent with the name of the child of the second sibling from the parent agent.
  Assert: Assert that the returned BaseAgent is not null and matches the child of the second sibling.
Validation:
  This assertion confirms that the method's depth-first search explores all branches of the tree, not just the first branch. It validates that the loop continues iterating through all subAgents and recursively searches each one until a match is found. This is important for ensuring comprehensive search coverage across the entire agent tree structure.

Scenario 13: Find Sub-Agent With Special Characters In Name

Details:
  TestName: findSubAgentWithSpecialCharactersInName
  Description: This test validates that the findSubAgent method correctly handles agent names containing special characters (e.g., "agent-1", "agent_test", "agent.name").
Execution:
  Arrange: Create a BaseAgent instance with a sub-agent whose name contains special characters (e.g., "agent-with-dashes").
  Act: Call findSubAgent("agent-with-dashes") on the parent agent.
  Assert: Assert that the returned BaseAgent is not null and its name() method returns "agent-with-dashes".
Validation:
  This test ensures that the equals() method used for name comparison correctly handles special characters without any encoding or parsing issues. Agent names may legitimately contain various characters, and the method should handle them correctly. This test validates that the string comparison is robust for various naming conventions.

Scenario 14: Find Sub-Agent When First Sub-Agent Has No Match But Has Children

Details:
  TestName: findSubAgentWhenFirstSubAgentHasNoMatchButHasChildren
  Description: This test verifies that when the first sub-agent's name doesn't match, the method correctly recurses into that sub-agent's children before moving to the next sibling.
Execution:
  Arrange: Create a parent agent with two sub-agents. The first sub-agent has a child whose name is the search target. The second sub-agent has no children.
  Act: Call findSubAgent with the name of the first sub-agent's child.
  Assert: Assert that the returned BaseAgent is not null and matches the child of the first sub-agent.
Validation:
  This assertion confirms that the method follows the correct depth-first search order: it checks the current agent's name, then recursively searches all of its children before moving to the next sibling. This test validates the proper implementation of the recursive search algorithm and ensures that all descendants are checked before moving to siblings.

Scenario 15: Find Sub-Agent Returns Null After Exhausting All Branches

Details:
  TestName: findSubAgentReturnsNullAfterExhaustingAllBranches
  Description: This test ensures that the findSubAgent method returns null only after checking all sub-agents and their descendants, not prematurely.
Execution:
  Arrange: Create a complex agent tree with multiple branches and levels, where no agent has the target name.
  Act: Call findSubAgent with a name that doesn't exist anywhere in the tree.
  Assert: Assert that the returned value is null.
Validation:
  This test validates that the method exhaustively searches the entire tree before returning null. It ensures that the loop completes all iterations and all recursive calls return null before the method itself returns null. This is important for guaranteeing that a null return value definitively means the agent doesn't exist in the tree, not that the search was incomplete.

*/

// ********RoostGPT********

package com.google.adk.agents;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

import com.google.adk.events.Event;
import com.google.common.collect.ImmutableList;
import io.reactivex.rxjava3.core.Flowable;
import java.util.List;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.MockitoAnnotations;

public class BaseAgentFindSubAgentTest {

	private TestBaseAgent parentAgent;

	private TestBaseAgent subAgent1;

	private TestBaseAgent subAgent2;

	private TestBaseAgent subAgent3;

	private TestBaseAgent grandchildAgent;

	@BeforeEach
	public void setUp() {
		MockitoAnnotations.openMocks(this);
	}

	@Test
	@Tag("valid")
	public void findDirectSubAgentByName() {
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("subAgent1");
		assertNotNull(result);
		assertEquals("subAgent1", result.name());
		assertSame(subAgent1, result);
	}

	@Test
	@Tag("valid")
	public void findNestedSubAgentByName() {
		grandchildAgent = new TestBaseAgent("grandchildAgent", "Grandchild Description", ImmutableList.of(), null,
				null);
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(grandchildAgent), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("grandchildAgent");
		assertNotNull(result);
		assertEquals("grandchildAgent", result.name());
		assertSame(grandchildAgent, result);
	}

	@Test
	@Tag("invalid")
	public void subAgentNotFoundReturnsNull() {
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("nonExistentAgent");
		assertNull(result);
	}

	@Test
	@Tag("boundary")
	public void findSubAgentWithEmptySubAgentsList() {
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", ImmutableList.of(), null, null);
		BaseAgent result = parentAgent.findSubAgent("anyName");
		assertNull(result);
	}

	@Test
	@Tag("invalid")
	public void findSubAgentWithNullNameParameter() {
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		assertThrows(NullPointerException.class, () -> {
			parentAgent.findSubAgent(null);
		});
	}

	@Test
	@Tag("valid")
	public void findFirstMatchingSubAgentWhenMultipleExist() {
		TestBaseAgent grandchildDuplicate = new TestBaseAgent("duplicateName", "Grandchild Duplicate",
				ImmutableList.of(), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(grandchildDuplicate), null, null);
		subAgent1 = new TestBaseAgent("duplicateName", "Description 1", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("duplicateName");
		assertNotNull(result);
		assertEquals("duplicateName", result.name());
		assertSame(subAgent1, result);
	}

	@Test
	@Tag("valid")
	public void findSubAgentInMiddleOfList() {
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(), null, null);
		subAgent3 = new TestBaseAgent("subAgent3", "Description 3", ImmutableList.of(), null, null);
		TestBaseAgent subAgent4 = new TestBaseAgent("subAgent4", "Description 4", ImmutableList.of(), null, null);
		TestBaseAgent subAgent5 = new TestBaseAgent("subAgent5", "Description 5", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2, subAgent3, subAgent4, subAgent5);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("subAgent3");
		assertNotNull(result);
		assertEquals("subAgent3", result.name());
		assertSame(subAgent3, result);
	}

	@Test
	@Tag("valid")
	public void findSubAgentWithCaseSensitiveNameMatch() {
		subAgent1 = new TestBaseAgent("TestAgent", "Description 1", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("testagent");
		assertNull(result);
	}

	@Test
	@Tag("valid")
	public void findSubAgentInDeepHierarchy() {
		TestBaseAgent level5 = new TestBaseAgent("level5Agent", "Level 5", ImmutableList.of(), null, null);
		TestBaseAgent level4 = new TestBaseAgent("level4Agent", "Level 4", ImmutableList.of(level5), null, null);
		TestBaseAgent level3 = new TestBaseAgent("level3Agent", "Level 3", ImmutableList.of(level4), null, null);
		TestBaseAgent level2 = new TestBaseAgent("level2Agent", "Level 2", ImmutableList.of(level3), null, null);
		TestBaseAgent level1 = new TestBaseAgent("level1Agent", "Level 1", ImmutableList.of(level2), null, null);

		parentAgent = new TestBaseAgent("rootAgent", "Root", ImmutableList.of(level1), null, null);
		BaseAgent result = parentAgent.findSubAgent("level5Agent");
		assertNotNull(result);
		assertEquals("level5Agent", result.name());
		assertSame(level5, result);
	}

	@Test
	@Tag("valid")
	public void findSubAgentReturnsCorrectInstance() {
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("subAgent1");
		assertNotNull(result);
		assertSame(subAgent1, result);
	}

	@Test
	@Tag("boundary")
	public void findSubAgentWithEmptyStringName() {
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("");
		assertNull(result);
	}

	@Test
	@Tag("valid")
	public void findSubAgentInSiblingBranch() {
		TestBaseAgent childOfSecondSibling = new TestBaseAgent("childAgent", "Child Description", ImmutableList.of(),
				null, null);
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(childOfSecondSibling), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("childAgent");
		assertNotNull(result);
		assertEquals("childAgent", result.name());
		assertSame(childOfSecondSibling, result);
	}

	@Test
	@Tag("valid")
	public void findSubAgentWithSpecialCharactersInName() {
		subAgent1 = new TestBaseAgent("agent-with-dashes", "Description 1", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("agent-with-dashes");
		assertNotNull(result);
		assertEquals("agent-with-dashes", result.name());
		assertSame(subAgent1, result);
	}

	@Test
	@Tag("valid")
	public void findSubAgentWhenFirstSubAgentHasNoMatchButHasChildren() {
		TestBaseAgent childOfFirstSubAgent = new TestBaseAgent("targetChild", "Target Child", ImmutableList.of(), null,
				null);
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(childOfFirstSubAgent), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("targetChild");
		assertNotNull(result);
		assertEquals("targetChild", result.name());
		assertSame(childOfFirstSubAgent, result);
	}

	@Test
	@Tag("invalid")
	public void findSubAgentReturnsNullAfterExhaustingAllBranches() {
		TestBaseAgent grandchild1 = new TestBaseAgent("grandchild1", "Grandchild 1", ImmutableList.of(), null, null);
		TestBaseAgent grandchild2 = new TestBaseAgent("grandchild2", "Grandchild 2", ImmutableList.of(), null, null);
		subAgent1 = new TestBaseAgent("subAgent1", "Description 1", ImmutableList.of(grandchild1), null, null);
		subAgent2 = new TestBaseAgent("subAgent2", "Description 2", ImmutableList.of(grandchild2), null, null);
		subAgent3 = new TestBaseAgent("subAgent3", "Description 3", ImmutableList.of(), null, null);

		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2, subAgent3);
		parentAgent = new TestBaseAgent("parentAgent", "Parent Description", subAgents, null, null);
		BaseAgent result = parentAgent.findSubAgent("nonExistentInComplexTree");
		assertNull(result);
	}

	private static class TestBaseAgent extends BaseAgent {

		public TestBaseAgent(String name, String description, List<? extends BaseAgent> subAgents,
				List<? extends Callbacks.BeforeAgentCallback> beforeAgentCallback,
				List<? extends Callbacks.AfterAgentCallback> afterAgentCallback) {
			super(name, description, subAgents, beforeAgentCallback, afterAgentCallback);
		}

		@Override
		protected Flowable<Event> runAsyncImpl(InvocationContext invocationContext) {
			return Flowable.empty();
		}

		@Override
		protected Flowable<Event> runLiveImpl(InvocationContext invocationContext) {
			return Flowable.empty();
		}

	}

}
