/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test zbio-dec-16-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=rootAgent_6e4a884323
ROOST_METHOD_SIG_HASH=rootAgent_641f462b7b

Scenario 1: Agent with No Parent Returns Itself as Root

Details:
  TestName: agentWithNoParentReturnsItselfAsRoot
  Description: This test verifies that when an agent has no parent (parentAgent is null), the rootAgent() method returns the agent itself. This is the base case where the agent is already at the root of the agent tree hierarchy.
Execution:
  Arrange: Create a BaseAgent instance with no parent agent (parentAgent is null).
  Act: Call the rootAgent() method on the agent instance.
  Assert: Verify that the returned agent is the same instance as the original agent.
Validation:
  The assertion verifies that an agent without a parent correctly identifies itself as the root agent. This is critical for establishing the base case in the agent hierarchy traversal. In the context of the application, this ensures that standalone agents or top-level agents in the hierarchy can correctly identify themselves as root agents, which is important for operations that need to access the root of the agent tree.

Scenario 2: Agent with Single Parent Returns Correct Root

Details:
  TestName: agentWithSingleParentReturnsCorrectRoot
  Description: This test checks that when an agent has exactly one parent, the rootAgent() method correctly traverses up one level and returns the parent agent as the root. This validates the basic parent-child relationship in a two-level hierarchy.
Execution:
  Arrange: Create two BaseAgent instances - a parent agent and a child agent. Set the parent agent's parentAgent to null and the child agent's parentAgent to the parent agent.
  Act: Call the rootAgent() method on the child agent.
  Assert: Verify that the returned agent is the parent agent instance.
Validation:
  This assertion confirms that the method correctly traverses one level up the hierarchy. This is essential for ensuring that agents can locate their root ancestor in simple hierarchies. In business logic, this enables child agents to access root-level configurations, permissions, or shared resources managed by the root agent.

Scenario 3: Agent with Multiple Levels Returns Topmost Root

Details:
  TestName: agentWithMultipleLevelsReturnsTopmostRoot
  Description: This test validates that in a multi-level agent hierarchy (grandparent -> parent -> child), the rootAgent() method correctly traverses all levels to return the topmost ancestor (grandparent). This tests the iterative traversal logic through multiple parent relationships.
Execution:
  Arrange: Create three BaseAgent instances representing a three-level hierarchy: grandparent (no parent), parent (parentAgent = grandparent), and child (parentAgent = parent).
  Act: Call the rootAgent() method on the child agent (deepest level).
  Assert: Verify that the returned agent is the grandparent agent instance.
Validation:
  The assertion ensures that the while loop in rootAgent() correctly iterates through multiple levels until reaching the root. This is crucial for complex agent hierarchies where agents may be nested several levels deep. In application behavior, this allows any agent in a deep hierarchy to access the root agent for system-wide operations, logging, or configuration management.

Scenario 4: Agent with Deep Hierarchy Returns Correct Root

Details:
  TestName: agentWithDeepHierarchyReturnsCorrectRoot
  Description: This test examines the behavior of rootAgent() in a deep hierarchy with five or more levels. It ensures the method can handle extensive parent chains without issues and correctly identifies the root regardless of depth.
Execution:
  Arrange: Create a chain of five BaseAgent instances (level1 -> level2 -> level3 -> level4 -> level5) where level1 has no parent and each subsequent level has the previous level as its parent.
  Act: Call the rootAgent() method on the deepest level agent (level5).
  Assert: Verify that the returned agent is the level1 agent instance (the root).
Validation:
  This assertion validates that the traversal algorithm works correctly for arbitrarily deep hierarchies. It confirms there are no limitations or performance issues with deeper nesting. This is important for enterprise applications where agent hierarchies might represent complex organizational structures or nested delegation patterns.

Scenario 5: Root Agent Called Multiple Times Returns Same Instance

Details:
  TestName: rootAgentCalledMultipleTimesReturnsSameInstance
  Description: This test verifies that calling rootAgent() multiple times on the same agent instance consistently returns the same root agent. This ensures the method is idempotent and deterministic.
Execution:
  Arrange: Create a multi-level agent hierarchy (root -> parent -> child).
  Act: Call the rootAgent() method on the child agent twice in succession.
  Assert: Verify that both calls return the same root agent instance and that they are equal.
Validation:
  The assertion confirms that the method produces consistent results across multiple invocations. This is essential for reliability in scenarios where rootAgent() might be called repeatedly during an agent's lifecycle. It ensures that caching or optimization strategies can be safely applied based on the assumption of consistent return values.

Scenario 6: Root Agent on Middle Level Agent Returns Correct Root

Details:
  TestName: rootAgentOnMiddleLevelAgentReturnsCorrectRoot
  Description: This test validates that calling rootAgent() on a middle-level agent (neither root nor leaf) in a hierarchy correctly returns the topmost root agent. This ensures the method works correctly regardless of where in the hierarchy it is invoked.
Execution:
  Arrange: Create a three-level hierarchy (root -> middle -> leaf) where middle has a parent and a child.
  Act: Call the rootAgent() method on the middle agent.
  Assert: Verify that the returned agent is the root agent instance.
Validation:
  This assertion ensures that the traversal logic works correctly when invoked from any position in the hierarchy, not just from leaf nodes. This is important for flexible agent architectures where any agent might need to access the root, regardless of its position in the tree structure.

Scenario 7: Root Agent with Null Parent Reference Handling

Details:
  TestName: rootAgentWithNullParentReferenceHandling
  Description: This test explicitly verifies that the method correctly handles the null check in the while loop condition when parentAgent() returns null. It ensures no NullPointerException occurs at the root level.
Execution:
  Arrange: Create a single BaseAgent instance where parentAgent is explicitly null.
  Act: Call the rootAgent() method on this agent.
  Assert: Verify that the method returns the agent itself without throwing any exception.
Validation:
  The assertion confirms that the null check in the while loop condition (agent.parentAgent() != null) correctly terminates the loop when reaching an agent with no parent. This prevents potential NullPointerException errors and ensures safe traversal termination. This is critical for robustness in production environments.

Scenario 8: Root Agent Preserves Agent Identity Through Traversal

Details:
  TestName: rootAgentPreservesAgentIdentityThroughTraversal
  Description: This test verifies that the rootAgent() method returns the actual root agent instance with all its properties intact (name, description, etc.), not a copy or modified version.
Execution:
  Arrange: Create a hierarchy where the root agent has specific name and description values. Create child agents in the hierarchy.
  Act: Call the rootAgent() method on a child agent and retrieve the name and description of the returned root agent.
  Assert: Verify that the returned agent's name and description match the original root agent's properties.
Validation:
  This assertion ensures that the method returns a reference to the actual root agent object, preserving its identity and state. This is important for scenarios where the root agent's properties or state need to be accessed or modified through the reference returned by rootAgent(). It confirms that the traversal doesn't create copies or proxies.

Scenario 9: Root Agent on Agent with Circular Reference Prevention

Details:
  TestName: rootAgentBehaviorWithProperHierarchy
  Description: This test validates the expected behavior of rootAgent() in a properly constructed hierarchy without circular references. While the method doesn't explicitly prevent circular references, this test documents the expected behavior with a valid tree structure.
Execution:
  Arrange: Create a valid three-level hierarchy ensuring no circular references (root -> child1 -> grandchild).
  Act: Call the rootAgent() method on the grandchild agent.
  Assert: Verify that the method returns the root agent and completes in a reasonable time.
Validation:
  This assertion documents the expected behavior with valid data structures. While the method itself doesn't protect against circular references (which would cause infinite loops), this test establishes the baseline behavior for properly constructed hierarchies. It serves as documentation that the caller is responsible for maintaining valid tree structures.

Scenario 10: Root Agent Returns Correct Instance in Sibling Scenario

Details:
  TestName: rootAgentReturnsCorrectInstanceInSiblingScenario
  Description: This test verifies that when multiple agents share the same parent (siblings), each sibling correctly identifies the same root agent when calling rootAgent(). This ensures consistency across sibling branches.
Execution:
  Arrange: Create a hierarchy where one root agent has two child agents (siblings). Both children have the same parent.
  Act: Call the rootAgent() method on both sibling agents.
  Assert: Verify that both siblings return the same root agent instance.
Validation:
  The assertion confirms that the traversal logic produces consistent results across different branches of the agent tree. This is important for maintaining consistency in distributed or parallel agent operations where multiple agents at the same level might need to access shared root resources. It ensures that all agents in the tree have a unified view of the root.

*/

// ********RoostGPT********

package com.google.adk.agents;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

import com.google.common.collect.ImmutableList;
import io.reactivex.rxjava3.core.Flowable;
import java.util.List;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.MockitoAnnotations;

public class BaseAgentRootAgentTest {

	private TestAgent testAgent;

	@BeforeEach
	public void setUp() {
		MockitoAnnotations.openMocks(this);
	}

	@Test
	@Tag("valid")
	public void agentWithNoParentReturnsItselfAsRoot() {
		// Arrange
		testAgent = new TestAgent("agent1", "Test Agent 1", null, null, null);

		// Act
		BaseAgent rootAgent = testAgent.rootAgent();

		// Assert
		assertNotNull(rootAgent);
		assertSame(testAgent, rootAgent);
		assertEquals("agent1", rootAgent.name());
	}

	@Test
	@Tag("valid")
	public void agentWithSingleParentReturnsCorrectRoot() {
		// Arrange
		TestAgent parentAgent = new TestAgent("parent", "Parent Agent", null, null, null);
		TestAgent childAgent = new TestAgent("child", "Child Agent", null, null, null);
		childAgent.parentAgent(parentAgent);

		// Act
		BaseAgent rootAgent = childAgent.rootAgent();

		// Assert
		assertNotNull(rootAgent);
		assertSame(parentAgent, rootAgent);
		assertEquals("parent", rootAgent.name());
	}

	@Test
	@Tag("valid")
	public void agentWithMultipleLevelsReturnsTopmostRoot() {
		// Arrange
		TestAgent grandparentAgent = new TestAgent("grandparent", "Grandparent Agent", null, null, null);
		TestAgent parentAgent = new TestAgent("parent", "Parent Agent", null, null, null);
		TestAgent childAgent = new TestAgent("child", "Child Agent", null, null, null);

		parentAgent.parentAgent(grandparentAgent);
		childAgent.parentAgent(parentAgent);

		// Act
		BaseAgent rootAgent = childAgent.rootAgent();

		// Assert
		assertNotNull(rootAgent);
		assertSame(grandparentAgent, rootAgent);
		assertEquals("grandparent", rootAgent.name());
	}

	@Test
	@Tag("valid")
	public void agentWithDeepHierarchyReturnsCorrectRoot() {
		// Arrange
		TestAgent level1 = new TestAgent("level1", "Level 1 Agent", null, null, null);
		TestAgent level2 = new TestAgent("level2", "Level 2 Agent", null, null, null);
		TestAgent level3 = new TestAgent("level3", "Level 3 Agent", null, null, null);
		TestAgent level4 = new TestAgent("level4", "Level 4 Agent", null, null, null);
		TestAgent level5 = new TestAgent("level5", "Level 5 Agent", null, null, null);

		level2.parentAgent(level1);
		level3.parentAgent(level2);
		level4.parentAgent(level3);
		level5.parentAgent(level4);

		// Act
		BaseAgent rootAgent = level5.rootAgent();

		// Assert
		assertNotNull(rootAgent);
		assertSame(level1, rootAgent);
		assertEquals("level1", rootAgent.name());
	}

	@Test
	@Tag("valid")
	public void rootAgentCalledMultipleTimesReturnsSameInstance() {
		// Arrange
		TestAgent rootAgent = new TestAgent("root", "Root Agent", null, null, null);
		TestAgent parentAgent = new TestAgent("parent", "Parent Agent", null, null, null);
		TestAgent childAgent = new TestAgent("child", "Child Agent", null, null, null);

		parentAgent.parentAgent(rootAgent);
		childAgent.parentAgent(parentAgent);

		// Act
		BaseAgent firstCall = childAgent.rootAgent();
		BaseAgent secondCall = childAgent.rootAgent();

		// Assert
		assertNotNull(firstCall);
		assertNotNull(secondCall);
		assertSame(firstCall, secondCall);
		assertSame(rootAgent, firstCall);
		assertSame(rootAgent, secondCall);
	}

	@Test
	@Tag("valid")
	public void rootAgentOnMiddleLevelAgentReturnsCorrectRoot() {
		// Arrange
		TestAgent rootAgent = new TestAgent("root", "Root Agent", null, null, null);
		TestAgent middleAgent = new TestAgent("middle", "Middle Agent", null, null, null);
		TestAgent leafAgent = new TestAgent("leaf", "Leaf Agent", null, null, null);

		middleAgent.parentAgent(rootAgent);
		leafAgent.parentAgent(middleAgent);

		// Act
		BaseAgent result = middleAgent.rootAgent();

		// Assert
		assertNotNull(result);
		assertSame(rootAgent, result);
		assertEquals("root", result.name());
	}

	@Test
	@Tag("valid")
	public void rootAgentWithNullParentReferenceHandling() {
		// Arrange
		TestAgent agent = new TestAgent("standalone", "Standalone Agent", null, null, null);
		agent.parentAgent(null);

		// Act & Assert
		assertDoesNotThrow(() -> {
			BaseAgent rootAgent = agent.rootAgent();
			assertNotNull(rootAgent);
			assertSame(agent, rootAgent);
		});
	}

	@Test
	@Tag("valid")
	public void rootAgentPreservesAgentIdentityThroughTraversal() {
		// Arrange
		String rootName = "rootAgent";
		String rootDescription = "This is the root agent";
		TestAgent rootAgent = new TestAgent(rootName, rootDescription, null, null, null);
		TestAgent childAgent = new TestAgent("child", "Child Agent", null, null, null);
		TestAgent grandchildAgent = new TestAgent("grandchild", "Grandchild Agent", null, null, null);

		childAgent.parentAgent(rootAgent);
		grandchildAgent.parentAgent(childAgent);

		// Act
		BaseAgent result = grandchildAgent.rootAgent();

		// Assert
		assertNotNull(result);
		assertEquals(rootName, result.name());
		assertEquals(rootDescription, result.description());
		assertSame(rootAgent, result);
	}

	@Test
	@Tag("valid")
	public void rootAgentBehaviorWithProperHierarchy() {
		// Arrange
		TestAgent root = new TestAgent("root", "Root Agent", null, null, null);
		TestAgent child1 = new TestAgent("child1", "Child 1 Agent", null, null, null);
		TestAgent grandchild = new TestAgent("grandchild", "Grandchild Agent", null, null, null);

		child1.parentAgent(root);
		grandchild.parentAgent(child1);

		// Act
		long startTime = System.currentTimeMillis();
		BaseAgent result = grandchild.rootAgent();
		long endTime = System.currentTimeMillis();

		// Assert
		assertNotNull(result);
		assertSame(root, result);
		assertTrue((endTime - startTime) < 1000, "Method should complete quickly");
	}

	@Test
	@Tag("valid")
	public void rootAgentReturnsCorrectInstanceInSiblingScenario() {
		// Arrange
		TestAgent rootAgent = new TestAgent("root", "Root Agent", null, null, null);
		TestAgent sibling1 = new TestAgent("sibling1", "Sibling 1 Agent", null, null, null);
		TestAgent sibling2 = new TestAgent("sibling2", "Sibling 2 Agent", null, null, null);

		sibling1.parentAgent(rootAgent);
		sibling2.parentAgent(rootAgent);

		// Act
		BaseAgent root1 = sibling1.rootAgent();
		BaseAgent root2 = sibling2.rootAgent();

		// Assert
		assertNotNull(root1);
		assertNotNull(root2);
		assertSame(root1, root2);
		assertSame(rootAgent, root1);
		assertSame(rootAgent, root2);
	}

	@Test
	@Tag("boundary")
	public void rootAgentWithVeryDeepHierarchy() {
		// Arrange
		TestAgent root = new TestAgent("root", "Root Agent", null, null, null);
		TestAgent current = root;

		for (int i = 1; i <= 10; i++) {
			TestAgent child = new TestAgent("agent" + i, "Agent " + i, null, null, null);
			child.parentAgent(current);
			current = child;
		}

		// Act
		BaseAgent result = current.rootAgent();

		// Assert
		assertNotNull(result);
		assertSame(root, result);
		assertEquals("root", result.name());
	}

	@Test
	@Tag("valid")
	public void rootAgentWithSubAgentsReturnsCorrectRoot() {
		// Arrange
		TestAgent subAgent1 = new TestAgent("sub1", "Sub Agent 1", null, null, null);
		TestAgent subAgent2 = new TestAgent("sub2", "Sub Agent 2", null, null, null);
		List<BaseAgent> subAgents = ImmutableList.of(subAgent1, subAgent2);

		TestAgent rootAgent = new TestAgent("root", "Root Agent", subAgents, null, null);
		TestAgent childAgent = new TestAgent("child", "Child Agent", null, null, null);
		childAgent.parentAgent(rootAgent);

		// Act
		BaseAgent result = childAgent.rootAgent();

		// Assert
		assertNotNull(result);
		assertSame(rootAgent, result);
		assertEquals(2, result.subAgents().size());
	}

	// Test helper class extending BaseAgent
	private static class TestAgent extends BaseAgent {

		public TestAgent(String name, String description, List<? extends BaseAgent> subAgents,
				List<? extends Callbacks.BeforeAgentCallback> beforeAgentCallback,
				List<? extends Callbacks.AfterAgentCallback> afterAgentCallback) {
			super(name, description, subAgents, beforeAgentCallback, afterAgentCallback);
		}

		@Override
		protected Flowable<com.google.adk.events.Event> runAsyncImpl(InvocationContext invocationContext) {
			return Flowable.empty();
		}

		@Override
		protected Flowable<com.google.adk.events.Event> runLiveImpl(InvocationContext invocationContext) {
			return Flowable.empty();
		}

	}

}
