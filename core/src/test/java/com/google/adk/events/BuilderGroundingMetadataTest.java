/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=groundingMetadata_0f4ca12f38
ROOST_METHOD_SIG_HASH=groundingMetadata_3f4f86a7bb

Scenario 1: Setting Non-Null GroundingMetadata Value

Details:
  TestName: groundingMetadataWithNonNullValue
  Description: This test verifies that the groundingMetadata method correctly accepts a non-null GroundingMetadata object and wraps it in an Optional. It ensures that the builder properly stores the provided grounding metadata and returns the builder instance for method chaining.
Execution:
  Arrange: Create a new Event.Builder instance and a mock or real GroundingMetadata object.
  Act: Invoke the groundingMetadata method with the non-null GroundingMetadata object.
  Assert: Verify that the method returns the same Builder instance (for fluent API chaining). Verify that the groundingMetadata field in the builder is set to an Optional containing the provided value by calling the groundingMetadata() getter method.
Validation:
  The assertion verifies that when a valid GroundingMetadata object is passed to the method, it is properly wrapped in an Optional and stored in the builder's groundingMetadata field. This is critical for ensuring that grounding metadata can be successfully attached to events during the building process. The test also confirms the builder pattern's fluent interface by checking that the same builder instance is returned, allowing for method chaining.

Scenario 2: Setting Null GroundingMetadata Value

Details:
  TestName: groundingMetadataWithNullValue
  Description: This test validates that the groundingMetadata method correctly handles a null input by wrapping it in an empty Optional. It ensures that passing null does not cause a NullPointerException and that the builder properly represents the absence of grounding metadata.
Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Invoke the groundingMetadata method with a null value.
  Assert: Verify that the method returns the same Builder instance. Verify that the groundingMetadata field in the builder is set to Optional.empty() by calling the groundingMetadata() getter method and checking that the Optional is not present.
Validation:
  The assertion confirms that the method safely handles null inputs by converting them to Optional.empty(), which is the standard Java pattern for representing absent values. This prevents potential NullPointerExceptions downstream when the Event object is built and used. The test ensures that the builder maintains a consistent state even when grounding metadata is not provided, which is important for optional fields in the Event class.

Scenario 3: Method Chaining with GroundingMetadata

Details:
  TestName: groundingMetadataMethodChaining
  Description: This test verifies that the groundingMetadata method supports fluent API method chaining by returning the builder instance. It ensures that multiple builder methods can be called in sequence, including the groundingMetadata method.
Execution:
  Arrange: Create a new Event.Builder instance and a GroundingMetadata object.
  Act: Chain multiple builder method calls including groundingMetadata, such as builder.id("test-id").author("test-author").groundingMetadata(groundingMetadataObject).invocationId("test-invocation").
  Assert: Verify that all chained method calls execute without errors. Verify that the final builder instance contains all the set values by calling the appropriate getter methods.
Validation:
  This test validates the fluent interface design pattern implementation, ensuring that the groundingMetadata method returns the builder instance (this) to enable method chaining. This is essential for providing a clean and readable API for constructing Event objects. The test confirms that the groundingMetadata method integrates seamlessly with other builder methods, maintaining the builder pattern's usability and developer experience.

Scenario 4: Overwriting Previously Set GroundingMetadata

Details:
  TestName: groundingMetadataOverwritesPreviousValue
  Description: This test ensures that calling the groundingMetadata method multiple times correctly overwrites the previously set value. It validates that the builder maintains only the most recently set grounding metadata value.
Execution:
  Arrange: Create a new Event.Builder instance and two different GroundingMetadata objects (firstMetadata and secondMetadata).
  Act: Call groundingMetadata(firstMetadata) followed by groundingMetadata(secondMetadata) on the same builder instance.
  Assert: Verify that the groundingMetadata field in the builder contains only the second GroundingMetadata object by calling the groundingMetadata() getter method and checking that the Optional contains secondMetadata.
Validation:
  The assertion confirms that the builder correctly implements the last-write-wins semantics, where subsequent calls to groundingMetadata replace the previous value rather than accumulating or causing conflicts. This behavior is expected in builder patterns and ensures predictable state management. The test is important for scenarios where grounding metadata might be conditionally set or updated during the building process.

Scenario 5: Setting GroundingMetadata After Setting to Null

Details:
  TestName: groundingMetadataSetAfterNull
  Description: This test verifies that the builder can transition from having no grounding metadata (null) to having a valid GroundingMetadata object. It ensures that the builder state can be updated from empty to populated.
Execution:
  Arrange: Create a new Event.Builder instance and a GroundingMetadata object.
  Act: First call groundingMetadata(null), then call groundingMetadata(groundingMetadataObject) on the same builder instance.
  Assert: Verify that the groundingMetadata field in the builder contains the GroundingMetadata object (not empty) by calling the groundingMetadata() getter method and checking that the Optional is present and contains the expected value.
Validation:
  This test validates that the builder's state can be modified from representing absent grounding metadata to present grounding metadata. It ensures that the Optional.ofNullable() implementation correctly handles the transition and that there are no residual effects from the previous null assignment. This is important for scenarios where grounding metadata availability might be determined dynamically during the building process.

Scenario 6: Setting Null After Setting Valid GroundingMetadata

Details:
  TestName: groundingMetadataSetNullAfterValue
  Description: This test ensures that the builder can transition from having a valid GroundingMetadata object to having no grounding metadata by passing null. It validates that the builder can clear previously set grounding metadata.
Execution:
  Arrange: Create a new Event.Builder instance and a GroundingMetadata object.
  Act: First call groundingMetadata(groundingMetadataObject), then call groundingMetadata(null) on the same builder instance.
  Assert: Verify that the groundingMetadata field in the builder is set to Optional.empty() by calling the groundingMetadata() getter method and checking that the Optional is not present.
Validation:
  The assertion confirms that passing null to the groundingMetadata method effectively clears any previously set value, resulting in an empty Optional. This behavior is important for scenarios where grounding metadata needs to be explicitly removed or reset during the building process. The test ensures that the builder provides a way to represent the absence of grounding metadata even after it has been set.

Scenario 7: Building Event with GroundingMetadata Set

Details:
  TestName: buildEventWithGroundingMetadata
  Description: This test verifies that when the groundingMetadata method is called with a valid value, the built Event object correctly contains the grounding metadata. It ensures end-to-end functionality from setting the value in the builder to retrieving it from the built Event.
Execution:
  Arrange: Create a new Event.Builder instance, a GroundingMetadata object, and set required fields (id, invocationId, author).
  Act: Call groundingMetadata(groundingMetadataObject) on the builder, then call build() to create an Event object.
  Assert: Verify that the Event object's groundingMetadata() method returns an Optional containing the expected GroundingMetadata object.
Validation:
  This integration test validates that the groundingMetadata value set in the builder is properly transferred to the built Event object through the build() method. It ensures that the setGroundingMetadata method in the Event class is correctly called during the build process and that the value is accessible through the Event's public API. This test is crucial for confirming that the builder pattern correctly constructs Event objects with grounding metadata.

Scenario 8: Building Event with Null GroundingMetadata

Details:
  TestName: buildEventWithNullGroundingMetadata
  Description: This test verifies that when the groundingMetadata method is called with null, the built Event object correctly represents the absence of grounding metadata with an empty Optional. It ensures that null values in the builder result in empty Optionals in the built Event.
Execution:
  Arrange: Create a new Event.Builder instance and set required fields (id, invocationId, author).
  Act: Call groundingMetadata(null) on the builder, then call build() to create an Event object.
  Assert: Verify that the Event object's groundingMetadata() method returns an empty Optional by checking that isPresent() returns false.
Validation:
  This test confirms that the builder correctly handles null grounding metadata values and that the built Event object properly represents the absence of grounding metadata. It validates the integration between the builder's Optional.ofNullable() logic and the Event's setGroundingMetadata method. This is important for ensuring that optional fields are consistently represented as empty Optionals rather than null values in the final Event object.

Scenario 9: Building Event Without Setting GroundingMetadata

Details:
  TestName: buildEventWithoutSettingGroundingMetadata
  Description: This test verifies the default behavior when the groundingMetadata method is never called on the builder. It ensures that the built Event object has an empty Optional for grounding metadata when the field is not explicitly set.
Execution:
  Arrange: Create a new Event.Builder instance and set only required fields (id, invocationId, author) without calling groundingMetadata.
  Act: Call build() to create an Event object.
  Assert: Verify that the Event object's groundingMetadata() method returns an empty Optional by checking that isPresent() returns false.
Validation:
  The assertion validates that the builder initializes the groundingMetadata field to Optional.empty() by default (as declared in the field initialization). This test ensures that when grounding metadata is not provided, the Event object correctly represents its absence rather than having a null value. This is important for maintaining consistent Optional usage throughout the API and preventing NullPointerExceptions when accessing optional fields.

Scenario 10: Multiple Builders with Different GroundingMetadata Values

Details:
  TestName: multipleBuildersWithDifferentGroundingMetadata
  Description: This test verifies that multiple builder instances maintain independent state for grounding metadata. It ensures that setting grounding metadata on one builder does not affect other builder instances.
Execution:
  Arrange: Create two separate Event.Builder instances (builder1 and builder2) and two different GroundingMetadata objects (metadata1 and metadata2).
  Act: Call groundingMetadata(metadata1) on builder1 and groundingMetadata(metadata2) on builder2.
  Assert: Verify that builder1.groundingMetadata() returns an Optional containing metadata1 and builder2.groundingMetadata() returns an Optional containing metadata2, confirming that the values are independent.
Validation:
  This test confirms that the Builder class properly encapsulates state and that each builder instance maintains its own independent copy of the groundingMetadata field. This is essential for ensuring that the builder pattern works correctly in multi-threaded environments or when multiple Event objects are being constructed simultaneously. The test validates that there are no shared static fields or other mechanisms that could cause state leakage between builder instances.

*/

// ********RoostGPT********

package com.google.adk.events;

import static com.google.common.truth.Truth.assertThat;
import static com.google.common.truth.Truth8.assertThat;

import com.google.genai.types.GroundingMetadata;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

class BuilderGroundingMetadataTest {

  private Event.Builder builder;

  private GroundingMetadata mockGroundingMetadata;

  @BeforeEach
  void setUp() {
    builder = Event.builder();
    mockGroundingMetadata = Mockito.mock(GroundingMetadata.class);
  }

  @Test
  @Tag("valid")
  void groundingMetadataWithNonNullValue() {
    Event.Builder result = builder.groundingMetadata(mockGroundingMetadata);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.groundingMetadata()).isPresent();
    assertThat(builder.groundingMetadata()).hasValue(mockGroundingMetadata);
  }

  @Test
  @Tag("valid")
  void groundingMetadataWithNullValue() {
    Event.Builder result = builder.groundingMetadata(null);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.groundingMetadata()).isEmpty();
  }

  @Test
  @Tag("valid")
  void groundingMetadataMethodChaining() {
    GroundingMetadata groundingMetadataObject = Mockito.mock(GroundingMetadata.class);
    Event.Builder result =
        builder
            .id("test-id")
            .author("test-author")
            .groundingMetadata(groundingMetadataObject)
            .invocationId("test-invocation");
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.groundingMetadata()).isPresent();
    assertThat(builder.groundingMetadata()).hasValue(groundingMetadataObject);
  }

  @Test
  @Tag("valid")
  void groundingMetadataOverwritesPreviousValue() {
    GroundingMetadata firstMetadata = Mockito.mock(GroundingMetadata.class);
    GroundingMetadata secondMetadata = Mockito.mock(GroundingMetadata.class);
    builder.groundingMetadata(firstMetadata);
    builder.groundingMetadata(secondMetadata);
    assertThat(builder.groundingMetadata()).isPresent();
    assertThat(builder.groundingMetadata()).hasValue(secondMetadata);
  }

  @Test
  @Tag("valid")
  void groundingMetadataSetAfterNull() {
    GroundingMetadata groundingMetadataObject = Mockito.mock(GroundingMetadata.class);
    builder.groundingMetadata(null);
    builder.groundingMetadata(groundingMetadataObject);
    assertThat(builder.groundingMetadata()).isPresent();
    assertThat(builder.groundingMetadata()).hasValue(groundingMetadataObject);
  }

  @Test
  @Tag("valid")
  void groundingMetadataSetNullAfterValue() {
    GroundingMetadata groundingMetadataObject = Mockito.mock(GroundingMetadata.class);
    builder.groundingMetadata(groundingMetadataObject);
    builder.groundingMetadata(null);
    assertThat(builder.groundingMetadata()).isEmpty();
  }

  @Test
  @Tag("integration")
  void buildEventWithGroundingMetadata() {
    GroundingMetadata groundingMetadataObject = Mockito.mock(GroundingMetadata.class);
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation")
            .author("test-author")
            .groundingMetadata(groundingMetadataObject)
            .build();
    assertThat(event.groundingMetadata()).isPresent();
    assertThat(event.groundingMetadata()).hasValue(groundingMetadataObject);
  }

  @Test
  @Tag("integration")
  void buildEventWithNullGroundingMetadata() {
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation")
            .author("test-author")
            .groundingMetadata(null)
            .build();
    assertThat(event.groundingMetadata()).isEmpty();
  }

  @Test
  @Tag("integration")
  void buildEventWithoutSettingGroundingMetadata() {
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.groundingMetadata()).isEmpty();
  }

  @Test
  @Tag("valid")
  void multipleBuildersWithDifferentGroundingMetadata() {
    GroundingMetadata metadata1 = Mockito.mock(GroundingMetadata.class);
    GroundingMetadata metadata2 = Mockito.mock(GroundingMetadata.class);
    Event.Builder builder1 = Event.builder();
    Event.Builder builder2 = Event.builder();
    builder1.groundingMetadata(metadata1);
    builder2.groundingMetadata(metadata2);
    assertThat(builder1.groundingMetadata()).isPresent();
    assertThat(builder1.groundingMetadata()).hasValue(metadata1);
    assertThat(builder2.groundingMetadata()).isPresent();
    assertThat(builder2.groundingMetadata()).hasValue(metadata2);
  }
}
