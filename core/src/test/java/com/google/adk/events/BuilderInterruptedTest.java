/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=interrupted_d9c53f175e
ROOST_METHOD_SIG_HASH=interrupted_e70f99cf15

Scenario 1: Setting interrupted field with a non-null true value

Details:
  TestName: interruptedWithTrueValue
  Description: This test verifies that the interrupted method correctly sets the interrupted field to an Optional containing true when a non-null Boolean true value is passed as a parameter. It ensures the builder properly wraps the Boolean value in an Optional and returns the builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call the interrupted method with Boolean.TRUE as the parameter and store the returned builder reference.
  Assert: Verify that the returned builder is the same instance (for method chaining). Verify that the interrupted field in the builder is set to Optional.of(true).

Validation:
  The assertion verifies that the builder correctly stores the true value wrapped in an Optional. This is important because the interrupted field indicates whether an event was interrupted, and the builder must properly handle true values to maintain accurate event state. The test also confirms that the builder pattern is correctly implemented by returning the same builder instance for method chaining.

---

Scenario 2: Setting interrupted field with a non-null false value

Details:
  TestName: interruptedWithFalseValue
  Description: This test validates that the interrupted method correctly handles a non-null Boolean false value by wrapping it in an Optional and storing it in the interrupted field. It ensures the builder can distinguish between false and null values.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Invoke the interrupted method with Boolean.FALSE as the argument.
  Assert: Verify that the interrupted field contains Optional.of(false). Confirm that the method returns the builder instance for chaining.

Validation:
  This test ensures that false values are properly stored and not confused with null or empty Optional values. In the context of event processing, explicitly setting interrupted to false is semantically different from leaving it unset, so the builder must preserve this distinction. This is critical for accurate event state representation.

---

Scenario 3: Setting interrupted field with a null value

Details:
  TestName: interruptedWithNullValue
  Description: This test checks that the interrupted method correctly handles null input by creating an empty Optional using Optional.ofNullable(). It verifies that null values result in Optional.empty() rather than a null Optional reference.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call the interrupted method with null as the parameter.
  Assert: Verify that the interrupted field is set to Optional.empty(). Confirm that the method returns the builder instance.

Validation:
  This test is crucial for null safety. The method uses Optional.ofNullable() which converts null to Optional.empty(), preventing NullPointerException issues downstream. In the business context, a null interrupted value indicates that the interrupted status is unknown or not applicable, which is different from explicitly being false. This test ensures proper handling of absent values.

---

Scenario 4: Method chaining capability verification

Details:
  TestName: interruptedReturnsBuilderForChaining
  Description: This test verifies that the interrupted method returns the builder instance (this) to enable fluent API method chaining. It ensures that multiple builder methods can be called in sequence.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call the interrupted method with Boolean.TRUE and capture the returned builder reference.
  Assert: Verify that the returned builder reference is the exact same instance as the original builder using reference equality (==).

Validation:
  This test validates the builder pattern implementation. The @CanIgnoreReturnValue annotation indicates the return value can be ignored, but when used, it should support method chaining. This is essential for creating a fluent API that allows developers to write concise, readable code like builder.interrupted(true).author("agent").build().

---

Scenario 5: Multiple consecutive calls to interrupted method

Details:
  TestName: interruptedOverwritesPreviousValue
  Description: This test verifies that calling the interrupted method multiple times with different values correctly overwrites the previous value, with the last call taking precedence.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call interrupted(Boolean.TRUE) first, then call interrupted(Boolean.FALSE) on the same builder instance.
  Assert: Verify that the interrupted field contains Optional.of(false), confirming the second call overwrote the first value.

Validation:
  This test ensures that the builder correctly handles state updates when the same method is called multiple times. In practical scenarios, developers might need to change builder values before calling build(), and the builder should always reflect the most recent value set. This behavior is critical for builder flexibility and predictability.

---

Scenario 6: Setting interrupted to true and building the Event object

Details:
  TestName: interruptedTrueValuePersistsInBuiltEvent
  Description: This test validates that when interrupted is set to true in the builder and the Event object is built, the resulting Event object correctly contains the interrupted value as Optional.of(true).

Execution:
  Arrange: Create a new Event.Builder instance and set required fields (id, invocationId, author).
  Act: Call interrupted(Boolean.TRUE) on the builder, then call build() to create an Event object.
  Assert: Verify that calling interrupted() on the built Event object returns Optional.of(true).

Validation:
  This end-to-end test ensures that the interrupted value is correctly transferred from the builder to the immutable Event object. It validates the complete lifecycle of the interrupted field from builder configuration to final object state. This is important for ensuring data integrity throughout the object construction process.

---

Scenario 7: Setting interrupted to false and building the Event object

Details:
  TestName: interruptedFalseValuePersistsInBuiltEvent
  Description: This test confirms that when interrupted is set to false in the builder, the built Event object correctly reflects this value as Optional.of(false).

Execution:
  Arrange: Create a new Event.Builder instance and set required fields (id, invocationId, author).
  Act: Call interrupted(Boolean.FALSE) on the builder, then invoke build() to create an Event object.
  Assert: Verify that the built Event's interrupted() method returns Optional.of(false).

Validation:
  This test ensures that false values are correctly propagated from builder to the final Event object. It's important to verify that false is not converted to empty Optional or any other value during the build process. This maintains the semantic difference between "explicitly not interrupted" and "interrupted status unknown".

---

Scenario 8: Not setting interrupted field results in empty Optional

Details:
  TestName: interruptedDefaultsToEmptyOptional
  Description: This test verifies that when the interrupted method is never called on the builder, the resulting Event object has an interrupted field set to Optional.empty() by default.

Execution:
  Arrange: Create a new Event.Builder instance and set only required fields (id, invocationId, author).
  Act: Build the Event object without calling the interrupted method.
  Assert: Verify that the built Event's interrupted() method returns Optional.empty().

Validation:
  This test validates the default behavior when optional fields are not explicitly set. The interrupted field is initialized to Optional.empty() in the builder, and this default should be preserved if not overridden. This is important for distinguishing between "interrupted status not set" versus "explicitly set to false", which have different semantic meanings in event processing.

---

Scenario 9: Setting interrupted to null and building the Event object

Details:
  TestName: interruptedNullValueResultsInEmptyOptionalInBuiltEvent
  Description: This test validates that when interrupted is set to null in the builder, the built Event object contains Optional.empty() for the interrupted field.

Execution:
  Arrange: Create a new Event.Builder instance and set required fields (id, invocationId, author).
  Act: Call interrupted(null) on the builder, then build the Event object.
  Assert: Verify that the built Event's interrupted() method returns Optional.empty().

Validation:
  This test ensures proper null handling throughout the entire object construction lifecycle. It confirms that null values are safely converted to Optional.empty() and remain that way in the final Event object. This is critical for null safety and prevents potential NullPointerException issues when consumers access the interrupted field.

---

Scenario 10: Combining interrupted with other builder methods

Details:
  TestName: interruptedWorksInFluentChainWithOtherMethods
  Description: This test verifies that the interrupted method works correctly when used in a fluent chain with other builder methods, ensuring proper method chaining and state management.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Chain multiple builder methods including id(), author(), interrupted(Boolean.TRUE), and partial(Boolean.FALSE), then build the Event.
  Assert: Verify that the built Event has the correct values for all set fields, including interrupted as Optional.of(true).

Validation:
  This integration test ensures that the interrupted method works correctly in realistic usage scenarios where multiple builder methods are chained together. It validates that setting interrupted doesn't interfere with other builder methods and that all values are correctly preserved in the final Event object. This is important for real-world usage where builders are typically configured with multiple fields in a single fluent chain.

---

Scenario 11: Type safety with Boolean wrapper class

Details:
  TestName: interruptedAcceptsBooleanWrapperType
  Description: This test verifies that the interrupted method correctly accepts Boolean wrapper class instances (not primitive boolean) and handles them appropriately, including potential null values.

Execution:
  Arrange: Create a new Event.Builder instance. Create a Boolean wrapper variable set to true.
  Act: Pass the Boolean wrapper variable to the interrupted method.
  Assert: Verify that the interrupted field contains Optional.of(true) and the method returns the builder instance.

Validation:
  This test confirms that the method signature using @Nullable Boolean (wrapper class) is correctly implemented. Using the wrapper class instead of primitive boolean allows the method to accept null values, which is essential for the Optional.ofNullable() pattern. This design choice enables proper representation of absent values in the builder pattern.

---

Scenario 12: Interrupted field independence from other Optional fields

Details:
  TestName: interruptedFieldIndependentFromOtherOptionalFields
  Description: This test verifies that setting the interrupted field does not affect other Optional fields in the builder (such as partial, turnComplete, errorMessage, etc.) and vice versa.

Execution:
  Arrange: Create a new Event.Builder instance. Set multiple Optional fields including partial, turnComplete, and interrupted.
  Act: Set interrupted to true, partial to false, and turnComplete to true.
  Assert: Verify that each field maintains its independently set value without interference from others.

Validation:
  This test ensures proper encapsulation and independence of builder fields. Each Optional field should maintain its own state without side effects on other fields. This is critical for builder reliability and prevents subtle bugs where setting one field might inadvertently affect another. The test validates that the builder correctly manages multiple Optional fields simultaneously.

*/

// ********RoostGPT********

package com.google.adk.events;

import static com.google.common.truth.Truth.assertThat;
import static com.google.common.truth.Truth8.assertThat;

import com.google.genai.types.FinishReason;
import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderInterruptedTest {

  private Event.Builder builder;

  @BeforeEach
  void setUp() {
    builder = Event.builder();
  }

  @Test
  @Tag("valid")
  void interruptedWithTrueValue() {
    Event.Builder returnedBuilder = builder.interrupted(Boolean.TRUE);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    assertThat(builder.build().interrupted()).hasValue(true);
  }

  @Test
  @Tag("valid")
  void interruptedWithFalseValue() {
    Event.Builder returnedBuilder = builder.interrupted(Boolean.FALSE);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    assertThat(builder.build().interrupted()).hasValue(false);
  }

  @Test
  @Tag("boundary")
  void interruptedWithNullValue() {
    Event.Builder returnedBuilder = builder.interrupted(null);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    assertThat(builder.build().interrupted()).isEmpty();
  }

  @Test
  @Tag("valid")
  void interruptedReturnsBuilderForChaining() {
    Event.Builder returnedBuilder = builder.interrupted(Boolean.TRUE);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void interruptedOverwritesPreviousValue() {
    builder.interrupted(Boolean.TRUE);
    builder.interrupted(Boolean.FALSE);
    assertThat(builder.build().interrupted()).hasValue(false);
  }

  @Test
  @Tag("integration")
  void interruptedTrueValuePersistsInBuiltEvent() {
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation-id")
            .author("test-author")
            .interrupted(Boolean.TRUE)
            .build();
    assertThat(event.interrupted()).hasValue(true);
  }

  @Test
  @Tag("integration")
  void interruptedFalseValuePersistsInBuiltEvent() {
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation-id")
            .author("test-author")
            .interrupted(Boolean.FALSE)
            .build();
    assertThat(event.interrupted()).hasValue(false);
  }

  @Test
  @Tag("boundary")
  void interruptedDefaultsToEmptyOptional() {
    Event event =
        builder.id("test-id").invocationId("test-invocation-id").author("test-author").build();
    assertThat(event.interrupted()).isEmpty();
  }

  @Test
  @Tag("boundary")
  void interruptedNullValueResultsInEmptyOptionalInBuiltEvent() {
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation-id")
            .author("test-author")
            .interrupted(null)
            .build();
    assertThat(event.interrupted()).isEmpty();
  }

  @Test
  @Tag("integration")
  void interruptedWorksInFluentChainWithOtherMethods() {
    Event event =
        builder
            .id("test-id")
            .author("test-author")
            .invocationId("test-invocation-id")
            .interrupted(Boolean.TRUE)
            .partial(Boolean.FALSE)
            .build();
    assertThat(event.interrupted()).hasValue(true);
    assertThat(event.partial()).hasValue(false);
    assertThat(event.id()).isEqualTo("test-id");
    assertThat(event.author()).isEqualTo("test-author");
  }

  @Test
  @Tag("valid")
  void interruptedAcceptsBooleanWrapperType() {
    Boolean wrapperValue = Boolean.TRUE;
    Event.Builder returnedBuilder = builder.interrupted(wrapperValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    assertThat(builder.build().interrupted()).hasValue(true);
  }

  @Test
  @Tag("integration")
  void interruptedFieldIndependentFromOtherOptionalFields() {
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation-id")
            .author("test-author")
            .interrupted(Boolean.TRUE)
            .partial(Boolean.FALSE)
            .turnComplete(Boolean.TRUE)
            .build();
    assertThat(event.interrupted()).hasValue(true);
    assertThat(event.partial()).hasValue(false);
    assertThat(event.turnComplete()).hasValue(true);
  }

  @Test
  @Tag("valid")
  void interruptedWithTrueThenNullOverwrites() {
    builder.interrupted(Boolean.TRUE);
    builder.interrupted(null);
    assertThat(builder.build().interrupted()).isEmpty();
  }

  @Test
  @Tag("valid")
  void interruptedWithNullThenTrueOverwrites() {
    builder.interrupted(null);
    builder.interrupted(Boolean.TRUE);
    assertThat(builder.build().interrupted()).hasValue(true);
  }

  @Test
  @Tag("integration")
  void interruptedWithOptionalMethodOverload() {
    builder.interrupted(Optional.of(Boolean.TRUE));
    assertThat(builder.build().interrupted()).hasValue(true);
  }

  @Test
  @Tag("integration")
  void interruptedWithEmptyOptionalMethodOverload() {
    builder.interrupted(Optional.empty());
    assertThat(builder.build().interrupted()).isEmpty();
  }

  @Test
  @Tag("integration")
  void interruptedInComplexEventBuild() {
    Event event =
        builder
            .id("complex-id")
            .invocationId("complex-invocation")
            .author("complex-author")
            .interrupted(Boolean.TRUE)
            .errorMessage("Test error")
            .finishReason(FinishReason.STOP)
            .avgLogprobs(-0.5)
            .branch("main")
            .modelVersion("v1.0")
            .build();
    assertThat(event.interrupted()).hasValue(true);
    assertThat(event.errorMessage()).hasValue("Test error");
    assertThat(event.finishReason()).hasValue(FinishReason.STOP);
    assertThat(event.avgLogprobs()).hasValue(-0.5);
    assertThat(event.branch()).hasValue("main");
    assertThat(event.modelVersion()).hasValue("v1.0");
  }

  @Test
  @Tag("valid")
  void interruptedMultipleChainedCalls() {
    Event.Builder result =
        builder.interrupted(Boolean.TRUE).interrupted(Boolean.FALSE).interrupted(Boolean.TRUE);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.build().interrupted()).hasValue(true);
  }
}
