/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=avgLogprobs_6497d3d543
ROOST_METHOD_SIG_HASH=avgLogprobs_d570b5d507

Scenario 1: Setting a Valid Non-Null Double Value

Details:
  TestName: avgLogprobsWithValidNonNullDoubleValue
  Description: This test verifies that the avgLogprobs method correctly accepts a valid non-null Double value and wraps it in an Optional. The method should store the provided value and return the Builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a valid Double value (e.g., -0.5).
  Act: Invoke the avgLogprobs method on the Builder instance with the valid Double value.
  Assert: Verify that the method returns the same Builder instance (for fluent API chaining). Verify that when build() is called, the resulting Event object's avgLogprobs() method returns an Optional containing the expected Double value.

Validation:
  The assertion verifies that the avgLogprobs method correctly wraps a non-null Double value in an Optional and maintains the builder pattern by returning the Builder instance. This is significant because it ensures that valid average log probability values can be set during Event construction, which is important for tracking model confidence metrics in the application's event system.

---

Scenario 2: Setting a Null Value

Details:
  TestName: avgLogprobsWithNullValue
  Description: This test verifies that the avgLogprobs method correctly handles a null input by creating an empty Optional. The method should accept null as a valid parameter and return the Builder instance.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a null Double value.
  Act: Invoke the avgLogprobs method on the Builder instance with null as the parameter.
  Assert: Verify that the method returns the same Builder instance. Verify that when build() is called, the resulting Event object's avgLogprobs() method returns an empty Optional (Optional.empty()).

Validation:
  The assertion confirms that the avgLogprobs method properly handles null values by converting them to empty Optionals, which is the expected behavior for optional fields. This is crucial for maintaining data integrity and preventing NullPointerExceptions when the average log probability value is not available or not applicable for a particular event.

---

Scenario 3: Setting a Positive Double Value

Details:
  TestName: avgLogprobsWithPositiveDoubleValue
  Description: This test verifies that the avgLogprobs method correctly accepts and stores a positive Double value, even though log probabilities are typically negative or zero.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a positive Double value (e.g., 1.5).
  Act: Invoke the avgLogprobs method on the Builder instance with the positive Double value.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing the positive Double value.

Validation:
  This test ensures that the method does not impose validation constraints on the numeric range of the input value. While log probabilities are typically negative, the method should accept any valid Double value, allowing flexibility for edge cases or different calculation methods. This demonstrates that the method focuses on data storage rather than business logic validation.

---

Scenario 4: Setting a Negative Double Value

Details:
  TestName: avgLogprobsWithNegativeDoubleValue
  Description: This test verifies that the avgLogprobs method correctly accepts and stores a negative Double value, which is the typical range for log probability values.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a negative Double value (e.g., -2.5).
  Act: Invoke the avgLogprobs method on the Builder instance with the negative Double value.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing the negative Double value.

Validation:
  This test confirms that the method correctly handles negative values, which are the most common values for average log probabilities in language model outputs. This is essential for accurately representing model confidence scores in the event system.

---

Scenario 5: Setting Zero as the Value

Details:
  TestName: avgLogprobsWithZeroValue
  Description: This test verifies that the avgLogprobs method correctly accepts and stores a Double value of zero (0.0).

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a Double value of 0.0.
  Act: Invoke the avgLogprobs method on the Builder instance with 0.0 as the parameter.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing 0.0.

Validation:
  This test ensures that zero, which represents a log probability of 1.0 (certainty), is correctly handled as a valid value. This is important for edge cases where the model has maximum confidence in its output.

---

Scenario 6: Setting Double.MAX_VALUE

Details:
  TestName: avgLogprobsWithMaxDoubleValue
  Description: This test verifies that the avgLogprobs method correctly handles the maximum possible Double value (Double.MAX_VALUE).

Execution:
  Arrange: Create a new Event.Builder instance. Prepare Double.MAX_VALUE as the input.
  Act: Invoke the avgLogprobs method on the Builder instance with Double.MAX_VALUE.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing Double.MAX_VALUE.

Validation:
  This test validates that the method can handle extreme boundary values without overflow or data loss. While such values are unlikely in practical scenarios, ensuring proper handling of edge cases is important for robustness.

---

Scenario 7: Setting Double.MIN_VALUE

Details:
  TestName: avgLogprobsWithMinDoubleValue
  Description: This test verifies that the avgLogprobs method correctly handles the minimum positive Double value (Double.MIN_VALUE).

Execution:
  Arrange: Create a new Event.Builder instance. Prepare Double.MIN_VALUE as the input.
  Act: Invoke the avgLogprobs method on the Builder instance with Double.MIN_VALUE.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing Double.MIN_VALUE.

Validation:
  This test ensures that the method can handle the smallest positive representable Double value without precision loss. This validates the method's ability to store very small values that might occur in certain probability calculations.

---

Scenario 8: Setting Double.NEGATIVE_INFINITY

Details:
  TestName: avgLogprobsWithNegativeInfinity
  Description: This test verifies that the avgLogprobs method correctly accepts and stores Double.NEGATIVE_INFINITY, which represents a log probability of zero (impossible event).

Execution:
  Arrange: Create a new Event.Builder instance. Prepare Double.NEGATIVE_INFINITY as the input.
  Act: Invoke the avgLogprobs method on the Builder instance with Double.NEGATIVE_INFINITY.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing Double.NEGATIVE_INFINITY.

Validation:
  This test confirms that special Double values like negative infinity are properly handled. In the context of log probabilities, negative infinity represents an impossible event (probability of 0), which is a valid scenario that the system should be able to represent.

---

Scenario 9: Setting Double.POSITIVE_INFINITY

Details:
  TestName: avgLogprobsWithPositiveInfinity
  Description: This test verifies that the avgLogprobs method correctly accepts and stores Double.POSITIVE_INFINITY.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare Double.POSITIVE_INFINITY as the input.
  Act: Invoke the avgLogprobs method on the Builder instance with Double.POSITIVE_INFINITY.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing Double.POSITIVE_INFINITY.

Validation:
  This test ensures that the method can handle positive infinity values without throwing exceptions. While positive infinity is not a typical value for log probabilities, the method should accept it to maintain flexibility and prevent runtime errors.

---

Scenario 10: Setting Double.NaN

Details:
  TestName: avgLogprobsWithNaNValue
  Description: This test verifies that the avgLogprobs method correctly accepts and stores Double.NaN (Not a Number).

Execution:
  Arrange: Create a new Event.Builder instance. Prepare Double.NaN as the input.
  Act: Invoke the avgLogprobs method on the Builder instance with Double.NaN.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing Double.NaN (use Double.isNaN() for comparison).

Validation:
  This test confirms that the method can handle NaN values, which might occur due to calculation errors or undefined operations. Proper handling of NaN is important for system stability and debugging purposes.

---

Scenario 11: Method Chaining with Multiple Builder Methods

Details:
  TestName: avgLogprobsMethodChainingWithOtherBuilderMethods
  Description: This test verifies that the avgLogprobs method properly supports method chaining by returning the Builder instance, allowing it to be used in conjunction with other Builder methods.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a Double value for avgLogprobs (e.g., -1.5), a String value for id (e.g., "test-id"), and a String value for author (e.g., "test-author").
  Act: Chain multiple Builder method calls including avgLogprobs, id, and author in a single statement.
  Assert: Verify that the chaining executes without errors. Verify that the built Event object contains all the set values: avgLogprobs returns Optional containing -1.5, id() returns "test-id", and author() returns "test-author".

Validation:
  This test validates that the avgLogprobs method correctly implements the builder pattern by returning the Builder instance, enabling fluent API usage. This is crucial for maintaining a clean and readable API that allows developers to construct Event objects with multiple properties in a single, readable statement.

---

Scenario 12: Overwriting Previously Set Value with New Value

Details:
  TestName: avgLogprobsOverwritePreviousValue
  Description: This test verifies that calling the avgLogprobs method multiple times on the same Builder instance correctly overwrites the previous value with the new value.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare two different Double values (e.g., -1.0 and -2.0).
  Act: First, invoke avgLogprobs with the first value (-1.0). Then, invoke avgLogprobs again with the second value (-2.0). Build the Event object.
  Assert: Verify that the built Event object's avgLogprobs() method returns an Optional containing only the second value (-2.0), not the first value.

Validation:
  This test ensures that the avgLogprobs method correctly implements value replacement behavior, where subsequent calls overwrite previous values rather than accumulating them. This is the expected behavior for setter methods in the builder pattern and is important for allowing developers to modify values during the building process.

---

Scenario 13: Overwriting Previously Set Value with Null

Details:
  TestName: avgLogprobsOverwriteValueWithNull
  Description: This test verifies that calling the avgLogprobs method with null after setting a non-null value correctly overwrites the previous value and results in an empty Optional.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a non-null Double value (e.g., -1.5).
  Act: First, invoke avgLogprobs with the non-null value. Then, invoke avgLogprobs again with null. Build the Event object.
  Assert: Verify that the built Event object's avgLogprobs() method returns an empty Optional (Optional.empty()).

Validation:
  This test confirms that null values can be used to clear previously set values, resulting in an empty Optional. This behavior is important for scenarios where a value needs to be explicitly unset or removed during the building process.

---

Scenario 14: Overwriting Null with Non-Null Value

Details:
  TestName: avgLogprobsOverwriteNullWithValue
  Description: This test verifies that calling the avgLogprobs method with a non-null value after setting null correctly overwrites the empty Optional with a populated Optional.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a non-null Double value (e.g., -2.5).
  Act: First, invoke avgLogprobs with null. Then, invoke avgLogprobs again with the non-null value. Build the Event object.
  Assert: Verify that the built Event object's avgLogprobs() method returns an Optional containing the non-null value (-2.5).

Validation:
  This test ensures that the builder allows flexibility in setting and unsetting values during the construction process. It validates that a null value can be replaced with a non-null value, which is useful for conditional building logic.

---

Scenario 15: Builder Returns Same Instance for Fluent API

Details:
  TestName: avgLogprobsReturnsSameBuilderInstance
  Description: This test verifies that the avgLogprobs method returns the exact same Builder instance that it was called on, which is essential for the fluent API pattern.

Execution:
  Arrange: Create a new Event.Builder instance and store a reference to it. Prepare a Double value (e.g., -1.0).
  Act: Invoke the avgLogprobs method on the Builder instance and capture the returned value.
  Assert: Verify that the returned Builder instance is the same object reference as the original Builder instance (using reference equality check).

Validation:
  This test confirms that the method correctly implements the builder pattern by returning 'this', enabling method chaining. This is a fundamental requirement for fluent APIs and ensures that the builder can be used efficiently in a chain of method calls without creating unnecessary object instances.

---

Scenario 16: Setting Value and Verifying Through Built Event

Details:
  TestName: avgLogprobsValuePersistsThroughBuild
  Description: This test verifies that the value set through the avgLogprobs method is correctly persisted and accessible through the built Event object's avgLogprobs() getter method.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a specific Double value (e.g., -3.14159).
  Act: Invoke avgLogprobs with the prepared value. Call build() to create an Event object. Call avgLogprobs() on the Event object to retrieve the value.
  Assert: Verify that the retrieved Optional contains the exact value that was set (-3.14159). Verify that the Optional is present (not empty).

Validation:
  This test validates the complete data flow from setting a value in the builder to retrieving it from the built object. It ensures that the builder correctly transfers the avgLogprobs value to the Event object during the build process, which is critical for data integrity in the event system.

---

Scenario 17: Default Value When Not Set

Details:
  TestName: avgLogprobsDefaultValueWhenNotSet
  Description: This test verifies the default behavior when the avgLogprobs method is never called on the Builder, ensuring that the built Event object has an empty Optional for avgLogprobs.

Execution:
  Arrange: Create a new Event.Builder instance. Set only required fields (id, invocationId, author) without calling avgLogprobs.
  Act: Build the Event object without calling avgLogprobs.
  Assert: Verify that the built Event object's avgLogprobs() method returns an empty Optional (Optional.empty()).

Validation:
  This test confirms that the avgLogprobs field has a sensible default value (empty Optional) when not explicitly set, which is consistent with the Optional pattern for optional fields. This ensures that the absence of a value is properly represented and can be safely checked by consumers of the Event object.

---

Scenario 18: Setting Very Small Negative Value

Details:
  TestName: avgLogprobsWithVerySmallNegativeValue
  Description: This test verifies that the avgLogprobs method correctly handles very small negative values that are close to negative infinity but still representable as Double.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a very small negative Double value (e.g., -1.0E308).
  Act: Invoke the avgLogprobs method with the very small negative value.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing the exact value.

Validation:
  This test ensures that the method can handle extreme negative values that might occur in scenarios with very low probabilities. It validates that no precision loss or underflow occurs when storing such values, which is important for accurate representation of model confidence in edge cases.

---

Scenario 19: Setting Value Close to Zero (Small Negative)

Details:
  TestName: avgLogprobsWithSmallNegativeValueCloseToZero
  Description: This test verifies that the avgLogprobs method correctly handles small negative values close to zero (e.g., -0.001), which represent high probabilities.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a small negative Double value close to zero (e.g., -0.001).
  Act: Invoke the avgLogprobs method with the small negative value.
  Assert: Verify that the method returns the Builder instance. Verify that the built Event object's avgLogprobs() method returns an Optional containing the exact value with full precision.

Validation:
  This test confirms that the method maintains precision for small negative values, which are common in scenarios where the model has high confidence. Accurate representation of these values is crucial for distinguishing between different levels of high confidence in model outputs.

---

Scenario 20: Multiple Builders with Different Values

Details:
  TestName: avgLogprobsMultipleBuildersIndependence
  Description: This test verifies that multiple Builder instances maintain independent state for the avgLogprobs field, ensuring that setting a value on one builder does not affect other builders.

Execution:
  Arrange: Create two separate Event.Builder instances. Prepare two different Double values (e.g., -1.0 and -2.0).
  Act: Set the first value on the first builder using avgLogprobs. Set the second value on the second builder using avgLogprobs. Build both Event objects.
  Assert: Verify that the first Event object's avgLogprobs() returns an Optional containing -1.0. Verify that the second Event object's avgLogprobs() returns an Optional containing -2.0. Verify that the values are independent and not mixed.

Validation:
  This test ensures that Builder instances maintain proper encapsulation and do not share state. This is critical for thread safety and preventing unexpected side effects when multiple Event objects are being constructed concurrently or sequentially in the application.

*/

// ********RoostGPT********

package com.google.adk.events;

import static com.google.common.truth.Truth.assertThat;
import static com.google.common.truth.Truth8.assertThat;

import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderAvgLogprobsTest {

  @Test
  @Tag("valid")
  void avgLogprobsWithValidNonNullDoubleValue() {
    Event.Builder builder = Event.builder();
    Double validValue = -0.5;
    Event.Builder returnedBuilder = builder.avgLogprobs(validValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(validValue);
  }

  @Test
  @Tag("valid")
  void avgLogprobsWithNullValue() {
    Event.Builder builder = Event.builder();
    Event.Builder returnedBuilder = builder.avgLogprobs(null);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).isEmpty();
  }

  @Test
  @Tag("valid")
  void avgLogprobsWithPositiveDoubleValue() {
    Event.Builder builder = Event.builder();
    Double positiveValue = 1.5;
    Event.Builder returnedBuilder = builder.avgLogprobs(positiveValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(positiveValue);
  }

  @Test
  @Tag("valid")
  void avgLogprobsWithNegativeDoubleValue() {
    Event.Builder builder = Event.builder();
    Double negativeValue = -2.5;
    Event.Builder returnedBuilder = builder.avgLogprobs(negativeValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(negativeValue);
  }

  @Test
  @Tag("boundary")
  void avgLogprobsWithZeroValue() {
    Event.Builder builder = Event.builder();
    Double zeroValue = 0.0;
    Event.Builder returnedBuilder = builder.avgLogprobs(zeroValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(zeroValue);
  }

  @Test
  @Tag("boundary")
  void avgLogprobsWithMaxDoubleValue() {
    Event.Builder builder = Event.builder();
    Double maxValue = Double.MAX_VALUE;
    Event.Builder returnedBuilder = builder.avgLogprobs(maxValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(maxValue);
  }

  @Test
  @Tag("boundary")
  void avgLogprobsWithMinDoubleValue() {
    Event.Builder builder = Event.builder();
    Double minValue = Double.MIN_VALUE;
    Event.Builder returnedBuilder = builder.avgLogprobs(minValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(minValue);
  }

  @Test
  @Tag("boundary")
  void avgLogprobsWithNegativeInfinity() {
    Event.Builder builder = Event.builder();
    Double negativeInfinity = Double.NEGATIVE_INFINITY;
    Event.Builder returnedBuilder = builder.avgLogprobs(negativeInfinity);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(negativeInfinity);
  }

  @Test
  @Tag("boundary")
  void avgLogprobsWithPositiveInfinity() {
    Event.Builder builder = Event.builder();
    Double positiveInfinity = Double.POSITIVE_INFINITY;
    Event.Builder returnedBuilder = builder.avgLogprobs(positiveInfinity);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(positiveInfinity);
  }

  @Test
  @Tag("boundary")
  void avgLogprobsWithNaNValue() {
    Event.Builder builder = Event.builder();
    Double nanValue = Double.NaN;
    Event.Builder returnedBuilder = builder.avgLogprobs(nanValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).isPresent();
    assertThat(Double.isNaN(event.avgLogprobs().get())).isTrue();
  }

  @Test
  @Tag("integration")
  void avgLogprobsMethodChainingWithOtherBuilderMethods() {
    Double avgLogprobsValue = -1.5;
    String idValue = "test-id";
    String authorValue = "test-author";
    Event event =
        Event.builder()
            .avgLogprobs(avgLogprobsValue)
            .id(idValue)
            .author(authorValue)
            .invocationId("test-invocation")
            .build();
    assertThat(event.avgLogprobs()).hasValue(avgLogprobsValue);
    assertThat(event.id()).isEqualTo(idValue);
    assertThat(event.author()).isEqualTo(authorValue);
  }

  @Test
  @Tag("valid")
  void avgLogprobsOverwritePreviousValue() {
    Event.Builder builder = Event.builder();
    Double firstValue = -1.0;
    Double secondValue = -2.0;
    builder.avgLogprobs(firstValue);
    builder.avgLogprobs(secondValue);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(secondValue);
  }

  @Test
  @Tag("valid")
  void avgLogprobsOverwriteValueWithNull() {
    Event.Builder builder = Event.builder();
    Double initialValue = -1.5;
    builder.avgLogprobs(initialValue);
    builder.avgLogprobs(null);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).isEmpty();
  }

  @Test
  @Tag("valid")
  void avgLogprobsOverwriteNullWithValue() {
    Event.Builder builder = Event.builder();
    Double value = -2.5;
    builder.avgLogprobs(null);
    builder.avgLogprobs(value);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(value);
  }

  @Test
  @Tag("valid")
  void avgLogprobsReturnsSameBuilderInstance() {
    Event.Builder builder = Event.builder();
    Double value = -1.0;
    Event.Builder returnedBuilder = builder.avgLogprobs(value);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
  }

  @Test
  @Tag("integration")
  void avgLogprobsValuePersistsThroughBuild() {
    Event.Builder builder = Event.builder();
    Double value = -3.14159;
    builder.avgLogprobs(value);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    Optional<Double> retrievedValue = event.avgLogprobs();
    assertThat(retrievedValue).isPresent();
    assertThat(retrievedValue).hasValue(value);
  }

  @Test
  @Tag("valid")
  void avgLogprobsDefaultValueWhenNotSet() {
    Event.Builder builder = Event.builder();
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).isEmpty();
  }

  @Test
  @Tag("boundary")
  void avgLogprobsWithVerySmallNegativeValue() {
    Event.Builder builder = Event.builder();
    Double verySmallNegative = -1.0E308;
    Event.Builder returnedBuilder = builder.avgLogprobs(verySmallNegative);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(verySmallNegative);
  }

  @Test
  @Tag("valid")
  void avgLogprobsWithSmallNegativeValueCloseToZero() {
    Event.Builder builder = Event.builder();
    Double smallNegative = -0.001;
    Event.Builder returnedBuilder = builder.avgLogprobs(smallNegative);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.avgLogprobs()).hasValue(smallNegative);
  }

  @Test
  @Tag("integration")
  void avgLogprobsMultipleBuildersIndependence() {
    Event.Builder builder1 = Event.builder();
    Event.Builder builder2 = Event.builder();
    Double value1 = -1.0;
    Double value2 = -2.0;
    builder1.avgLogprobs(value1);
    builder2.avgLogprobs(value2);
    Event event1 =
        builder1.id("test-id-1").invocationId("test-invocation-1").author("test-author-1").build();
    Event event2 =
        builder2.id("test-id-2").invocationId("test-invocation-2").author("test-author-2").build();
    assertThat(event1.avgLogprobs()).hasValue(value1);
    assertThat(event2.avgLogprobs()).hasValue(value2);
  }
}
