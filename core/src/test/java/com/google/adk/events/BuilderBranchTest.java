/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=branch_5586b2d799
ROOST_METHOD_SIG_HASH=branch_555b372c6d

Scenario 1: Setting a Non-Null Branch Value

Details:
  TestName: branchWithNonNullValue
  Description: This test verifies that the branch method correctly accepts a non-null String value and wraps it in an Optional. It ensures that the builder properly stores the branch value and returns the builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance and prepare a non-null String value representing a branch hierarchy (e.g., "agent1.agent2.agent3").
  Act: Invoke the branch method on the builder with the non-null String value and capture the returned builder instance.
  Assert: Verify that the returned builder is the same instance (method chaining works), and verify that the branch field in the builder contains an Optional with the provided value using the builder's branch() getter method.

Validation:
  This assertion verifies that the branch method correctly handles non-null input values and properly wraps them in an Optional. This is critical for the builder pattern to function correctly, allowing users to set branch information for event hierarchy tracking. The test ensures that the branch value is stored correctly and can be retrieved for building the Event object.


Scenario 2: Setting a Null Branch Value

Details:
  TestName: branchWithNullValue
  Description: This test verifies that the branch method correctly handles null input by wrapping it in an Optional.empty(). It ensures that passing null to the method results in an empty Optional rather than a NullPointerException.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Invoke the branch method on the builder with a null value and capture the returned builder instance.
  Assert: Verify that the returned builder is the same instance, and verify that the branch field in the builder contains an empty Optional using the builder's branch() getter method.

Validation:
  This assertion confirms that the method safely handles null values using the @Nullable annotation and Optional.ofNullable() pattern. This is important for preventing NullPointerExceptions and allowing users to explicitly clear or omit branch information. The test validates that the defensive programming approach is correctly implemented.


Scenario 3: Method Chaining Capability

Details:
  TestName: branchReturnsBuilderForChaining
  Description: This test verifies that the branch method returns the builder instance (this) to support fluent API method chaining. It ensures that multiple builder methods can be called in sequence.

Execution:
  Arrange: Create a new Event.Builder instance and prepare a branch value.
  Act: Invoke the branch method and immediately chain another builder method (e.g., id() or author()) to verify chaining works.
  Assert: Verify that the chaining completes without errors and that both the branch value and the chained method's value are properly set in the builder.

Validation:
  This test validates the builder pattern's fluent API design, which is essential for creating readable and maintainable code. The @CanIgnoreReturnValue annotation indicates the return value can be ignored, but for proper builder usage, returning 'this' enables method chaining. This test ensures the implementation follows the builder pattern correctly.


Scenario 4: Setting Branch with Empty String

Details:
  TestName: branchWithEmptyString
  Description: This test verifies that the branch method correctly handles an empty string ("") as input. It ensures that an empty string is treated as a valid value and wrapped in an Optional containing the empty string, not as Optional.empty().

Execution:
  Arrange: Create a new Event.Builder instance and prepare an empty String value ("").
  Act: Invoke the branch method on the builder with the empty string.
  Assert: Verify that the branch field in the builder contains an Optional with an empty string value (Optional.of("")) rather than Optional.empty().

Validation:
  This assertion verifies the distinction between null and empty string handling. An empty string is a valid value that should be preserved, whereas null results in Optional.empty(). This is important for scenarios where an empty branch might have semantic meaning different from no branch at all.


Scenario 5: Setting Branch Multiple Times (Overwriting Previous Value)

Details:
  TestName: branchOverwritesPreviousValue
  Description: This test verifies that calling the branch method multiple times on the same builder instance correctly overwrites the previous value with the new value. It ensures that only the last set value is retained.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Invoke the branch method with a first value (e.g., "agent1.agent2"), then invoke it again with a different value (e.g., "agent3.agent4").
  Assert: Verify that the branch field in the builder contains an Optional with only the second value, confirming the first value was overwritten.

Validation:
  This test validates that the builder correctly implements the last-write-wins semantics for the branch field. This is important for ensuring predictable behavior when the same builder method is called multiple times, which might happen in complex builder construction scenarios or when reusing builder instances.


Scenario 6: Setting Branch with Whitespace-Only String

Details:
  TestName: branchWithWhitespaceOnlyString
  Description: This test verifies that the branch method correctly handles a string containing only whitespace characters (e.g., "   "). It ensures that whitespace-only strings are treated as valid values and not converted to null or empty.

Execution:
  Arrange: Create a new Event.Builder instance and prepare a String containing only whitespace characters.
  Act: Invoke the branch method on the builder with the whitespace-only string.
  Assert: Verify that the branch field in the builder contains an Optional with the whitespace string value, preserving the exact input.

Validation:
  This assertion confirms that the method does not perform any trimming or validation on the input string. It preserves the exact value provided, which is important for maintaining data integrity. If whitespace validation is required, it should be handled elsewhere in the application logic, not in the builder setter method.


Scenario 7: Setting Branch with Special Characters

Details:
  TestName: branchWithSpecialCharacters
  Description: This test verifies that the branch method correctly handles strings containing special characters such as dots, underscores, hyphens, and other valid characters that might appear in agent names or branch identifiers.

Execution:
  Arrange: Create a new Event.Builder instance and prepare a String with special characters (e.g., "agent-1.agent_2.agent@3").
  Act: Invoke the branch method on the builder with the special character string.
  Assert: Verify that the branch field in the builder contains an Optional with the exact string value including all special characters.

Validation:
  This test ensures that the branch method does not perform any character filtering or validation, allowing the full range of valid String characters. This is important because branch identifiers might use various naming conventions with different special characters. The method should preserve the exact input for downstream processing.


Scenario 8: Setting Branch with Very Long String

Details:
  TestName: branchWithVeryLongString
  Description: This test verifies that the branch method correctly handles very long strings that might represent deeply nested agent hierarchies. It ensures there are no length restrictions or performance issues with large inputs.

Execution:
  Arrange: Create a new Event.Builder instance and prepare a very long String representing a deep agent hierarchy (e.g., 1000+ characters with multiple agent names separated by dots).
  Act: Invoke the branch method on the builder with the very long string.
  Assert: Verify that the branch field in the builder contains an Optional with the complete long string value without truncation.

Validation:
  This test validates that the method can handle edge cases involving very long branch hierarchies without data loss or errors. This is important for systems that might have deeply nested agent structures. The test ensures the implementation doesn't impose artificial length limits that could cause issues in production.


Scenario 9: Building Event After Setting Branch

Details:
  TestName: buildEventAfterSettingBranch
  Description: This test verifies that after setting a branch value using the branch method, the built Event object correctly contains the branch value. It ensures the integration between the builder and the Event object construction.

Execution:
  Arrange: Create a new Event.Builder instance, set required fields (id, invocationId, author), and set a branch value.
  Act: Invoke the branch method with a test value, then call the build() method to create an Event object.
  Assert: Verify that the Event object's branch() method returns an Optional containing the expected branch value.

Validation:
  This integration test validates that the branch value set in the builder is correctly transferred to the Event object during the build process. This ensures the complete workflow from setting the value to using it in the final object works correctly. It verifies that the build() method properly calls event.branch(branch) to set the value.


Scenario 10: Setting Branch to Null After Setting a Value

Details:
  TestName: branchSetToNullAfterValue
  Description: This test verifies that setting the branch to null after previously setting it to a non-null value correctly clears the branch value, resulting in an empty Optional.

Execution:
  Arrange: Create a new Event.Builder instance and first set a non-null branch value.
  Act: Invoke the branch method with a non-null value, then invoke it again with null.
  Assert: Verify that the branch field in the builder contains an empty Optional after the second call.

Validation:
  This test ensures that the branch field can be explicitly cleared by passing null, which is important for builder reuse scenarios or when conditionally setting values. The test validates that Optional.ofNullable() correctly converts null to Optional.empty(), allowing users to reset the branch value.


Scenario 11: Setting Branch with Unicode Characters

Details:
  TestName: branchWithUnicodeCharacters
  Description: This test verifies that the branch method correctly handles strings containing Unicode characters from various languages and emoji, ensuring international character support.

Execution:
  Arrange: Create a new Event.Builder instance and prepare a String with Unicode characters (e.g., "agent_日本語.agent_العربية.agent_🤖").
  Act: Invoke the branch method on the builder with the Unicode string.
  Assert: Verify that the branch field in the builder contains an Optional with the exact Unicode string value preserved.

Validation:
  This test ensures that the branch method supports internationalization and can handle agent names or identifiers in any language or with emoji. This is important for global applications where agent names might be in different languages. The test validates that String handling preserves all Unicode characters correctly.

*/

// ********RoostGPT********

package com.google.adk.events;

import static org.junit.jupiter.api.Assertions.*;

import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderBranchTest {

  private Event.Builder builder;

  @BeforeEach
  void setUp() {
    builder = Event.builder();
  }

  @Test
  @Tag("valid")
  void branchWithNonNullValue() {
    String branchValue = "agent1.agent2.agent3";

    Event.Builder returnedBuilder = builder.branch(branchValue);

    assertSame(builder, returnedBuilder);
    assertTrue(builder.branch().isPresent());
    assertEquals(branchValue, builder.branch().get());
  }

  @Test
  @Tag("valid")
  void branchWithNullValue() {
    Event.Builder returnedBuilder = builder.branch(null);

    assertSame(builder, returnedBuilder);
    assertTrue(builder.branch().isEmpty());
  }

  @Test
  @Tag("valid")
  void branchReturnsBuilderForChaining() {
    String branchValue = "agent1.agent2";
    String idValue = "test-id-123";
    String authorValue = "test-author";

    Event.Builder returnedBuilder = builder.branch(branchValue).id(idValue).author(authorValue);

    assertSame(builder, returnedBuilder);
    assertTrue(builder.branch().isPresent());
    assertEquals(branchValue, builder.branch().get());
  }

  @Test
  @Tag("boundary")
  void branchWithEmptyString() {
    String emptyString = "";

    builder.branch(emptyString);

    assertTrue(builder.branch().isPresent());
    assertEquals(emptyString, builder.branch().get());
    assertNotEquals(Optional.empty(), builder.branch());
  }

  @Test
  @Tag("valid")
  void branchOverwritesPreviousValue() {
    String firstValue = "agent1.agent2";
    String secondValue = "agent3.agent4";

    builder.branch(firstValue);
    builder.branch(secondValue);

    assertTrue(builder.branch().isPresent());
    assertEquals(secondValue, builder.branch().get());
    assertNotEquals(firstValue, builder.branch().get());
  }

  @Test
  @Tag("boundary")
  void branchWithWhitespaceOnlyString() {
    String whitespaceString = "   ";

    builder.branch(whitespaceString);

    assertTrue(builder.branch().isPresent());
    assertEquals(whitespaceString, builder.branch().get());
  }

  @Test
  @Tag("valid")
  void branchWithSpecialCharacters() {
    String specialCharsString = "agent-1.agent_2.agent@3";

    builder.branch(specialCharsString);

    assertTrue(builder.branch().isPresent());
    assertEquals(specialCharsString, builder.branch().get());
  }

  @Test
  @Tag("boundary")
  void branchWithVeryLongString() {
    StringBuilder longStringBuilder = new StringBuilder();
    for (int i = 0; i < 100; i++) {
      longStringBuilder.append("agent").append(i).append(".");
    }
    String veryLongString = longStringBuilder.toString();

    builder.branch(veryLongString);

    assertTrue(builder.branch().isPresent());
    assertEquals(veryLongString, builder.branch().get());
    assertEquals(veryLongString.length(), builder.branch().get().length());
  }

  @Test
  @Tag("integration")
  void buildEventAfterSettingBranch() {
    String branchValue = "agent1.agent2.agent3";
    String idValue = "test-id-123";
    String invocationIdValue = "invocation-123";
    String authorValue = "test-author";

    Event event =
        builder
            .id(idValue)
            .invocationId(invocationIdValue)
            .author(authorValue)
            .branch(branchValue)
            .build();

    assertNotNull(event);
    assertTrue(event.branch().isPresent());
    assertEquals(branchValue, event.branch().get());
  }

  @Test
  @Tag("valid")
  void branchSetToNullAfterValue() {
    String initialValue = "agent1.agent2";

    builder.branch(initialValue);
    assertTrue(builder.branch().isPresent());

    builder.branch(null);
    assertTrue(builder.branch().isEmpty());
  }

  @Test
  @Tag("valid")
  void branchWithUnicodeCharacters() {
    String unicodeString = "agent_日本語.agent_العربية.agent_🤖";

    builder.branch(unicodeString);

    assertTrue(builder.branch().isPresent());
    assertEquals(unicodeString, builder.branch().get());
  }

  @Test
  @Tag("valid")
  void branchWithDotSeparatedHierarchy() {
    String hierarchyString = "root.parent.child.grandchild";

    builder.branch(hierarchyString);

    assertTrue(builder.branch().isPresent());
    assertEquals(hierarchyString, builder.branch().get());
  }

  @Test
  @Tag("boundary")
  void branchWithSingleCharacter() {
    String singleChar = "a";

    builder.branch(singleChar);

    assertTrue(builder.branch().isPresent());
    assertEquals(singleChar, builder.branch().get());
  }

  @Test
  @Tag("valid")
  void branchWithNumericString() {
    String numericString = "123.456.789";

    builder.branch(numericString);

    assertTrue(builder.branch().isPresent());
    assertEquals(numericString, builder.branch().get());
  }

  @Test
  @Tag("integration")
  void buildEventWithoutSettingBranch() {
    String idValue = "test-id-123";
    String invocationIdValue = "invocation-123";
    String authorValue = "test-author";

    Event event = builder.id(idValue).invocationId(invocationIdValue).author(authorValue).build();

    assertNotNull(event);
    assertTrue(event.branch().isEmpty());
  }

  @Test
  @Tag("valid")
  void branchMultipleCallsInChain() {
    String firstBranch = "agent1";
    String secondBranch = "agent2";
    String thirdBranch = "agent3";

    builder.branch(firstBranch).branch(secondBranch).branch(thirdBranch);

    assertTrue(builder.branch().isPresent());
    assertEquals(thirdBranch, builder.branch().get());
  }

  @Test
  @Tag("boundary")
  void branchWithNewlineCharacters() {
    String stringWithNewlines = "agent1\nagent2\nagent3";

    builder.branch(stringWithNewlines);

    assertTrue(builder.branch().isPresent());
    assertEquals(stringWithNewlines, builder.branch().get());
  }

  @Test
  @Tag("boundary")
  void branchWithTabCharacters() {
    String stringWithTabs = "agent1\tagent2\tagent3";

    builder.branch(stringWithTabs);

    assertTrue(builder.branch().isPresent());
    assertEquals(stringWithTabs, builder.branch().get());
  }

  @Test
  @Tag("valid")
  void branchWithMixedCaseCharacters() {
    String mixedCaseString = "Agent1.AGENT2.agent3.AgEnT4";

    builder.branch(mixedCaseString);

    assertTrue(builder.branch().isPresent());
    assertEquals(mixedCaseString, builder.branch().get());
  }

  @Test
  @Tag("integration")
  void buildEventWithBranchAndOtherOptionalFields() {
    String branchValue = "agent1.agent2";
    String idValue = "test-id-123";
    String invocationIdValue = "invocation-123";
    String authorValue = "test-author";
    String modelVersionValue = "model-v1";

    Event event =
        builder
            .id(idValue)
            .invocationId(invocationIdValue)
            .author(authorValue)
            .branch(branchValue)
            .modelVersion(modelVersionValue)
            .build();

    assertNotNull(event);
    assertTrue(event.branch().isPresent());
    assertEquals(branchValue, event.branch().get());
    assertTrue(event.modelVersion().isPresent());
    assertEquals(modelVersionValue, event.modelVersion().get());
  }

  @Test
  @Tag("valid")
  void branchReturnsCorrectBuilderInstance() {
    Event.Builder returnedBuilder = builder.branch("test-branch");

    assertNotNull(returnedBuilder);
    assertSame(builder, returnedBuilder);
  }
}
