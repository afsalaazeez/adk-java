/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=content_3edd4599b9
ROOST_METHOD_SIG_HASH=content_4abfb11e61

Scenario 1: Setting Content with a Non-Null Value

Details:
  TestName: contentWithNonNullValue
  Description: This test verifies that the content method correctly accepts a non-null Content object and stores it as an Optional containing that value. It ensures the builder pattern works correctly by returning the builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance and a mock or actual Content object.
  Act: Invoke the content method with the non-null Content object and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance (for method chaining). Verify that the content field in the builder is set to an Optional containing the provided Content object.

Validation:
  The assertion verifies that the builder correctly wraps the non-null Content value in an Optional and returns itself for fluent API usage. This is critical for the builder pattern implementation, ensuring that clients can chain multiple setter calls. The test confirms that valid Content objects are properly stored and can be used when building the Event object.

---

Scenario 2: Setting Content with a Null Value

Details:
  TestName: contentWithNullValue
  Description: This test checks that the content method correctly handles null input by storing it as an empty Optional. This is important for optional fields where null represents the absence of a value.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Invoke the content method with a null value and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance. Verify that the content field in the builder is set to Optional.empty().

Validation:
  The assertion confirms that null values are properly converted to empty Optionals, following Java best practices for handling optional values. This prevents NullPointerExceptions and ensures consistent behavior throughout the application. The @Nullable annotation on the parameter indicates this is expected behavior, and the test validates that the implementation correctly uses Optional.ofNullable() to handle both null and non-null cases.

---

Scenario 3: Method Chaining Capability

Details:
  TestName: contentReturnsBuilderForChaining
  Description: This test validates that the content method returns the Builder instance itself, enabling fluent API method chaining. This is a key characteristic of the builder pattern.

Execution:
  Arrange: Create a new Event.Builder instance and a Content object.
  Act: Invoke the content method and store the returned value. Then attempt to chain another builder method call (such as author or id) on the returned value.
  Assert: Verify that the method chaining executes without errors and that the returned Builder from content() is the same instance as the original builder.

Validation:
  This test ensures the builder pattern is correctly implemented with the @CanIgnoreReturnValue annotation allowing flexibility while still supporting method chaining. The ability to chain methods improves code readability and reduces the need for intermediate variables. This is essential for a good developer experience when constructing Event objects with multiple optional fields.

---

Scenario 4: Multiple Invocations Overwrite Previous Value

Details:
  TestName: contentMultipleInvocationsOverwritesPreviousValue
  Description: This test verifies that calling the content method multiple times on the same Builder instance overwrites the previous value rather than accumulating values.

Execution:
  Arrange: Create a new Event.Builder instance and two different Content objects (content1 and content2).
  Act: First invoke content(content1), then invoke content(content2) on the same builder instance. Build the Event object.
  Assert: Verify that the final Event object's content field contains only content2, confirming that the second invocation overwrote the first value.

Validation:
  This test confirms that the builder follows standard setter semantics where the last value set is the one that persists. This is important for understanding builder behavior, especially in scenarios where configuration might be applied conditionally or in multiple stages. The test ensures predictable behavior and prevents confusion about how multiple calls to the same setter method are handled.

---

Scenario 5: Content Value Persists Through Build

Details:
  TestName: contentValuePersistsThroughBuild
  Description: This test ensures that the Content value set via the content method is correctly transferred to the Event object when build() is called.

Execution:
  Arrange: Create a new Event.Builder instance and a Content object with identifiable properties.
  Act: Invoke content(contentObject) on the builder, then call build() to create an Event instance.
  Assert: Verify that the Event object's content() method returns an Optional containing the exact Content object that was set in the builder.

Validation:
  This test validates the complete lifecycle of the builder pattern, ensuring that data set during the building phase is correctly transferred to the immutable Event object. This is crucial for the integrity of the builder pattern implementation, confirming that the setContent method in the build() process correctly assigns the Optional<Content> field. It ensures that clients can rely on the builder to accurately construct Event objects with the specified content.

---

Scenario 6: Content Not Set Results in Empty Optional

Details:
  TestName: contentNotSetResultsInEmptyOptional
  Description: This test verifies that if the content method is never called on the builder, the resulting Event object has an empty Optional for its content field, matching the default initialization.

Execution:
  Arrange: Create a new Event.Builder instance without calling the content method.
  Act: Set other required fields (id, invocationId, author) and call build() to create an Event instance.
  Assert: Verify that the Event object's content() method returns Optional.empty().

Validation:
  This test confirms that the default initialization of the content field (Optional.empty()) is preserved when the setter is not invoked. This is important for optional fields where the absence of a value should be explicitly represented as an empty Optional rather than null. It ensures that the Event class can be safely constructed without setting all optional fields, and that the content field has a safe, non-null default value.

---

Scenario 7: Setting Content After Setting to Null

Details:
  TestName: contentSetAfterNull
  Description: This test verifies that after setting content to null (resulting in an empty Optional), a subsequent call with a non-null Content object correctly updates the field.

Execution:
  Arrange: Create a new Event.Builder instance and a Content object.
  Act: First invoke content(null), then invoke content(contentObject) on the same builder. Build the Event object.
  Assert: Verify that the final Event object's content field contains an Optional with the non-null Content object.

Validation:
  This test ensures that the builder state can be modified multiple times and that null values don't permanently affect the builder's ability to accept valid values. This is important for scenarios where builder configuration might be done conditionally or where a builder instance might be reused or reconfigured. It confirms that the Optional.ofNullable() implementation correctly handles transitions from null to non-null values.

---

Scenario 8: Setting Content to Null After Setting Valid Value

Details:
  TestName: contentSetToNullAfterValidValue
  Description: This test checks that setting a valid Content object first and then setting it to null results in an empty Optional, confirming that null overwrites previous values.

Execution:
  Arrange: Create a new Event.Builder instance and a Content object.
  Act: First invoke content(contentObject), then invoke content(null) on the same builder. Build the Event object.
  Assert: Verify that the final Event object's content field is Optional.empty().

Validation:
  This test validates that null values can be used to explicitly clear previously set content, which might be useful in scenarios where builder configuration needs to be reset or where conditional logic determines whether content should be included. It ensures that the builder provides full control over the optional field's state, including the ability to transition from a present value back to an absent value.

---

Scenario 9: Builder Independence After Content Setting

Details:
  TestName: builderIndependenceAfterContentSetting
  Description: This test verifies that multiple Builder instances maintain independent state for the content field, ensuring that setting content on one builder doesn't affect other builders.

Execution:
  Arrange: Create two separate Event.Builder instances (builder1 and builder2) and two different Content objects.
  Act: Set content1 on builder1 and content2 on builder2. Build both Event objects.
  Assert: Verify that event1 contains content1 and event2 contains content2, confirming that the builders maintained independent state.

Validation:
  This test ensures that the Builder class correctly encapsulates its state and that multiple builder instances don't share mutable state. This is crucial for thread safety and for scenarios where multiple Event objects are being constructed concurrently or in the same scope. It validates that the builder pattern implementation follows proper encapsulation principles and that each builder instance maintains its own independent copy of all fields.

---

Scenario 10: Content Method with Optional Parameter Variant

Details:
  TestName: contentWithOptionalParameter
  Description: This test verifies the overloaded content method that accepts Optional<Content> directly, ensuring it correctly stores the provided Optional value.

Execution:
  Arrange: Create a new Event.Builder instance and an Optional<Content> containing a Content object.
  Act: Invoke the content(Optional<Content>) method variant with the Optional parameter. Build the Event object.
  Assert: Verify that the Event object's content field contains the same Optional value that was passed to the builder method.

Validation:
  This test validates the alternative setter method that accepts Optional directly, which is useful when the calling code already has an Optional<Content> and wants to avoid unwrapping and rewrapping it. This overload provides flexibility in how clients interact with the builder API and ensures that both setter variants (nullable parameter and Optional parameter) work correctly and produce consistent results.

---

Scenario 11: Content Method with Empty Optional Parameter

Details:
  TestName: contentWithEmptyOptionalParameter
  Description: This test checks that passing Optional.empty() to the content(Optional<Content>) method variant correctly results in an empty Optional in the built Event object.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Invoke content(Optional.empty()) on the builder. Build the Event object.
  Assert: Verify that the Event object's content() method returns Optional.empty().

Validation:
  This test ensures that the Optional-based setter method correctly handles empty Optionals, maintaining consistency with the nullable parameter variant. This is important for API consistency and for scenarios where Optional values are being passed through from other parts of the application. It confirms that both ways of representing "no value" (null and Optional.empty()) result in the same final state.

---

Scenario 12: Content Integration with Event Build Process

Details:
  TestName: contentIntegrationWithEventBuild
  Description: This test validates that the content field integrates correctly with the complete Event building process, including interaction with other fields and the setContent method called during build().

Execution:
  Arrange: Create a new Event.Builder instance with all required fields (id, invocationId, author) and a Content object.
  Act: Set the content along with other fields, then call build() to create a complete Event object.
  Assert: Verify that the built Event object has all fields correctly set, including the content field, and that the Event can be used normally (e.g., calling content(), toString(), equals()).

Validation:
  This test ensures that the content field works correctly as part of the larger Event object construction process. It validates that the setContent method in the Event class correctly receives and stores the Optional<Content> from the builder, and that the content field integrates properly with other Event functionality like serialization, equality checking, and accessor methods. This is important for ensuring that the content field is a fully functional part of the Event class's public API.

*/

// ********RoostGPT********

package com.google.adk.events;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

import com.google.genai.types.Content;
import com.google.genai.types.FinishReason;
import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderContentTest {

  private Event.Builder builder;

  private Content mockContent;

  private Content mockContent2;

  @BeforeEach
  void setUp() {
    builder = Event.builder();
    mockContent = mock(Content.class);
    mockContent2 = mock(Content.class);
  }

  @Test
  @Tag("valid")
  void contentWithNonNullValue() {
    Event.Builder returnedBuilder = builder.content(mockContent);
    assertSame(builder, returnedBuilder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertTrue(event.content().isPresent());
    assertSame(mockContent, event.content().get());
  }

  @Test
  @Tag("valid")
  void contentWithNullValue() {
    Event.Builder returnedBuilder = builder.content((Content) null);
    assertSame(builder, returnedBuilder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertFalse(event.content().isPresent());
  }

  @Test
  @Tag("valid")
  void contentReturnsBuilderForChaining() {
    Event.Builder returnedBuilder = builder.content(mockContent);
    assertSame(builder, returnedBuilder);

    Event event =
        returnedBuilder
            .id("chained-id")
            .author("chained-author")
            .invocationId("chained-invocation")
            .build();
    assertNotNull(event);
    assertEquals("chained-id", event.id());
    assertEquals("chained-author", event.author());
  }

  @Test
  @Tag("valid")
  void contentMultipleInvocationsOverwritesPreviousValue() {
    builder.content(mockContent);
    builder.content(mockContent2);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertTrue(event.content().isPresent());
    assertSame(mockContent2, event.content().get());
    assertNotSame(mockContent, event.content().get());
  }

  @Test
  @Tag("integration")
  void contentValuePersistsThroughBuild() {
    builder.content(mockContent);
    Event event =
        builder
            .id("persist-id")
            .invocationId("persist-invocation")
            .author("persist-author")
            .build();
    assertTrue(event.content().isPresent());
    assertSame(mockContent, event.content().get());
  }

  @Test
  @Tag("valid")
  void contentNotSetResultsInEmptyOptional() {
    Event event =
        builder
            .id("no-content-id")
            .invocationId("no-content-invocation")
            .author("no-content-author")
            .build();
    assertFalse(event.content().isPresent());
    assertEquals(Optional.empty(), event.content());
  }

  @Test
  @Tag("valid")
  void contentSetAfterNull() {
    builder.content((Content) null);
    builder.content(mockContent);
    Event event =
        builder
            .id("after-null-id")
            .invocationId("after-null-invocation")
            .author("after-null-author")
            .build();
    assertTrue(event.content().isPresent());
    assertSame(mockContent, event.content().get());
  }

  @Test
  @Tag("valid")
  void contentSetToNullAfterValidValue() {
    builder.content(mockContent);
    builder.content((Content) null);
    Event event =
        builder
            .id("null-after-valid-id")
            .invocationId("null-after-valid-invocation")
            .author("null-after-valid-author")
            .build();
    assertFalse(event.content().isPresent());
    assertEquals(Optional.empty(), event.content());
  }

  @Test
  @Tag("valid")
  void builderIndependenceAfterContentSetting() {
    Event.Builder builder1 = Event.builder();
    Event.Builder builder2 = Event.builder();
    builder1.content(mockContent);
    builder2.content(mockContent2);
    Event event1 =
        builder1
            .id("builder1-id")
            .invocationId("builder1-invocation")
            .author("builder1-author")
            .build();
    Event event2 =
        builder2
            .id("builder2-id")
            .invocationId("builder2-invocation")
            .author("builder2-author")
            .build();
    assertTrue(event1.content().isPresent());
    assertTrue(event2.content().isPresent());
    assertSame(mockContent, event1.content().get());
    assertSame(mockContent2, event2.content().get());
    assertNotSame(event1.content().get(), event2.content().get());
  }

  @Test
  @Tag("valid")
  void contentWithOptionalParameter() {
    Optional<Content> optionalContent = Optional.of(mockContent);
    builder.content(optionalContent);
    Event event =
        builder
            .id("optional-id")
            .invocationId("optional-invocation")
            .author("optional-author")
            .build();
    assertTrue(event.content().isPresent());
    assertSame(mockContent, event.content().get());
  }

  @Test
  @Tag("valid")
  void contentWithEmptyOptionalParameter() {
    builder.content(Optional.empty());
    Event event =
        builder
            .id("empty-optional-id")
            .invocationId("empty-optional-invocation")
            .author("empty-optional-author")
            .build();
    assertFalse(event.content().isPresent());
    assertEquals(Optional.empty(), event.content());
  }

  @Test
  @Tag("integration")
  void contentIntegrationWithEventBuild() {
    builder
        .id("integration-id")
        .invocationId("integration-invocation")
        .author("integration-author")
        .content(mockContent)
        .partial(true)
        .turnComplete(false)
        .errorMessage("test error")
        .finishReason(FinishReason.STOP)
        .avgLogprobs(-0.5)
        .interrupted(false)
        .branch("main")
        .modelVersion("v1.0");
    Event event = builder.build();
    assertNotNull(event);
    assertEquals("integration-id", event.id());
    assertEquals("integration-invocation", event.invocationId());
    assertEquals("integration-author", event.author());
    assertTrue(event.content().isPresent());
    assertSame(mockContent, event.content().get());
    assertTrue(event.partial().isPresent());
    assertTrue(event.partial().get());
    assertTrue(event.turnComplete().isPresent());
    assertFalse(event.turnComplete().get());
    assertTrue(event.errorMessage().isPresent());
    assertEquals("test error", event.errorMessage().get());
    assertTrue(event.finishReason().isPresent());
    assertEquals(FinishReason.STOP, event.finishReason().get());
    assertTrue(event.avgLogprobs().isPresent());
    assertEquals(-0.5, event.avgLogprobs().get(), 0.001);
    assertTrue(event.interrupted().isPresent());
    assertFalse(event.interrupted().get());
    assertTrue(event.branch().isPresent());
    assertEquals("main", event.branch().get());
    assertTrue(event.modelVersion().isPresent());
    assertEquals("v1.0", event.modelVersion().get());
  }

  @Test
  @Tag("valid")
  void contentWithNullValueAndOtherFields() {
    builder
        .id("null-content-id")
        .invocationId("null-content-invocation")
        .author("null-content-author")
        .content((Content) null)
        .partial(true);
    Event event = builder.build();
    assertNotNull(event);
    assertFalse(event.content().isPresent());
    assertTrue(event.partial().isPresent());
    assertTrue(event.partial().get());
  }

  @Test
  @Tag("boundary")
  void contentMultipleNullInvocations() {
    builder.content((Content) null);
    builder.content((Content) null);
    builder.content((Content) null);
    Event event =
        builder
            .id("multiple-null-id")
            .invocationId("multiple-null-invocation")
            .author("multiple-null-author")
            .build();
    assertFalse(event.content().isPresent());
  }

  @Test
  @Tag("valid")
  void contentAlternatingNullAndValidValues() {
    builder.content(mockContent);
    builder.content((Content) null);
    builder.content(mockContent2);
    Event event =
        builder
            .id("alternating-id")
            .invocationId("alternating-invocation")
            .author("alternating-author")
            .build();
    assertTrue(event.content().isPresent());
    assertSame(mockContent2, event.content().get());
  }
}
