/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=partial_63d95e2c2a
ROOST_METHOD_SIG_HASH=partial_8ee90caf8b

Scenario 1: Setting partial field with Optional containing true value

Details:
  TestName: partialWithOptionalContainingTrue
  Description: This test verifies that the partial method correctly sets the partial field when provided with an Optional containing a Boolean true value. The method should store the Optional value and return the Builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare an Optional<Boolean> containing the value true.
  Act: Invoke the partial method on the builder with Optional.of(true) as the parameter.
  Assert: Verify that the method returns the same Builder instance (for fluent API chaining). Build the Event object and verify that the partial field contains Optional.of(true) using the partial() getter method.

Validation:
  This assertion verifies that the Builder correctly stores the Optional<Boolean> value when it contains true, which is essential for indicating incomplete chunks from LLM streaming responses. This test ensures the fluent API pattern works correctly and the value is properly propagated to the built Event object.

---

Scenario 2: Setting partial field with Optional containing false value

Details:
  TestName: partialWithOptionalContainingFalse
  Description: This test verifies that the partial method correctly sets the partial field when provided with an Optional containing a Boolean false value. This represents the final chunk in a streaming response.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare an Optional<Boolean> containing the value false.
  Act: Invoke the partial method on the builder with Optional.of(false) as the parameter.
  Assert: Verify that the method returns the same Builder instance. Build the Event object and verify that the partial field contains Optional.of(false) using the partial() getter method.

Validation:
  This assertion confirms that the Builder can store false values in the Optional, which is critical for marking the last chunk in a streaming response where partial should be false. This ensures proper handling of complete response chunks.

---

Scenario 3: Setting partial field with empty Optional

Details:
  TestName: partialWithEmptyOptional
  Description: This test verifies that the partial method correctly handles an empty Optional value, which represents the absence of partial information.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare an empty Optional<Boolean> using Optional.empty().
  Act: Invoke the partial method on the builder with Optional.empty() as the parameter.
  Assert: Verify that the method returns the same Builder instance. Build the Event object and verify that the partial field is empty using the partial() getter method and Optional.isEmpty() or asserting it equals Optional.empty().

Validation:
  This assertion ensures that the Builder correctly handles the absence of partial information. An empty Optional indicates that the partial status is not specified, which is a valid state. This test validates proper handling of optional fields in the builder pattern.

---

Scenario 4: Method chaining with partial method

Details:
  TestName: partialMethodChainingWithOtherBuilderMethods
  Description: This test verifies that the partial method supports fluent API method chaining by returning the Builder instance, allowing multiple builder methods to be called in sequence.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare test values for multiple fields including an Optional<Boolean> for partial.
  Act: Chain multiple builder method calls including partial(Optional.of(true)), followed by other builder methods like id("test-id"), author("test-author"), and invocationId("test-invocation").
  Assert: Verify that all chained method calls execute successfully without throwing exceptions. Build the Event object and verify that all fields are set correctly, including partial containing Optional.of(true).

Validation:
  This assertion validates that the partial method properly implements the builder pattern by returning 'this', enabling fluent method chaining. This is crucial for maintaining a clean and readable API where multiple properties can be set in a single statement chain.

---

Scenario 5: Overwriting partial field with different Optional values

Details:
  TestName: partialOverwritingPreviousValue
  Description: This test verifies that calling the partial method multiple times correctly overwrites the previous value, with the last call taking precedence.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: First, invoke partial(Optional.of(true)) on the builder. Then, invoke partial(Optional.of(false)) on the same builder instance. Finally, build the Event object.
  Assert: Verify that the partial field in the built Event contains Optional.of(false), confirming that the second call overwrote the first value.

Validation:
  This assertion ensures that the Builder correctly implements value overwriting behavior, where subsequent calls to the same setter method replace previous values rather than accumulating them. This is standard builder pattern behavior and critical for allowing users to modify values during the building process.

---

Scenario 6: Setting partial field then building Event with default timestamp

Details:
  TestName: partialWithDefaultTimestampInBuiltEvent
  Description: This test verifies that setting the partial field works correctly in conjunction with the build() method's default behavior of setting a timestamp when none is provided.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare an Optional<Boolean> containing true.
  Act: Invoke partial(Optional.of(true)) on the builder without setting a timestamp. Build the Event object.
  Assert: Verify that the partial field contains Optional.of(true) and that the timestamp field is set to a non-zero value (default behavior from build() method).

Validation:
  This assertion validates that the partial field setting is independent of other field initialization logic in the build() method. It ensures that the build() method's default timestamp assignment doesn't interfere with the partial field value, confirming proper field isolation in the builder.

---

Scenario 7: Setting partial field in combination with actions field

Details:
  TestName: partialWithActionsFieldSet
  Description: This test verifies that the partial field can be set correctly alongside the actions field, ensuring no interference between different Optional fields in the builder.

Execution:
  Arrange: Create a new Event.Builder instance. Create an EventActions object using EventActions.builder().build(). Prepare an Optional<Boolean> containing true.
  Act: Invoke partial(Optional.of(true)) and actions(eventActions) on the builder. Build the Event object.
  Assert: Verify that both the partial field contains Optional.of(true) and the actions field is properly set in the built Event object.

Validation:
  This assertion ensures that setting multiple fields, including Optional fields like partial and regular object fields like actions, works correctly without any field interference. This validates the builder's ability to handle multiple field types simultaneously.

---

Scenario 8: Null safety when passing null wrapped in Optional

Details:
  TestName: partialWithOptionalOfNullable
  Description: This test verifies the behavior when passing Optional.ofNullable(null) to the partial method, which should result in an empty Optional.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Invoke partial(Optional.ofNullable(null)) on the builder. Build the Event object.
  Assert: Verify that the partial field in the built Event is empty using Optional.isEmpty() or equals Optional.empty().

Validation:
  This assertion validates that the method correctly handles Optional.ofNullable(null), which creates an empty Optional. This is important for null safety and ensures that null values are properly converted to empty Optionals rather than causing NullPointerExceptions.

---

Scenario 9: Builder instance reusability after setting partial

Details:
  TestName: builderReusabilityAfterSettingPartial
  Description: This test verifies that a Builder instance can be reused to build multiple Event objects after setting the partial field, and that each built Event maintains the correct partial value.

Execution:
  Arrange: Create a new Event.Builder instance. Set partial to Optional.of(true).
  Act: Build the first Event object. Then build a second Event object from the same builder instance without modifying the partial field.
  Assert: Verify that both Event objects have the partial field set to Optional.of(true).

Validation:
  This assertion ensures that the Builder maintains its state across multiple build() calls, which is important for scenarios where the same configuration needs to be used to create multiple similar Event objects. This validates builder state persistence.

---

Scenario 10: Setting partial with Optional in combination with other Optional fields

Details:
  TestName: partialWithMultipleOptionalFieldsSet
  Description: This test verifies that the partial field can be set correctly alongside other Optional fields like turnComplete, interrupted, and errorMessage without any conflicts.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare Optional values for multiple Optional fields.
  Act: Invoke partial(Optional.of(true)), turnComplete(Optional.of(false)), interrupted(Optional.of(false)), and errorMessage(Optional.of("test error")) on the builder. Build the Event object.
  Assert: Verify that all Optional fields are correctly set in the built Event: partial contains Optional.of(true), turnComplete contains Optional.of(false), interrupted contains Optional.of(false), and errorMessage contains Optional.of("test error").

Validation:
  This assertion validates that the Builder can handle multiple Optional fields simultaneously without any interference or data corruption. This is crucial for ensuring that the complex Event object with many Optional fields can be constructed reliably with all fields maintaining their independent values.

*/

// ********RoostGPT********

package com.google.adk.events;

import static org.junit.jupiter.api.Assertions.*;

import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderPartial175Test {

  @Test
  @Tag("valid")
  void partialWithOptionalContainingTrue() {
    Event.Builder builder = Event.builder();
    Optional<Boolean> partialValue = Optional.of(true);
    Event.Builder result = builder.partial(partialValue);
    assertSame(builder, result, "Builder should return itself for method chaining");
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertEquals(
        Optional.of(true), event.partial(), "Partial field should contain Optional.of(true)");
  }

  @Test
  @Tag("valid")
  void partialWithOptionalContainingFalse() {
    Event.Builder builder = Event.builder();
    Optional<Boolean> partialValue = Optional.of(false);
    Event.Builder result = builder.partial(partialValue);
    assertSame(builder, result, "Builder should return itself for method chaining");
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertEquals(
        Optional.of(false), event.partial(), "Partial field should contain Optional.of(false)");
  }

  @Test
  @Tag("valid")
  void partialWithEmptyOptional() {
    Event.Builder builder = Event.builder();
    Optional<Boolean> partialValue = Optional.empty();
    Event.Builder result = builder.partial(partialValue);
    assertSame(builder, result, "Builder should return itself for method chaining");
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertTrue(event.partial().isEmpty(), "Partial field should be empty");
    assertEquals(Optional.empty(), event.partial(), "Partial field should equal Optional.empty()");
  }

  @Test
  @Tag("valid")
  void partialMethodChainingWithOtherBuilderMethods() {
    Event.Builder builder = Event.builder();
    assertDoesNotThrow(
        () -> {
          Event event =
              builder
                  .partial(Optional.of(true))
                  .id("test-id")
                  .author("test-author")
                  .invocationId("test-invocation")
                  .build();
          assertEquals(
              Optional.of(true), event.partial(), "Partial field should contain Optional.of(true)");
          assertEquals("test-id", event.id(), "ID should be set correctly");
          assertEquals("test-author", event.author(), "Author should be set correctly");
          assertEquals(
              "test-invocation", event.invocationId(), "InvocationId should be set correctly");
        },
        "Method chaining should execute without exceptions");
  }

  @Test
  @Tag("valid")
  void partialOverwritingPreviousValue() {
    Event.Builder builder = Event.builder();
    builder.partial(Optional.of(true));
    builder.partial(Optional.of(false));
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertEquals(
        Optional.of(false),
        event.partial(),
        "Partial field should contain the last set value Optional.of(false)");
  }

  @Test
  @Tag("valid")
  void partialWithDefaultTimestampInBuiltEvent() {
    Event.Builder builder = Event.builder();
    builder.partial(Optional.of(true));
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertEquals(
        Optional.of(true), event.partial(), "Partial field should contain Optional.of(true)");
    assertNotNull(event.timestamp(), "Timestamp should not be null");
    assertTrue(event.timestamp() > 0, "Timestamp should be set to a non-zero value");
  }

  @Test
  @Tag("valid")
  void partialWithActionsFieldSet() {
    Event.Builder builder = Event.builder();
    EventActions eventActions = EventActions.builder().build();
    builder.partial(Optional.of(true));
    builder.actions(eventActions);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertEquals(
        Optional.of(true), event.partial(), "Partial field should contain Optional.of(true)");
    assertNotNull(event.actions(), "Actions field should be set");
    assertEquals(eventActions, event.actions(), "Actions field should match the set value");
  }

  @Test
  @Tag("boundary")
  void partialWithOptionalOfNullable() {
    Event.Builder builder = Event.builder();
    builder.partial(Optional.ofNullable(null));
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertTrue(
        event.partial().isEmpty(),
        "Partial field should be empty when Optional.ofNullable(null) is passed");
    assertEquals(Optional.empty(), event.partial(), "Partial field should equal Optional.empty()");
  }

  @Test
  @Tag("valid")
  void builderReusabilityAfterSettingPartial() {
    Event.Builder builder = Event.builder();
    builder.partial(Optional.of(true));
    builder.id("test-id");
    builder.invocationId("test-invocation");
    builder.author("test-author");
    Event event1 = builder.build();
    Event event2 = builder.build();
    assertEquals(
        Optional.of(true),
        event1.partial(),
        "First Event should have partial set to Optional.of(true)");
    assertEquals(
        Optional.of(true),
        event2.partial(),
        "Second Event should have partial set to Optional.of(true)");
  }

  @Test
  @Tag("valid")
  void partialWithMultipleOptionalFieldsSet() {
    Event.Builder builder = Event.builder();
    builder.partial(Optional.of(true));
    builder.turnComplete(Optional.of(false));
    builder.interrupted(Optional.of(false));
    builder.errorMessage(Optional.of("test error"));
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertEquals(
        Optional.of(true), event.partial(), "Partial field should contain Optional.of(true)");
    assertEquals(
        Optional.of(false),
        event.turnComplete(),
        "TurnComplete field should contain Optional.of(false)");
    assertEquals(
        Optional.of(false),
        event.interrupted(),
        "Interrupted field should contain Optional.of(false)");
    assertEquals(
        Optional.of("test error"),
        event.errorMessage(),
        "ErrorMessage field should contain Optional.of('test error')");
  }
}
