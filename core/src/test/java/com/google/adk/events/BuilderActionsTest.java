/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=actions_1f920f1d26
ROOST_METHOD_SIG_HASH=actions_17fc9e647b

Scenario 1: Setting a Valid Non-Null EventActions Object

Details:
  TestName: actionsWithValidEventActionsObject
  Description: This test verifies that the actions() method correctly accepts and stores a valid non-null EventActions object. It ensures that the builder properly sets the actions field and returns the builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance and construct a valid EventActions object using EventActions.builder().build().
  Act: Invoke the actions() method on the builder with the created EventActions object.
  Assert: Verify that the returned object is the same builder instance (for fluent API chaining). Additionally, verify that when build() is called, the resulting Event object contains the correct EventActions object by calling actions() on the built Event.

Validation:
  The assertion verifies that the builder pattern is correctly implemented with method chaining support. The test ensures that the actions field is properly set in the builder and subsequently transferred to the Event object during the build() process. This is critical for maintaining the fluent API design pattern and ensuring data integrity in the builder pattern.


Scenario 2: Setting EventActions and Verifying Builder Returns Self

Details:
  TestName: actionsReturnsBuilderInstance
  Description: This test specifically validates that the actions() method returns the same builder instance (this) to support method chaining, which is a fundamental requirement for the builder pattern.

Execution:
  Arrange: Create a new Event.Builder instance and a valid EventActions object.
  Act: Call the actions() method with the EventActions object and capture the returned builder reference.
  Assert: Use assertSame() to verify that the returned builder is the exact same instance as the original builder.

Validation:
  This assertion confirms that the builder implements the fluent interface pattern correctly by returning 'this'. This is essential for allowing developers to chain multiple builder method calls in a single statement, improving code readability and usability.


Scenario 3: Setting EventActions Multiple Times with Different Values

Details:
  TestName: actionsOverwritesPreviousValue
  Description: This test verifies that calling the actions() method multiple times with different EventActions objects results in the last value being retained, ensuring proper overwrite behavior.

Execution:
  Arrange: Create a new Event.Builder instance and two different EventActions objects (firstActions and secondActions).
  Act: Call actions() first with firstActions, then call actions() again with secondActions. Finally, build the Event object.
  Assert: Verify that the built Event object contains secondActions by calling actions() on the Event and comparing it with secondActions using assertEquals().

Validation:
  This test ensures that the builder correctly handles multiple assignments to the same field, with the last assignment taking precedence. This behavior is important for scenarios where builder configuration might be modified during the building process, and it confirms that there are no unexpected side effects from multiple assignments.


Scenario 4: Building Event Without Setting Actions Explicitly

Details:
  TestName: actionsDefaultsToEmptyEventActionsWhenNotSet
  Description: This test verifies the default behavior when the actions() method is never called on the builder. According to the build() method implementation, it should default to an empty EventActions object created via EventActions.builder().build().

Execution:
  Arrange: Create a new Event.Builder instance and set only the required fields (id, invocationId, author) without calling actions().
  Act: Call build() on the builder to create an Event object.
  Assert: Verify that the Event object has a non-null actions field by calling actions() on the Event. Additionally, verify that it equals an EventActions object created with EventActions.builder().build().

Validation:
  This assertion validates the default initialization logic in the build() method, which ensures that the actions field is never null in a built Event object. This is crucial for preventing NullPointerException in code that uses Event objects and expects actions to always be present.


Scenario 5: Setting Null EventActions Value

Details:
  TestName: actionsWithNullValue
  Description: This test examines the behavior when null is passed to the actions() method. Since the method parameter is not annotated with @Nullable and there's no explicit null handling, this test verifies how the builder handles null input.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call the actions() method with a null value.
  Assert: Verify that the method call completes without throwing an exception. When build() is called, verify that the resulting Event has a non-null actions field due to the default initialization in build() method (actions().orElse(EventActions.builder().build())).

Validation:
  This test is important for understanding the null-handling behavior of the builder. Since the build() method uses Optional.ofNullable(actions).orElse(...), passing null to actions() will result in the default EventActions being used. This ensures robustness and prevents null pointer exceptions in the final Event object.


Scenario 6: Method Chaining with Multiple Builder Methods Including Actions

Details:
  TestName: actionsInMethodChain
  Description: This test verifies that the actions() method works correctly when used as part of a fluent method chain with other builder methods, ensuring seamless integration with the builder pattern.

Execution:
  Arrange: Create valid values for multiple Event fields including EventActions, id, author, and invocationId.
  Act: Use method chaining to call multiple builder methods in sequence: builder.id(id).author(author).actions(eventActions).invocationId(invocationId).build().
  Assert: Verify that the resulting Event object contains all the correctly set values by calling the respective getter methods (id(), author(), actions(), invocationId()).

Validation:
  This assertion confirms that the actions() method integrates properly with the fluent API and doesn't break the method chain. This is essential for real-world usage where developers typically set multiple properties in a single chained statement, and it validates the overall builder implementation quality.


Scenario 7: Setting Actions After Building and Using toBuilder

Details:
  TestName: actionsWithToBuilderPattern
  Description: This test verifies that the actions field is correctly preserved and can be modified when using the toBuilder() pattern to create a modified copy of an existing Event.

Execution:
  Arrange: Create an Event with a specific EventActions object. Then create a new EventActions object for modification.
  Act: Call toBuilder() on the original Event, then call actions() with the new EventActions object, and build a new Event.
  Assert: Verify that the new Event has the updated EventActions object while the original Event retains its original EventActions object.

Validation:
  This test validates the immutability pattern and the toBuilder() functionality. It ensures that modifying a builder created from an existing Event doesn't affect the original Event, which is crucial for maintaining data integrity in immutable object patterns. This is particularly important in multi-threaded environments or when Events are shared across different parts of an application.


Scenario 8: Actions Field Persistence Through Build Process

Details:
  TestName: actionsPersistsThroughBuildProcess
  Description: This test ensures that the EventActions object set via the actions() method is correctly transferred from the builder to the built Event object without any data loss or transformation.

Execution:
  Arrange: Create a new Event.Builder and an EventActions object with specific configuration.
  Act: Set the actions on the builder using actions() method and call build().
  Assert: Verify that calling actions() on the built Event returns an EventActions object that is equal to the original EventActions object set on the builder.

Validation:
  This assertion verifies the data integrity during the build process. It ensures that the builder correctly transfers the actions field to the Event object without any unintended modifications, cloning, or reference issues. This is fundamental to the builder pattern's contract of accurately constructing objects with the specified properties.

*/

// ********RoostGPT********

package com.google.adk.events;

import static org.junit.jupiter.api.Assertions.*;

import com.google.genai.types.FinishReason;
import java.util.concurrent.ConcurrentHashMap;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderActionsTest {

  private Event.Builder builder;

  private EventActions validEventActions;

  @BeforeEach
  void setUp() {
    builder = Event.builder();
    validEventActions = EventActions.builder().build();
  }

  @Test
  @Tag("valid")
  void actionsWithValidEventActionsObject() {
    EventActions eventActions = EventActions.builder().build();

    Event.Builder result = builder.actions(eventActions);

    assertNotNull(result);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();

    assertNotNull(event.actions());
    assertEquals(eventActions, event.actions());
  }

  @Test
  @Tag("valid")
  void actionsReturnsBuilderInstance() {
    EventActions eventActions = EventActions.builder().build();

    Event.Builder returnedBuilder = builder.actions(eventActions);

    assertSame(builder, returnedBuilder);
  }

  @Test
  @Tag("valid")
  void actionsOverwritesPreviousValue() {
    EventActions firstActions = EventActions.builder().skipSummarization(true).build();
    EventActions secondActions = EventActions.builder().skipSummarization(false).build();

    builder.actions(firstActions);
    builder.actions(secondActions);

    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();

    assertEquals(secondActions, event.actions());
    assertNotEquals(firstActions, event.actions());
  }

  @Test
  @Tag("boundary")
  void actionsDefaultsToEmptyEventActionsWhenNotSet() {
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();

    assertNotNull(event.actions());
    assertEquals(EventActions.builder().build(), event.actions());
  }

  @Test
  @Tag("boundary")
  void actionsWithNullValue() {
    builder.actions(null);

    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();

    assertNotNull(event.actions());
    assertEquals(EventActions.builder().build(), event.actions());
  }

  @Test
  @Tag("valid")
  void actionsInMethodChain() {
    String testId = "chain-test-id";
    String testAuthor = "chain-test-author";
    String testInvocationId = "chain-test-invocation";
    EventActions eventActions = EventActions.builder().skipSummarization(true).build();

    Event event =
        builder
            .id(testId)
            .author(testAuthor)
            .actions(eventActions)
            .invocationId(testInvocationId)
            .build();

    assertEquals(testId, event.id());
    assertEquals(testAuthor, event.author());
    assertEquals(eventActions, event.actions());
    assertEquals(testInvocationId, event.invocationId());
  }

  @Test
  @Tag("integration")
  void actionsWithToBuilderPattern() {
    EventActions originalActions =
        EventActions.builder().skipSummarization(true).escalate(false).build();

    Event originalEvent =
        builder
            .id("original-id")
            .invocationId("original-invocation")
            .author("original-author")
            .actions(originalActions)
            .build();

    EventActions newActions =
        EventActions.builder().skipSummarization(false).escalate(true).build();

    Event modifiedEvent = originalEvent.toBuilder().actions(newActions).build();

    assertEquals(originalActions, originalEvent.actions());
    assertEquals(newActions, modifiedEvent.actions());
    assertNotEquals(originalEvent.actions(), modifiedEvent.actions());
  }

  @Test
  @Tag("valid")
  void actionsPersistsThroughBuildProcess() {
    EventActions eventActions =
        EventActions.builder().skipSummarization(true).escalate(true).endInvocation(false).build();

    builder.actions(eventActions);

    Event event =
        builder
            .id("persist-test-id")
            .invocationId("persist-test-invocation")
            .author("persist-test-author")
            .build();

    assertEquals(eventActions, event.actions());
    assertEquals(eventActions.skipSummarization(), event.actions().skipSummarization());
    assertEquals(eventActions.escalate(), event.actions().escalate());
    assertEquals(eventActions.endInvocation(), event.actions().endInvocation());
  }

  @Test
  @Tag("valid")
  void actionsWithComplexEventActionsConfiguration() {
    ConcurrentHashMap<String, Object> stateDelta = new ConcurrentHashMap<>();
    stateDelta.put("key1", "value1");
    stateDelta.put("key2", 123);

    EventActions complexActions =
        EventActions.builder()
            .skipSummarization(true)
            .escalate(false)
            .endInvocation(true)
            .transferToAgent("agent-123")
            .build();
    complexActions.setStateDelta(stateDelta);

    Event event =
        builder
            .id("complex-test-id")
            .invocationId("complex-test-invocation")
            .author("complex-test-author")
            .actions(complexActions)
            .build();

    assertNotNull(event.actions());
    assertEquals(complexActions, event.actions());
    assertEquals(stateDelta, event.actions().stateDelta());
  }

  @Test
  @Tag("valid")
  void actionsWithMultipleBuilderMethodsInDifferentOrder() {
    EventActions eventActions = EventActions.builder().skipSummarization(false).build();

    Event event =
        builder
            .actions(eventActions)
            .id("order-test-id")
            .author("order-test-author")
            .invocationId("order-test-invocation")
            .partial(true)
            .turnComplete(false)
            .build();

    assertEquals(eventActions, event.actions());
    assertEquals("order-test-id", event.id());
    assertEquals("order-test-author", event.author());
    assertEquals("order-test-invocation", event.invocationId());
    assertTrue(event.partial().isPresent());
    assertTrue(event.partial().get());
  }

  @Test
  @Tag("valid")
  void actionsPreservesBuilderStateAfterMultipleCalls() {
    EventActions firstActions = EventActions.builder().skipSummarization(true).build();
    EventActions secondActions = EventActions.builder().skipSummarization(false).build();
    EventActions thirdActions = EventActions.builder().escalate(true).build();

    builder.actions(firstActions);
    builder.id("state-test-id");
    builder.actions(secondActions);
    builder.author("state-test-author");
    builder.actions(thirdActions);

    Event event = builder.invocationId("state-test-invocation").build();

    assertEquals(thirdActions, event.actions());
    assertEquals("state-test-id", event.id());
    assertEquals("state-test-author", event.author());
  }

  @Test
  @Tag("integration")
  void actionsWithAllEventFieldsSet() {
    EventActions eventActions =
        EventActions.builder().skipSummarization(true).escalate(false).endInvocation(true).build();

    Event event =
        builder
            .id("full-test-id")
            .invocationId("full-test-invocation")
            .author("full-test-author")
            .actions(eventActions)
            .partial(false)
            .turnComplete(true)
            .errorMessage("test error")
            .finishReason(FinishReason.STOP)
            .avgLogprobs(-0.5)
            .interrupted(false)
            .branch("main")
            .modelVersion("v1.0")
            .timestamp(1234567890L)
            .build();

    assertEquals(eventActions, event.actions());
    assertEquals("full-test-id", event.id());
    assertEquals("full-test-invocation", event.invocationId());
    assertEquals("full-test-author", event.author());
    assertTrue(event.partial().isPresent());
    assertFalse(event.partial().get());
    assertTrue(event.turnComplete().isPresent());
    assertTrue(event.turnComplete().get());
  }
}
