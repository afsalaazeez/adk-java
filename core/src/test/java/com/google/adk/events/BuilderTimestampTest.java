/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=timestamp_ee4c7c0053
ROOST_METHOD_SIG_HASH=timestamp_d7acfd5a78

Scenario 1: Setting a positive timestamp value

Details:
  TestName: timestampWithPositiveValue
  Description: This test verifies that the timestamp method correctly sets a positive long value and wraps it in an Optional. It ensures that the builder properly stores the timestamp value and returns the builder instance for method chaining.
Execution:
  Arrange: Create a new Event.Builder instance and define a positive long value representing a valid timestamp (e.g., 1234567890L).
  Act: Invoke the timestamp method with the positive long value and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance (for method chaining). Verify that the timestamp field in the builder is set to Optional.of(1234567890L) by calling the timestamp() getter method.
Validation:
  The assertion verifies that the timestamp method correctly wraps the provided long value in an Optional and stores it in the builder's timestamp field. This is important because the timestamp represents when an event occurred, and the method must properly handle positive timestamp values. The test also confirms that the builder pattern is correctly implemented by returning the same builder instance for method chaining.

Scenario 2: Setting a zero timestamp value

Details:
  TestName: timestampWithZeroValue
  Description: This test checks that the timestamp method can handle a zero value, which represents the Unix epoch (January 1, 1970, 00:00:00 UTC). It verifies that zero is treated as a valid timestamp value.
Execution:
  Arrange: Create a new Event.Builder instance and define a long value of 0L.
  Act: Invoke the timestamp method with the zero value and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance. Verify that the timestamp field in the builder is set to Optional.of(0L) by calling the timestamp() getter method.
Validation:
  The assertion confirms that zero is a valid timestamp value representing the Unix epoch. This is significant because some systems may use epoch time as a baseline or default value. The test ensures that the method doesn't reject zero as an invalid input and properly stores it in an Optional.

Scenario 3: Setting a negative timestamp value

Details:
  TestName: timestampWithNegativeValue
  Description: This test verifies that the timestamp method can handle negative long values, which represent dates before the Unix epoch (before January 1, 1970). It ensures the method doesn't reject negative values.
Execution:
  Arrange: Create a new Event.Builder instance and define a negative long value (e.g., -1234567890L).
  Act: Invoke the timestamp method with the negative long value and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance. Verify that the timestamp field in the builder is set to Optional.of(-1234567890L) by calling the timestamp() getter method.
Validation:
  The assertion verifies that negative timestamp values are accepted and properly stored. This is important for systems that need to represent historical dates before 1970. The test ensures that the method doesn't impose unnecessary restrictions on the timestamp range.

Scenario 4: Setting maximum long value as timestamp

Details:
  TestName: timestampWithMaxLongValue
  Description: This test checks the boundary condition by setting the timestamp to Long.MAX_VALUE (9223372036854775807L). It verifies that the method can handle the maximum possible long value without overflow or errors.
Execution:
  Arrange: Create a new Event.Builder instance and use Long.MAX_VALUE as the timestamp value.
  Act: Invoke the timestamp method with Long.MAX_VALUE and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance. Verify that the timestamp field in the builder is set to Optional.of(Long.MAX_VALUE) by calling the timestamp() getter method.
Validation:
  The assertion confirms that the method can handle extreme boundary values without issues. This is critical for ensuring robustness, as some systems might use very large timestamp values for future dates or special purposes. The test validates that no overflow or data loss occurs.

Scenario 5: Setting minimum long value as timestamp

Details:
  TestName: timestampWithMinLongValue
  Description: This test checks the lower boundary condition by setting the timestamp to Long.MIN_VALUE (-9223372036854775808L). It verifies that the method can handle the minimum possible long value.
Execution:
  Arrange: Create a new Event.Builder instance and use Long.MIN_VALUE as the timestamp value.
  Act: Invoke the timestamp method with Long.MIN_VALUE and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance. Verify that the timestamp field in the builder is set to Optional.of(Long.MIN_VALUE) by calling the timestamp() getter method.
Validation:
  The assertion verifies that the method properly handles the minimum boundary value. This test is important for ensuring that the method doesn't have any implicit restrictions on the lower bound of acceptable timestamp values and that it can store the full range of long values.

Scenario 6: Method chaining with timestamp

Details:
  TestName: timestampMethodChaining
  Description: This test verifies that the timestamp method supports proper method chaining by returning the Builder instance. It ensures that multiple builder methods can be called in sequence.
Execution:
  Arrange: Create a new Event.Builder instance and prepare a timestamp value (e.g., 1000000L) and other builder values like id and author.
  Act: Chain multiple builder method calls including timestamp(1000000L), id("test-id"), and author("test-author") in sequence.
  Assert: Verify that all methods can be chained without errors. Verify that the final builder contains all the set values by calling the respective getter methods.
Validation:
  The assertion confirms that the builder pattern is correctly implemented with proper method chaining support. This is essential for providing a fluent API that allows developers to construct Event objects in a readable and concise manner. The test ensures that the timestamp method doesn't break the chain.

Scenario 7: Overwriting previously set timestamp value

Details:
  TestName: timestampOverwritesPreviousValue
  Description: This test verifies that calling the timestamp method multiple times correctly overwrites the previous value with the new one. It ensures that the latest value is retained.
Execution:
  Arrange: Create a new Event.Builder instance and define two different timestamp values (e.g., 1111111L and 2222222L).
  Act: First invoke timestamp(1111111L), then invoke timestamp(2222222L) on the same builder instance.
  Assert: Verify that the timestamp field in the builder contains Optional.of(2222222L) by calling the timestamp() getter method, confirming the first value was overwritten.
Validation:
  The assertion verifies that the method correctly implements value replacement behavior. This is important because builders should allow developers to modify values before building the final object. The test ensures that there's no accumulation or merging of values, and that the most recent call takes precedence.

Scenario 8: Building Event with set timestamp

Details:
  TestName: buildEventWithSetTimestamp
  Description: This test verifies that when a timestamp is set on the builder and the build() method is called, the resulting Event object contains the specified timestamp value rather than a default value.
Execution:
  Arrange: Create a new Event.Builder instance, set required fields (id, invocationId, author), and set a specific timestamp value (e.g., 5555555L).
  Act: Invoke the timestamp(5555555L) method and then call build() to create an Event instance.
  Assert: Verify that the built Event object's timestamp() method returns 5555555L, confirming the value was properly transferred from the builder to the Event.
Validation:
  The assertion confirms that the timestamp value set in the builder is correctly transferred to the final Event object during the build process. This is crucial for ensuring that the builder pattern works end-to-end and that timestamp values are preserved throughout the object construction lifecycle.

Scenario 9: Setting timestamp with current time value

Details:
  TestName: timestampWithCurrentTimeValue
  Description: This test verifies that the timestamp method can handle realistic timestamp values representing current time, such as those obtained from System.currentTimeMillis() or Instant.now().toEpochMilli().
Execution:
  Arrange: Create a new Event.Builder instance and obtain the current time in milliseconds using Instant.now().toEpochMilli().
  Act: Invoke the timestamp method with the current time value and capture the returned Builder instance.
  Assert: Verify that the returned object is the same Builder instance. Verify that the timestamp field in the builder contains the current time value by calling the timestamp() getter method.
Validation:
  The assertion verifies that the method works correctly with realistic, current timestamp values. This is the most common use case in production code where events are timestamped with the current time. The test ensures that the method integrates properly with Java's time APIs.

Scenario 10: Timestamp value independence from other builder fields

Details:
  TestName: timestampIndependentFromOtherFields
  Description: This test verifies that setting the timestamp value doesn't affect or interfere with other builder fields, and vice versa. It ensures proper field isolation within the builder.
Execution:
  Arrange: Create a new Event.Builder instance and prepare values for multiple fields including timestamp, id, author, and partial.
  Act: Set timestamp(9999999L), id("event-123"), author("test-author"), and partial(true) in various orders.
  Assert: Verify that each field retains its set value independently by calling the respective getter methods. Confirm that timestamp is Optional.of(9999999L), id is "event-123", author is "test-author", and partial is Optional.of(true).
Validation:
  The assertion confirms that the builder maintains proper field isolation and that setting the timestamp doesn't cause side effects on other fields. This is important for ensuring the builder's reliability and predictability, as each field should be independently settable without affecting others.

*/

// ********RoostGPT********

package com.google.adk.events;

import static com.google.common.truth.Truth.assertThat;
import static com.google.common.truth.Truth8.assertThat;

import java.time.Instant;
import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderTimestampTest {

  private Event.Builder builder;

  @BeforeEach
  void setUp() {
    builder = Event.builder();
  }

  @Test
  @Tag("valid")
  void timestampWithPositiveValue() {
    long positiveTimestamp = 1234567890L;
    Event.Builder result = builder.timestamp(positiveTimestamp);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(positiveTimestamp);
  }

  @Test
  @Tag("boundary")
  void timestampWithZeroValue() {
    long zeroTimestamp = 0L;
    Event.Builder result = builder.timestamp(zeroTimestamp);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(zeroTimestamp);
  }

  @Test
  @Tag("valid")
  void timestampWithNegativeValue() {
    long negativeTimestamp = -1234567890L;
    Event.Builder result = builder.timestamp(negativeTimestamp);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(negativeTimestamp);
  }

  @Test
  @Tag("boundary")
  void timestampWithMaxLongValue() {
    long maxTimestamp = Long.MAX_VALUE;
    Event.Builder result = builder.timestamp(maxTimestamp);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(maxTimestamp);
  }

  @Test
  @Tag("boundary")
  void timestampWithMinLongValue() {
    long minTimestamp = Long.MIN_VALUE;
    Event.Builder result = builder.timestamp(minTimestamp);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(minTimestamp);
  }

  @Test
  @Tag("valid")
  void timestampMethodChaining() {
    long timestamp = 1000000L;
    String testId = "test-id";
    String testAuthor = "test-author";
    Event.Builder result = builder.timestamp(timestamp).id(testId).author(testAuthor);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(timestamp);
  }

  @Test
  @Tag("valid")
  void timestampOverwritesPreviousValue() {
    long firstTimestamp = 1111111L;
    long secondTimestamp = 2222222L;
    builder.timestamp(firstTimestamp);
    builder.timestamp(secondTimestamp);
    assertThat(builder.timestamp()).hasValue(secondTimestamp);
  }

  @Test
  @Tag("integration")
  void buildEventWithSetTimestamp() {
    long timestamp = 5555555L;
    String testId = "event-id";
    String testInvocationId = "invocation-id";
    String testAuthor = "test-author";
    Event event =
        builder
            .id(testId)
            .invocationId(testInvocationId)
            .author(testAuthor)
            .timestamp(timestamp)
            .build();
    assertThat(event).isNotNull();
    assertThat(event.timestamp()).isEqualTo(timestamp);
  }

  @Test
  @Tag("valid")
  void timestampWithCurrentTimeValue() {
    long currentTime = Instant.now().toEpochMilli();
    Event.Builder result = builder.timestamp(currentTime);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(currentTime);
  }

  @Test
  @Tag("valid")
  void timestampIndependentFromOtherFields() {
    long timestamp = 9999999L;
    String testId = "event-123";
    String testAuthor = "test-author";
    boolean partialValue = true;
    builder.timestamp(timestamp).id(testId).author(testAuthor).partial(partialValue);
    assertThat(builder.timestamp()).hasValue(timestamp);
    assertThat(builder.partial(Optional.of(partialValue)).partial()).hasValue(partialValue);
  }

  @Test
  @Tag("integration")
  void buildEventWithoutSetTimestampUsesDefaultValue() {
    String testId = "event-id";
    String testInvocationId = "invocation-id";
    String testAuthor = "test-author";
    long beforeBuild = Instant.now().toEpochMilli();
    Event event = builder.id(testId).invocationId(testInvocationId).author(testAuthor).build();
    long afterBuild = Instant.now().toEpochMilli();
    assertThat(event).isNotNull();
    assertThat(event.timestamp()).isAtLeast(beforeBuild);
    assertThat(event.timestamp()).isAtMost(afterBuild);
  }

  @Test
  @Tag("valid")
  void timestampWithMultipleChainedCalls() {
    long timestamp1 = 1000L;
    long timestamp2 = 2000L;
    long timestamp3 = 3000L;
    builder
        .timestamp(timestamp1)
        .id("test-id")
        .timestamp(timestamp2)
        .author("test-author")
        .timestamp(timestamp3);
    assertThat(builder.timestamp()).hasValue(timestamp3);
  }

  @Test
  @Tag("valid")
  void timestampReturnsBuilderForFurtherConfiguration() {
    long timestamp = 7777777L;
    Event.Builder result = builder.timestamp(timestamp);
    assertThat(result).isNotNull();
    assertThat(result).isInstanceOf(Event.Builder.class);
    assertThat(result.timestamp(8888888L)).isSameInstanceAs(result);
  }

  @Test
  @Tag("integration")
  void timestampPreservedThroughBuildCycle() {
    long timestamp = 1234567890123L;
    String testId = "preserve-test-id";
    String testInvocationId = "preserve-invocation-id";
    String testAuthor = "preserve-author";
    Event event =
        builder
            .id(testId)
            .invocationId(testInvocationId)
            .author(testAuthor)
            .timestamp(timestamp)
            .build();
    assertThat(event.timestamp()).isEqualTo(timestamp);
    assertThat(event.id()).isEqualTo(testId);
    assertThat(event.invocationId()).isEqualTo(testInvocationId);
    assertThat(event.author()).isEqualTo(testAuthor);
  }

  @Test
  @Tag("valid")
  void timestampWithRealisticFutureValue() {
    long futureTimestamp = Instant.now().plusSeconds(86400).toEpochMilli();
    Event.Builder result = builder.timestamp(futureTimestamp);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(futureTimestamp);
  }

  @Test
  @Tag("valid")
  void timestampWithRealisticPastValue() {
    long pastTimestamp = Instant.now().minusSeconds(86400).toEpochMilli();
    Event.Builder result = builder.timestamp(pastTimestamp);
    assertThat(result).isSameInstanceAs(builder);
    assertThat(builder.timestamp()).hasValue(pastTimestamp);
  }
}
