/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=partial_be28dbd274
ROOST_METHOD_SIG_HASH=partial_db0ff3ad1c

Scenario 1: Setting partial field with a non-null true value

Details:
  TestName: partialWithTrueValue
  Description: This test verifies that the partial method correctly sets the partial field to an Optional containing true when a non-null Boolean true value is passed as a parameter. It ensures the builder properly wraps the Boolean value in an Optional and returns the builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call the partial method with Boolean.TRUE as the parameter and store the returned builder reference.
  Assert: Verify that the returned builder is the same instance (method chaining works correctly). Verify that the partial field in the builder is set to Optional.of(true).

Validation:
  The assertion verifies that the partial method correctly wraps a non-null true Boolean value in an Optional and maintains the builder pattern by returning the same builder instance. This is important for indicating incomplete chunks from LLM streaming responses, where partial=true signifies that more data is expected.

---

Scenario 2: Setting partial field with a non-null false value

Details:
  TestName: partialWithFalseValue
  Description: This test verifies that the partial method correctly sets the partial field to an Optional containing false when a non-null Boolean false value is passed as a parameter. This represents the final chunk in a streaming response.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call the partial method with Boolean.FALSE as the parameter.
  Assert: Verify that the partial field in the builder is set to Optional.of(false). Verify that the method returns the builder instance for chaining.

Validation:
  The assertion confirms that the partial method properly handles the false value, which is significant as it indicates the last chunk in an LLM streaming response. This is a critical state that signals completion of the streaming data.

---

Scenario 3: Setting partial field with a null value

Details:
  TestName: partialWithNullValue
  Description: This test verifies that the partial method correctly handles null input by setting the partial field to Optional.empty(). This tests the @Nullable annotation behavior and ensures proper null safety.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call the partial method with null as the parameter.
  Assert: Verify that the partial field in the builder is set to Optional.empty(). Verify that the method returns the builder instance.

Validation:
  The assertion validates that null values are properly converted to Optional.empty() using Optional.ofNullable(), which is the expected behavior for optional fields. This ensures null safety and prevents NullPointerException in downstream code that accesses the partial field.

---

Scenario 4: Method chaining capability with partial method

Details:
  TestName: partialMethodChainingCapability
  Description: This test verifies that the partial method supports fluent API design by returning the builder instance, allowing multiple builder methods to be chained together in a single statement.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Chain multiple method calls including partial(true), id("test-id"), and author("test-author").
  Assert: Verify that all methods execute successfully without throwing exceptions. Verify that the partial field is set correctly to Optional.of(true).

Validation:
  The assertion confirms that the @CanIgnoreReturnValue annotation and return type of Builder enable proper method chaining, which is a fundamental aspect of the builder pattern. This improves code readability and allows for concise object construction.

---

Scenario 5: Overwriting previously set partial value with a new value

Details:
  TestName: partialOverwritePreviousValue
  Description: This test verifies that calling the partial method multiple times correctly overwrites the previous value with the new value, ensuring the last call takes precedence.

Execution:
  Arrange: Create a new Event.Builder instance.
  Act: Call partial(true) first, then call partial(false) on the same builder instance.
  Assert: Verify that the partial field contains Optional.of(false), confirming the second call overwrote the first value.

Validation:
  The assertion validates that the builder allows mutable state changes and that subsequent calls to partial replace previous values rather than accumulating them. This is important for scenarios where the partial state needs to be updated during event construction.

---

Scenario 6: Overwriting previously set partial value with null

Details:
  TestName: partialOverwriteWithNull
  Description: This test verifies that calling the partial method with null after setting a non-null value correctly clears the partial field by setting it to Optional.empty().

Execution:
  Arrange: Create a new Event.Builder instance and set partial to true.
  Act: Call partial(null) on the builder instance.
  Assert: Verify that the partial field is set to Optional.empty(), confirming the null value cleared the previous setting.

Validation:
  The assertion confirms that the builder allows resetting optional fields to empty state, which is useful for scenarios where a previously set value needs to be cleared or when building events from templates that need certain fields removed.

---

Scenario 7: Setting partial value and building the Event object

Details:
  TestName: partialValuePersistsInBuiltEvent
  Description: This test verifies that the partial value set in the builder is correctly transferred to the Event object when the build() method is called, ensuring data integrity throughout the building process.

Execution:
  Arrange: Create a new Event.Builder instance and set required fields (id, invocationId, author).
  Act: Call partial(true) on the builder and then call build() to create an Event object.
  Assert: Verify that the built Event object's partial() method returns Optional.of(true).

Validation:
  The assertion validates the complete builder-to-object pipeline, ensuring that the partial field is properly copied from the builder to the immutable Event object. This is critical for the builder pattern to function correctly and maintain data consistency.

---

Scenario 8: Default partial value when not explicitly set

Details:
  TestName: partialDefaultValueWhenNotSet
  Description: This test verifies the default state of the partial field when the partial method is never called on the builder, ensuring it defaults to Optional.empty() as declared in the field initialization.

Execution:
  Arrange: Create a new Event.Builder instance with required fields but without calling the partial method.
  Act: Build the Event object without setting the partial field.
  Assert: Verify that the built Event object's partial() method returns Optional.empty().

Validation:
  The assertion confirms that the default initialization (Optional.empty()) is maintained when the partial method is not called, which is important for backward compatibility and for events that don't require the partial flag to be set.

---

Scenario 9: Setting partial with boxed Boolean object

Details:
  TestName: partialWithBoxedBooleanObject
  Description: This test verifies that the partial method correctly handles boxed Boolean objects (not just primitives), ensuring compatibility with various Boolean representations.

Execution:
  Arrange: Create a new Event.Builder instance and create a Boolean object using Boolean.valueOf(true).
  Act: Call partial with the boxed Boolean object.
  Assert: Verify that the partial field is set to Optional.of(true) and the value is correctly stored.

Validation:
  The assertion validates that the method signature accepting @Nullable Boolean (capital B) properly handles both primitive boolean values (through autoboxing) and Boolean objects, ensuring flexibility in how the method can be called.

---

Scenario 10: Multiple builders with independent partial values

Details:
  TestName: partialIndependentAcrossMultipleBuilders
  Description: This test verifies that multiple Event.Builder instances maintain independent partial field values, ensuring no shared state between different builder instances.

Execution:
  Arrange: Create two separate Event.Builder instances.
  Act: Set partial(true) on the first builder and partial(false) on the second builder.
  Assert: Verify that the first builder's partial field is Optional.of(true) and the second builder's partial field is Optional.of(false), confirming independence.

Validation:
  The assertion confirms that each builder instance maintains its own state without interference from other instances, which is essential for thread safety and correct behavior when multiple events are being constructed simultaneously in the application.

*/

// ********RoostGPT********

package com.google.adk.events;

import static org.junit.jupiter.api.Assertions.*;

import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderPartialTest {

  @Test
  @Tag("valid")
  void partialWithTrueValue() {
    Event.Builder builder = Event.builder();

    Event.Builder returnedBuilder = builder.partial(Boolean.TRUE);

    assertSame(builder, returnedBuilder, "Builder should return same instance for method chaining");
    assertEquals(
        Optional.of(true), getPartialField(builder), "Partial field should be Optional.of(true)");
  }

  @Test
  @Tag("valid")
  void partialWithFalseValue() {
    Event.Builder builder = Event.builder();

    Event.Builder returnedBuilder = builder.partial(Boolean.FALSE);

    assertSame(builder, returnedBuilder, "Builder should return same instance for method chaining");
    assertEquals(
        Optional.of(false), getPartialField(builder), "Partial field should be Optional.of(false)");
  }

  @Test
  @Tag("boundary")
  void partialWithNullValue() {
    Event.Builder builder = Event.builder();

    Event.Builder returnedBuilder = builder.partial(null);

    assertSame(builder, returnedBuilder, "Builder should return same instance for method chaining");
    assertEquals(
        Optional.empty(), getPartialField(builder), "Partial field should be Optional.empty()");
  }

  @Test
  @Tag("valid")
  void partialMethodChainingCapability() {
    Event.Builder builder = Event.builder();

    assertDoesNotThrow(
        () -> {
          builder.partial(true).id("test-id").author("test-author");
        },
        "Method chaining should execute without exceptions");

    assertEquals(
        Optional.of(true), getPartialField(builder), "Partial field should be Optional.of(true)");
  }

  @Test
  @Tag("valid")
  void partialOverwritePreviousValue() {
    Event.Builder builder = Event.builder();

    builder.partial(true);
    builder.partial(false);

    assertEquals(
        Optional.of(false),
        getPartialField(builder),
        "Partial field should be overwritten to Optional.of(false)");
  }

  @Test
  @Tag("boundary")
  void partialOverwriteWithNull() {
    Event.Builder builder = Event.builder();

    builder.partial(true);
    builder.partial(null);

    assertEquals(
        Optional.empty(),
        getPartialField(builder),
        "Partial field should be cleared to Optional.empty()");
  }

  @Test
  @Tag("integration")
  void partialValuePersistsInBuiltEvent() {
    Event.Builder builder =
        Event.builder()
            .id("test-id")
            .invocationId("test-invocation-id")
            .author("test-author")
            .partial(true);

    Event event = builder.build();

    assertNotNull(event, "Built event should not be null");
    assertEquals(
        Optional.of(true),
        event.partial(),
        "Built event should have partial value of Optional.of(true)");
  }

  @Test
  @Tag("boundary")
  void partialDefaultValueWhenNotSet() {
    Event.Builder builder =
        Event.builder().id("test-id").invocationId("test-invocation-id").author("test-author");

    Event event = builder.build();

    assertNotNull(event, "Built event should not be null");
    assertEquals(
        Optional.empty(),
        event.partial(),
        "Built event should have default partial value of Optional.empty()");
  }

  @Test
  @Tag("valid")
  void partialWithBoxedBooleanObject() {
    Event.Builder builder = Event.builder();
    Boolean boxedBoolean = Boolean.valueOf(true);

    builder.partial(boxedBoolean);

    assertEquals(
        Optional.of(true),
        getPartialField(builder),
        "Partial field should correctly handle boxed Boolean object");
  }

  @Test
  @Tag("valid")
  void partialIndependentAcrossMultipleBuilders() {
    Event.Builder builder1 = Event.builder();
    Event.Builder builder2 = Event.builder();

    builder1.partial(true);
    builder2.partial(false);

    assertEquals(
        Optional.of(true),
        getPartialField(builder1),
        "First builder should have partial value of Optional.of(true)");
    assertEquals(
        Optional.of(false),
        getPartialField(builder2),
        "Second builder should have partial value of Optional.of(false)");
  }

  private Optional<Boolean> getPartialField(Event.Builder builder) {
    try {
      java.lang.reflect.Field field = builder.getClass().getDeclaredField("partial");
      field.setAccessible(true);
      return (Optional<Boolean>) field.get(builder);
    } catch (Exception e) {
      fail("Failed to access partial field: " + e.getMessage());
      return Optional.empty();
    }
  }
}
