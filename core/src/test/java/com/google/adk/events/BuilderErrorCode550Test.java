/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=errorCode_9ae152b80a
ROOST_METHOD_SIG_HASH=errorCode_2889de02eb

Scenario 1: Setting errorCode with a non-empty Optional containing a valid FinishReason value

Details:
  TestName: errorCodeWithNonEmptyOptionalContainingValidFinishReason
  Description: This test verifies that the errorCode method correctly sets the errorCode field when provided with a non-empty Optional containing a valid FinishReason value. The method should store the provided Optional and return the Builder instance for method chaining.

Execution:
  Arrange: Create a new Event.Builder instance. Create a non-empty Optional containing a valid FinishReason value (e.g., FinishReason with a specific error state).
  Act: Invoke the errorCode method on the Builder instance, passing the non-empty Optional<FinishReason> as the parameter.
  Assert: Verify that the method returns the same Builder instance (for fluent API chaining). Build the Event object and verify that the errorCode field contains the expected Optional<FinishReason> value using the errorCode() getter method.

Validation:
  This assertion verifies that the Builder correctly stores the errorCode value when a non-empty Optional is provided. This is critical for tracking error states in events, allowing downstream components to identify and handle specific error conditions. The test ensures the builder pattern's fluent interface works correctly by returning the Builder instance.


Scenario 2: Setting errorCode with an empty Optional

Details:
  TestName: errorCodeWithEmptyOptional
  Description: This test validates that the errorCode method properly handles an empty Optional value. The method should accept Optional.empty() and store it in the errorCode field, allowing events to be created without error codes.

Execution:
  Arrange: Create a new Event.Builder instance. Create an empty Optional using Optional.empty().
  Act: Call the errorCode method with the empty Optional<FinishReason> parameter.
  Assert: Verify that the method returns the Builder instance. Build the Event and confirm that the errorCode() method returns an empty Optional using Optional.isEmpty() or asserting that Optional.isPresent() returns false.

Validation:
  This test ensures that the Builder can handle cases where no error code is present, which is the normal state for successful operations. The empty Optional pattern is a standard way to represent the absence of a value in Java, and this test confirms the Builder respects this convention. This is important for events that complete successfully without errors.


Scenario 3: Setting errorCode multiple times with different values

Details:
  TestName: errorCodeSetMultipleTimesWithDifferentValues
  Description: This test checks the behavior when the errorCode method is called multiple times on the same Builder instance with different FinishReason values. The last value set should be the one retained in the final Event object.

Execution:
  Arrange: Create a new Event.Builder instance. Create two different Optional<FinishReason> values (e.g., Optional.of(firstFinishReason) and Optional.of(secondFinishReason)).
  Act: Call errorCode with the first Optional<FinishReason>, then call errorCode again with the second Optional<FinishReason>.
  Assert: Build the Event object and verify that the errorCode field contains only the second FinishReason value, confirming that the last set value overwrites previous values.

Validation:
  This test validates the Builder's behavior when errorCode is set multiple times, ensuring that the most recent value takes precedence. This is important for scenarios where error states might be updated during event construction, and the final state should reflect the most current error condition. This behavior is consistent with standard builder pattern implementations.


Scenario 4: Setting errorCode to empty Optional after setting a non-empty value

Details:
  TestName: errorCodeSetToEmptyAfterNonEmptyValue
  Description: This test verifies that the errorCode field can be cleared by setting it to an empty Optional after it has been set to a non-empty value. This simulates scenarios where an error condition might be resolved or cleared during event construction.

Execution:
  Arrange: Create a new Event.Builder instance. Create a non-empty Optional<FinishReason> with a valid FinishReason value.
  Act: First, call errorCode with the non-empty Optional. Then, call errorCode again with Optional.empty().
  Assert: Build the Event and verify that the errorCode() method returns an empty Optional, confirming that the field was successfully cleared.

Validation:
  This assertion confirms that the Builder allows resetting the errorCode field to an empty state after it has been populated. This flexibility is important for dynamic event construction scenarios where error states might be conditionally set or cleared. It ensures the Builder provides full control over the errorCode field's state.


Scenario 5: Chaining errorCode with other Builder methods

Details:
  TestName: errorCodeMethodChainingWithOtherBuilderMethods
  Description: This test validates that the errorCode method can be properly chained with other Builder methods, confirming the fluent API design. The test ensures that setting errorCode doesn't break the method chaining pattern.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare test values for multiple fields including a FinishReason for errorCode, a String for id, a String for author, and an Optional<String> for errorMessage.
  Act: Chain multiple Builder method calls together, including errorCode, in a single statement (e.g., builder.id("test-id").author("test-author").errorCode(Optional.of(finishReason)).errorMessage(Optional.of("error message"))).
  Assert: Build the Event object and verify that all chained values were correctly set, including the errorCode field containing the expected FinishReason value.

Validation:
  This test ensures the Builder's fluent interface works correctly when errorCode is used in combination with other setter methods. Method chaining is a key feature of the builder pattern, improving code readability and usability. This test confirms that errorCode integrates seamlessly into the fluent API without breaking the chain.


Scenario 6: Setting errorCode with null Optional reference

Details:
  TestName: errorCodeWithNullOptionalReference
  Description: This test examines the behavior when a null Optional reference is passed to the errorCode method. While the method signature accepts Optional<FinishReason>, passing null directly tests the robustness of the implementation.

Execution:
  Arrange: Create a new Event.Builder instance. Prepare a null reference for Optional<FinishReason>.
  Act: Call the errorCode method with the null Optional reference.
  Assert: Build the Event and verify the behavior - either the errorCode field is set to null, or verify if a NullPointerException is thrown, depending on the implementation's null-handling strategy.

Validation:
  This test validates the Builder's null-handling behavior for the errorCode field. While Optional is designed to avoid null references, this test ensures the implementation handles edge cases where null might be passed. The expected behavior depends on whether the implementation includes null-checking logic or allows null values to be stored.


Scenario 7: Building Event with only errorCode set

Details:
  TestName: buildEventWithOnlyErrorCodeSet
  Description: This test verifies that an Event can be successfully built when only the errorCode field is explicitly set, with all other fields using their default values. This tests the Builder's ability to create valid Event objects with minimal configuration.

Execution:
  Arrange: Create a new Event.Builder instance. Create an Optional<FinishReason> with a valid FinishReason value.
  Act: Call only the errorCode method on the Builder, then call build() to create the Event object.
  Assert: Verify that the Event object is successfully created and that the errorCode field contains the expected value. Also verify that other fields have their default values (e.g., actions is not null due to default initialization in build(), timestamp is set to current time).

Validation:
  This assertion confirms that the Builder can create valid Event objects even when only errorCode is explicitly set. This is important for scenarios where events need to be created quickly with minimal information, particularly for error reporting. The test also validates that the build() method properly initializes default values for fields not explicitly set.


Scenario 8: Verifying errorCode immutability after building Event

Details:
  TestName: errorCodeImmutabilityAfterBuildingEvent
  Description: This test ensures that the errorCode value in the built Event object is independent of subsequent changes to the Builder. This validates that the Builder creates a proper copy of the errorCode value during the build process.

Execution:
  Arrange: Create a new Event.Builder instance. Create an Optional<FinishReason> with a specific FinishReason value and set it using errorCode.
  Act: Build the first Event object. Then, call errorCode on the same Builder with a different FinishReason value and build a second Event object.
  Assert: Verify that the first Event's errorCode still contains the original FinishReason value, while the second Event contains the new value. This confirms that modifying the Builder after building doesn't affect previously built objects.

Validation:
  This test validates the immutability principle of the builder pattern, ensuring that once an Event is built, its errorCode value cannot be affected by subsequent Builder operations. This is crucial for thread safety and preventing unintended side effects when reusing Builder instances. It confirms proper encapsulation and data isolation between built objects.


Scenario 9: Setting errorCode in combination with finishReason

Details:
  TestName: errorCodeSetWithFinishReason
  Description: This test validates that both errorCode and finishReason can be set independently on the same Event, as they are separate Optional<FinishReason> fields. This tests the Builder's ability to handle multiple similar-typed fields correctly.

Execution:
  Arrange: Create a new Event.Builder instance. Create two different Optional<FinishReason> values - one for errorCode and one for finishReason.
  Act: Call errorCode with the first Optional<FinishReason> and finishReason with the second Optional<FinishReason> on the Builder.
  Assert: Build the Event and verify that both errorCode() and finishReason() return their respective values, confirming that the two fields are independently maintained.

Validation:
  This assertion ensures that the Builder correctly distinguishes between the errorCode and finishReason fields, even though they share the same type. This is important because the Event class uses both fields for different purposes - errorCode for error states and finishReason for completion states. The test confirms no field collision or confusion occurs during building.


Scenario 10: Using errorCode with toBuilder pattern

Details:
  TestName: errorCodePreservedInToBuilderPattern
  Description: This test verifies that the errorCode value is correctly preserved when using the toBuilder() method to create a new Builder from an existing Event. This validates the round-trip conversion between Event and Builder.

Execution:
  Arrange: Create an Event.Builder instance and set errorCode with a specific Optional<FinishReason> value. Build the Event object.
  Act: Call toBuilder() on the Event object to create a new Builder instance. Build a second Event from this new Builder without modifying any fields.
  Assert: Verify that the second Event's errorCode field contains the same FinishReason value as the original Event, confirming that toBuilder() correctly copies the errorCode field.

Validation:
  This test ensures that the toBuilder() method properly preserves the errorCode field when creating a Builder from an existing Event. This is critical for scenarios where Events need to be modified or cloned, as it ensures data integrity during the conversion process. The test validates the bidirectional relationship between Event and Builder.

*/

// ********RoostGPT********

package com.google.adk.events;

import static com.google.common.truth.Truth.assertThat;

import com.google.genai.types.FinishReason;
import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

public class BuilderErrorCode550Test {

  private Event.Builder builder;

  private FinishReason firstFinishReason;

  private FinishReason secondFinishReason;

  @BeforeEach
  public void setUp() {
    builder = Event.builder();
    firstFinishReason = FinishReason.STOP;
    secondFinishReason = FinishReason.MAX_TOKENS;
  }

  @Test
  @Tag("valid")
  public void errorCodeWithNonEmptyOptionalContainingValidFinishReason() {
    Optional<FinishReason> errorCodeValue = Optional.of(firstFinishReason);
    Event.Builder result = builder.errorCode(errorCodeValue);
    assertThat(result).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.errorCode()).isPresent();
    assertThat(event.errorCode().get()).isEqualTo(firstFinishReason);
  }

  @Test
  @Tag("valid")
  public void errorCodeWithEmptyOptional() {
    Optional<FinishReason> emptyOptional = Optional.empty();
    Event.Builder result = builder.errorCode(emptyOptional);
    assertThat(result).isSameInstanceAs(builder);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.errorCode()).isEmpty();
  }

  @Test
  @Tag("valid")
  public void errorCodeSetMultipleTimesWithDifferentValues() {
    Optional<FinishReason> firstValue = Optional.of(firstFinishReason);
    Optional<FinishReason> secondValue = Optional.of(secondFinishReason);
    builder.errorCode(firstValue);
    builder.errorCode(secondValue);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.errorCode()).isPresent();
    assertThat(event.errorCode().get()).isEqualTo(secondFinishReason);
    assertThat(event.errorCode().get()).isNotEqualTo(firstFinishReason);
  }

  @Test
  @Tag("valid")
  public void errorCodeSetToEmptyAfterNonEmptyValue() {
    Optional<FinishReason> nonEmptyValue = Optional.of(firstFinishReason);
    Optional<FinishReason> emptyValue = Optional.empty();
    builder.errorCode(nonEmptyValue);
    builder.errorCode(emptyValue);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.errorCode()).isEmpty();
  }

  @Test
  @Tag("valid")
  public void errorCodeMethodChainingWithOtherBuilderMethods() {
    Optional<FinishReason> errorCodeValue = Optional.of(firstFinishReason);
    Optional<String> errorMessageValue = Optional.of("Test error message");
    Event event =
        builder
            .id("test-id")
            .author("test-author")
            .invocationId("test-invocation")
            .errorCode(errorCodeValue)
            .errorMessage(errorMessageValue)
            .partial(true)
            .build();
    assertThat(event.id()).isEqualTo("test-id");
    assertThat(event.author()).isEqualTo("test-author");
    assertThat(event.invocationId()).isEqualTo("test-invocation");
    assertThat(event.errorCode()).isPresent();
    assertThat(event.errorCode().get()).isEqualTo(firstFinishReason);
    assertThat(event.errorMessage()).isPresent();
    assertThat(event.errorMessage().get()).isEqualTo("Test error message");
    assertThat(event.partial()).isPresent();
    assertThat(event.partial().get()).isTrue();
  }

  @Test
  @Tag("invalid")
  public void errorCodeWithNullOptionalReference() {
    Optional<FinishReason> nullOptional = null;
    builder.errorCode(nullOptional);
    Event event =
        builder.id("test-id").invocationId("test-invocation").author("test-author").build();
    assertThat(event.errorCode()).isNull();
  }

  @Test
  @Tag("boundary")
  public void buildEventWithOnlyErrorCodeSet() {
    Optional<FinishReason> errorCodeValue = Optional.of(firstFinishReason);
    Event event = builder.errorCode(errorCodeValue).build();
    assertThat(event.errorCode()).isPresent();
    assertThat(event.errorCode().get()).isEqualTo(firstFinishReason);
    assertThat(event.actions()).isNotNull();
    assertThat(event.timestamp()).isNotNull();
    assertThat(event.timestamp()).isGreaterThan(0L);
  }

  @Test
  @Tag("valid")
  public void errorCodeImmutabilityAfterBuildingEvent() {
    Optional<FinishReason> firstValue = Optional.of(firstFinishReason);
    Optional<FinishReason> secondValue = Optional.of(secondFinishReason);
    builder.errorCode(firstValue);
    Event firstEvent =
        builder.id("first-id").invocationId("first-invocation").author("first-author").build();
    builder.errorCode(secondValue);
    Event secondEvent =
        builder.id("second-id").invocationId("second-invocation").author("second-author").build();
    assertThat(firstEvent.errorCode()).isPresent();
    assertThat(firstEvent.errorCode().get()).isEqualTo(firstFinishReason);
    assertThat(secondEvent.errorCode()).isPresent();
    assertThat(secondEvent.errorCode().get()).isEqualTo(secondFinishReason);
  }

  @Test
  @Tag("valid")
  public void errorCodeSetWithFinishReason() {
    Optional<FinishReason> errorCodeValue = Optional.of(firstFinishReason);
    Optional<FinishReason> finishReasonValue = Optional.of(secondFinishReason);
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation")
            .author("test-author")
            .errorCode(errorCodeValue)
            .finishReason(finishReasonValue)
            .build();
    assertThat(event.errorCode()).isPresent();
    assertThat(event.errorCode().get()).isEqualTo(firstFinishReason);
    assertThat(event.finishReason()).isPresent();
    assertThat(event.finishReason().get()).isEqualTo(secondFinishReason);
    assertThat(event.errorCode().get()).isNotEqualTo(event.finishReason().get());
  }

  @Test
  @Tag("integration")
  public void errorCodePreservedInToBuilderPattern() {
    Optional<FinishReason> errorCodeValue = Optional.of(firstFinishReason);
    Event originalEvent =
        builder
            .id("original-id")
            .invocationId("original-invocation")
            .author("original-author")
            .errorCode(errorCodeValue)
            .build();
    Event.Builder newBuilder = originalEvent.toBuilder();
    Event newEvent = newBuilder.build();
    assertThat(newEvent.errorCode()).isPresent();
    assertThat(newEvent.errorCode().get()).isEqualTo(firstFinishReason);
    assertThat(newEvent.errorCode()).isEqualTo(originalEvent.errorCode());
  }

  @Test
  @Tag("valid")
  public void errorCodeWithDifferentFinishReasonValues() {
    FinishReason[] finishReasons = {
      FinishReason.STOP,
      FinishReason.MAX_TOKENS,
      FinishReason.SAFETY,
      FinishReason.RECITATION,
      FinishReason.OTHER
    };
    for (FinishReason reason : finishReasons) {
      Event.Builder testBuilder = Event.builder();
      Optional<FinishReason> errorCodeValue = Optional.of(reason);
      Event event =
          testBuilder
              .id("test-id-" + reason)
              .invocationId("test-invocation")
              .author("test-author")
              .errorCode(errorCodeValue)
              .build();
      assertThat(event.errorCode()).isPresent();
      assertThat(event.errorCode().get()).isEqualTo(reason);
    }
  }

  @Test
  @Tag("valid")
  public void errorCodeReturnsSameBuilderInstance() {
    Optional<FinishReason> errorCodeValue = Optional.of(firstFinishReason);
    Event.Builder returnedBuilder = builder.errorCode(errorCodeValue);
    assertThat(returnedBuilder).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  public void errorCodeWithCompleteEventConfiguration() {
    Optional<FinishReason> errorCodeValue = Optional.of(firstFinishReason);
    Optional<String> errorMessageValue = Optional.of("Complete error message");
    Optional<Boolean> partialValue = Optional.of(true);
    Optional<Boolean> turnCompleteValue = Optional.of(false);
    Event event =
        builder
            .id("complete-id")
            .invocationId("complete-invocation")
            .author("complete-author")
            .errorCode(errorCodeValue)
            .errorMessage(errorMessageValue)
            .partial(partialValue)
            .turnComplete(turnCompleteValue)
            .interrupted(false)
            .build();
    assertThat(event.errorCode()).isPresent();
    assertThat(event.errorCode().get()).isEqualTo(firstFinishReason);
    assertThat(event.errorMessage()).isPresent();
    assertThat(event.errorMessage().get()).isEqualTo("Complete error message");
    assertThat(event.partial()).isPresent();
    assertThat(event.partial().get()).isTrue();
    assertThat(event.turnComplete()).isPresent();
    assertThat(event.turnComplete().get()).isFalse();
  }

  @Test
  @Tag("boundary")
  public void errorCodeWithEmptyOptionalAndOtherFields() {
    Optional<FinishReason> emptyErrorCode = Optional.empty();
    Optional<String> errorMessage = Optional.of("Error without code");
    Event event =
        builder
            .id("test-id")
            .invocationId("test-invocation")
            .author("test-author")
            .errorCode(emptyErrorCode)
            .errorMessage(errorMessage)
            .build();
    assertThat(event.errorCode()).isEmpty();
    assertThat(event.errorMessage()).isPresent();
    assertThat(event.errorMessage().get()).isEqualTo("Error without code");
  }

  @Test
  @Tag("valid")
  public void errorCodeMultipleBuilderInstancesIndependence() {
    Event.Builder builder1 = Event.builder();
    Event.Builder builder2 = Event.builder();
    Optional<FinishReason> errorCode1 = Optional.of(firstFinishReason);
    Optional<FinishReason> errorCode2 = Optional.of(secondFinishReason);
    builder1.errorCode(errorCode1);
    builder2.errorCode(errorCode2);
    Event event1 = builder1.id("id-1").invocationId("invocation-1").author("author-1").build();
    Event event2 = builder2.id("id-2").invocationId("invocation-2").author("author-2").build();
    assertThat(event1.errorCode()).isPresent();
    assertThat(event1.errorCode().get()).isEqualTo(firstFinishReason);
    assertThat(event2.errorCode()).isPresent();
    assertThat(event2.errorCode().get()).isEqualTo(secondFinishReason);
  }
}
