/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=author_76f647477c
ROOST_METHOD_SIG_HASH=author_76f647477c

Scenario 1: Setting a Valid Non-Null Author String

Details:
  TestName: authorWithValidNonNullString
  Description: This test verifies that the author() method correctly accepts and stores a valid non-null string value. The method should successfully set the author field when provided with a standard string containing alphanumeric characters.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a valid author string such as "John Doe".
  Act: Invoke the author() method on the builder instance with the prepared author string.
  Assert: Build the MemoryEntry object and verify that the author() getter returns the exact string that was set.

Validation:
  The assertion verifies that the builder correctly stores and returns the author value without modification. This is essential for maintaining data integrity when creating memory entries with author attribution. This test ensures the basic functionality of the author setter works as expected for typical use cases.


Scenario 2: Setting Author to Null Value

Details:
  TestName: authorWithNullValue
  Description: This test validates that the author() method correctly handles null values, as indicated by the @Nullable annotation. The method should accept null without throwing exceptions and store it appropriately.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance.
  Act: Invoke the author() method with a null parameter.
  Assert: Build the MemoryEntry object and verify that the author() getter returns null.

Validation:
  This assertion confirms that the builder properly handles optional author information. Since the @Nullable annotation is present, the method must accept null values gracefully. This test is critical for scenarios where author information is not available or not applicable, ensuring the builder doesn't enforce mandatory author attribution.


Scenario 3: Setting Author to Empty String

Details:
  TestName: authorWithEmptyString
  Description: This test checks the behavior when an empty string is passed to the author() method. It verifies that the method accepts empty strings as valid input, distinguishing between null and empty string values.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare an empty string ("").
  Act: Call the author() method with the empty string.
  Assert: Build the MemoryEntry and verify that author() returns an empty string, not null.

Validation:
  The assertion ensures that empty strings are treated as valid values distinct from null. This is important for scenarios where an explicit empty author field is meaningful (e.g., indicating an anonymous author versus unknown author). This test validates proper handling of edge cases in string input.


Scenario 4: Setting Author with Whitespace Only String

Details:
  TestName: authorWithWhitespaceOnlyString
  Description: This test examines how the author() method handles strings containing only whitespace characters (spaces, tabs, newlines). It verifies that such strings are accepted and stored without trimming or validation.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a string containing only whitespace such as "   " or "\t\n".
  Act: Invoke the author() method with the whitespace-only string.
  Assert: Build the MemoryEntry and verify that author() returns the exact whitespace string that was provided.

Validation:
  This assertion confirms that the method does not perform automatic trimming or validation of input strings. It stores the value as-is, which is important for understanding the method's contract. This test helps identify whether additional validation is needed at the application level.


Scenario 5: Setting Author with Special Characters

Details:
  TestName: authorWithSpecialCharacters
  Description: This test validates that the author() method correctly handles strings containing special characters, including Unicode characters, symbols, and punctuation marks.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a string with special characters such as "José García-Martínez @#$%".
  Act: Call the author() method with the special character string.
  Assert: Build the MemoryEntry and verify that author() returns the exact string including all special characters.

Validation:
  The assertion ensures that the method properly handles international characters and special symbols without corruption or loss of data. This is crucial for supporting global users with names containing diacritics, non-Latin scripts, or other special characters. This test validates Unicode support and data preservation.


Scenario 6: Setting Author with Very Long String

Details:
  TestName: authorWithVeryLongString
  Description: This test checks the behavior when a very long string is passed to the author() method. It verifies that the method can handle strings of significant length without truncation or errors.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Generate a very long string (e.g., 10,000 characters) containing repeated text.
  Act: Invoke the author() method with the long string.
  Assert: Build the MemoryEntry and verify that author() returns the complete string with the correct length.

Validation:
  This assertion confirms that there are no implicit length restrictions in the author field. It's important to understand the method's behavior with large inputs to prevent potential data loss or performance issues. This test helps identify if any length validation or limits should be implemented at the application level.


Scenario 7: Overwriting Previously Set Author Value

Details:
  TestName: authorOverwritesPreviousValue
  Description: This test verifies that calling the author() method multiple times on the same builder instance correctly overwrites the previous value with the new one.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Set an initial author value such as "First Author".
  Act: Call the author() method again with a different value such as "Second Author".
  Assert: Build the MemoryEntry and verify that author() returns only the second value ("Second Author").

Validation:
  The assertion ensures that the builder follows the expected pattern where subsequent calls to the same setter method replace previous values rather than accumulating them. This is critical for builder pattern correctness and prevents unexpected behavior when modifying builder state.


Scenario 8: Overwriting Non-Null Author with Null

Details:
  TestName: authorOverwriteNonNullWithNull
  Description: This test validates that a previously set non-null author value can be overwritten with null, effectively clearing the author field.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Set an initial author value such as "Initial Author".
  Act: Call the author() method with null to clear the value.
  Assert: Build the MemoryEntry and verify that author() returns null.

Validation:
  This assertion confirms that the builder allows clearing previously set values by passing null. This is important for scenarios where author information needs to be removed or reset during the building process. It validates the flexibility of the builder pattern in modifying field values.


Scenario 9: Builder Method Chaining Returns Builder Instance

Details:
  TestName: authorReturnsBuilderForChaining
  Description: This test verifies that the author() method returns the Builder instance itself, enabling method chaining as is standard in the builder pattern.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance.
  Act: Call the author() method with a valid string value and capture the return value.
  Assert: Verify that the returned object is the same Builder instance (using reference equality or type checking).

Validation:
  The assertion ensures that the method follows the fluent interface pattern, allowing developers to chain multiple builder method calls in a single statement. This is a fundamental aspect of the builder pattern that improves code readability and usability. This test validates the method signature and return type behavior.


Scenario 10: Setting Author in Complete Builder Chain

Details:
  TestName: authorInCompleteBuilderChain
  Description: This test validates that the author() method works correctly when used as part of a complete builder chain with other builder methods, ensuring proper integration with the overall builder pattern.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a Content object for the required content field and an author string.
  Act: Chain multiple builder methods including content(), author(), and build() in a single statement.
  Assert: Verify that the resulting MemoryEntry has the correct author value set.

Validation:
  This assertion confirms that the author() method integrates properly with other builder methods and doesn't interfere with the building process. It validates the complete workflow of creating a MemoryEntry with author information, which is how the method will typically be used in production code. This test ensures end-to-end functionality.


Scenario 11: Setting Author with Newline Characters

Details:
  TestName: authorWithNewlineCharacters
  Description: This test examines how the author() method handles strings containing newline characters (\n, \r, \r\n), which might be present in multi-line author information.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a string containing newline characters such as "First Line\nSecond Line".
  Act: Call the author() method with the string containing newlines.
  Assert: Build the MemoryEntry and verify that author() returns the string with newlines preserved.

Validation:
  The assertion ensures that newline characters are preserved in the author field without being stripped or converted. This is important for understanding how the method handles control characters and whether any sanitization occurs. This test helps identify potential issues with multi-line text handling.


Scenario 12: Setting Author with Leading and Trailing Spaces

Details:
  TestName: authorWithLeadingAndTrailingSpaces
  Description: This test verifies the behavior when the author string has leading and/or trailing spaces, checking whether the method preserves or trims these spaces.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a string with leading and trailing spaces such as "  Author Name  ".
  Act: Invoke the author() method with the padded string.
  Assert: Build the MemoryEntry and verify that author() returns the string with spaces preserved.

Validation:
  This assertion confirms whether the method performs automatic trimming of whitespace. Understanding this behavior is crucial for data consistency and validation requirements. If spaces are preserved, it indicates that input validation should be performed at a higher level if needed.


Scenario 13: Setting Author with JSON Special Characters

Details:
  TestName: authorWithJsonSpecialCharacters
  Description: This test validates that the author() method correctly handles strings containing characters that have special meaning in JSON (quotes, backslashes, etc.), given that the class uses Jackson annotations for JSON serialization.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a string with JSON special characters such as "Author \"Nickname\" \\Name\\".
  Act: Call the author() method with the string containing JSON special characters.
  Assert: Build the MemoryEntry and verify that author() returns the exact string with all special characters intact.

Validation:
  The assertion ensures that the method stores the raw string value without performing JSON escaping or unescaping at the builder level. This is important because JSON serialization should be handled by Jackson during actual serialization, not during object construction. This test validates proper separation of concerns.


Scenario 14: Setting Author Multiple Times in Sequence

Details:
  TestName: authorSetMultipleTimesInSequence
  Description: This test verifies the behavior when the author() method is called multiple times consecutively with different values, ensuring that only the last value is retained.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance.
  Act: Call the author() method three times in sequence with different values: "First", "Second", "Third".
  Assert: Build the MemoryEntry and verify that author() returns only "Third".

Validation:
  This assertion confirms that the builder maintains only the most recent value for the author field, which is the expected behavior for setter methods in the builder pattern. This test ensures there's no accumulation or concatenation of values, which would be incorrect behavior.


Scenario 15: Setting Author with Single Character

Details:
  TestName: authorWithSingleCharacter
  Description: This test checks the behavior when a single-character string is passed to the author() method, verifying that minimal-length strings are handled correctly.

Execution:
  Arrange: Create a new MemoryEntry.Builder instance. Prepare a single-character string such as "A".
  Act: Invoke the author() method with the single-character string.
  Assert: Build the MemoryEntry and verify that author() returns the single character.

Validation:
  The assertion ensures that there are no minimum length requirements for the author field. This is important for edge cases and validates that the method accepts any valid string regardless of length. This test helps ensure flexibility in the data model.

*/

// ********RoostGPT********

package com.google.adk.memory;
import com.google.genai.types.Content;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import static com.google.common.truth.Truth.assertThat;
import org.junit.jupiter.api.*;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.google.auto.value.AutoValue;
import java.time.Instant;
import javax.annotation.Nullable;

class BuilderAuthorTest {
    @Test
    @Tag("valid")
    void authorWithValidNonNullString() {
        // Arrange
        String authorName = "John Doe";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(authorName);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(authorName);
    }
    @Test
    @Tag("valid")
    void authorWithNullValue() {
        // Arrange
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(null);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isNull();
    }
    @Test
    @Tag("boundary")
    void authorWithEmptyString() {
        // Arrange
        String emptyAuthor = "";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(emptyAuthor);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo("");
        assertThat(memoryEntry.author()).isNotNull();
    }
    @Test
    @Tag("boundary")
    void authorWithWhitespaceOnlyString() {
        // Arrange
        String whitespaceAuthor = "   ";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(whitespaceAuthor);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo("   ");
    }
    @Test
    @Tag("valid")
    void authorWithSpecialCharacters() {
        // Arrange
        String specialCharAuthor = "José García-Martínez @#$%";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(specialCharAuthor);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(specialCharAuthor);
    }
    @Test
    @Tag("boundary")
    void authorWithVeryLongString() {
        // Arrange
        String longAuthor = "A".repeat(10000);
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(longAuthor);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(longAuthor);
        assertThat(memoryEntry.author().length()).isEqualTo(10000);
    }
    @Test
    @Tag("valid")
    void authorOverwritesPreviousValue() {
        // Arrange
        String firstAuthor = "First Author";
        String secondAuthor = "Second Author";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(firstAuthor);
        builder.author(secondAuthor);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(secondAuthor);
        assertThat(memoryEntry.author()).isNotEqualTo(firstAuthor);
    }
    @Test
    @Tag("valid")
    void authorOverwriteNonNullWithNull() {
        // Arrange
        String initialAuthor = "Initial Author";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(initialAuthor);
        builder.author(null);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isNull();
    }
    @Test
    @Tag("valid")
    void authorReturnsBuilderForChaining() {
        // Arrange
        String authorName = "Test Author";
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        MemoryEntry.Builder returnedBuilder = builder.author(authorName);
        
        // Assert
        assertThat(returnedBuilder).isSameInstanceAs(builder);
        assertThat(returnedBuilder).isInstanceOf(MemoryEntry.Builder.class);
    }
    @Test
    @Tag("integration")
    void authorInCompleteBuilderChain() {
        // Arrange
        Content mockContent = Content.newBuilder().build();
        String authorName = "Chain Author";
        
        // Act
        MemoryEntry memoryEntry = MemoryEntry.builder()
                .content(mockContent)
                .author(authorName)
                .timestamp("2024-01-01T00:00:00Z")
                .build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(authorName);
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
        assertThat(memoryEntry.timestamp()).isEqualTo("2024-01-01T00:00:00Z");
    }
    @Test
    @Tag("boundary")
    void authorWithNewlineCharacters() {
        // Arrange
        String authorWithNewlines = "First Line\nSecond Line";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(authorWithNewlines);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(authorWithNewlines);
        assertThat(memoryEntry.author()).contains("\n");
    }
    @Test
    @Tag("boundary")
    void authorWithLeadingAndTrailingSpaces() {
        // Arrange
        String paddedAuthor = "  Author Name  ";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(paddedAuthor);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(paddedAuthor);
        assertThat(memoryEntry.author()).startsWith("  ");
        assertThat(memoryEntry.author()).endsWith("  ");
    }
    @Test
    @Tag("valid")
    void authorWithJsonSpecialCharacters() {
        // Arrange
        String jsonSpecialAuthor = "Author \"Nickname\" \\Name\\";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(jsonSpecialAuthor);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo(jsonSpecialAuthor);
        assertThat(memoryEntry.author()).contains("\"");
        assertThat(memoryEntry.author()).contains("\\");
    }
    @Test
    @Tag("valid")
    void authorSetMultipleTimesInSequence() {
        // Arrange
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author("First");
        builder.author("Second");
        builder.author("Third");
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo("Third");
        assertThat(memoryEntry.author()).isNotEqualTo("First");
        assertThat(memoryEntry.author()).isNotEqualTo("Second");
    }
    @Test
    @Tag("boundary")
    void authorWithSingleCharacter() {
        // Arrange
        String singleChar = "A";
        Content mockContent = Content.newBuilder().build();
        
        // Act
        MemoryEntry.Builder builder = MemoryEntry.builder();
        builder.content(mockContent);
        builder.author(singleChar);
        MemoryEntry memoryEntry = builder.build();
        
        // Assert
        assertThat(memoryEntry.author()).isEqualTo("A");
        assertThat(memoryEntry.author().length()).isEqualTo(1);
    }
}