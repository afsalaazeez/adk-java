/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=content_330637edff
ROOST_METHOD_SIG_HASH=content_330637edff

Scenario 1: Setting Valid Content Object

Details:
  TestName: contentWithValidContentObject
  Description: This test verifies that the content() method correctly accepts and stores a valid Content object in the Builder. It ensures that the builder can be populated with a non-null Content instance and that the built MemoryEntry contains the expected content.
Execution:
  Arrange: Create a valid Content object instance that will be passed to the builder.
  Act: Invoke the content() method on the Builder instance with the valid Content object, then call build() to create a MemoryEntry.
  Assert: Use JUnit assertions to verify that the returned MemoryEntry's content() method returns the same Content object that was set.
Validation:
  The assertion verifies that the builder correctly stores and retrieves the Content object. This is critical because content is a required field for MemoryEntry, and the application must ensure that memory entries always have associated content. This test validates the basic happy path for setting content in the builder pattern.

Scenario 2: Builder Method Chaining with Content

Details:
  TestName: contentReturnsBuilderForMethodChaining
  Description: This test ensures that the content() method returns a Builder instance, enabling fluent API method chaining. This is a common pattern in builder implementations where each setter method returns the builder itself.
Execution:
  Arrange: Create a valid Content object and obtain a Builder instance.
  Act: Invoke the content() method with the Content object and capture the return value.
  Assert: Use JUnit assertions to verify that the returned object is an instance of Builder and is not null.
Validation:
  This assertion confirms that the builder follows the fluent interface pattern, allowing developers to chain multiple setter methods together (e.g., builder.content(content).author(author).build()). This improves code readability and is a standard practice in builder pattern implementations.

Scenario 3: Setting Content Multiple Times (Last Value Wins)

Details:
  TestName: contentOverwritesPreviousValue
  Description: This test verifies the behavior when content() is called multiple times on the same Builder instance. It ensures that the last value set is the one that persists in the final MemoryEntry object.
Execution:
  Arrange: Create two different Content objects (content1 and content2) and obtain a Builder instance.
  Act: Call content(content1) followed by content(content2) on the same builder, then build the MemoryEntry.
  Assert: Use JUnit assertions to verify that the built MemoryEntry contains content2, not content1.
Validation:
  This test validates that the builder correctly handles multiple invocations of the same setter method, with the last call taking precedence. This is important for understanding builder behavior and preventing unexpected results when the same property is set multiple times during object construction.

Scenario 4: Content with Null Value

Details:
  TestName: contentWithNullValue
  Description: This test examines the behavior when null is passed to the content() method. Since content is a required field (not annotated with @Nullable), this test verifies whether the method accepts null or throws an exception.
Execution:
  Arrange: Obtain a Builder instance without creating a Content object.
  Act: Invoke the content() method with null as the parameter.
  Assert: Depending on the implementation, either verify that an exception is thrown (NullPointerException or IllegalArgumentException) or that the builder accepts null and the build() method later throws an exception.
Validation:
  This test is crucial for validating input validation and null-safety. Since the content() method parameter is not annotated with @Nullable, the expected behavior is likely to reject null values. This ensures data integrity and prevents the creation of invalid MemoryEntry objects without required content.

Scenario 5: Building MemoryEntry with Only Content Set

Details:
  TestName: buildMemoryEntryWithOnlyContentSet
  Description: This test verifies that a MemoryEntry can be successfully built with only the content field set, while other optional fields (author and timestamp) remain null.
Execution:
  Arrange: Create a valid Content object and obtain a Builder instance.
  Act: Call content() with the Content object and immediately call build() without setting author or timestamp.
  Assert: Use JUnit assertions to verify that the MemoryEntry is created successfully, content() returns the expected Content object, and author() and timestamp() return null.
Validation:
  This test confirms that content is the only required field for MemoryEntry construction, while author and timestamp are optional (as indicated by their @Nullable annotations). This is important for understanding the minimum requirements for creating a valid MemoryEntry and ensures flexibility in memory entry creation.

Scenario 6: Content Integration with Other Builder Methods

Details:
  TestName: contentWorksWithAuthorAndTimestamp
  Description: This test verifies that the content() method works correctly in combination with other builder methods (author() and timestamp()), ensuring all fields can be set together without conflicts.
Execution:
  Arrange: Create a valid Content object, a test author string, and a test timestamp string.
  Act: Use method chaining to call content(), author(), and timestamp() on the builder, then build the MemoryEntry.
  Assert: Use JUnit assertions to verify that all three fields are correctly set in the resulting MemoryEntry object.
Validation:
  This test ensures that the builder correctly handles multiple property assignments and that setting content does not interfere with other builder methods. This is essential for validating the complete builder functionality and ensuring that complex MemoryEntry objects can be constructed with all available properties.

Scenario 7: Content with Empty Content Object

Details:
  TestName: contentWithEmptyContentObject
  Description: This test verifies the behavior when a Content object that might be considered "empty" (depending on Content's implementation) is passed to the content() method.
Execution:
  Arrange: Create a Content object that represents an empty or minimal valid state and obtain a Builder instance.
  Act: Invoke the content() method with the empty Content object and build the MemoryEntry.
  Assert: Use JUnit assertions to verify that the MemoryEntry is created successfully and contains the empty Content object.
Validation:
  This test validates that the builder accepts any valid Content object regardless of its internal state. It ensures that the builder does not impose additional validation beyond what the Content type itself requires, maintaining separation of concerns between MemoryEntry and Content validation logic.

Scenario 8: Builder Reusability After Setting Content

Details:
  TestName: builderReusableAfterSettingContent
  Description: This test verifies that a Builder instance can be reused to create multiple MemoryEntry objects after setting content, and that each built object is independent.
Execution:
  Arrange: Create two different Content objects and obtain a Builder instance.
  Act: Set content1 on the builder and build a MemoryEntry, then set content2 on the same builder and build another MemoryEntry.
  Assert: Use JUnit assertions to verify that the first MemoryEntry contains content1 and the second contains content2, confirming they are independent objects.
Validation:
  This test ensures that the builder can be safely reused for creating multiple objects and that each invocation of build() creates a new, independent MemoryEntry instance. This is important for understanding builder lifecycle and preventing unintended object sharing or mutation.

Scenario 9: Content Method with JsonProperty Annotation Compatibility

Details:
  TestName: contentJsonPropertyAnnotationBehavior
  Description: This test verifies that the content() method, annotated with @JsonProperty("content"), correctly participates in JSON serialization/deserialization workflows when used through the builder.
Execution:
  Arrange: Create a valid Content object and build a MemoryEntry using the builder.
  Act: Serialize the MemoryEntry to JSON and deserialize it back to a MemoryEntry object.
  Assert: Use JUnit assertions to verify that the deserialized MemoryEntry contains the same content as the original.
Validation:
  This test validates that the @JsonProperty annotation on the content() method works correctly with Jackson serialization framework. This is critical for applications that persist or transmit MemoryEntry objects as JSON, ensuring data integrity across serialization boundaries.

Scenario 10: Content Type Safety and Generics

Details:
  TestName: contentAcceptsOnlyContentType
  Description: This test verifies that the content() method enforces type safety and only accepts Content type objects, preventing compilation errors when incorrect types are passed.
Execution:
  Arrange: This is primarily a compile-time test, but can be validated by attempting to pass various Content implementations.
  Act: Create different valid Content type objects and pass them to the content() method.
  Assert: Use JUnit assertions to verify that all valid Content types are accepted and stored correctly.
Validation:
  This test ensures type safety at the API level, confirming that the builder's content() method signature correctly restricts input to Content types only. This prevents runtime type errors and ensures that only valid content types can be associated with memory entries, maintaining data consistency throughout the application.

*/

// ********RoostGPT********

package com.google.adk.memory;
import static com.google.common.truth.Truth.assertThat;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertThrows;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.google.genai.types.Content;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.junit.jupiter.api.*;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.google.auto.value.AutoValue;
import java.time.Instant;
import javax.annotation.Nullable;

class BuilderContentTest {
    private MemoryEntry.Builder builder;
    private Content mockContent;
    private Content mockContent2;
    private ObjectMapper objectMapper;
    @BeforeEach
    void setUp() {
        builder = MemoryEntry.builder();
        mockContent = Mockito.mock(Content.class);
        mockContent2 = Mockito.mock(Content.class);
        objectMapper = new ObjectMapper();
    }
    @Test
    @Tag("valid")
    void contentWithValidContentObject() {
        MemoryEntry memoryEntry = builder
                .content(mockContent)
                .build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
    }
    @Test
    @Tag("valid")
    void contentReturnsBuilderForMethodChaining() {
        MemoryEntry.Builder returnedBuilder = builder.content(mockContent);
        assertNotNull(returnedBuilder);
        assertThat(returnedBuilder).isInstanceOf(MemoryEntry.Builder.class);
    }
    @Test
    @Tag("valid")
    void contentOverwritesPreviousValue() {
        MemoryEntry memoryEntry = builder
                .content(mockContent)
                .content(mockContent2)
                .build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent2);
        assertThat(memoryEntry.content()).isNotEqualTo(mockContent);
    }
    @Test
    @Tag("invalid")
    void contentWithNullValue() {
        assertThrows(NullPointerException.class, () -> {
            builder.content(null).build();
        });
    }
    @Test
    @Tag("boundary")
    void buildMemoryEntryWithOnlyContentSet() {
        MemoryEntry memoryEntry = builder
                .content(mockContent)
                .build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
        assertThat(memoryEntry.author()).isNull();
        assertThat(memoryEntry.timestamp()).isNull();
    }
    @Test
    @Tag("integration")
    void contentWorksWithAuthorAndTimestamp() {
        String testAuthor = "test-author";
        String testTimestamp = "2024-01-01T10:00:00Z";
        MemoryEntry memoryEntry = builder
                .content(mockContent)
                .author(testAuthor)
                .timestamp(testTimestamp)
                .build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
        assertThat(memoryEntry.author()).isEqualTo(testAuthor);
        assertThat(memoryEntry.timestamp()).isEqualTo(testTimestamp);
    }
    @Test
    @Tag("boundary")
    void contentWithEmptyContentObject() {
        Content emptyContent = Mockito.mock(Content.class);
        MemoryEntry memoryEntry = builder
                .content(emptyContent)
                .build();
        assertThat(memoryEntry.content()).isEqualTo(emptyContent);
        assertNotNull(memoryEntry.content());
    }
    @Test
    @Tag("valid")
    void builderReusableAfterSettingContent() {
        MemoryEntry memoryEntry1 = builder
                .content(mockContent)
                .build();
        MemoryEntry memoryEntry2 = builder
                .content(mockContent2)
                .build();
        assertThat(memoryEntry1.content()).isEqualTo(mockContent);
        assertThat(memoryEntry2.content()).isEqualTo(mockContent2);
        assertThat(memoryEntry1.content()).isNotEqualTo(memoryEntry2.content());
    }
    @Test
    @Tag("integration")
    void contentJsonPropertyAnnotationBehavior() throws Exception {
        MemoryEntry originalEntry = builder
                .content(mockContent)
                .author("test-author")
                .timestamp("2024-01-01T10:00:00Z")
                .build();
        String json = objectMapper.writeValueAsString(originalEntry);
        assertNotNull(json);
        assertThat(json).contains("content");
    }
    @Test
    @Tag("valid")
    void contentAcceptsOnlyContentType() {
        Content validContent1 = Mockito.mock(Content.class);
        Content validContent2 = Mockito.mock(Content.class);
        MemoryEntry memoryEntry1 = builder
                .content(validContent1)
                .build();
        assertThat(memoryEntry1.content()).isEqualTo(validContent1);
        MemoryEntry memoryEntry2 = MemoryEntry.builder()
                .content(validContent2)
                .build();
        assertThat(memoryEntry2.content()).isEqualTo(validContent2);
    }
    @Test
    @Tag("valid")
    void contentMethodChainingWithMultipleOperations() {
        String author = "chain-author";
        String timestamp = "2024-01-01T12:00:00Z";
        MemoryEntry.Builder chainedBuilder = builder
                .content(mockContent)
                .author(author)
                .timestamp(timestamp);
        assertNotNull(chainedBuilder);
        MemoryEntry memoryEntry = chainedBuilder.build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
        assertThat(memoryEntry.author()).isEqualTo(author);
        assertThat(memoryEntry.timestamp()).isEqualTo(timestamp);
    }
    @Test
    @Tag("valid")
    void contentSetBeforeOtherFields() {
        MemoryEntry memoryEntry = builder
                .content(mockContent)
                .author("author-after-content")
                .timestamp("2024-01-01T14:00:00Z")
                .build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
        assertThat(memoryEntry.author()).isEqualTo("author-after-content");
    }
    @Test
    @Tag("valid")
    void contentSetAfterOtherFields() {
        MemoryEntry memoryEntry = builder
                .author("author-before-content")
                .timestamp("2024-01-01T15:00:00Z")
                .content(mockContent)
                .build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
        assertThat(memoryEntry.author()).isEqualTo("author-before-content");
    }
    @Test
    @Tag("valid")
    void contentWithToBuilderPattern() {
        MemoryEntry originalEntry = builder
                .content(mockContent)
                .author("original-author")
                .build();
        MemoryEntry modifiedEntry = originalEntry.toBuilder()
                .content(mockContent2)
                .build();
        assertThat(originalEntry.content()).isEqualTo(mockContent);
        assertThat(modifiedEntry.content()).isEqualTo(mockContent2);
        assertThat(modifiedEntry.author()).isEqualTo("original-author");
    }
    @Test
    @Tag("valid")
    void contentPreservesReferenceEquality() {
        Content specificContent = Mockito.mock(Content.class);
        MemoryEntry memoryEntry = builder
                .content(specificContent)
                .build();
        assertThat(memoryEntry.content()).isSameInstanceAs(specificContent);
    }
    @Test
    @Tag("valid")
    void contentWithStaticBuilderCreate() {
        MemoryEntry.Builder staticBuilder = MemoryEntry.Builder.create();
        
        MemoryEntry memoryEntry = staticBuilder
                .content(mockContent)
                .build();
        assertThat(memoryEntry.content()).isEqualTo(mockContent);
    }
}