// ********RoostGPT********
/*
Test generated by RoostGPT for test maven-adkjava-github using AI Type Azure Open AI and AI Model gpt-4.1

ROOST_METHOD_HASH=build_e10a7d38b3
ROOST_METHOD_SIG_HASH=build_0ae4dbae96

Scenario 1: Build WithDefaultValues

Details:
  TestName: buildWithDefaultValues
  Description: Verifies that build() produces an AutoValue_Model instance with both modelName and model fields as Optional.empty() when no values are set on Builder. This checks that default construction populates the model with empty optionals as intended.
Execution:
  Arrange: Instantiate AutoValue_Model.Builder without setting any fields.
  Act: Invoke build() to produce an AutoValue_Model instance.
  Assert: Assert that modelName() and model() methods return Optional.empty().
Validation:
  Ensures the builder, when not modified, results in a model object reflecting no data was set, confirming default behavior and preventing inappropriate non-empty initialization.

Scenario 2: Build WithModelNameOnly

Details:
  TestName: buildWithModelNameOnly
  Description: Ensures that when only the modelName is set on the builder, build() produces an AutoValue_Model instance with modelName set to the provided value wrapped in Optional, and model remains Optional.empty().
Execution:
  Arrange: Create Builder and use modelName(String) to set a model name (e.g., "gpt-4").
  Act: Call build() to obtain an AutoValue_Model instance.
  Assert: Assert that modelName() returns Optional.of("gpt-4") and model() returns Optional.empty().
Validation:
  Guarantees that the builder correctly preserves modelName when set alone, helping to validate partial field construction and guard against cross-field contamination.

Scenario 3: Build WithModelOnly

Details:
  TestName: buildWithModelOnly
  Description: Validates that if only the model field is set via Builder, invoking build() creates an AutoValue_Model with model containing the given BaseLlm instance wrapped in Optional, and modelName as Optional.empty().
Execution:
  Arrange: Create a mock or dummy BaseLlm instance. Set only the model field on Builder via model(BaseLlm).
  Act: Use build() to create AutoValue_Model.
  Assert: model() returns Optional.of(the-instance), modelName() returns Optional.empty().
Validation:
  Confirms model-specific construction works as designed and verifies defaulting behavior for unspecified fields, helping detect incorrect optionals population.

Scenario 4: Build WithAllFieldsSet

Details:
  TestName: buildWithAllFieldsSet
  Description: Checks that build() returns a model instance reflecting all explicitly-set values when both modelName and model are provided to Builder.
Execution:
  Arrange: Set both modelName(String) with a valid string and model(BaseLlm) with a valid object in the Builder.
  Act: Call build().
  Assert: modelName() returns the set name in Optional, and model() returns the set BaseLlm in Optional.
Validation:
  Verifies that the builder stores and passes all configuration into the product, proving intended multi-field construction.

Scenario 5: Build WithNullModelNameThrowsException

Details:
  TestName: buildWithNullModelNameThrowsException
  Description: Tests builder reaction to passing null to modelName(String), expecting an exception (likely NullPointerException) as Optional.of(null) is illegal.
Execution:
  Arrange: Create Builder and attempt to set modelName(null).
  Act: Call build() if not already failed.
  Assert: Confirm exception is thrown on modelName(null).
Validation:
  Ensures the API guards against null values resulting in illegally-constructed Optionals, supporting contract safety and fail-fast design.

Scenario 6: Build WithNullModelThrowsException

Details:
  TestName: buildWithNullModelThrowsException
  Description: Validates protection from passing null when setting model(BaseLlm), mirroring Scenario 5 for the model field.
Execution:
  Arrange: Create Builder and attempt model(null).
  Act: Call build() if not already failed.
  Assert: Assert an exception is thrown upon setting model(null).
Validation:
  Confirms defensive programming for Optionals and prevents runtime errors caused by illegal field values.

Scenario 7: BuildAfterCallingModelNameMultipleTimes

Details:
  TestName: buildAfterCallingModelNameMultipleTimes
  Description: Checks whether only the last-set value for modelName is persisted after multiple calls to modelName().
Execution:
  Arrange: Set modelName(String) twice with two different values before calling build().
  Act: Build the model.
  Assert: Assert that modelName() returns the last set string in Optional.
Validation:
  Verifies setter overwriting within the builder, a common scenario in "fluid" API use, ensuring that latest-call semantics are honored.

Scenario 8: BuildAfterCallingModelMultipleTimes

Details:
  TestName: buildAfterCallingModelMultipleTimes
  Description: Ensures the model field in AutoValue_Model is assigned the most recent value set on the builder, even after setting it multiple times.
Execution:
  Arrange: Assign model(BaseLlm) twice with two different instances.
  Act: Run build().
  Assert: model() returns only the last instance passed.
Validation:
  Offers coverage for field replacement logic, demonstrating correct handling of method chaining with repeated setters.

Scenario 9: BuildFromBuilderInitializedWithModelSource

Details:
  TestName: buildFromBuilderInitializedWithModelSource
  Description: Ensures the constructor Builder(Model source) correctly initializes fields from a pre-existing Model instance, and build() returns a logically-identical product.
Execution:
  Arrange: Create a valid Model (possibly built using the builder), use it to construct a new Builder via Builder(Model source), and immediately build().
  Act: Build new instance.
  Assert: The two Model instances are equal by equals().
Validation:
  This guarantees copy-construction logic is unbroken and that round-tripping through Builder preserves all state, which is vital for immutability and copy-on-edit use cases.

Scenario 10: BuildProducesDistinctInstances

Details:
  TestName: buildProducesDistinctInstances
  Description: Verifies that multiple invocations of build() (without modifying Builder in between) produce equal but distinct AutoValue_Model instances, confirming builder’s repeatability but model object independence.
Execution:
  Arrange: Set values and call build() twice on same Builder.
  Act: Compare references and equals().
  Assert: Asserts equality is true but reference equality (==) is false.
Validation:
  This test reassures users about the builder’s ability to generate new immutable objects repeatedly from the same state, supporting value semantics over object identity.

Scenario 11: BuildWithEmptyStringModelName

Details:
  TestName: buildWithEmptyStringModelName
  Description: Evaluates the case where modelName is set to an empty String, assessing if Optional.of("") is returned rather than Optional.empty().
Execution:
  Arrange: Set modelName("") on the builder.
  Act: Call build().
  Assert: modelName() returns Optional.of("").
Validation:
  Important for distinguishing between "no value" and "present but empty value," influencing how model objects are interpreted elsewhere in the application.

*/

// ********RoostGPT********
package com.google.adk.models;

import java.util.Optional;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

public class AutoValueModelBuilderBuildTest {

  // Test 1: Build with default values.
  @Test
  @Tag("boundary")
  public void testBuildWithDefaultValues() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    Model model = builder.build();
    Assertions.assertEquals(Optional.empty(), model.modelName());
    Assertions.assertEquals(Optional.empty(), model.model());
  }

  // Test 2: Build with model name only.
  @Test
  @Tag("valid")
  public void testBuildWithModelNameOnly() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    builder.modelName("gpt-4");
    Model model = builder.build();
    Assertions.assertEquals(Optional.of("gpt-4"), model.modelName());
    Assertions.assertEquals(Optional.empty(), model.model());
  }

  // Test 3: Build with model only.
  @Test
  @Tag("valid")
  public void testBuildWithModelOnly() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    BaseLlm mockLlm = Mockito.mock(BaseLlm.class);
    builder.model(mockLlm);
    Model model = builder.build();
    Assertions.assertEquals(Optional.of(mockLlm), model.model());
    Assertions.assertEquals(Optional.empty(), model.modelName());
  }

  // Test 4: Build with all fields set.
  @Test
  @Tag("valid")
  public void testBuildWithAllFieldsSet() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    builder.modelName("gpt-4");
    BaseLlm mockLlm = Mockito.mock(BaseLlm.class);
    builder.model(mockLlm);
    Model model = builder.build();
    Assertions.assertEquals(Optional.of("gpt-4"), model.modelName());
    Assertions.assertEquals(Optional.of(mockLlm), model.model());
  }

  // Test 5: Build with null model name throws exception.
  @Test
  @Tag("invalid")
  public void testBuildWithNullModelNameThrowsException() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    Assertions.assertThrows(
        NullPointerException.class,
        () -> {
          builder.modelName(null); // This should throw
          builder.build();
        });
  }

  // Test 6: Build with null model throws exception.
  @Test
  @Tag("invalid")
  public void testBuildWithNullModelThrowsException() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    Assertions.assertThrows(
        NullPointerException.class,
        () -> {
          builder.model(null); // This should throw
          builder.build();
        });
  }

  // Test 7: Build after calling modelName multiple times.
  @Test
  @Tag("boundary")
  public void testBuildAfterCallingModelNameMultipleTimes() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    builder.modelName("gpt-3");
    builder.modelName("gpt-4"); // Last one should persist
    Model model = builder.build();
    Assertions.assertEquals(Optional.of("gpt-4"), model.modelName());
  }

  // Test 8: Build after calling model multiple times.
  @Test
  @Tag("boundary")
  public void testBuildAfterCallingModelMultipleTimes() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    BaseLlm mockLlm1 = Mockito.mock(BaseLlm.class);
    BaseLlm mockLlm2 = Mockito.mock(BaseLlm.class);
    builder.model(mockLlm1);
    builder.model(mockLlm2); // Last one should persist
    Model model = builder.build();
    Assertions.assertEquals(Optional.of(mockLlm2), model.model());
  }

  // Test 9: Build from builder initialized with model source.
  @Test
  @Tag("integration")
  public void testBuildFromBuilderInitializedWithModelSource() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    builder.modelName("gpt-4");
    BaseLlm mockLlm = Mockito.mock(BaseLlm.class);
    builder.model(mockLlm);
    Model originalModel = builder.build();
    AutoValue_Model.Builder copyBuilder = new AutoValue_Model.Builder(originalModel);
    Model copyModel = copyBuilder.build();
    Assertions.assertEquals(originalModel, copyModel);
  }

  // Test 10: Build produces distinct instances.
  @Test
  @Tag("boundary")
  public void testBuildProducesDistinctInstances() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    builder.modelName("gpt-4");
    BaseLlm mockLlm = Mockito.mock(BaseLlm.class);
    builder.model(mockLlm);
    Model modelOne = builder.build();
    Model modelTwo = builder.build();
    Assertions.assertEquals(modelOne, modelTwo);
    Assertions.assertNotSame(modelOne, modelTwo);
  }

  // Test 11: Build with empty string model name.
  @Test
  @Tag("boundary")
  public void testBuildWithEmptyStringModelName() {
    AutoValue_Model.Builder builder = new AutoValue_Model.Builder();
    builder.modelName("");
    Model model = builder.build();
    Assertions.assertEquals(Optional.of(""), model.modelName());
  }
}
