/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=build_a5a48bce54
ROOST_METHOD_SIG_HASH=build_6248639c03

Scenario 1: Build Gemini with Model Name and API Client

Details:
  TestName: buildWithModelNameAndApiClient
  Description: This test verifies that when both modelName and apiClient are set in the Builder, the build() method successfully creates a Gemini instance using the provided apiClient. This tests the highest priority path in the conditional logic where apiClient takes precedence over other authentication methods.
Execution:
  Arrange: Create a Builder instance, set a valid modelName (e.g., "gemini-2.0-flash"), and set a mock or real Client instance as apiClient.
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance is not null and that it was constructed with the provided modelName and apiClient.
Validation:
  The assertion verifies that when an explicit apiClient is provided, it is used to construct the Gemini instance regardless of whether other authentication methods (apiKey or vertexCredentials) are also set. This is critical because it establishes the precedence order in the builder pattern, ensuring that developers can override default authentication mechanisms with a custom client configuration.

Scenario 2: Build Gemini with Model Name and API Key

Details:
  TestName: buildWithModelNameAndApiKey
  Description: This test validates that when modelName and apiKey are set (but apiClient is not), the build() method creates a Gemini instance using the apiKey constructor. This tests the second priority path in the conditional logic.
Execution:
  Arrange: Create a Builder instance, set a valid modelName, and set a valid apiKey string. Ensure apiClient is not set (remains null).
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance is not null and was constructed using the apiKey path.
Validation:
  This assertion confirms that the apiKey authentication method works correctly when no explicit apiClient is provided. This is important for users who want to use simple API key authentication without creating a custom Client instance. The test ensures that the builder correctly prioritizes apiKey over vertexCredentials but defers to apiClient when present.

Scenario 3: Build Gemini with Model Name and Vertex Credentials

Details:
  TestName: buildWithModelNameAndVertexCredentials
  Description: This test ensures that when modelName and vertexCredentials are set (but apiClient and apiKey are not), the build() method creates a Gemini instance using the vertexCredentials constructor. This tests the third priority path in the conditional logic.
Execution:
  Arrange: Create a Builder instance, set a valid modelName, and set a valid VertexCredentials instance. Ensure both apiClient and apiKey are not set (remain null).
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance is not null and was constructed using the vertexCredentials path.
Validation:
  This assertion validates that Vertex AI authentication works correctly when neither apiClient nor apiKey is provided. This is essential for enterprise users who need to use Vertex AI with service account credentials. The test confirms the correct precedence order where vertexCredentials is only used when higher-priority authentication methods are absent.

Scenario 4: Build Gemini with Model Name Only (Default Client)

Details:
  TestName: buildWithModelNameOnly
  Description: This test verifies that when only modelName is set (apiClient, apiKey, and vertexCredentials are all null), the build() method creates a Gemini instance with a default Client that includes TRACKING_HEADERS. This tests the fallback path in the conditional logic.
Execution:
  Arrange: Create a Builder instance and set only a valid modelName. Ensure apiClient, apiKey, and vertexCredentials are all not set (remain null).
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance is not null and was constructed with a default Client instance that has HttpOptions configured with TRACKING_HEADERS.
Validation:
  This assertion confirms that the builder provides a sensible default when no authentication method is explicitly provided. This is important for testing scenarios or situations where authentication is handled externally. The test ensures that even in the default case, proper tracking headers are included for telemetry purposes.

Scenario 5: Build Throws NullPointerException When Model Name is Null

Details:
  TestName: buildThrowsNullPointerExceptionWhenModelNameIsNull
  Description: This test validates that the build() method throws a NullPointerException with an appropriate message when modelName is not set (is null). This tests the null validation at the beginning of the build() method.
Execution:
  Arrange: Create a Builder instance without setting modelName (leave it as null). Optionally set apiClient, apiKey, or vertexCredentials.
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that a NullPointerException is thrown with the message "modelName must be set."
Validation:
  This assertion ensures that the builder enforces the required modelName parameter, preventing the creation of invalid Gemini instances. This is critical for fail-fast behavior and clear error messaging, helping developers identify configuration issues early in the development process rather than encountering obscure errors later.

Scenario 6: Build with API Client Takes Precedence Over API Key

Details:
  TestName: buildWithApiClientTakesPrecedenceOverApiKey
  Description: This test verifies that when both apiClient and apiKey are set, the build() method uses apiClient to construct the Gemini instance, demonstrating the precedence of apiClient over apiKey.
Execution:
  Arrange: Create a Builder instance, set a valid modelName, set both a mock/real Client instance as apiClient and a valid apiKey string.
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance was constructed using the apiClient path (first constructor) and not the apiKey path (second constructor).
Validation:
  This assertion confirms the documented precedence order where explicit apiClient configuration takes priority over simpler authentication methods. This is important for scenarios where developers need fine-grained control over the Client configuration and want to ensure their custom client is used even if an apiKey is accidentally also set.

Scenario 7: Build with API Client Takes Precedence Over Vertex Credentials

Details:
  TestName: buildWithApiClientTakesPrecedenceOverVertexCredentials
  Description: This test ensures that when both apiClient and vertexCredentials are set, the build() method uses apiClient to construct the Gemini instance, demonstrating the precedence of apiClient over vertexCredentials.
Execution:
  Arrange: Create a Builder instance, set a valid modelName, set both a mock/real Client instance as apiClient and a valid VertexCredentials instance.
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance was constructed using the apiClient path and not the vertexCredentials path.
Validation:
  This assertion validates that explicit client configuration always takes precedence over any authentication credentials. This is crucial for testing and advanced use cases where developers need complete control over the Client instance, including custom HTTP configurations, retry policies, or interceptors.

Scenario 8: Build with API Key Takes Precedence Over Vertex Credentials

Details:
  TestName: buildWithApiKeyTakesPrecedenceOverVertexCredentials
  Description: This test validates that when both apiKey and vertexCredentials are set (but apiClient is not), the build() method uses apiKey to construct the Gemini instance, demonstrating the precedence of apiKey over vertexCredentials.
Execution:
  Arrange: Create a Builder instance, set a valid modelName, set both a valid apiKey string and a valid VertexCredentials instance. Ensure apiClient is not set.
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance was constructed using the apiKey path and not the vertexCredentials path.
Validation:
  This assertion confirms the middle-tier precedence of apiKey authentication. This is important for scenarios where both authentication methods might be available in the environment, ensuring predictable behavior. The test validates that simpler authentication methods (API key) take precedence over more complex ones (Vertex credentials) when both are available.

Scenario 9: Build with All Authentication Methods Set Uses API Client

Details:
  TestName: buildWithAllAuthenticationMethodsUsesApiClient
  Description: This test verifies that when all authentication methods (apiClient, apiKey, and vertexCredentials) are set, the build() method uses apiClient, confirming it has the highest precedence.
Execution:
  Arrange: Create a Builder instance, set a valid modelName, and set all three authentication methods: apiClient, apiKey, and vertexCredentials.
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the returned Gemini instance was constructed using the apiClient path.
Validation:
  This assertion provides comprehensive validation of the precedence hierarchy by testing the scenario where all options are available. This is important for ensuring that the documented precedence order is correctly implemented and that developers can rely on predictable behavior when multiple authentication methods are configured, whether intentionally or accidentally.

Scenario 10: Build with Empty String Model Name Throws NullPointerException

Details:
  TestName: buildWithEmptyStringModelNameDoesNotThrowException
  Description: This test verifies the behavior when modelName is set to an empty string. Since the validation uses Objects.requireNonNull(), an empty string will not throw a NullPointerException, and the build() method will proceed with the empty string.
Execution:
  Arrange: Create a Builder instance, set modelName to an empty string (""), and set a valid authentication method (e.g., apiKey).
  Act: Invoke the build() method on the Builder instance.
  Assert: Verify that the build() method completes without throwing a NullPointerException and returns a Gemini instance (though the empty model name may cause issues in actual API calls).
Validation:
  This assertion clarifies that the null check only validates against null values, not empty strings. This is important for understanding the validation boundaries of the builder. While an empty model name is likely invalid for actual API operations, the builder itself does not enforce this constraint, delegating that validation to the API client or subsequent operations.

Scenario 11: Build Multiple Times with Same Builder Instance

Details:
  TestName: buildMultipleTimesWithSameBuilderInstance
  Description: This test verifies that calling build() multiple times on the same Builder instance creates multiple independent Gemini instances with the same configuration.
Execution:
  Arrange: Create a Builder instance, set a valid modelName and apiKey.
  Act: Invoke the build() method twice on the same Builder instance to create two Gemini instances.
  Assert: Verify that both returned Gemini instances are not null, are distinct objects (not the same reference), but have the same configuration.
Validation:
  This assertion ensures that the Builder can be reused to create multiple instances, which is a common pattern in builder implementations. This is important for scenarios where the same configuration needs to be used to create multiple Gemini instances, such as in connection pooling or multi-threaded applications. The test confirms that the builder maintains its state across multiple build() calls.

Scenario 12: Build After Modifying Builder State

Details:
  TestName: buildAfterModifyingBuilderState
  Description: This test validates that modifying the Builder's state after a build() call affects subsequent build() calls, ensuring that the builder maintains mutable state.
Execution:
  Arrange: Create a Builder instance, set modelName and apiKey, and call build() to create the first Gemini instance. Then modify the builder by setting a different apiClient.
  Act: Invoke the build() method again on the modified Builder instance.
  Assert: Verify that the second Gemini instance uses the newly set apiClient instead of the previously set apiKey, confirming that the builder state was updated.
Validation:
  This assertion confirms that the Builder maintains mutable state and that changes to the builder affect subsequent build() calls. This is important for understanding the builder's lifecycle and ensuring that developers can reuse and reconfigure builders as needed. The test validates that the precedence rules are re-evaluated on each build() call based on the current state.

*/

// ********RoostGPT********

package com.google.adk.models;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

import com.google.genai.Client;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

class BuilderBuildTest {

  @Mock private Client mockApiClient;

  @Mock private VertexCredentials mockVertexCredentials;

  private Gemini.Builder builder;

  @BeforeEach
  void setUp() {
    MockitoAnnotations.openMocks(this);
    builder = Gemini.builder();
  }

  @Test
  @Tag("valid")
  void buildWithModelNameAndApiClient() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    builder.modelName(modelName).apiClient(mockApiClient);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithModelNameAndApiKey() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    String apiKey = "test-api-key-12345";
    builder.modelName(modelName).apiKey(apiKey);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithModelNameAndVertexCredentials() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    builder.modelName(modelName).vertexCredentials(mockVertexCredentials);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithModelNameOnly() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    builder.modelName(modelName);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("invalid")
  void buildThrowsNullPointerExceptionWhenModelNameIsNull() {
    // Arrange
    builder.apiKey("test-api-key");
    // Act & Assert
    NullPointerException exception =
        assertThrows(
            NullPointerException.class,
            () -> {
              builder.build();
            });
    assertEquals("modelName must be set.", exception.getMessage());
  }

  @Test
  @Tag("valid")
  void buildWithApiClientTakesPrecedenceOverApiKey() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    String apiKey = "test-api-key-12345";
    builder.modelName(modelName).apiClient(mockApiClient).apiKey(apiKey);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithApiClientTakesPrecedenceOverVertexCredentials() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    builder.modelName(modelName).apiClient(mockApiClient).vertexCredentials(mockVertexCredentials);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithApiKeyTakesPrecedenceOverVertexCredentials() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    String apiKey = "test-api-key-12345";
    builder.modelName(modelName).apiKey(apiKey).vertexCredentials(mockVertexCredentials);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithAllAuthenticationMethodsUsesApiClient() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    String apiKey = "test-api-key-12345";
    builder
        .modelName(modelName)
        .apiClient(mockApiClient)
        .apiKey(apiKey)
        .vertexCredentials(mockVertexCredentials);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("boundary")
  void buildWithEmptyStringModelNameDoesNotThrowException() {
    // Arrange
    String emptyModelName = "";
    String apiKey = "test-api-key-12345";
    builder.modelName(emptyModelName).apiKey(apiKey);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(emptyModelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildMultipleTimesWithSameBuilderInstance() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    String apiKey = "test-api-key-12345";
    builder.modelName(modelName).apiKey(apiKey);
    // Act
    Gemini gemini1 = builder.build();
    Gemini gemini2 = builder.build();
    // Assert
    assertNotNull(gemini1);
    assertNotNull(gemini2);
    assertNotSame(gemini1, gemini2);
    assertEquals(gemini1.model(), gemini2.model());
  }

  @Test
  @Tag("valid")
  void buildAfterModifyingBuilderState() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    String apiKey = "test-api-key-12345";
    builder.modelName(modelName).apiKey(apiKey);
    // Act
    Gemini gemini1 = builder.build();
    builder.apiClient(mockApiClient);
    Gemini gemini2 = builder.build();
    // Assert
    assertNotNull(gemini1);
    assertNotNull(gemini2);
    assertNotSame(gemini1, gemini2);
    assertEquals(modelName, gemini1.model());
    assertEquals(modelName, gemini2.model());
  }

  @Test
  @Tag("invalid")
  void buildWithNullModelNameAndApiClient() {
    // Arrange
    builder.apiClient(mockApiClient);
    // Act & Assert
    NullPointerException exception =
        assertThrows(
            NullPointerException.class,
            () -> {
              builder.build();
            });
    assertEquals("modelName must be set.", exception.getMessage());
  }

  @Test
  @Tag("invalid")
  void buildWithNullModelNameAndVertexCredentials() {
    // Arrange
    builder.vertexCredentials(mockVertexCredentials);
    // Act & Assert
    NullPointerException exception =
        assertThrows(
            NullPointerException.class,
            () -> {
              builder.build();
            });
    assertEquals("modelName must be set.", exception.getMessage());
  }

  @Test
  @Tag("valid")
  void buildWithLongModelName() {
    // Arrange
    String longModelName = "gemini-2.0-flash-exp-very-long-model-name-for-testing-purposes";
    String apiKey = "test-api-key-12345";
    builder.modelName(longModelName).apiKey(apiKey);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(longModelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithSpecialCharactersInModelName() {
    // Arrange
    String modelName = "gemini-2.0-flash_test@model#123";
    String apiKey = "test-api-key-12345";
    builder.modelName(modelName).apiKey(apiKey);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(modelName, gemini.model());
  }

  @Test
  @Tag("boundary")
  void buildWithWhitespaceModelName() {
    // Arrange
    String whitespaceModelName = "   ";
    String apiKey = "test-api-key-12345";
    builder.modelName(whitespaceModelName).apiKey(apiKey);
    // Act
    Gemini gemini = builder.build();
    // Assert
    assertNotNull(gemini);
    assertEquals(whitespaceModelName, gemini.model());
  }

  @Test
  @Tag("valid")
  void buildWithDifferentModelNames() {
    // Arrange
    String modelName1 = "gemini-1.5-pro";
    String modelName2 = "gemini-2.0-flash";
    String apiKey = "test-api-key-12345";
    // Act
    Gemini gemini1 = builder.modelName(modelName1).apiKey(apiKey).build();
    Gemini gemini2 = Gemini.builder().modelName(modelName2).apiKey(apiKey).build();
    // Assert
    assertNotNull(gemini1);
    assertNotNull(gemini2);
    assertEquals(modelName1, gemini1.model());
    assertEquals(modelName2, gemini2.model());
    assertNotEquals(gemini1.model(), gemini2.model());
  }

  @Test
  @Tag("valid")
  void buildReturnsNewInstanceEachTime() {
    // Arrange
    String modelName = "gemini-2.0-flash";
    builder.modelName(modelName).apiClient(mockApiClient);
    // Act
    Gemini gemini1 = builder.build();
    Gemini gemini2 = builder.build();
    Gemini gemini3 = builder.build();
    // Assert
    assertNotNull(gemini1);
    assertNotNull(gemini2);
    assertNotNull(gemini3);
    assertNotSame(gemini1, gemini2);
    assertNotSame(gemini2, gemini3);
    assertNotSame(gemini1, gemini3);
  }
}
