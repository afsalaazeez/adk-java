/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test maven-adkjava-github using AI Type Azure Open AI and AI Model gpt-4.1

ROOST_METHOD_HASH=prepareGenenerateContentRequest_3310317bd1
ROOST_METHOD_SIG_HASH=prepareGenenerateContentRequest_1be48efb9a

Scenario 1: Handling a request with empty content list

Details:
  TestName: emptyContentsRequestReturnsUserContinuationContent
  Description: This test checks that when prepareGenenerateContentRequest is called with an LlmRequest having an empty content list, it returns a request whose content contains a single user-role message with CONTINUE_OUTPUT_MESSAGE. This verifies that the method ensures the model will always respond, even if the original request is empty.
Execution:
  Arrange: Construct an LlmRequest with an empty content list.
  Act: Call prepareGenenerateContentRequest with this LlmRequest and sanitize set to false.
  Assert: Use JUnit assertions to confirm that the returned LlmRequest has exactly one Content, which has the role "user" and the expected USER_CONTINUE_OUTPUT_MESSAGE text.
Validation:
  The validation verifies that the method guarantees a user prompt, which is necessary for the backend to generate a response. This is essential in maintaining consistent model behavior when faced with empty input.


Scenario 2: The last content is NOT from user, should append user continuation

Details:
  TestName: nonUserLastContentAppendsUserContinuation
  Description: The test checks that if the last Content in the list is not from the "user" role, the method appends a new user Content with CONTINUE_OUTPUT_MESSAGE. This ensures the API contract for model response prompting is met.
Execution:
  Arrange: Create an LlmRequest with a non-empty content list, where the last Content is not from user (e.g., role "system").
  Act: Call prepareGenenerateContentRequest with this request and sanitize = false.
  Assert: Use JUnit assertion to verify that the last Content in the returned request is from "user" and contains CONTINUE_OUTPUT_MESSAGE.
Validation:
  The scenario tests proper role enforcement for the model to respond correctly by always ensuring a terminal user prompt is present.


Scenario 3: The last content IS from user, should not append extra message

Details:
  TestName: userLastContentNoExtraUserAppend
  Description: This ensures that if the last Content in the request's contents is already from the user, no extra message is appended and contents are preserved aside from possible stripping of thoughts.
Execution:
  Arrange: Build an LlmRequest whose final Content has role "user".
  Act: Call prepareGenenerateContentRequest with sanitize = false.
  Assert: Assert that the contents remain the same except for possible thought-stripping.
Validation:
  The test confirms that the method avoids redundant additions when requirements are already satisfied, preserving data integrity.


Scenario 4: Stripping thought-only parts from the contents

Details:
  TestName: stripThoughtPartsFromContents
  Description: Validates that thought-only Parts in all Content objects are stripped when prepareGenenerateContentRequest is invoked. This checks the "stripThoughts" logic in the method.
Execution:
  Arrange: Prepare an LlmRequest containing Parts marked as thought=true in at least one Content.
  Act: Call prepareGenenerateContentRequest with sanitize = false.
  Assert: Assert that all thought-only Parts have been removed from the returned request's contents, and non-thought parts remain.
Validation:
  This scenario checks compliance with backend expectations, ensuring hidden/internal logic ("thoughts") does not affect the model response.


Scenario 5: Request with sanitize = true and content contains file displayName

Details:
  TestName: sanitizeRemovesFileDisplayName
  Description: Ensures that when sanitize is true, any fileData or inlineData Parts with a displayName property have that property removed.
Execution:
  Arrange: Construct an LlmRequest where at least one Part contains fileData or inlineData with a displayName.
  Act: Call prepareGenenerateContentRequest with sanitize = true.
  Assert: Use assertions to check that the returned request's Parts no longer have displayName present in fileData or inlineData.
Validation:
  This ensures requests are made compatible with Gemini API, preventing runtime errors due to disallowed displayName fields.


Scenario 6: Request with config containing labels (sanitize should remove labels)

Details:
  TestName: sanitizeRemovesLabelsFromConfig
  Description: Verifies that if the request includes config labels, setting sanitize=true will remove them by setting to an empty map.
Execution:
  Arrange: Build an LlmRequest whose config has labels populated.
  Act: Call prepareGenenerateContentRequest with sanitize = true.
  Assert: Assert that the returned request's config has labels as an empty ImmutableMap.
Validation:
  This prevents backend errors from unsupported config fields, demonstrating robust input compatibility handling.


Scenario 7: Request with mixed-thought parts (true and false)

Details:
  TestName: mixedThoughtPartsKeepNonThoughts
  Description: Checks that in a Content object containing both thought=true and thought=false Parts, only the non-thought Parts are retained after processing.
Execution:
  Arrange: Create an LlmRequest with such a Content.
  Act: Call prepareGenenerateContentRequest with sanitize = false.
  Assert: Assert that the returned Content objects have removed all thought=true Parts but retained all thought=false Parts.
Validation:
  This validates selective stripping and assures that informative/non-thought data isn't lost or mistakenly stripped.


Scenario 8: Request with no "thought" annotation in Parts

Details:
  TestName: partsWithoutThoughtAnnotationRemain
  Description: Ensures that if a Part has no "thought" property (Optional.empty()), it is kept in the returned result.
Execution:
  Arrange: Construct an LlmRequest with Parts missing the "thought" property.
  Act: Call prepareGenenerateContentRequest and examine the output.
  Assert: Confirm that all original Parts are present since none qualify for removal.
Validation:
  Validates safe default logic so only explicit thought markings cause stripping, which increases robustness.


Scenario 9: Sanitize and stripThoughts both enabled with complex input

Details:
  TestName: sanitizeAndStripThoughtsCombined
  Description: Tests the combined behavior of sanitizing displayName fields and stripping thought-only Parts in a complex LlmRequest.
Execution:
  Arrange: Create an LlmRequest with config labels, fileData displayName, and some thought-only Parts.
  Act: Call prepareGenenerateContentRequest with sanitize=true.
  Assert: Assert that labels are removed, displayNames are sanitized, and thought-only Parts are stripped.
Validation:
  This comprehensive scenario verifies correct behavior when multiple transforming logics are applied, ensuring consistent API compliance and quality filtering.


Scenario 10: Request with content parts, but all get stripped leaving empty parts

Details:
  TestName: allContentPartsStrippedLeavesEmptyParts
  Description: Checks that if all Parts in every Content object are marked as thought=true, the returned request's Contents have empty Parts lists but are otherwise preserved.
Execution:
  Arrange: Build a request containing only thought=true Parts in all Content objects.
  Act: Call prepareGenenerateContentRequest.
  Assert: Assert that the result contains Contents with empty Parts (and potentially with appended user content at the end if necessary).
Validation:
  This scenario tests robustness in handling extreme cases, ensuring empty but appropriate Content structures are retained.


Scenario 11: Validating the logic when both parameters are toggled

Details:
  TestName: differentCombinationsOfSanitizeAndContents
  Description: Validates the output for all possible value combinations of sanitize parameter and input content structures (empty, already user, mixed roles, with/without thoughts).
Execution:
  Arrange: Compose multiple LlmRequests for each case (empty content, user last, system last, mixed thoughts).
  Act: Call prepareGenenerateContentRequest with both sanitize = true and false.
  Assert: Assert that for each combination, the returned request matches expected behavior—sanitized, stripped, content-appended or preserved as appropriate.
Validation:
  This scenario covers the method's contract holistically, checking for regressions or inconsistent handling in all branches.


Scenario 12: Request with nested Optional values in Content and Part objects

Details:
  TestName: handlesNestedOptionalFieldsGracefully
  Description: Checks method robustness when encountering deeply nested Optional fields (such as empty Optionals for role, parts, displayName, labels).
Execution:
  Arrange: Construct an LlmRequest with Parts and Content objects, populating Optionals with some empty and some present values.
  Act: Call prepareGenenerateContentRequest.
  Assert: Validate that the method does not throw and handles Optionals as designed (for example, falls back to default values or strips correctly as needed).
Validation:
  Verifies method resilience in the presence of optional and possibly missing values across its input types.


*/

// ********RoostGPT********

package com.google.adk.models;

import com.google.common.collect.ImmutableList;
import com.google.common.collect.ImmutableMap;
import com.google.genai.types.Blob;
import com.google.genai.types.Content;
import com.google.genai.types.FileData;
import com.google.genai.types.Part;
import com.google.truth.Truth;
import java.util.List;
import java.util.Map;
import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

public class GeminiUtilPrepareGenenerateContentRequestTest {

	public static final String CONTINUE_OUTPUT_MESSAGE = GeminiUtil.CONTINUE_OUTPUT_MESSAGE;

	@Test
	@Tag("boundary")
	public void testEmptyContentsRequestReturnsUserContinuationContent() {
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of()).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		// Should have one content: user role, CONTINUE_OUTPUT_MESSAGE
		List<Content> contents = result.contents();
		Truth.assertThat((int) contents.size()).isEqualTo(1);
		Content content = contents.get(0);
		Truth.assertThat((String) content.role().orElse("")).isEqualTo("user");
		Truth.assertThat((String) content.parts().get().get(0).text().orElse("")).isEqualTo(CONTINUE_OUTPUT_MESSAGE);
	}

	@Test
	@Tag("boundary")
	public void testNonUserLastContentAppendsUserContinuation() {
		Content systemContent = Content.builder()
			.role("system")
			.parts(ImmutableList.of(Part.fromText("System content message")))
			.build();
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of(systemContent)).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		List<Content> resultContents = result.contents();
		Truth.assertThat((String) resultContents.get(resultContents.size() - 1).role().orElse("")).isEqualTo("user");
		Truth.assertThat((String) resultContents.get(resultContents.size() - 1).parts().get().get(0).text().orElse(""))
			.isEqualTo(CONTINUE_OUTPUT_MESSAGE);
		// Ensure previous content remains
		Truth.assertThat((String) resultContents.get(0).role().orElse("")).isEqualTo("system");
	}

	@Test
	@Tag("valid")
	public void testUserLastContentNoExtraUserAppend() {
		Content userContent = Content.builder()
			.role("user")
			.parts(ImmutableList.of(Part.fromText("Previous message")))
			.build();
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of(userContent)).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		List<Content> resultContents = result.contents();
		Truth.assertThat((int) resultContents.size()).isEqualTo(1);
		Truth.assertThat((String) resultContents.get(0).role().orElse("")).isEqualTo("user");
		Truth.assertThat((String) resultContents.get(0).parts().get().get(0).text().orElse(""))
			.isEqualTo("Previous message");
	}

	@Test
	@Tag("valid")
	public void testStripThoughtPartsFromContents() {
		Part thoughtPart = Part.builder().thought(true).text(Optional.of("Internal thought")).build();
		Part normalPart = Part.fromText("Visible text");
		Content content = Content.builder().role("user").parts(ImmutableList.of(thoughtPart, normalPart)).build();
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of(content)).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		List<Content> resultContents = result.contents();
		Truth.assertThat((int) resultContents.size()).isEqualTo(1);
		ImmutableList<Part> resultParts = resultContents.get(0).parts().orElse(ImmutableList.of());
		Truth.assertThat((int) resultParts.size()).isEqualTo(1);
		Truth.assertThat((String) resultParts.get(0).text().orElse("")).isEqualTo("Visible text");
		Truth.assertThat((boolean) resultParts.get(0).thought().orElse(false)).isFalse();
	}

	@Test
	@Tag("valid")
	public void testSanitizeRemovesFileDisplayName() {
		FileData fileDataWithDisplayName = FileData.builder()
			.displayName(Optional.of("Should be removed"))
			.fileUri(Optional.of("file://dummy.txt"))
			.mimeType(Optional.of("text/plain"))
			.build();
		Part part = Part.builder().fileData(fileDataWithDisplayName).build();
		Blob inlineBlobWithDisplayName = Blob.builder()
			.displayName(Optional.of("InlineDisplay"))
			.data(Optional.of("blobdata".getBytes()))
			.mimeType(Optional.of("application/octet-stream"))
			.build();
		Part inlinePart = Part.builder().inlineData(inlineBlobWithDisplayName).build();
		Content content = Content.builder().role("user").parts(ImmutableList.of(part, inlinePart)).build();
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of(content)).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, true);
		ImmutableList<Part> resultParts = result.contents().get(0).parts().orElse(ImmutableList.of());
		FileData sanitizedFileData = resultParts.get(0).fileData().get();
		Truth.assertThat((Optional<String>) sanitizedFileData.displayName()).isEqualTo(Optional.empty());
		Blob sanitizedBlob = resultParts.get(1).inlineData().get();
		Truth.assertThat((Optional<String>) sanitizedBlob.displayName()).isEqualTo(Optional.empty());
	}

	@Test
	@Tag("valid")
	public void testSanitizeRemovesLabelsFromConfig() {
		Map<String, String> labels = ImmutableMap.of("DEBUG", "TRUE");
		GenerateContentConfig config = GenerateContentConfig.builder().labels(Optional.of(labels)).build();
		LlmRequest request = LlmRequest.builder().config(Optional.of(config)).contents(ImmutableList.of()).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, true);
		Optional<GenerateContentConfig> sanitizedConfigOpt = result.config();
		Truth.assertThat((boolean) sanitizedConfigOpt.isPresent()).isTrue();
		Map<String, String> resultLabels = sanitizedConfigOpt.get().labels().orElse(null);
		Truth.assertThat((int) resultLabels.size()).isEqualTo(0);
	}

	@Test
	@Tag("valid")
	public void testMixedThoughtPartsKeepNonThoughts() {
		Part thoughtPart = Part.builder().thought(true).text(Optional.of("Thought to strip")).build();
		Part nonThoughtPart = Part.builder().thought(false).text(Optional.of("Keep me")).build();
		Content content = Content.builder().role("user").parts(ImmutableList.of(thoughtPart, nonThoughtPart)).build();
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of(content)).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		ImmutableList<Part> resultParts = result.contents().get(0).parts().orElse(ImmutableList.of());
		Truth.assertThat((int) resultParts.size()).isEqualTo(1);
		Truth.assertThat((String) resultParts.get(0).text().orElse("")).isEqualTo("Keep me");
		Truth.assertThat((Optional<Boolean>) resultParts.get(0).thought()).isEqualTo(Optional.of(false));
	}

	@Test
	@Tag("valid")
	public void testPartsWithoutThoughtAnnotationRemain() {
		Part partWithoutThought = Part.fromText("Retain me");
		Content content = Content.builder().role("user").parts(ImmutableList.of(partWithoutThought)).build();
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of(content)).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		ImmutableList<Part> resultParts = result.contents().get(0).parts().orElse(ImmutableList.of());
		Truth.assertThat((int) resultParts.size()).isEqualTo(1);
		Truth.assertThat((String) resultParts.get(0).text().orElse("")).isEqualTo("Retain me");
		Truth.assertThat((Optional<Boolean>) resultParts.get(0).thought()).isEqualTo(Optional.empty());
	}

	@Test
	@Tag("integration")
	public void testSanitizeAndStripThoughtsCombined() {
		FileData fileData = FileData.builder()
			.displayName(Optional.of("ToSanitize"))
			.fileUri(Optional.of("file://dummy.txt"))
			.mimeType(Optional.of("text/plain"))
			.build();
		Part filePart = Part.builder().fileData(fileData).thought(true).build();
		Blob blob = Blob.builder()
			.displayName(Optional.of("InlineToSanitize"))
			.data(Optional.of("blob".getBytes()))
			.mimeType(Optional.of("application/octet-stream"))
			.build();
		Part inlinePart = Part.builder().inlineData(blob).thought(false).text(Optional.of("KeepMe")).build();
		Map<String, String> labels = ImmutableMap.of("LABEL1", "VAL1");
		GenerateContentConfig config = GenerateContentConfig.builder().labels(Optional.of(labels)).build();
		Content content = Content.builder().role("user").parts(ImmutableList.of(filePart, inlinePart)).build();
		LlmRequest request = LlmRequest.builder()
			.config(Optional.of(config))
			.contents(ImmutableList.of(content))
			.build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, true);
		// Labels removed
		Map<String, String> resultLabels = result.config().get().labels().orElse(null);
		Truth.assertThat((int) resultLabels.size()).isEqualTo(0);
		// FileData displayName is removed, thought part is stripped
		ImmutableList<Part> resultParts = result.contents().get(0).parts().orElse(ImmutableList.of());
		Truth.assertThat((int) resultParts.size()).isEqualTo(1);
		// InlineData displayName stripped, part retained
		Blob sanitizedBlob = resultParts.get(0).inlineData().get();
		Truth.assertThat((Optional<String>) sanitizedBlob.displayName()).isEqualTo(Optional.empty());
	}

	@Test
	@Tag("boundary")
	public void testAllContentPartsStrippedLeavesEmptyParts() {
		Part onlyThoughtPart = Part.builder().thought(true).text(Optional.of("ShouldRemove")).build();
		Content content = Content.builder().role("user").parts(ImmutableList.of(onlyThoughtPart)).build();
		LlmRequest request = LlmRequest.builder().contents(ImmutableList.of(content)).build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		ImmutableList<Part> resultParts = result.contents().get(0).parts().orElse(ImmutableList.of());
		Truth.assertThat((int) resultParts.size()).isEqualTo(0);
	}

	@Test
	@Tag("integration")
	public void testDifferentCombinationsOfSanitizeAndContents() {
		// Case 1: empty content, sanitize false
		LlmRequest emptyReq = LlmRequest.builder().contents(ImmutableList.of()).build();
		LlmRequest res1 = GeminiUtil.prepareGenenerateContentRequest(emptyReq, false);
		Truth.assertThat((int) res1.contents().size()).isEqualTo(1);
		// Case 2: empty content, sanitize true
		LlmRequest res2 = GeminiUtil.prepareGenenerateContentRequest(emptyReq, true);
		Truth.assertThat((int) res2.contents().size()).isEqualTo(1);
		// Case 3: last user, sanitize true
		Content userFinal = Content.builder().role("user").parts(ImmutableList.of(Part.fromText("Umsg"))).build();
		LlmRequest userReq = LlmRequest.builder().contents(ImmutableList.of(userFinal)).build();
		LlmRequest res3 = GeminiUtil.prepareGenenerateContentRequest(userReq, true);
		Truth.assertThat((int) res3.contents().size()).isEqualTo(1);
		// Case 4: last system, sanitize false
		Content systemFinal = Content.builder().role("system").parts(ImmutableList.of(Part.fromText("Sysmsg"))).build();
		LlmRequest sysReq = LlmRequest.builder().contents(ImmutableList.of(systemFinal)).build();
		LlmRequest res4 = GeminiUtil.prepareGenenerateContentRequest(sysReq, false);
		Truth.assertThat((String) res4.contents().get(res4.contents().size() - 1).role().orElse("")).isEqualTo("user");
		// Case 5: parts with thought annotation mixed, sanitize true
		Part t1 = Part.builder().thought(true).text(Optional.of("Think")).build();
		Part t2 = Part.builder().thought(false).text(Optional.of("GiveText")).build();
		Content mixedContent = Content.builder().role("user").parts(ImmutableList.of(t1, t2)).build();
		LlmRequest mixedReq = LlmRequest.builder().contents(ImmutableList.of(mixedContent)).build();
		LlmRequest res5 = GeminiUtil.prepareGenenerateContentRequest(mixedReq, true);
		Truth.assertThat((String) res5.contents().get(0).parts().get().get(0).text().orElse("")).isEqualTo("GiveText");
	}

	@Test
	@Tag("integration")
	public void testHandlesNestedOptionalFieldsGracefully() {
		// Content with Optional.empty() & Optional.of values in various places
		Part noTextPart = Part.builder().thought(false).text(Optional.empty()).build();
		Part withTextPart = Part.builder().thought(Optional.empty()).text(Optional.of("TextExist")).build();
		Content emptyRoleContent = Content.builder().role("").parts(ImmutableList.of(noTextPart, withTextPart)).build();
		GenerateContentConfig configNoLabels = GenerateContentConfig.builder().labels(Optional.empty()).build();
		LlmRequest request = LlmRequest.builder()
			.config(Optional.of(configNoLabels))
			.contents(ImmutableList.of(emptyRoleContent))
			.build();
		LlmRequest result = GeminiUtil.prepareGenenerateContentRequest(request, false);
		// Validate all parts are present, no exception
		List<Content> contents = result.contents();
		Truth.assertThat((int) contents.size()).isEqualTo(1);
		ImmutableList<Part> parts = contents.get(0).parts().orElse(ImmutableList.of());
		Truth.assertThat((int) parts.size()).isEqualTo(2);
		Truth.assertThat((Optional<String>) parts.get(0).text()).isEqualTo(Optional.empty());
		Truth.assertThat((Optional<Boolean>) parts.get(1).thought()).isEqualTo(Optional.empty());
		Truth.assertThat((String) parts.get(1).text().orElse("")).isEqualTo("TextExist");
	}

}
