/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=vertexCredentials_05534933e8
ROOST_METHOD_SIG_HASH=vertexCredentials_ad6dd8135d

Scenario 1: Set Valid VertexCredentials

Details:
  TestName: vertexCredentialsWithValidCredentials
  Description: This test verifies that the vertexCredentials method correctly sets a valid VertexCredentials object in the Builder and returns the Builder instance for method chaining. It ensures that the builder pattern works as expected when valid credentials are provided.
Execution:
  Arrange: Create a mock or valid VertexCredentials object. Create a new Builder instance using Gemini.builder().
  Act: Invoke the vertexCredentials method on the Builder instance with the valid VertexCredentials object.
  Assert: Verify that the method returns the same Builder instance (for method chaining). Verify that the Builder can successfully build a Gemini object without throwing exceptions.
Validation:
  The assertion verifies that the Builder correctly stores the VertexCredentials and maintains the fluent interface pattern by returning itself. This is critical for ensuring that users can chain multiple builder methods together. The test confirms that valid credentials are accepted and stored properly, which is essential for authenticating with Vertex AI services.

Scenario 2: Set Null VertexCredentials

Details:
  TestName: vertexCredentialsWithNullCredentials
  Description: This test checks the behavior when null is passed as the vertexCredentials parameter. It verifies whether the method accepts null values or if validation is performed at the builder level versus the build() method level.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Set a valid modelName on the builder.
  Act: Invoke the vertexCredentials method with null as the parameter.
  Assert: Verify that the method returns the Builder instance. Verify the behavior when build() is called - it should either accept null or throw a NullPointerException depending on the implementation requirements.
Validation:
  This test validates the null-handling behavior of the builder. Based on the constructor code that uses Objects.requireNonNull, the build() method should handle null credentials appropriately. This test ensures that the API contract is clear regarding null values and prevents unexpected runtime errors.

Scenario 3: Method Chaining with VertexCredentials

Details:
  TestName: vertexCredentialsSupportsMethodChaining
  Description: This test verifies that the vertexCredentials method properly supports method chaining by returning the Builder instance, allowing multiple builder methods to be called in sequence.
Execution:
  Arrange: Create a valid VertexCredentials object. Create a new Builder instance.
  Act: Chain multiple builder methods together: modelName(), vertexCredentials(), and potentially other builder methods in a single statement.
  Assert: Verify that all methods can be chained without compilation errors. Verify that the final build() call produces a valid Gemini instance.
Validation:
  This test confirms that the @CanIgnoreReturnValue annotation is correctly applied and that the fluent interface pattern is properly implemented. Method chaining is a core feature of the builder pattern, and this test ensures that developers can write concise, readable code when constructing Gemini instances.

Scenario 4: VertexCredentials Overrides Previous Value

Details:
  TestName: vertexCredentialsOverridesPreviousValue
  Description: This test verifies that calling vertexCredentials multiple times on the same Builder instance results in the last value being used, confirming that the method properly overwrites any previously set credentials.
Execution:
  Arrange: Create two different VertexCredentials objects (credentials1 and credentials2). Create a new Builder instance with a valid modelName.
  Act: Call vertexCredentials(credentials1) followed by vertexCredentials(credentials2) on the same Builder instance. Build the Gemini object.
  Assert: Verify that the Gemini instance is created successfully. Verify that the second credentials object is the one actually used (this may require additional inspection or behavior verification).
Validation:
  This test ensures that the builder correctly handles multiple calls to the same setter method, following the standard builder pattern where the last value set takes precedence. This is important for scenarios where configuration might be conditionally applied or overridden.

Scenario 5: VertexCredentials with Complete Builder Configuration

Details:
  TestName: vertexCredentialsWithCompleteBuilderConfiguration
  Description: This test verifies that vertexCredentials works correctly when used alongside all other builder configuration methods (modelName, apiKey, apiClient), ensuring no conflicts or unexpected interactions.
Execution:
  Arrange: Create valid instances of VertexCredentials, an apiKey string, and a Client object. Create a new Builder instance.
  Act: Set modelName, apiKey, apiClient, and vertexCredentials on the builder. Call build() to create the Gemini instance.
  Assert: Verify that the Gemini instance is created successfully. Verify that the precedence rules are followed (apiClient takes precedence over apiKey, which takes precedence over vertexCredentials).
Validation:
  This test validates the interaction between different authentication methods and ensures that the precedence rules documented in the Builder class are correctly implemented. It confirms that setting vertexCredentials doesn't interfere with other configuration options and that the builder handles multiple authentication methods gracefully.

Scenario 6: VertexCredentials with Only ModelName Set

Details:
  TestName: vertexCredentialsWithOnlyModelName
  Description: This test verifies that a Gemini instance can be successfully built when only modelName and vertexCredentials are set, without providing apiKey or apiClient.
Execution:
  Arrange: Create a valid VertexCredentials object. Create a new Builder instance.
  Act: Set only modelName and vertexCredentials on the builder. Call build() to create the Gemini instance.
  Assert: Verify that the Gemini instance is created successfully without throwing exceptions. Verify that the instance is properly initialized with the provided credentials.
Validation:
  This test confirms that vertexCredentials is a valid standalone authentication method and that the builder correctly constructs a Gemini instance using only these credentials when no other authentication methods are provided. This is essential for users who exclusively use Vertex AI for their Gemini model access.

Scenario 7: VertexCredentials Returns Same Builder Instance

Details:
  TestName: vertexCredentialsReturnsSameBuilderInstance
  Description: This test explicitly verifies that the vertexCredentials method returns the exact same Builder instance (not a new copy), which is essential for the builder pattern's fluent interface.
Execution:
  Arrange: Create a valid VertexCredentials object. Create a new Builder instance and store a reference to it.
  Act: Call vertexCredentials on the builder and capture the returned value.
  Assert: Use reference equality (==) or assertEquals to verify that the returned Builder is the same instance as the original. Verify using System.identityHashCode or similar that both references point to the same object.
Validation:
  This test ensures that the builder pattern is correctly implemented by returning 'this' rather than creating a new builder instance. This is crucial for performance and for maintaining any state that might be set on the builder across multiple method calls.

Scenario 8: VertexCredentials with Empty Optional Fields

Details:
  TestName: vertexCredentialsWithEmptyOptionalFields
  Description: This test verifies that the builder correctly handles VertexCredentials objects where the optional fields (project, location, credentials) are empty Optional values.
Execution:
  Arrange: Create a VertexCredentials object with empty Optional values for project, location, and credentials. Create a new Builder instance with a valid modelName.
  Act: Set the vertexCredentials with the empty-optional credentials object. Call build() to create the Gemini instance.
  Assert: Verify that the Gemini instance is created successfully. Verify that the Client is built with default values when optional fields are not present.
Validation:
  This test validates that the builder gracefully handles VertexCredentials with empty optional fields, which is a valid use case. It ensures that the ifPresent() calls in the constructor work correctly and that the system can operate with minimal credential configuration when appropriate.

*/

// ********RoostGPT********

package com.google.adk.models;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

import com.google.genai.Client;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

class BuilderVertexCredentialsTest {

  @Mock private VertexCredentials mockVertexCredentials;

  @Mock private Client mockClient;

  private static final String VALID_MODEL_NAME = "gemini-pro";

  @BeforeEach
  void setUp() {
    MockitoAnnotations.openMocks(this);
  }

  @Test
  @Tag("valid")
  void vertexCredentialsWithValidCredentials() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();

    Gemini.Builder builder = Gemini.builder();
    Gemini.Builder returnedBuilder =
        builder.modelName(VALID_MODEL_NAME).vertexCredentials(validCredentials);

    assertNotNull(returnedBuilder);
    assertDoesNotThrow(() -> returnedBuilder.build());

    Gemini gemini = returnedBuilder.build();
    assertNotNull(gemini);
  }

  @Test
  @Tag("invalid")
  void vertexCredentialsWithNullCredentials() {
    Gemini.Builder builder = Gemini.builder();

    Gemini.Builder returnedBuilder = builder.modelName(VALID_MODEL_NAME).vertexCredentials(null);

    assertNotNull(returnedBuilder);
    assertDoesNotThrow(() -> returnedBuilder.build());
  }

  @Test
  @Tag("valid")
  void vertexCredentialsSupportsMethodChaining() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();

    Gemini gemini =
        Gemini.builder().modelName(VALID_MODEL_NAME).vertexCredentials(validCredentials).build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("valid")
  void vertexCredentialsOverridesPreviousValue() {
    VertexCredentials credentials1 = VertexCredentials.builder().build();
    VertexCredentials credentials2 = VertexCredentials.builder().build();

    Gemini.Builder builder =
        Gemini.builder()
            .modelName(VALID_MODEL_NAME)
            .vertexCredentials(credentials1)
            .vertexCredentials(credentials2);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertDoesNotThrow(() -> builder.build());
  }

  @Test
  @Tag("integration")
  void vertexCredentialsWithCompleteBuilderConfiguration() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();
    String apiKey = "test-api-key";
    Client client = Client.builder().build();

    Gemini.Builder builder =
        Gemini.builder()
            .modelName(VALID_MODEL_NAME)
            .apiKey(apiKey)
            .apiClient(client)
            .vertexCredentials(validCredentials);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("valid")
  void vertexCredentialsWithOnlyModelName() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();

    Gemini.Builder builder =
        Gemini.builder().modelName(VALID_MODEL_NAME).vertexCredentials(validCredentials);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("valid")
  void vertexCredentialsReturnsSameBuilderInstance() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();

    Gemini.Builder builder = Gemini.builder();
    Gemini.Builder returnedBuilder = builder.vertexCredentials(validCredentials);

    assertSame(builder, returnedBuilder);
    assertEquals(System.identityHashCode(builder), System.identityHashCode(returnedBuilder));
  }

  @Test
  @Tag("boundary")
  void vertexCredentialsWithEmptyOptionalFields() {
    VertexCredentials credentialsWithEmptyOptionals = VertexCredentials.builder().build();

    Gemini.Builder builder =
        Gemini.builder()
            .modelName(VALID_MODEL_NAME)
            .vertexCredentials(credentialsWithEmptyOptionals);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("valid")
  void vertexCredentialsMultipleCallsWithDifferentValues() {
    VertexCredentials credentials1 = VertexCredentials.builder().build();
    VertexCredentials credentials2 = VertexCredentials.builder().build();
    VertexCredentials credentials3 = VertexCredentials.builder().build();

    Gemini.Builder builder =
        Gemini.builder()
            .modelName(VALID_MODEL_NAME)
            .vertexCredentials(credentials1)
            .vertexCredentials(credentials2)
            .vertexCredentials(credentials3);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
  }

  @Test
  @Tag("valid")
  void vertexCredentialsWithMockCredentials() {
    Gemini.Builder builder =
        Gemini.builder().modelName(VALID_MODEL_NAME).vertexCredentials(mockVertexCredentials);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("invalid")
  void vertexCredentialsWithNullModelNameThrowsException() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();

    Gemini.Builder builder = Gemini.builder().vertexCredentials(validCredentials);

    assertThrows(NullPointerException.class, () -> builder.build());
  }

  @Test
  @Tag("valid")
  void vertexCredentialsSetBeforeModelName() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();

    Gemini.Builder builder =
        Gemini.builder().vertexCredentials(validCredentials).modelName(VALID_MODEL_NAME);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("integration")
  void vertexCredentialsPrecedenceWithApiKey() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();
    String apiKey = "test-api-key";

    Gemini.Builder builder =
        Gemini.builder()
            .modelName(VALID_MODEL_NAME)
            .vertexCredentials(validCredentials)
            .apiKey(apiKey);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("integration")
  void vertexCredentialsPrecedenceWithApiClient() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();
    Client client = Client.builder().build();

    Gemini.Builder builder =
        Gemini.builder()
            .modelName(VALID_MODEL_NAME)
            .vertexCredentials(validCredentials)
            .apiClient(client);

    Gemini gemini = builder.build();

    assertNotNull(gemini);
    assertEquals(VALID_MODEL_NAME, gemini.model());
  }

  @Test
  @Tag("valid")
  void vertexCredentialsReturnsBuilderForFurtherConfiguration() {
    VertexCredentials validCredentials = VertexCredentials.builder().build();

    Gemini.Builder builder = Gemini.builder();
    Gemini.Builder returnedBuilder = builder.vertexCredentials(validCredentials);

    assertNotNull(returnedBuilder);
    assertSame(builder, returnedBuilder);

    returnedBuilder.modelName(VALID_MODEL_NAME);
    Gemini gemini = returnedBuilder.build();

    assertNotNull(gemini);
  }
}
