/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test maven-adkjava-github using AI Type Azure Open AI and AI Model gpt-4.1

ROOST_METHOD_HASH=proxyUrl_3e147df46b
ROOST_METHOD_SIG_HASH=proxyUrl_611a9955d6

Scenario 1: Setting proxyUrl with a valid HTTP URL string

Details:
  TestName: proxyUrlSetsValidHttpUrl
  Description: This test ensures that when the proxyUrl method is called with a valid HTTP URL string, the Builder's proxyUrl field is correctly assigned with the provided value. The method should return the same Builder instance for chaining.

Execution:
  Arrange: Create a Builder instance and prepare a valid HTTP URL string (e.g., "http://example.com/proxy").
  Act: Call the proxyUrl method on the Builder with this URL.
  Assert: Verify the Builder’s proxyUrl field matches the input string and the returned Builder object is the same instance.
Validation:
  This assertion checks that the method properly stores the proxy address for use in subsequent build operations and supports method chaining, which is a fundamental part of the Builder pattern.

Scenario 2: Setting proxyUrl with a valid HTTPS URL string

Details:
  TestName: proxyUrlSetsValidHttpsUrl
  Description: Verifies that passing an HTTPS proxy URL to proxyUrl correctly sets the proxyUrl field in Builder. Also checks method chaining.

Execution:
  Arrange: Initialize a Builder instance and a valid HTTPS URL (e.g., "https://secure.example.com/proxy").
  Act: Invoke proxyUrl using the HTTPS URL.
  Assert: Confirm that proxyUrl field of Builder equals the input string, and the returned Builder instance is the same object.
Validation:
  Validates that the proxy URL field accepts and retains secure URLs, which are often required for production deployments.

Scenario 3: Passing an empty string ("") to proxyUrl

Details:
  TestName: proxyUrlHandlesEmptyString
  Description: Checks whether the Builder's proxyUrl field is set to an empty string when proxyUrl is called with an empty string. Ensures method chaining works.

Execution:
  Arrange: Create a Builder object, and define an empty string.
  Act: Call proxyUrl("").
  Assert: The Builder’s proxyUrl field should be an empty string, and the returned Builder should be the same instance.
Validation:
  This tests handling of empty values, which may prompt subsequent logic to default to environment variables during build. Ensures proper assignment and safe chaining.

Scenario 4: Passing a null value to proxyUrl

Details:
  TestName: proxyUrlHandlesNullValue
  Description: Tests whether the Builder stores null in proxyUrl field when invoked with null and returns self for chaining.

Execution:
  Arrange: Instantiate a Builder.
  Act: Call proxyUrl(null).
  Assert: Builder's proxyUrl field is null, and the returned object is the Builder instance itself.
Validation:
  Important for verifying robustness against null input. A null proxyUrl triggers further logic to check environment variables in build, so correct assignment is necessary.

Scenario 5: Setting proxyUrl with an invalid format string

Details:
  TestName: proxyUrlStoresInvalidUrlString
  Description: Tests if proxyUrl method simply assigns any string value (even an invalid URL, e.g., "abc123" or "not-a-url") to the proxyUrl field.

Execution:
  Arrange: Create a Builder, and use a string that is not a valid URL.
  Act: Call proxyUrl("not-a-url").
  Assert: The proxyUrl field equals the string "not-a-url"; Builder instance is unchanged.
Validation:
  Shows that proxyUrl does not validate the format, and relies on later validation or business logic. Confirms the method only stores values and supports chaining.

Scenario 6: Chaining modelName and proxyUrl methods

Details:
  TestName: builderChainingModelNameAndProxyUrl
  Description: Ensures that proxyUrl supports chaining with other setter methods like modelName, allowing construction of complex Builder objects with multiple calls.

Execution:
  Arrange: Instantiate a Builder, invoke modelName() and proxyUrl() in succession.
  Act: builder.modelName("apigee/gemini-2.5-flash").proxyUrl("http://proxy").
  Assert: Both fields (modelName, proxyUrl) are correctly set, and returned Builder supports further chaining.
Validation:
  This confirms compliance with the fluent Builder API, ensuring clients can conveniently set multiple fields.

Scenario 7: Chaining proxyUrl with itself multiple times

Details:
  TestName: proxyUrlAllowsMultipleAssignments
  Description: Verify that repeated calls to proxyUrl update the field to the latest value and always return the same Builder object.

Execution:
  Arrange: Create a Builder, set proxyUrl multiple times with different values.
  Act: builder.proxyUrl("http://first").proxyUrl("http://second").
  Assert: The proxyUrl field is "http://second" and chaining works.
Validation:
  This guarantees that clients can overwrite this field if needed mid-construction, and the latest value always takes precedence.

Scenario 8: Setting proxyUrl with special characters and unicode

Details:
  TestName: proxyUrlAcceptsSpecialCharacters
  Description: Evaluates whether proxyUrl preserves values with special characters or unicode symbols (e.g., "https://example.com/proxy?token=☂️").

Execution:
  Arrange: Create Builder; define a URL with special characters.
  Act: Call proxyUrl("https://example.com/proxy?token=☂️").
  Assert: proxyUrl field equals the input, returned Builder is self.
Validation:
  Ensures no unexpected encoding or truncation, supporting internationalization or unusual tokens.

Scenario 9: Setting proxyUrl with trailing spaces

Details:
  TestName: proxyUrlHandlesTrailingSpaces
  Description: Tests assignment of proxy URL strings with trailing spaces, checking if they are stored as provided.

Execution:
  Arrange: Builder instance, string "http://example.com/proxy ".
  Act: builder.proxyUrl("http://example.com/proxy ").
  Assert: proxyUrl field is "http://example.com/proxy ", identical to input.
Validation:
  Ensures the method doesn’t trim input or modify client-provided values, giving flexibility to downstream parsing.

Scenario 10: Setting proxyUrl when customHeaders field is already populated

Details:
  TestName: proxyUrlWorksWithCustomHeadersSet
  Description: Verifies that proxyUrl sets its value irrespective of existing customHeaders field in Builder.

Execution:
  Arrange: Set up a Builder instance, populate customHeaders, then call proxyUrl.
  Act: builder.customHeaders(headers).proxyUrl("http://proxy.example").
  Assert: proxyUrl field is set as expected, customHeaders remains unchanged.
Validation:
  Ensures separation of concerns and composability within Builder, vital for robust configuration management.

Scenario 11: Setting proxyUrl with a very long string

Details:
  TestName: proxyUrlHandlesVeryLongUrl
  Description: Tests whether proxyUrl can store very long URL strings without issues.

Execution:
  Arrange: Builder instance; define a string with length over 2048 characters.
  Act: builder.proxyUrl(longUrlString).
  Assert: proxyUrl field contains longUrlString, Builder returned is self.
Validation:
  Ensures scalability and robustness for large URLs, which can occur in complex cloud deployments.

Scenario 12: Comparing proxyUrl before and after calling method

Details:
  TestName: proxyUrlUpdatesFieldValue
  Description: Confirm that proxyUrl field is updated only after method invocation, not before.

Execution:
  Arrange: Builder instance, check proxyUrl field before and after calling proxyUrl.
  Act: Compare state before and after builder.proxyUrl("http://new-proxy").
  Assert: Field changes from initial (null or previous value) to new value.
Validation:
  Validates the assignment logic and timing, ensuring correct sequencing relative to other Builder steps.

Scenario 13: Setting proxyUrl with a URL containing environment variable reference

Details:
  TestName: proxyUrlStoresEnvVariableReference
  Description: Validates that a proxyUrl method call with a string containing environment variable syntax (e.g., "$APIGEE_PROXY_URL") is stored verbatim.

Execution:
  Arrange: Builder instance, URL string "$APIGEE_PROXY_URL/example".
  Act: builder.proxyUrl("$APIGEE_PROXY_URL/example").
  Assert: proxyUrl field equals the input.
Validation:
  This scenario confirms that proxyUrl does not substitute or process environment variables, preserving all literal values for further processing.

Scenario 14: Setting proxyUrl given other fields are null or empty

Details:
  TestName: proxyUrlWorksWithOtherFieldsUnset
  Description: Ensures proxyUrl can be set even if modelName and customHeaders fields are uninitialized, without error.

Execution:
  Arrange: Builder instance; ensure other fields are null or empty.
  Act: builder.proxyUrl("http://proxy.example").
  Assert: proxyUrl field is set, Builder returned is self.
Validation:
  Confirms consistency and resilience of the setter method regardless of Builder initialization state.

Scenario 15: Setting proxyUrl after build was called and reusing Builder instance (if allowed)

Details:
  TestName: proxyUrlCalledAfterBuild
  Description: Tests behavior when proxyUrl is invoked after build, to see if Builder supports mutation post-build or if user gets an error or unintended outcome.

Execution:
  Arrange: Builder instance, call build() to create ApigeeLlm, then call proxyUrl.
  Act: builder.build(); builder.proxyUrl("http://reused-proxy").
  Assert: proxyUrl field is updated in Builder, but original ApigeeLlm instance is unchanged.
Validation:
  Confirms proper isolation between Builder and built object, and mutation safety after build. Ensures Builder can be reused for new objects with updated configuration.

Scenario 16: proxyUrl returns Builder for method chaining (structure validation)

Details:
  TestName: proxyUrlSupportsFluentApiChaining
  Description: Checks that the returned value from proxyUrl is always the same Builder object, supporting fluent chaining with multiple configuration calls.

Execution:
  Arrange: Builder instance.
  Act: Call builder.proxyUrl("http://proxy").proxyUrl("http://next").
  Assert: Returned object is always Builder.
Validation:
  Validates Builder pattern contracts, fundamental for client code readability and maintainability.

Scenario 17: Setting proxyUrl with input containing newline characters

Details:
  TestName: proxyUrlAcceptsNewlineCharacters
  Description: Provides input like "http://proxy\nanother" to proxyUrl to see if it stores even multi-line values without issue.

Execution:
  Arrange: Builder instance; string with newline.
  Act: builder.proxyUrl("http://proxy\nanother").
  Assert: proxyUrl field equals input string.
Validation:
  Tests robustness and transparency of storage; ensures no inadvertent filtering or alteration.

Scenario 18: Setting proxyUrl with input containing URL-encoded characters

Details:
  TestName: proxyUrlStoresUrlEncodedCharacters
  Description: Checks that proxyUrl can handle values with percent-encoded characters (e.g., "http://proxy.com/path?query=%20").

Execution:
  Arrange: Builder instance; string with URL encoding.
  Act: builder.proxyUrl("http://proxy.com/path?query=%20").
  Assert: proxyUrl field equals input.
Validation:
  Ensures support for technically correct but odd URL values, facilitating interoperability.

Scenario 19: proxyUrl invocation with multiple Builder instances

Details:
  TestName: proxyUrlWorksIndependentlyPerBuilderInstance
  Description: Validates that invoking proxyUrl on two separate Builder objects sets proxyUrl independently, with no interference.

Execution:
  Arrange: Create two Builder instances.
  Act: builder1.proxyUrl("http://proxy1"); builder2.proxyUrl("http://proxy2").
  Assert: Each Builder’s proxyUrl field is set as called.
Validation:
  Ensures proper use of local fields per Builder instance, avoiding static/global state bugs.

Scenario 20: Setting proxyUrl with an IPv6 URL

Details:
  TestName: proxyUrlAcceptsIPv6Url
  Description: Checks if proxyUrl stores URLs using IPv6 address notation (e.g., "http://[2001:db8::1]/proxy") correctly.

Execution:
  Arrange: Builder instance; IPv6-formatted URL.
  Act: builder.proxyUrl("http://[2001:db8::1]/proxy").
  Assert: proxyUrl field equals the input string.
Validation:
  Tests compliance with modern networking practices and full URL storage support.


*/

// ********RoostGPT********

package com.google.adk.models;

import java.util.HashMap;
import java.util.Map;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

public class BuilderProxyUrlTest {

  @Test
  @Tag("valid")
  @DisplayName("Scenario 1: Setting proxyUrl with a valid HTTP URL string")
  public void testProxyUrlSetsValidHttpUrl() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String validHttpUrl = "http://example.com/proxy";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(validHttpUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) validHttpUrl, getProxyUrl(builder));
  }

  @Test
  @Tag("valid")
  @DisplayName("Scenario 2: Setting proxyUrl with a valid HTTPS URL string")
  public void testProxyUrlSetsValidHttpsUrl() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String validHttpsUrl = "https://secure.example.com/proxy";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(validHttpsUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) validHttpsUrl, getProxyUrl(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 3: Passing an empty string ('') to proxyUrl")
  public void testProxyUrlHandlesEmptyString() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String emptyString = "";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(emptyString);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) emptyString, getProxyUrl(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 4: Passing a null value to proxyUrl")
  public void testProxyUrlHandlesNullValue() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(null);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertNull((String) getProxyUrl(builder));
  }

  @Test
  @Tag("invalid")
  @DisplayName("Scenario 5: Setting proxyUrl with an invalid format string")
  public void testProxyUrlStoresInvalidUrlString() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String invalidUrl = "not-a-url";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(invalidUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) invalidUrl, getProxyUrl(builder));
  }

  @Test
  @Tag("valid")
  @DisplayName("Scenario 6: Chaining modelName and proxyUrl methods")
  public void testBuilderChainingModelNameAndProxyUrl() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String model = "apigee/gemini-2.5-flash";
    String url = "http://proxy";
    ApigeeLlm.Builder returnedBuilder = builder.modelName(model).proxyUrl(url);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) model, getModelName(builder));
    Assertions.assertEquals((String) url, getProxyUrl(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 7: Chaining proxyUrl with itself multiple times")
  public void testProxyUrlAllowsMultipleAssignments() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String firstUrl = "http://first";
    String secondUrl = "http://second";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(firstUrl).proxyUrl(secondUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) secondUrl, getProxyUrl(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 8: Setting proxyUrl with special characters and unicode")
  public void testProxyUrlAcceptsSpecialCharacters() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String specialCharUrl = "https://example.com/proxy?token=☂️";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(specialCharUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) specialCharUrl, getProxyUrl(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 9: Setting proxyUrl with trailing spaces")
  public void testProxyUrlHandlesTrailingSpaces() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String urlWithSpace = "http://example.com/proxy ";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(urlWithSpace);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) urlWithSpace, getProxyUrl(builder));
  }

  @Test
  @Tag("integration")
  @DisplayName("Scenario 10: Setting proxyUrl when customHeaders field is already populated")
  public void testProxyUrlWorksWithCustomHeadersSet() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    Map<String, String> headers = new HashMap<>();
    headers.put("X-Test", "value");
    builder.customHeaders(headers);
    String url = "http://proxy.example";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(url);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) url, getProxyUrl(builder));
    Assertions.assertEquals((Map<String, String>) headers, getCustomHeaders(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 11: Setting proxyUrl with a very long string")
  public void testProxyUrlHandlesVeryLongUrl() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    StringBuilder longUrlBuilder = new StringBuilder();
    for (int i = 0; i < 2050; i++) {
      longUrlBuilder.append('a');
    }
    String longUrl = longUrlBuilder.toString();
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(longUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) longUrl, getProxyUrl(builder));
    Assertions.assertTrue(getProxyUrl(builder).length() > 2048);
  }

  @Test
  @Tag("valid")
  @DisplayName("Scenario 12: Comparing proxyUrl before and after calling method")
  public void testProxyUrlUpdatesFieldValue() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String initialValue = getProxyUrl(builder);
    String newUrl = "http://new-proxy";
    builder.proxyUrl(newUrl);
    String afterValue = getProxyUrl(builder);
    Assertions.assertNotEquals((String) newUrl, initialValue);
    Assertions.assertEquals((String) newUrl, afterValue);
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 13: Setting proxyUrl with a URL containing environment variable reference")
  public void testProxyUrlStoresEnvVariableReference() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String envRefUrl = "$APIGEE_PROXY_URL/example";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(envRefUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) envRefUrl, getProxyUrl(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 14: Setting proxyUrl given other fields are null or empty")
  public void testProxyUrlWorksWithOtherFieldsUnset() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    // Do not set modelName or customHeaders
    String url = "http://proxy.example";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(url);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) url, getProxyUrl(builder));
  }

  @Test
  @Tag("integration")
  @DisplayName("Scenario 15: Setting proxyUrl after build was called and reusing Builder instance")
  public void testProxyUrlCalledAfterBuild() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    builder.modelName("apigee/gemini/v1/flash"); // TODO: Change to valid model string
    // as needed
    builder.proxyUrl("http://old-proxy");
    builder.customHeaders(new HashMap<>());
    ApigeeLlm apigeeLlmInstance = builder.build();
    String newUrl = "http://reused-proxy";
    builder.proxyUrl(newUrl);
    Assertions.assertEquals((String) newUrl, getProxyUrl(builder));
    // Comment: apigeeLlmInstance.proxyUrl is not accessible. Business logic should
    // expose proxyUrl getter for ApigeeLlm instance if comparison is required.
    // Assertions.assertNotEquals((String)newUrl, apigeeLlmInstance.proxyUrl);
  }

  @Test
  @Tag("valid")
  @DisplayName("Scenario 16: proxyUrl returns Builder for method chaining (structure validation)")
  public void testProxyUrlSupportsFluentApiChaining() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    ApigeeLlm.Builder returnedBuilder1 = builder.proxyUrl("http://proxy");
    ApigeeLlm.Builder returnedBuilder2 = returnedBuilder1.proxyUrl("http://next");
    Assertions.assertSame(builder, returnedBuilder1);
    Assertions.assertSame(builder, returnedBuilder2);
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 17: Setting proxyUrl with input containing newline characters")
  public void testProxyUrlAcceptsNewlineCharacters() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String newlineUrl = "http://proxy\nanother";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(newlineUrl);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) newlineUrl, getProxyUrl(builder));
  }

  @Test
  @Tag("boundary")
  @DisplayName("Scenario 18: Setting proxyUrl with input containing URL-encoded characters")
  public void testProxyUrlStoresUrlEncodedCharacters() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String urlEncoded = "http://proxy.com/path?query=%20";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(urlEncoded);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) urlEncoded, getProxyUrl(builder));
  }

  @Test
  @Tag("integration")
  @DisplayName("Scenario 19: proxyUrl invocation with multiple Builder instances")
  public void testProxyUrlWorksIndependentlyPerBuilderInstance() {
    ApigeeLlm.Builder builder1 = ApigeeLlm.builder();
    ApigeeLlm.Builder builder2 = ApigeeLlm.builder();
    String url1 = "http://proxy1";
    String url2 = "http://proxy2";
    builder1.proxyUrl(url1);
    builder2.proxyUrl(url2);
    Assertions.assertEquals((String) url1, getProxyUrl(builder1));
    Assertions.assertEquals((String) url2, getProxyUrl(builder2));
    Assertions.assertNotEquals((String) getProxyUrl(builder1), getProxyUrl(builder2));
  }

  @Test
  @Tag("valid")
  @DisplayName("Scenario 20: Setting proxyUrl with an IPv6 URL")
  public void testProxyUrlAcceptsIPv6Url() {
    ApigeeLlm.Builder builder = ApigeeLlm.builder();
    String ipv6Url = "http://[2001:db8::1]/proxy";
    ApigeeLlm.Builder returnedBuilder = builder.proxyUrl(ipv6Url);
    Assertions.assertSame(builder, returnedBuilder);
    Assertions.assertEquals((String) ipv6Url, getProxyUrl(builder));
  }

  // Helper functions for reflection-based access of private fields (since business
  // logic doesn't have getters)
  private String getProxyUrl(ApigeeLlm.Builder builder) {
    try {
      java.lang.reflect.Field field = builder.getClass().getDeclaredField("proxyUrl");
      field.setAccessible(true);
      return (String) field.get(builder);
    } catch (Exception e) {
      throw new RuntimeException("Unable to access proxyUrl field", e);
    }
  }

  private String getModelName(ApigeeLlm.Builder builder) {
    try {
      java.lang.reflect.Field field = builder.getClass().getDeclaredField("modelName");
      field.setAccessible(true);
      return (String) field.get(builder);
    } catch (Exception e) {
      throw new RuntimeException("Unable to access modelName field", e);
    }
  }

  private Map<String, String> getCustomHeaders(ApigeeLlm.Builder builder) {
    try {
      java.lang.reflect.Field field = builder.getClass().getDeclaredField("customHeaders");
      field.setAccessible(true);
      return (Map<String, String>) field.get(builder);
    } catch (Exception e) {
      throw new RuntimeException("Unable to access customHeaders field", e);
    }
  }
}
