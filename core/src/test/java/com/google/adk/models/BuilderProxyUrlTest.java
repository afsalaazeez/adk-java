/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=proxyUrl_3e147df46b
ROOST_METHOD_SIG_HASH=proxyUrl_611a9955d6

Scenario 1: Setting a Valid Proxy URL

Details:
  TestName: setValidProxyUrl
  Description: Verify that the proxyUrl method correctly sets a valid proxy URL string and returns the Builder instance for method chaining.

Execution:
  Arrange: Create a new Builder instance using ApigeeLlm.builder().
  Act: Invoke the proxyUrl method with a valid URL string "https://api.example.com/proxy".
  Assert: Verify that the returned object is the same Builder instance (for method chaining support).

Validation:
  The assertion verifies that the method returns the Builder instance itself, enabling fluent API design. This is essential for the builder pattern implementation where multiple configuration methods can be chained together before calling build().

---

Scenario 2: Setting a Null Proxy URL

Details:
  TestName: setNullProxyUrl
  Description: Verify that the proxyUrl method accepts a null value without throwing an exception, as the null handling is deferred to the build() method or constructor.

Execution:
  Arrange: Create a new Builder instance using ApigeeLlm.builder().
  Act: Invoke the proxyUrl method with null as the parameter.
  Assert: Verify that the method returns the Builder instance without throwing an exception.

Validation:
  The assertion confirms that the proxyUrl method does not perform null validation itself. According to the ApigeeLlm constructor logic, null proxy URLs are handled later by checking the APIGEE_PROXY_URL environment variable. This test ensures the builder remains flexible during configuration.

---

Scenario 3: Setting an Empty String Proxy URL

Details:
  TestName: setEmptyStringProxyUrl
  Description: Verify that the proxyUrl method accepts an empty string value, with validation deferred to the build phase.

Execution:
  Arrange: Create a new Builder instance using ApigeeLlm.builder().
  Act: Invoke the proxyUrl method with an empty string "".
  Assert: Verify that the method returns the Builder instance without throwing an exception.

Validation:
  The assertion confirms that empty string handling is deferred to the ApigeeLlm constructor, which uses isNullOrEmpty to check and potentially fall back to environment variables. This maintains separation of concerns between configuration and validation.

---

Scenario 4: Method Chaining with proxyUrl

Details:
  TestName: methodChainingWithProxyUrl
  Description: Verify that the proxyUrl method can be chained with other Builder methods like modelName and customHeaders.

Execution:
  Arrange: Create a new Builder instance using ApigeeLlm.builder().
  Act: Chain multiple method calls: modelName("apigee/gemini-2.5-flash").proxyUrl("https://proxy.example.com").customHeaders(new HashMap<>()).
  Assert: Verify that all chained methods execute successfully and return Builder instances.

Validation:
  The assertion validates the fluent API design pattern, ensuring that proxyUrl integrates seamlessly with other builder methods. This is critical for developer experience and code readability when configuring ApigeeLlm instances.

---

Scenario 5: Overwriting Previously Set Proxy URL

Details:
  TestName: overwritePreviouslySetProxyUrl
  Description: Verify that calling proxyUrl multiple times overwrites the previous value with the new one.

Execution:
  Arrange: Create a new Builder instance using ApigeeLlm.builder().
  Act: Call proxyUrl("https://first-proxy.com") followed by proxyUrl("https://second-proxy.com").
  Assert: Verify that the method returns the Builder instance after each call.

Validation:
  The assertion confirms that the builder follows standard behavior where subsequent calls to the same setter method replace the previous value. This is important for scenarios where configuration might be modified conditionally.

---

Scenario 6: Setting Proxy URL with Special Characters

Details:
  TestName: setProxyUrlWithSpecialCharacters
  Description: Verify that the proxyUrl method accepts URLs containing special characters such as query parameters and encoded values.

Execution:
  Arrange: Create a new Builder instance using ApigeeLlm.builder().
  Act: Invoke the proxyUrl method with a URL containing special characters "https://api.example.com/proxy?key=value&param=test%20data".
  Assert: Verify that the method returns the Builder instance without modification or exception.

Validation:
  The assertion ensures that the proxyUrl method acts as a simple setter without URL validation or encoding. URL validation, if needed, would occur during the actual HTTP connection phase, maintaining the builder's simplicity.

---

Scenario 7: Setting Proxy URL with Whitespace

Details:
  TestName: setProxyUrlWithWhitespace
  Description: Verify that the proxyUrl method accepts a string with leading or trailing whitespace without trimming.

Execution:
  Arrange: Create a new Builder instance using ApigeeLlm.builder().
  Act: Invoke the proxyUrl method with "  https://proxy.example.com  " (whitespace padded).
  Assert: Verify that the method returns the Builder instance.

Validation:
  The assertion confirms that the proxyUrl method stores the value as-is without modification. Any whitespace handling would be the responsibility of downstream components, maintaining the builder's role as a simple configuration holder.

---

Scenario 8: Setting Proxy URL After Other Builder Methods

Details:
  TestName: setProxyUrlAfterOtherBuilderMethods
  Description: Verify that proxyUrl can be called after other builder methods have been invoked, maintaining all previously set values.

Execution:
  Arrange: Create a new Builder instance and set modelName to "apigee/vertex_ai/gemini-2.5-flash" and customHeaders to a map with one entry.
  Act: Invoke the proxyUrl method with "https://proxy.example.com".
  Assert: Verify that the method returns the Builder instance and that the builder can still be used to build an ApigeeLlm (assuming environment is properly configured).

Validation:
  The assertion validates that the order of builder method calls does not affect the final configuration. This flexibility is essential for various coding styles and conditional configuration scenarios.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;

import java.util.HashMap;
import java.util.Map;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderProxyUrlTest {

  private ApigeeLlm.Builder builder;

  @BeforeEach
  void setUp() {
    builder = ApigeeLlm.builder();
  }

  @Test
  @Tag("valid")
  void setValidProxyUrl() {
    // Arrange
    String validProxyUrl = "https://api.example.com/proxy";
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(validProxyUrl);
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void setNullProxyUrl() {
    // Arrange - builder already created in setUp
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(null);
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("boundary")
  void setEmptyStringProxyUrl() {
    // Arrange - builder already created in setUp
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl("");
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void methodChainingWithProxyUrl() {
    // Arrange
    Map<String, String> headers = new HashMap<>();
    headers.put("X-Custom-Header", "value");
    // Act
    ApigeeLlm.Builder result =
        builder
            .modelName("apigee/gemini-2.5-flash")
            .proxyUrl("https://proxy.example.com")
            .customHeaders(headers);
    // Assert
    assertThat(result).isNotNull();
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void overwritePreviouslySetProxyUrl() {
    // Arrange
    String firstProxyUrl = "https://first-proxy.com";
    String secondProxyUrl = "https://second-proxy.com";
    // Act
    ApigeeLlm.Builder result1 = builder.proxyUrl(firstProxyUrl);
    ApigeeLlm.Builder result2 = builder.proxyUrl(secondProxyUrl);
    // Assert
    assertThat(result1).isSameInstanceAs(builder);
    assertThat(result2).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void setProxyUrlWithSpecialCharacters() {
    // Arrange
    String urlWithSpecialChars = "https://api.example.com/proxy?key=value&param=test%20data";
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(urlWithSpecialChars);
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("boundary")
  void setProxyUrlWithWhitespace() {
    // Arrange
    String urlWithWhitespace = "  https://proxy.example.com  ";
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(urlWithWhitespace);
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void setProxyUrlAfterOtherBuilderMethods() {
    // Arrange
    Map<String, String> headers = new HashMap<>();
    headers.put("Authorization", "Bearer token");

    builder.modelName("apigee/vertex_ai/v1/gemini-2.5-flash").customHeaders(headers);
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl("https://proxy.example.com");
    // Assert
    assertThat(result).isSameInstanceAs(builder);
    assertThat(result).isNotNull();
  }

  @Test
  @Tag("valid")
  void setProxyUrlReturnsBuilderForFurtherChaining() {
    // Arrange
    String proxyUrl = "https://proxy.example.com";
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(proxyUrl).modelName("apigee/gemini-2.5-flash");
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("boundary")
  void setProxyUrlWithOnlyWhitespace() {
    // Arrange
    String whitespaceOnly = "   ";
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(whitespaceOnly);
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void setProxyUrlWithPortNumber() {
    // Arrange
    String urlWithPort = "https://proxy.example.com:8443/api";
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(urlWithPort);
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void setProxyUrlWithHttpProtocol() {
    // Arrange
    String httpUrl = "http://proxy.example.com/api";
    // Act
    ApigeeLlm.Builder result = builder.proxyUrl(httpUrl);
    // Assert
    assertThat(result).isSameInstanceAs(builder);
  }

  @Test
  @Tag("valid")
  void multipleProxyUrlCallsReturnSameBuilder() {
    // Arrange & Act
    ApigeeLlm.Builder result1 = builder.proxyUrl("https://first.com");
    ApigeeLlm.Builder result2 = builder.proxyUrl("https://second.com");
    ApigeeLlm.Builder result3 = builder.proxyUrl("https://third.com");
    // Assert
    assertThat(result1).isSameInstanceAs(builder);
    assertThat(result2).isSameInstanceAs(builder);
    assertThat(result3).isSameInstanceAs(builder);
  }
}
