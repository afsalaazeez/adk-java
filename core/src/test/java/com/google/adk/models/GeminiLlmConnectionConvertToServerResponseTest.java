/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=convertToServerResponse_ab43d27414
ROOST_METHOD_SIG_HASH=convertToServerResponse_6349e4b91b

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/adk-java-repo/87694200-c46c-408c-b0b1-408cb60f3b49/source/adk-java/core/src/test/java/com/google/adk/models/GeminiLlmConnectionTest.java
Tests:
    "@Test
public void convertToServerResponse_withInterruptedTrue_mapsInterruptedField() {
    LiveServerContent serverContent = LiveServerContent.builder().modelTurn(Content.fromParts(Part.fromText("Model response"))).turnComplete(false).interrupted(true).build();
    LiveServerMessage message = LiveServerMessage.builder().serverContent(serverContent).build();
    LlmResponse response = GeminiLlmConnection.convertToServerResponse(message).get();
    assertThat(response.content()).isPresent();
    assertThat(response.content().get().text()).isEqualTo("Model response");
    assertThat(response.partial()).hasValue(true);
    assertThat(response.turnComplete()).hasValue(false);
    assertThat(response.interrupted()).hasValue(true);
}
"
    "@Test
public void convertToServerResponse_withInterruptedFalse_mapsInterruptedField() {
    LiveServerContent serverContent = LiveServerContent.builder().modelTurn(Content.fromParts(Part.fromText("Continuing response"))).turnComplete(false).interrupted(false).build();
    LiveServerMessage message = LiveServerMessage.builder().serverContent(serverContent).build();
    LlmResponse response = GeminiLlmConnection.convertToServerResponse(message).get();
    assertThat(response.interrupted()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
}
"
    "@Test
public void convertToServerResponse_withoutInterruptedField_mapsEmptyOptional() {
    LiveServerContent serverContent = LiveServerContent.builder().modelTurn(Content.fromParts(Part.fromText("Normal response"))).turnComplete(true).build();
    LiveServerMessage message = LiveServerMessage.builder().serverContent(serverContent).build();
    LlmResponse response = GeminiLlmConnection.convertToServerResponse(message).get();
    assertThat(response.interrupted()).isEmpty();
    assertThat(response.turnComplete()).hasValue(true);
}
"
    "@Test
public void convertToServerResponse_withTurnCompleteTrue_mapsPartialFalse() {
    LiveServerContent serverContent = LiveServerContent.builder().modelTurn(Content.fromParts(Part.fromText("Final response"))).turnComplete(true).build();
    LiveServerMessage message = LiveServerMessage.builder().serverContent(serverContent).build();
    LlmResponse response = GeminiLlmConnection.convertToServerResponse(message).get();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(true);
}
"
    "@Test
public void convertToServerResponse_withTurnCompleteFalse_mapsPartialTrue() {
    LiveServerContent serverContent = LiveServerContent.builder().modelTurn(Content.fromParts(Part.fromText("Partial response"))).turnComplete(false).build();
    LiveServerMessage message = LiveServerMessage.builder().serverContent(serverContent).build();
    LlmResponse response = GeminiLlmConnection.convertToServerResponse(message).get();
    assertThat(response.partial()).hasValue(true);
    assertThat(response.turnComplete()).hasValue(false);
}
"
    "@Test
public void convertToServerResponse_withToolCall_mapsContentWithFunctionCall() {
    FunctionCall functionCall = FunctionCall.builder().name("tool").build();
    LiveServerToolCall toolCall = LiveServerToolCall.builder().functionCalls(ImmutableList.of(functionCall)).build();
    LiveServerMessage message = LiveServerMessage.builder().toolCall(toolCall).build();
    LlmResponse response = GeminiLlmConnection.convertToServerResponse(message).get();
    assertThat(response.content()).isPresent();
    assertThat(response.content().get().parts()).isPresent();
    assertThat(response.content().get().parts().get()).hasSize(1);
    assertThat(response.content().get().parts().get().get(0).functionCall()).hasValue(functionCall);
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
}
"
    "@Test
public void convertToServerResponse_withUsageMetadata_returnsEmptyOptional() {
    LiveServerMessage message = LiveServerMessage.builder().usageMetadata(UsageMetadata.builder().build()).build();
    assertThat(GeminiLlmConnection.convertToServerResponse(message)).isEmpty();
}
"
    "@Test
public void convertToServerResponse_withToolCallCancellation_returnsEmptyOptional() {
    LiveServerMessage message = LiveServerMessage.builder().toolCallCancellation(LiveServerToolCallCancellation.builder().build()).build();
    assertThat(GeminiLlmConnection.convertToServerResponse(message)).isEmpty();
}
"
    "@Test
public void convertToServerResponse_withSetupComplete_returnsEmptyOptional() {
    LiveServerMessage message = LiveServerMessage.builder().setupComplete(LiveServerSetupComplete.builder().build()).build();
    assertThat(GeminiLlmConnection.convertToServerResponse(message)).isEmpty();
}
"
    "@Test
public void convertToServerResponse_withUnknownMessage_returnsErrorResponse() {
    LiveServerMessage message = LiveServerMessage.builder().build();
    LlmResponse response = GeminiLlmConnection.convertToServerResponse(message).get();
    assertThat(response.errorCode()).isPresent();
    assertThat(response.errorMessage()).hasValue("Received unknown server message.");
}
"Scenario 1: Server Content with Model Turn and No Turn Complete Field

Details:
  TestName: convertToServerResponse_withModelTurnAndNoTurnComplete_mapsPartialFalseAndTurnCompleteFalse
  Description: Verify that when a LiveServerMessage contains serverContent with a modelTurn but no turnComplete field, the response correctly defaults partial to false and turnComplete to false.

Execution:
  Arrange: Create a LiveServerContent with only modelTurn set (no turnComplete field), then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned Optional contains an LlmResponse with content present, partial set to false, and turnComplete set to false.

Validation:
  This test verifies the default behavior when turnComplete is absent. The method uses orElse(false) for both partial calculation and turnComplete, so when turnComplete is not present, partial should be false (since !completed maps to false when completed defaults to false) and turnComplete should be false. This ensures consistent behavior when the server omits the turnComplete field.

---

Scenario 2: Server Content with Empty Model Turn

Details:
  TestName: convertToServerResponse_withEmptyModelTurn_mapsContentAbsent
  Description: Verify that when serverContent is present but modelTurn is empty, the response is built without content but with appropriate partial and turnComplete values.

Execution:
  Arrange: Create a LiveServerContent with no modelTurn set but with turnComplete set to true, then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned Optional contains an LlmResponse with content absent, partial set to false, and turnComplete set to true.

Validation:
  This test ensures that the method handles cases where serverContent exists but contains no model turn. The ifPresent on modelTurn should not set content, but the partial and turnComplete fields should still be properly calculated. This is important for handling server acknowledgment messages that don't contain actual content.

---

Scenario 3: Tool Call with Multiple Function Calls

Details:
  TestName: convertToServerResponse_withMultipleFunctionCalls_mapsAllFunctionCallsToContent
  Description: Verify that when a LiveServerMessage contains a toolCall with multiple function calls, each function call is mapped to a separate content entry in the response.

Execution:
  Arrange: Create a LiveServerToolCall with a list containing multiple FunctionCall objects, then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned LlmResponse contains content with parts for each function call, and that partial is false and turnComplete is false.

Validation:
  This test verifies that the method correctly iterates through all function calls and adds each one to the builder. The loop in the method processes each FunctionCall individually, creating Content with a single Part containing the function call. This is critical for supporting multi-tool invocations from the model.

---

Scenario 4: Tool Call with Empty Function Calls List

Details:
  TestName: convertToServerResponse_withEmptyFunctionCallsList_returnsResponseWithNoContent
  Description: Verify that when a toolCall is present but contains an empty list of function calls, the response is built with partial false and turnComplete false but no content.

Execution:
  Arrange: Create a LiveServerToolCall with an empty ImmutableList of function calls, then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned Optional contains an LlmResponse with no content set, partial set to false, and turnComplete set to false.

Validation:
  This test ensures graceful handling of edge cases where a tool call message arrives but contains no actual function calls. The ifPresent on functionCalls will execute but the for loop will not iterate, resulting in no content being added. This prevents errors when the server sends malformed or empty tool call messages.

---

Scenario 5: Tool Call with No Function Calls Field

Details:
  TestName: convertToServerResponse_withToolCallNoFunctionCallsField_returnsResponseWithNoContent
  Description: Verify that when a toolCall is present but the functionCalls Optional is empty, the response is built correctly without content.

Execution:
  Arrange: Create a LiveServerToolCall without setting the functionCalls field, then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned Optional contains an LlmResponse with content absent, partial set to false, and turnComplete set to false.

Validation:
  This test verifies behavior when functionCalls Optional is not present. The ifPresent on functionCalls will not execute, so no content will be added. This handles cases where the server sends a toolCall message structure without the actual function calls data.

---

Scenario 6: Server Content with Turn Complete True and Interrupted True

Details:
  TestName: convertToServerResponse_withTurnCompleteTrueAndInterruptedTrue_mapsAllFieldsCorrectly
  Description: Verify that when serverContent has both turnComplete true and interrupted true, all fields are correctly mapped in the response.

Execution:
  Arrange: Create a LiveServerContent with modelTurn, turnComplete set to true, and interrupted set to true, then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned LlmResponse has content present, partial set to false (since turnComplete is true), turnComplete set to true, and interrupted set to true.

Validation:
  This test verifies the interaction between turnComplete and interrupted fields. When turnComplete is true, partial should be false (since partial = !completed). The interrupted field should be mapped independently. This scenario represents a case where the model's turn was completed but was also interrupted, which is an important edge case for conversation flow control.

---

Scenario 7: Server Content with Only Interrupted Field Set

Details:
  TestName: convertToServerResponse_withOnlyInterruptedFieldSet_mapsDefaultsCorrectly
  Description: Verify that when serverContent has only the interrupted field set (no modelTurn, no turnComplete), the response uses appropriate defaults.

Execution:
  Arrange: Create a LiveServerContent with only interrupted set to true (no modelTurn, no turnComplete), then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned LlmResponse has content absent, partial set to false, turnComplete set to false, and interrupted set to true.

Validation:
  This test ensures that the method handles serverContent with minimal fields. The defaults for turnComplete (false via orElse) should apply, making partial false as well. This represents a server notification about an interruption without accompanying content.

---

Scenario 8: Message with Both Server Content and Tool Call Present

Details:
  TestName: convertToServerResponse_withBothServerContentAndToolCall_prioritizesServerContent
  Description: Verify that when a LiveServerMessage has both serverContent and toolCall present, the serverContent branch is processed first due to the if-else structure.

Execution:
  Arrange: Create a LiveServerMessage with both serverContent (containing modelTurn) and toolCall (containing function calls) set.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned LlmResponse contains the content from serverContent, not from toolCall.

Validation:
  This test verifies the priority order of the if-else chain in the method. Since serverContent is checked first, it should take precedence over toolCall. This ensures predictable behavior when the server sends a message with multiple fields populated, which could indicate a protocol issue or edge case.

---

Scenario 9: Single Function Call in Tool Call

Details:
  TestName: convertToServerResponse_withSingleFunctionCall_mapsContentCorrectly
  Description: Verify that a tool call with exactly one function call creates a response with properly structured content containing that single function call.

Execution:
  Arrange: Create a LiveServerToolCall with a single FunctionCall that has a specific name and arguments, then wrap it in a LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned LlmResponse has content with exactly one part containing the function call with the correct name.

Validation:
  This test confirms the basic tool call handling for the most common case of a single function invocation. The content should be structured with a Part containing the functionCall. This is essential for the agent's ability to execute tools requested by the model.

---

Scenario 10: Server Content with Model Turn Containing Multiple Parts

Details:
  TestName: convertToServerResponse_withModelTurnMultipleParts_preservesAllParts
  Description: Verify that when serverContent contains a modelTurn with multiple parts (e.g., text and other content), all parts are preserved in the response.

Execution:
  Arrange: Create a Content object with multiple Part objects, use it as modelTurn in LiveServerContent with turnComplete true, then wrap in LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned LlmResponse content contains all the original parts from the modelTurn.

Validation:
  This test ensures that the method preserves the complete structure of the model's response when it contains multiple parts. The ifPresent on modelTurn passes the entire Content object to the builder, so all parts should be retained. This is important for handling rich responses that may include text, code, or other content types.

---

Scenario 11: Empty LiveServerMessage with All Optional Fields Empty

Details:
  TestName: convertToServerResponse_withAllFieldsEmpty_returnsErrorResponse
  Description: Verify that when a LiveServerMessage has all optional fields empty (serverContent, toolCall, usageMetadata, toolCallCancellation, setupComplete), an error response is returned.

Execution:
  Arrange: Create a LiveServerMessage using builder with no fields set.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that the returned Optional contains an LlmResponse with errorCode containing "Unknown server message." and errorMessage set to "Received unknown server message."

Validation:
  This test verifies the fallback error handling when none of the expected message types are present. The else branch should be triggered, setting appropriate error information. This is crucial for debugging and handling unexpected server behavior gracefully.

---

Scenario 12: Server Content with Turn Complete Absent and Interrupted Absent

Details:
  TestName: convertToServerResponse_withTurnCompleteAbsentAndInterruptedAbsent_usesDefaults
  Description: Verify that when serverContent has modelTurn but both turnComplete and interrupted are absent, the response uses default values correctly.

Execution:
  Arrange: Create a LiveServerContent with only modelTurn set (no turnComplete, no interrupted), then wrap in LiveServerMessage.
  Act: Call GeminiLlmConnection.convertToServerResponse with the message.
  Assert: Verify that partial is false, turnComplete is false, and interrupted is empty Optional.

Validation:
  This test confirms the default value behavior for all optional fields in serverContent. The orElse(false) calls should provide false for both partial calculation and turnComplete, while interrupted should remain as whatever the serverContent provides (empty if not set). This ensures consistent behavior with minimal server data.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import com.google.common.collect.ImmutableList;
import com.google.genai.types.Content;
import com.google.genai.types.FunctionCall;
import com.google.genai.types.LiveServerContent;
import com.google.genai.types.LiveServerMessage;
import com.google.genai.types.LiveServerSetupComplete;
import com.google.genai.types.LiveServerToolCall;
import com.google.genai.types.LiveServerToolCallCancellation;
import com.google.genai.types.Part;
import com.google.genai.types.UsageMetadata;
import java.util.Optional;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class GeminiLlmConnectionConvertToServerResponseTest {

  @Test
  @Tag("valid")
  void
      convertToServerResponse_withModelTurnAndNoTurnComplete_mapsPartialFalseAndTurnCompleteFalse() {
    // Arrange
    Content modelTurn =
        Content.builder().parts(ImmutableList.of(Part.builder().text("Hello").build())).build();
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.of(modelTurn));
    when(serverContent.turnComplete()).thenReturn(Optional.empty());
    when(serverContent.interrupted()).thenReturn(Optional.empty());
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isPresent();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withEmptyModelTurn_mapsContentAbsent() {
    // Arrange
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.empty());
    when(serverContent.turnComplete()).thenReturn(Optional.of(true));
    when(serverContent.interrupted()).thenReturn(Optional.empty());
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isEmpty();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(true);
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withMultipleFunctionCalls_mapsAllFunctionCallsToContent() {
    // Arrange
    FunctionCall functionCall1 = FunctionCall.builder().name("function1").build();
    FunctionCall functionCall2 = FunctionCall.builder().name("function2").build();
    ImmutableList<FunctionCall> functionCalls = ImmutableList.of(functionCall1, functionCall2);
    LiveServerToolCall toolCall = mock(LiveServerToolCall.class);
    when(toolCall.functionCalls()).thenReturn(Optional.of(functionCalls));
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.of(toolCall));
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isPresent();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
  }

  @Test
  @Tag("boundary")
  void convertToServerResponse_withEmptyFunctionCallsList_returnsResponseWithNoContent() {
    // Arrange
    ImmutableList<FunctionCall> emptyFunctionCalls = ImmutableList.of();
    LiveServerToolCall toolCall = mock(LiveServerToolCall.class);
    when(toolCall.functionCalls()).thenReturn(Optional.of(emptyFunctionCalls));
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.of(toolCall));
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
  }

  @Test
  @Tag("boundary")
  void convertToServerResponse_withToolCallNoFunctionCallsField_returnsResponseWithNoContent() {
    // Arrange
    LiveServerToolCall toolCall = mock(LiveServerToolCall.class);
    when(toolCall.functionCalls()).thenReturn(Optional.empty());
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.of(toolCall));
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isEmpty();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withTurnCompleteTrueAndInterruptedTrue_mapsAllFieldsCorrectly() {
    // Arrange
    Content modelTurn =
        Content.builder().parts(ImmutableList.of(Part.builder().text("Response").build())).build();
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.of(modelTurn));
    when(serverContent.turnComplete()).thenReturn(Optional.of(true));
    when(serverContent.interrupted()).thenReturn(Optional.of(true));
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isPresent();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(true);
    assertThat(response.interrupted()).hasValue(true);
  }

  @Test
  @Tag("boundary")
  void convertToServerResponse_withOnlyInterruptedFieldSet_mapsDefaultsCorrectly() {
    // Arrange
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.empty());
    when(serverContent.turnComplete()).thenReturn(Optional.empty());
    when(serverContent.interrupted()).thenReturn(Optional.of(true));
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isEmpty();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
    assertThat(response.interrupted()).hasValue(true);
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withBothServerContentAndToolCall_prioritizesServerContent() {
    // Arrange
    Content modelTurn =
        Content.builder()
            .parts(ImmutableList.of(Part.builder().text("Server content").build()))
            .build();
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.of(modelTurn));
    when(serverContent.turnComplete()).thenReturn(Optional.of(true));
    when(serverContent.interrupted()).thenReturn(Optional.empty());
    FunctionCall functionCall = FunctionCall.builder().name("testFunction").build();
    LiveServerToolCall toolCall = mock(LiveServerToolCall.class);
    when(toolCall.functionCalls()).thenReturn(Optional.of(ImmutableList.of(functionCall)));
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.of(toolCall));
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isPresent();
    assertThat(response.turnComplete()).hasValue(true);
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withSingleFunctionCall_mapsContentCorrectly() {
    // Arrange
    FunctionCall functionCall = FunctionCall.builder().name("singleFunction").build();
    ImmutableList<FunctionCall> functionCalls = ImmutableList.of(functionCall);
    LiveServerToolCall toolCall = mock(LiveServerToolCall.class);
    when(toolCall.functionCalls()).thenReturn(Optional.of(functionCalls));
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.of(toolCall));
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isPresent();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withModelTurnMultipleParts_preservesAllParts() {
    // Arrange
    Part textPart = Part.builder().text("Hello").build();
    Part codePart = Part.builder().text("code block").build();
    Content modelTurn = Content.builder().parts(ImmutableList.of(textPart, codePart)).build();
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.of(modelTurn));
    when(serverContent.turnComplete()).thenReturn(Optional.of(true));
    when(serverContent.interrupted()).thenReturn(Optional.empty());
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.content()).isPresent();
    assertThat(response.content().get().parts()).isPresent();
    assertThat(response.content().get().parts().get()).hasSize(2);
  }

  @Test
  @Tag("invalid")
  void convertToServerResponse_withAllFieldsEmpty_returnsErrorResponse() {
    // Arrange
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    when(message.toJson()).thenReturn("{}");
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.errorCode()).isPresent();
    assertThat(response.errorMessage()).hasValue("Received unknown server message.");
  }

  @Test
  @Tag("boundary")
  void convertToServerResponse_withTurnCompleteAbsentAndInterruptedAbsent_usesDefaults() {
    // Arrange
    Content modelTurn =
        Content.builder().parts(ImmutableList.of(Part.builder().text("Content").build())).build();
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.of(modelTurn));
    when(serverContent.turnComplete()).thenReturn(Optional.empty());
    when(serverContent.interrupted()).thenReturn(Optional.empty());
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.partial()).hasValue(false);
    assertThat(response.turnComplete()).hasValue(false);
    assertThat(response.interrupted()).isEmpty();
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withUsageMetadata_returnsEmptyOptional() {
    // Arrange
    UsageMetadata usageMetadata = mock(UsageMetadata.class);
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.of(usageMetadata));
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isEmpty();
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withToolCallCancellation_returnsEmptyOptional() {
    // Arrange
    LiveServerToolCallCancellation cancellation = mock(LiveServerToolCallCancellation.class);
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.of(cancellation));
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isEmpty();
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withSetupComplete_returnsEmptyOptional() {
    // Arrange
    LiveServerSetupComplete setupComplete = mock(LiveServerSetupComplete.class);
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.empty());
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.of(setupComplete));
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isEmpty();
  }

  @Test
  @Tag("valid")
  void convertToServerResponse_withTurnCompleteFalse_mapsPartialTrue() {
    // Arrange
    Content modelTurn =
        Content.builder()
            .parts(ImmutableList.of(Part.builder().text("Partial content").build()))
            .build();
    LiveServerContent serverContent = mock(LiveServerContent.class);
    when(serverContent.modelTurn()).thenReturn(Optional.of(modelTurn));
    when(serverContent.turnComplete()).thenReturn(Optional.of(false));
    when(serverContent.interrupted()).thenReturn(Optional.empty());
    LiveServerMessage message = mock(LiveServerMessage.class);
    when(message.serverContent()).thenReturn(Optional.of(serverContent));
    when(message.toolCall()).thenReturn(Optional.empty());
    when(message.usageMetadata()).thenReturn(Optional.empty());
    when(message.toolCallCancellation()).thenReturn(Optional.empty());
    when(message.setupComplete()).thenReturn(Optional.empty());
    // Act
    Optional<LlmResponse> result = GeminiLlmConnection.convertToServerResponse(message);
    // Assert
    assertThat(result).isPresent();
    LlmResponse response = result.get();
    assertThat(response.partial()).hasValue(true);
    assertThat(response.turnComplete()).hasValue(false);
  }
}
