/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=create_c0b4ba7866
ROOST_METHOD_SIG_HASH=create_c0b4ba7866

Scenario 1: Create LLM Instance with Valid Gemini Model Name

Details:
  TestName: createWithValidGeminiModelName
  Description: Verify that the create method of LlmFactory successfully creates a BaseLlm instance when provided with a valid Gemini model name that matches the registered pattern "gemini-.*".

Execution:
  Arrange: Obtain a reference to the LlmFactory registered for the "gemini-.*" pattern from the llmFactories map.
  Act: Invoke the create method with a valid model name such as "gemini-pro".
  Assert: Verify that the returned object is not null and is an instance of BaseLlm.

Validation:
  The assertion confirms that the factory correctly instantiates a BaseLlm object for Gemini models. This is significant because it validates the core functionality of the factory pattern implementation, ensuring that model creation works as expected for the default registered Gemini factory.

---

Scenario 2: Create LLM Instance with Valid Apigee Model Name

Details:
  TestName: createWithValidApigeeModelName
  Description: Verify that the create method of LlmFactory successfully creates a BaseLlm instance when provided with a valid Apigee model name that matches the registered pattern "apigee/.*".

Execution:
  Arrange: Obtain a reference to the LlmFactory registered for the "apigee/.*" pattern from the llmFactories map.
  Act: Invoke the create method with a valid model name such as "apigee/my-model".
  Assert: Verify that the returned object is not null and is an instance of BaseLlm.

Validation:
  The assertion confirms that the factory correctly instantiates a BaseLlm object for Apigee models. This validates that multiple factory registrations work independently and correctly produce their respective LLM types.

---

Scenario 3: Create LLM Instance with Null Model Name

Details:
  TestName: createWithNullModelName
  Description: Verify the behavior of the create method when invoked with a null model name parameter, expecting either a NullPointerException or appropriate error handling.

Execution:
  Arrange: Obtain a reference to any registered LlmFactory from the llmFactories map.
  Act: Invoke the create method with null as the model name parameter.
  Assert: Verify that a NullPointerException is thrown or the method handles the null input appropriately.

Validation:
  The assertion verifies that the factory implementation handles null inputs gracefully. This is important for defensive programming and ensures that the system fails fast with a clear error when invalid input is provided.

---

Scenario 4: Create LLM Instance with Empty String Model Name

Details:
  TestName: createWithEmptyStringModelName
  Description: Verify the behavior of the create method when invoked with an empty string as the model name parameter.

Execution:
  Arrange: Obtain a reference to any registered LlmFactory from the llmFactories map.
  Act: Invoke the create method with an empty string "" as the model name.
  Assert: Verify the behavior - either an exception is thrown or an LLM instance is created with an empty name.

Validation:
  The assertion validates how the factory handles edge case inputs. Empty strings may cause issues in downstream processing, so understanding this behavior is crucial for proper error handling in the application.

---

Scenario 5: Create LLM Instance with Model Name Containing Special Characters

Details:
  TestName: createWithSpecialCharactersInModelName
  Description: Verify that the create method handles model names containing special characters such as hyphens, underscores, and numbers correctly.

Execution:
  Arrange: Obtain a reference to the LlmFactory registered for the "gemini-.*" pattern.
  Act: Invoke the create method with a model name containing special characters like "gemini-1.5-pro-latest".
  Assert: Verify that a BaseLlm instance is successfully created.

Validation:
  The assertion confirms that the factory can handle complex model names with various characters. This is important because real-world model names often include version numbers and descriptive suffixes.

---

Scenario 6: Create Multiple LLM Instances with Same Model Name

Details:
  TestName: createMultipleInstancesWithSameModelName
  Description: Verify that calling the create method multiple times with the same model name produces new instances each time (factory creates new objects, not cached).

Execution:
  Arrange: Obtain a reference to the LlmFactory registered for the "gemini-.*" pattern.
  Act: Invoke the create method twice with the same model name "gemini-pro".
  Assert: Verify that both returned instances are not null and are separate object instances.

Validation:
  The assertion confirms that the factory creates new instances on each invocation. This is important to understand the factory's behavior versus the caching behavior implemented in getLlm method.

---

Scenario 7: Create LLM Instance with Custom Registered Factory

Details:
  TestName: createWithCustomRegisteredFactory
  Description: Verify that a custom LlmFactory registered via registerLlm method correctly creates BaseLlm instances when its pattern matches.

Execution:
  Arrange: Register a custom LlmFactory using registerLlm with a unique pattern like "custom-.*" and a mock or test implementation.
  Act: Invoke the create method on the custom factory with a matching model name like "custom-model".
  Assert: Verify that the returned BaseLlm instance is the one created by the custom factory.

Validation:
  The assertion validates the extensibility of the LlmRegistry system, confirming that users can register their own factories and have them work correctly within the framework.

---

Scenario 8: Create LLM Instance with Very Long Model Name

Details:
  TestName: createWithVeryLongModelName
  Description: Verify that the create method handles extremely long model names without issues.

Execution:
  Arrange: Obtain a reference to the LlmFactory registered for the "gemini-.*" pattern.
  Act: Invoke the create method with a very long model name like "gemini-" followed by 1000 characters.
  Assert: Verify that the method either successfully creates an instance or throws an appropriate exception.

Validation:
  The assertion tests the robustness of the factory implementation against edge cases involving unusually long input strings, which is important for preventing potential buffer overflow or memory issues.

---

Scenario 9: Create LLM Instance with Whitespace in Model Name

Details:
  TestName: createWithWhitespaceInModelName
  Description: Verify the behavior of the create method when the model name contains leading, trailing, or embedded whitespace characters.

Execution:
  Arrange: Obtain a reference to the LlmFactory registered for the "gemini-.*" pattern.
  Act: Invoke the create method with model names containing whitespace like " gemini-pro " or "gemini- pro".
  Assert: Verify the behavior - whether whitespace is trimmed, causes pattern mismatch, or is preserved.

Validation:
  The assertion clarifies how the factory handles whitespace in model names, which is important for input validation and ensuring consistent behavior across different input formats.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertThrows;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import org.junit.jupiter.api.*;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;

public class LlmFactoryCreateTest {

  private BaseLlm mockBaseLlm;

  private LlmRegistry.LlmFactory mockFactory;

  @BeforeEach
  void setUp() {
    mockBaseLlm = mock(BaseLlm.class);
    mockFactory = mock(LlmRegistry.LlmFactory.class);
  }

  @AfterEach
  void tearDown() {
    // Clean up any test registrations
  }

  @Test
  @Tag("valid")
  void createWithValidGeminiModelName() {
    // Arrange
    String modelName = "gemini-pro";
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("gemini-.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }

  @Test
  @Tag("valid")
  void createWithValidApigeeModelName() {
    // Arrange
    String modelName = "apigee/my-model";
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("apigee/.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }

  @Test
  @Tag("invalid")
  void createWithNullModelName() {
    // Arrange
    LlmRegistry.registerTestLlm("test-.*", mockFactory);
    // Act & Assert
    assertThrows(
        Exception.class,
        () -> {
          LlmRegistry.getLlm(null);
        });
  }

  @Test
  @Tag("boundary")
  void createWithEmptyStringModelName() {
    // Arrange
    String modelName = "";
    LlmRegistry.registerTestLlm(".*", mockFactory);
    // Act & Assert
    assertThrows(
        IllegalArgumentException.class,
        () -> {
          LlmRegistry.getLlm(modelName);
        });
  }

  @Test
  @Tag("valid")
  void createWithSpecialCharactersInModelName() {
    // Arrange
    String modelName = "gemini-1.5-pro-latest";
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("gemini-.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }

  @Test
  @Tag("valid")
  void createMultipleInstancesWithSameModelName() {
    // Arrange
    String modelName = "gemini-pro";
    BaseLlm mockBaseLlm1 = mock(BaseLlm.class);
    BaseLlm mockBaseLlm2 = mock(BaseLlm.class);

    LlmRegistry.LlmFactory factoryForMultiple = mock(LlmRegistry.LlmFactory.class);
    when(factoryForMultiple.create(modelName)).thenReturn(mockBaseLlm1).thenReturn(mockBaseLlm2);
    LlmRegistry.registerTestLlm("gemini-.*", factoryForMultiple);
    // Act
    BaseLlm result1 = LlmRegistry.getLlm(modelName);

    // Note: getLlm caches instances, so same instance is returned
    BaseLlm result2 = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result1);
    assertNotNull(result2);
    // Due to caching in getLlm, both should be the same instance
    assertThat(result1).isSameInstanceAs(result2);
  }

  @Test
  @Tag("integration")
  void createWithCustomRegisteredFactory() {
    // Arrange
    String customPattern = "custom-.*";
    String modelName = "custom-model";
    BaseLlm customMockLlm = mock(BaseLlm.class);

    LlmRegistry.LlmFactory customFactory = mock(LlmRegistry.LlmFactory.class);
    when(customFactory.create(modelName)).thenReturn(customMockLlm);

    LlmRegistry.registerLlm(customPattern, customFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isEqualTo(customMockLlm);
  }

  @Test
  @Tag("boundary")
  void createWithVeryLongModelName() {
    // Arrange
    StringBuilder longNameBuilder = new StringBuilder("gemini-");
    for (int i = 0; i < 1000; i++) {
      longNameBuilder.append("a");
    }
    String longModelName = longNameBuilder.toString();

    when(mockFactory.create(longModelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("gemini-.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(longModelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }

  @Test
  @Tag("boundary")
  void createWithWhitespaceInModelName() {
    // Arrange
    String modelNameWithWhitespace = " gemini-pro ";

    // Act & Assert - whitespace likely causes pattern mismatch
    assertThrows(
        IllegalArgumentException.class,
        () -> {
          LlmRegistry.getLlm(modelNameWithWhitespace);
        });
  }

  @Test
  @Tag("invalid")
  void createWithUnsupportedModelName() {
    // Arrange
    String unsupportedModelName = "unsupported-model-xyz";
    // Act & Assert
    assertThrows(
        IllegalArgumentException.class,
        () -> {
          LlmRegistry.getLlm(unsupportedModelName);
        });
  }

  @ParameterizedTest
  @ValueSource(strings = {"gemini-pro", "gemini-1.0", "gemini-ultra", "gemini-nano"})
  @Tag("valid")
  void createWithVariousGeminiModelNames(String modelName) {
    // Arrange
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("gemini-.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }

  @ParameterizedTest
  @ValueSource(strings = {"apigee/model1", "apigee/test-model", "apigee/prod/v1"})
  @Tag("valid")
  void createWithVariousApigeeModelNames(String modelName) {
    // Arrange
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("apigee/.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }

  @Test
  @Tag("boundary")
  void createWithModelNameContainingOnlyPattern() {
    // Arrange
    String modelName = "gemini-";
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("gemini-.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
  }

  @Test
  @Tag("valid")
  void createWithModelNameContainingNumbers() {
    // Arrange
    String modelName = "gemini-2024-01-15";
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("gemini-.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }

  @Test
  @Tag("valid")
  void createWithModelNameContainingUnderscores() {
    // Arrange
    String modelName = "gemini-pro_v2_latest";
    when(mockFactory.create(modelName)).thenReturn(mockBaseLlm);
    LlmRegistry.registerTestLlm("gemini-.*", mockFactory);
    // Act
    BaseLlm result = LlmRegistry.getLlm(modelName);
    // Assert
    assertNotNull(result);
    assertThat(result).isInstanceOf(BaseLlm.class);
  }
}
