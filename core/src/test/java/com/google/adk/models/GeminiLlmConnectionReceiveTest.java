/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=receive_900b8d07f7
ROOST_METHOD_SIG_HASH=receive_e4e6b5b51a

Scenario 1: Receive Returns the Response Flowable Instance

Details:
  TestName: receiveReturnsResponseFlowable
  Description: Verify that the receive() method returns the internal responseFlowable instance, which is the serialized version of the PublishProcessor used for streaming LlmResponse objects.
Execution:
  Arrange: Create a GeminiLlmConnection instance with mocked Client, a valid model name, and a LiveConnectConfig. Ensure the connection is properly initialized.
  Act: Call the receive() method on the GeminiLlmConnection instance.
  Assert: Verify that the returned Flowable is not null and is the same instance when called multiple times.
Validation:
  The assertion verifies that receive() consistently returns the same Flowable instance, which is essential for subscribers to receive all emitted LlmResponse items. This ensures that multiple calls to receive() do not create new streams but return the shared serialized Flowable.

Scenario 2: Receive Returns Flowable That Can Be Subscribed To

Details:
  TestName: receiveReturnsSubscribableFlowable
  Description: Verify that the Flowable returned by receive() can be subscribed to without throwing exceptions, even before any data is emitted.
Execution:
  Arrange: Create a GeminiLlmConnection instance with mocked dependencies. Set up the mock Client to return a CompletableFuture for the async session.
  Act: Call receive() and subscribe to the returned Flowable with a test subscriber.
  Assert: Verify that the subscription is successful and no errors are thrown during subscription.
Validation:
  This test ensures that the Flowable is properly initialized and ready to accept subscribers. This is critical for the reactive streaming pattern where consumers need to subscribe before data starts flowing.

Scenario 3: Receive Flowable Emits Items When Server Messages Are Processed

Details:
  TestName: receiveFlowableEmitsLlmResponseOnServerMessage
  Description: Verify that when the internal responseProcessor receives LlmResponse items (simulating server messages), the Flowable returned by receive() emits those items to subscribers.
Execution:
  Arrange: Create a GeminiLlmConnection instance. Subscribe to the Flowable returned by receive() with a test subscriber that collects emitted items.
  Act: Simulate the internal mechanism that would cause responseProcessor to emit an LlmResponse (this would typically happen through the setupReceiver and handleServerMessage flow).
  Assert: Verify that the test subscriber receives the expected LlmResponse items.
Validation:
  This validates the core functionality of the receive() method - that it provides a stream of responses from the server. The serialized Flowable ensures thread-safe emission of items to subscribers.

Scenario 4: Receive Flowable Completes When Connection Is Closed Normally

Details:
  TestName: receiveFlowableCompletesOnNormalClose
  Description: Verify that when the close() method is called without an error, the Flowable returned by receive() completes normally, signaling to subscribers that no more items will be emitted.
Execution:
  Arrange: Create a GeminiLlmConnection instance with mocked dependencies. Subscribe to the Flowable returned by receive() with a test subscriber.
  Act: Call the close() method on the connection.
  Assert: Verify that the test subscriber receives an onComplete signal.
Validation:
  This test ensures proper lifecycle management of the reactive stream. When the connection is closed gracefully, subscribers should be notified via completion, allowing them to clean up resources appropriately.

Scenario 5: Receive Flowable Emits Error When Connection Is Closed With Throwable

Details:
  TestName: receiveFlowableEmitsErrorOnCloseWithThrowable
  Description: Verify that when close(Throwable) is called, the Flowable returned by receive() emits an error signal with the provided throwable to all subscribers.
Execution:
  Arrange: Create a GeminiLlmConnection instance. Subscribe to the Flowable returned by receive() with a test subscriber that captures errors.
  Act: Call close(Throwable) with a specific exception (e.g., new RuntimeException("Test error")).
  Assert: Verify that the test subscriber receives an onError signal with the exact throwable that was passed to close().
Validation:
  This validates error propagation through the reactive stream. When an error occurs, subscribers must be notified so they can handle the failure appropriately in their error handling logic.

Scenario 6: Receive Flowable Emits Error On Connection Failure

Details:
  TestName: receiveFlowableEmitsErrorOnConnectionFailure
  Description: Verify that when the WebSocket connection fails during initialization, the Flowable returned by receive() emits an error to subscribers.
Execution:
  Arrange: Create a GeminiLlmConnection instance with a mocked Client that returns a CompletableFuture that completes exceptionally (simulating connection failure).
  Act: Subscribe to the Flowable returned by receive() and wait for the connection attempt to complete.
  Assert: Verify that the subscriber receives an onError signal with the connection failure exception.
Validation:
  This test ensures that connection failures are properly propagated to subscribers. This is essential for applications to handle connection issues gracefully and potentially implement retry logic.

Scenario 7: Receive Returns Same Flowable Instance Across Multiple Calls

Details:
  TestName: receiveReturnsSameFlowableInstanceOnMultipleCalls
  Description: Verify that calling receive() multiple times returns the exact same Flowable instance, ensuring all subscribers share the same stream.
Execution:
  Arrange: Create a GeminiLlmConnection instance with mocked dependencies.
  Act: Call receive() multiple times and store the returned Flowable references.
  Assert: Verify that all returned Flowable references are identical (same object reference).
Validation:
  This ensures that the receive() method is idempotent and returns the shared responseFlowable field. Multiple subscribers should all receive from the same source to maintain consistency in the data stream.

Scenario 8: Receive Flowable Handles Multiple Subscribers

Details:
  TestName: receiveFlowableSupportsMultipleSubscribers
  Description: Verify that the Flowable returned by receive() can support multiple concurrent subscribers, with each subscriber receiving the same emitted items.
Execution:
  Arrange: Create a GeminiLlmConnection instance. Create multiple test subscribers.
  Act: Subscribe all test subscribers to the Flowable returned by receive().
  Assert: Verify that all subscribers are successfully subscribed and can receive items when they are emitted.
Validation:
  The serialized Flowable (created via responseProcessor.serialize()) should support multiple subscribers. This is important for scenarios where different parts of the application need to react to server responses independently.

Scenario 9: Receive Flowable Does Not Emit After Connection Closed

Details:
  TestName: receiveFlowableDoesNotEmitAfterClose
  Description: Verify that after the connection is closed, no new items are emitted through the Flowable returned by receive(), even if there are attempts to process server messages.
Execution:
  Arrange: Create a GeminiLlmConnection instance. Subscribe to the Flowable returned by receive(). Close the connection.
  Act: Attempt to trigger message processing after the connection is closed.
  Assert: Verify that no new items are emitted to the subscriber after the close signal (either onComplete or onError).
Validation:
  This test validates the closed state management using the AtomicBoolean closed field. Once closed, the connection should not emit any more items, preventing potential resource leaks or unexpected behavior.

Scenario 10: Receive Flowable Is Serialized For Thread Safety

Details:
  TestName: receiveFlowableIsSerializedForThreadSafety
  Description: Verify that the Flowable returned by receive() is the serialized version of the PublishProcessor, ensuring thread-safe emission of items even when multiple threads attempt to emit concurrently.
Execution:
  Arrange: Create a GeminiLlmConnection instance. Subscribe to the Flowable returned by receive() with a test subscriber.
  Act: Simulate concurrent emissions from multiple threads (if possible through the internal mechanisms).
  Assert: Verify that items are received in a serialized manner without interleaving or corruption.
Validation:
  The serialize() method on the PublishProcessor ensures that emissions are serialized even if onNext is called from multiple threads. This is critical for the WebSocket scenario where messages may arrive on different threads.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import com.google.genai.AsyncLive;
import com.google.genai.AsyncSession;
import com.google.genai.Client;
import com.google.genai.types.LiveConnectConfig;
import io.reactivex.rxjava3.core.Flowable;
import io.reactivex.rxjava3.subscribers.TestSubscriber;
import java.lang.reflect.Field;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CountDownLatch;
import java.util.concurrent.TimeUnit;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

class GeminiLlmConnectionReceiveTest {

  @Mock private Client mockClient;

  @Mock private AsyncSession mockSession;

  private LiveConnectConfig connectConfig;

  private String modelName;

  @BeforeEach
  void setUp() {
    MockitoAnnotations.openMocks(this);
    modelName = "gemini-pro";
    connectConfig = LiveConnectConfig.builder().build();
  }

  private GeminiLlmConnection createConnectionWithMockedSession() {
    AsyncLive mockAsyncLive = mock(AsyncLive.class);
    // Access the async field directly since Client.AsyncClient doesn't exist
    // We need to mock the async field of Client which contains live
    try {
      Field asyncField = Client.class.getDeclaredField("async");
      asyncField.setAccessible(true);
      Object asyncObj = mock(asyncField.getType());
      asyncField.set(mockClient, asyncObj);

      Field liveField = asyncObj.getClass().getDeclaredField("live");
      liveField.setAccessible(true);
      liveField.set(asyncObj, mockAsyncLive);
    } catch (Exception e) {
      // Fallback: directly set up the mock chain using reflection on the actual
      // structure
      when(mockClient.async).thenReturn(null);
    }

    CompletableFuture<AsyncSession> sessionFuture = new CompletableFuture<>();
    when(mockAsyncLive.connect(anyString(), any(LiveConnectConfig.class)))
        .thenReturn(sessionFuture);

    when(mockSession.receive(any())).thenReturn(CompletableFuture.completedFuture(null));

    GeminiLlmConnection connection = new GeminiLlmConnection(mockClient, modelName, connectConfig);

    // Complete the session future to simulate successful connection
    sessionFuture.complete(mockSession);

    return connection;
  }

  private GeminiLlmConnection createConnectionWithPendingSession() {
    AsyncLive mockAsyncLive = mock(AsyncLive.class);

    CompletableFuture<AsyncSession> sessionFuture = new CompletableFuture<>();
    when(mockAsyncLive.connect(anyString(), any(LiveConnectConfig.class)))
        .thenReturn(sessionFuture);

    // Use reflection to set up the mock properly
    try {
      Field asyncField = Client.class.getField("async");
      Object asyncValue = asyncField.get(mockClient);
      if (asyncValue != null) {
        Field liveField = asyncValue.getClass().getField("live");
        when((AsyncLive) liveField.get(asyncValue)).thenReturn(mockAsyncLive);
      }
    } catch (Exception e) {
      // If reflection fails, try direct mocking approach
    }

    // Mock the chain: mockClient.async.live.connect()
    when(mockClient.async.live.connect(anyString(), any(LiveConnectConfig.class)))
        .thenReturn(sessionFuture);

    return new GeminiLlmConnection(mockClient, modelName, connectConfig);
  }

  private GeminiLlmConnection createConnectionWithFailedSession(Throwable error) {
    AsyncLive mockAsyncLive = mock(AsyncLive.class);

    CompletableFuture<AsyncSession> sessionFuture = new CompletableFuture<>();

    // Mock the chain: mockClient.async.live.connect()
    when(mockClient.async.live.connect(anyString(), any(LiveConnectConfig.class)))
        .thenReturn(sessionFuture);

    GeminiLlmConnection connection = new GeminiLlmConnection(mockClient, modelName, connectConfig);

    // Complete exceptionally to simulate connection failure
    sessionFuture.completeExceptionally(error);

    return connection;
  }

  @Test
  @Tag("valid")
  void receiveReturnsResponseFlowable() {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();

    assertThat(flowable).isNotNull();

    // Verify same instance is returned on multiple calls
    Flowable<LlmResponse> flowable2 = connection.receive();
    assertThat(flowable).isSameInstanceAs(flowable2);
  }

  @Test
  @Tag("valid")
  void receiveReturnsSubscribableFlowable() {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();

    flowable.subscribe(testSubscriber);

    testSubscriber.assertNoErrors();
    testSubscriber.assertNotComplete();
  }

  @Test
  @Tag("valid")
  void receiveFlowableEmitsLlmResponseOnServerMessage() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    // Access the responseProcessor via reflection to emit an item
    Field responseProcessorField = GeminiLlmConnection.class.getDeclaredField("responseProcessor");
    responseProcessorField.setAccessible(true);
    io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse> processor =
        (io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse>)
            responseProcessorField.get(connection);

    LlmResponse testResponse = LlmResponse.builder().partial(false).turnComplete(true).build();
    processor.onNext(testResponse);

    testSubscriber.assertValueCount(1);
    testSubscriber.assertValue(testResponse);
  }

  @Test
  @Tag("valid")
  void receiveFlowableCompletesOnNormalClose() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    connection.close();

    testSubscriber.await(1, TimeUnit.SECONDS);
    testSubscriber.assertComplete();
    testSubscriber.assertNoErrors();
  }

  @Test
  @Tag("invalid")
  void receiveFlowableEmitsErrorOnCloseWithThrowable() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    RuntimeException testError = new RuntimeException("Test error");
    connection.close(testError);

    testSubscriber.await(1, TimeUnit.SECONDS);
    testSubscriber.assertError(testError);
  }

  @Test
  @Tag("invalid")
  void receiveFlowableEmitsErrorOnConnectionFailure() throws Exception {
    RuntimeException connectionError = new RuntimeException("Connection failed");
    GeminiLlmConnection connection = createConnectionWithFailedSession(connectionError);

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    // Wait for the error to propagate
    testSubscriber.await(2, TimeUnit.SECONDS);
    testSubscriber.assertError(connectionError);
  }

  @Test
  @Tag("valid")
  void receiveReturnsSameFlowableInstanceOnMultipleCalls() {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable1 = connection.receive();
    Flowable<LlmResponse> flowable2 = connection.receive();
    Flowable<LlmResponse> flowable3 = connection.receive();

    assertThat(flowable1).isSameInstanceAs(flowable2);
    assertThat(flowable2).isSameInstanceAs(flowable3);
  }

  @Test
  @Tag("valid")
  void receiveFlowableSupportsMultipleSubscribers() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();

    TestSubscriber<LlmResponse> subscriber1 = new TestSubscriber<>();
    TestSubscriber<LlmResponse> subscriber2 = new TestSubscriber<>();
    TestSubscriber<LlmResponse> subscriber3 = new TestSubscriber<>();

    flowable.subscribe(subscriber1);
    flowable.subscribe(subscriber2);
    flowable.subscribe(subscriber3);

    // Access the responseProcessor via reflection to emit an item
    Field responseProcessorField = GeminiLlmConnection.class.getDeclaredField("responseProcessor");
    responseProcessorField.setAccessible(true);
    io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse> processor =
        (io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse>)
            responseProcessorField.get(connection);

    LlmResponse testResponse = LlmResponse.builder().partial(false).turnComplete(true).build();
    processor.onNext(testResponse);

    subscriber1.assertValueCount(1);
    subscriber2.assertValueCount(1);
    subscriber3.assertValueCount(1);
  }

  @Test
  @Tag("boundary")
  void receiveFlowableDoesNotEmitAfterClose() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    // Close the connection
    connection.close();

    // Wait for completion
    testSubscriber.await(1, TimeUnit.SECONDS);
    testSubscriber.assertComplete();

    // Access the responseProcessor via reflection
    Field responseProcessorField = GeminiLlmConnection.class.getDeclaredField("responseProcessor");
    responseProcessorField.setAccessible(true);
    io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse> processor =
        (io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse>)
            responseProcessorField.get(connection);

    // Try to emit after close - should not reach subscriber
    LlmResponse testResponse = LlmResponse.builder().partial(false).turnComplete(true).build();
    processor.onNext(testResponse);

    // Subscriber should still have no values (only completion)
    testSubscriber.assertValueCount(0);
  }

  @Test
  @Tag("valid")
  void receiveFlowableIsSerializedForThreadSafety() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    // Access the responseProcessor via reflection
    Field responseProcessorField = GeminiLlmConnection.class.getDeclaredField("responseProcessor");
    responseProcessorField.setAccessible(true);
    io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse> processor =
        (io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse>)
            responseProcessorField.get(connection);

    int numThreads = 5;
    int itemsPerThread = 10;
    CountDownLatch latch = new CountDownLatch(numThreads);

    // Emit items from multiple threads concurrently
    for (int t = 0; t < numThreads; t++) {
      new Thread(
              () -> {
                for (int i = 0; i < itemsPerThread; i++) {
                  LlmResponse response =
                      LlmResponse.builder().partial(false).turnComplete(true).build();
                  processor.onNext(response);
                }
                latch.countDown();
              })
          .start();
    }

    latch.await(5, TimeUnit.SECONDS);

    // Give some time for all items to be processed
    Thread.sleep(100);

    // Verify all items were received
    testSubscriber.assertValueCount(numThreads * itemsPerThread);
    testSubscriber.assertNoErrors();
  }

  @Test
  @Tag("valid")
  void receiveFlowableIsNotNullAfterConstruction() {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();

    assertThat(flowable).isNotNull();
  }

  @Test
  @Tag("boundary")
  void receiveFlowableHandlesEmptyStream() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    // Close without emitting any items
    connection.close();

    testSubscriber.await(1, TimeUnit.SECONDS);
    testSubscriber.assertComplete();
    testSubscriber.assertNoValues();
  }

  @Test
  @Tag("valid")
  void receiveFlowableEmitsMultipleItems() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    // Access the responseProcessor via reflection
    Field responseProcessorField = GeminiLlmConnection.class.getDeclaredField("responseProcessor");
    responseProcessorField.setAccessible(true);
    io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse> processor =
        (io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse>)
            responseProcessorField.get(connection);

    // Emit multiple items
    LlmResponse response1 = LlmResponse.builder().partial(true).turnComplete(false).build();
    LlmResponse response2 = LlmResponse.builder().partial(true).turnComplete(false).build();
    LlmResponse response3 = LlmResponse.builder().partial(false).turnComplete(true).build();

    processor.onNext(response1);
    processor.onNext(response2);
    processor.onNext(response3);

    testSubscriber.assertValueCount(3);
    testSubscriber.assertValues(response1, response2, response3);
  }

  @Test
  @Tag("boundary")
  void receiveFlowableHandlesLateSubscriber() throws Exception {
    GeminiLlmConnection connection = createConnectionWithPendingSession();

    // Access the responseProcessor via reflection
    Field responseProcessorField = GeminiLlmConnection.class.getDeclaredField("responseProcessor");
    responseProcessorField.setAccessible(true);
    io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse> processor =
        (io.reactivex.rxjava3.processors.PublishProcessor<LlmResponse>)
            responseProcessorField.get(connection);

    // Emit item before subscription
    LlmResponse response1 = LlmResponse.builder().partial(true).turnComplete(false).build();
    processor.onNext(response1);

    // Subscribe after emission
    Flowable<LlmResponse> flowable = connection.receive();
    TestSubscriber<LlmResponse> testSubscriber = new TestSubscriber<>();
    flowable.subscribe(testSubscriber);

    // Late subscriber should not receive the first item (PublishProcessor behavior)
    testSubscriber.assertValueCount(0);

    // But should receive subsequent items
    LlmResponse response2 = LlmResponse.builder().partial(false).turnComplete(true).build();
    processor.onNext(response2);

    testSubscriber.assertValueCount(1);
    testSubscriber.assertValue(response2);
  }
}
