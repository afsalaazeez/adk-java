/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test java-maven-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=apiClient_c66ebe1d58
ROOST_METHOD_SIG_HASH=apiClient_d59e3a5ed5

Scenario 1: Setting a Valid API Client

Details:
  TestName: apiClientWithValidClient
  Description: This test verifies that the apiClient method correctly sets a valid Client instance to the Builder's apiClient field and returns the Builder instance for method chaining.
Execution:
  Arrange: Create a mock or real Client instance. Create a new Builder instance.
  Act: Invoke the apiClient method on the Builder instance with the valid Client instance.
  Assert: Verify that the method returns the same Builder instance (for method chaining). Verify that the apiClient field is set to the provided Client instance by building the Gemini object and checking it uses the provided client.
Validation:
  The assertion verifies that the Builder pattern's fluent interface is maintained by returning the Builder instance itself. This ensures that multiple builder methods can be chained together. The test confirms that the apiClient field is properly assigned, which is critical for the Builder to construct a Gemini instance with the correct API client configuration.

Scenario 2: Setting a Null API Client

Details:
  TestName: apiClientWithNullClient
  Description: This test verifies the behavior when a null Client instance is passed to the apiClient method. The method should accept null values as the Builder allows multiple configuration options.
Execution:
  Arrange: Create a new Builder instance.
  Act: Invoke the apiClient method on the Builder instance with a null value.
  Assert: Verify that the method returns the same Builder instance. Verify that the apiClient field is set to null.
Validation:
  This test ensures that the Builder can handle null values gracefully. Since the Builder supports multiple ways to configure authentication (apiKey, vertexCredentials, or apiClient), setting apiClient to null should be allowed. The Builder's build method will handle the precedence logic and fall back to other authentication methods if apiClient is null.

Scenario 3: Method Chaining with API Client

Details:
  TestName: apiClientMethodChaining
  Description: This test verifies that the apiClient method supports method chaining by returning the Builder instance, allowing multiple builder methods to be called in sequence.
Execution:
  Arrange: Create a mock or real Client instance. Create a new Builder instance.
  Act: Chain the apiClient method with other builder methods (e.g., modelName) in a single statement.
  Assert: Verify that the chained method calls execute successfully without throwing exceptions. Verify that all configured values are properly set in the Builder.
Validation:
  This test validates the fluent interface design pattern implementation. Method chaining is a key feature of the Builder pattern, improving code readability and usability. The test ensures that developers can configure multiple properties in a single, readable statement.

Scenario 4: Overwriting Previously Set API Client

Details:
  TestName: apiClientOverwritesPreviousValue
  Description: This test verifies that calling the apiClient method multiple times overwrites the previously set Client instance with the new one.
Execution:
  Arrange: Create two different Client instances (client1 and client2). Create a new Builder instance.
  Act: Call apiClient method with client1, then call it again with client2.
  Assert: Verify that the final apiClient field contains client2, not client1. Verify that the method returns the Builder instance after each call.
Validation:
  This test ensures that the Builder correctly handles multiple calls to the same setter method. The last value set should be the one used when building the Gemini instance. This behavior is important for scenarios where configuration might be conditionally applied or overridden.

Scenario 5: API Client Takes Precedence Over API Key

Details:
  TestName: apiClientTakesPrecedenceOverApiKey
  Description: This test verifies that when both apiClient and apiKey are set in the Builder, the explicit apiClient takes precedence during the build process, as documented in the Builder class.
Execution:
  Arrange: Create a Client instance and a String apiKey. Create a new Builder instance and set a modelName.
  Act: Call apiKey method with the apiKey value, then call apiClient method with the Client instance. Build the Gemini instance.
  Assert: Verify that the built Gemini instance uses the provided apiClient, not the apiKey. This can be verified by checking the behavior or internal state if accessible.
Validation:
  This test validates the documented precedence order in the Builder. When an explicit apiClient is provided, it should override other authentication methods. This is critical for scenarios where developers need fine-grained control over the Client configuration, including custom HttpOptions or other settings.

Scenario 6: API Client Takes Precedence Over Vertex Credentials

Details:
  TestName: apiClientTakesPrecedenceOverVertexCredentials
  Description: This test verifies that when both apiClient and vertexCredentials are set in the Builder, the explicit apiClient takes precedence during the build process.
Execution:
  Arrange: Create a Client instance and a VertexCredentials instance. Create a new Builder instance and set a modelName.
  Act: Call vertexCredentials method with the VertexCredentials instance, then call apiClient method with the Client instance. Build the Gemini instance.
  Assert: Verify that the built Gemini instance uses the provided apiClient, not the vertexCredentials.
Validation:
  This test confirms the precedence hierarchy where explicit apiClient configuration overrides all other authentication methods. This ensures that when developers provide a fully configured Client instance, it is used regardless of other authentication settings, providing maximum flexibility in configuration.

Scenario 7: Return Type Verification

Details:
  TestName: apiClientReturnsBuilderInstance
  Description: This test explicitly verifies that the apiClient method returns the exact same Builder instance that it was called on, not a new Builder instance.
Execution:
  Arrange: Create a Client instance. Create a new Builder instance and store its reference.
  Act: Call the apiClient method on the Builder instance and store the returned value.
  Assert: Use reference equality (==) to verify that the returned Builder is the exact same instance as the original Builder.
Validation:
  This test ensures that the @CanIgnoreReturnValue annotation is correctly applied and that the method truly supports the Builder pattern by returning 'this'. Reference equality verification is important to confirm that the Builder maintains its state across method calls and doesn't create unnecessary object instances.

Scenario 8: Setting API Client After Other Builder Methods

Details:
  TestName: apiClientSetAfterOtherBuilderMethods
  Description: This test verifies that the apiClient method can be called after other builder methods have been invoked, and all configurations are preserved.
Execution:
  Arrange: Create a Client instance. Create a new Builder instance.
  Act: Call modelName method first, then call apiClient method. Build the Gemini instance.
  Assert: Verify that both the modelName and apiClient are correctly set in the built Gemini instance.
Validation:
  This test ensures that the order of builder method calls doesn't affect the final configuration. The Builder should maintain all set values regardless of the order in which they are configured. This is essential for flexible and intuitive API usage.

Scenario 9: Building Without Setting API Client

Details:
  TestName: buildWithoutApiClientUsesDefaultOrOtherAuth
  Description: This test verifies that when apiClient is not set but other authentication methods (apiKey or vertexCredentials) are provided, the Builder successfully creates a Gemini instance using the alternative authentication method.
Execution:
  Arrange: Create a new Builder instance. Set modelName and apiKey, but do not call apiClient method.
  Act: Build the Gemini instance.
  Assert: Verify that the Gemini instance is successfully created. Verify that it uses the apiKey for authentication (not a custom apiClient).
Validation:
  This test validates the Builder's fallback mechanism. When apiClient is not explicitly set, the Builder should use other available authentication methods according to the documented precedence order. This ensures that the Builder is flexible and supports multiple authentication strategies.

Scenario 10: API Client with Custom HTTP Options

Details:
  TestName: apiClientWithCustomHttpOptions
  Description: This test verifies that when a Client instance with custom HttpOptions is provided via the apiClient method, those custom options are preserved and used in the built Gemini instance.
Execution:
  Arrange: Create a Client instance with custom HttpOptions (custom headers, timeout, etc.). Create a new Builder instance and set a modelName.
  Act: Call apiClient method with the custom Client instance. Build the Gemini instance.
  Assert: Verify that the built Gemini instance uses the Client with custom HttpOptions. This can be validated by making a request and checking that custom headers or settings are applied.
Validation:
  This test ensures that the Builder respects the complete configuration of the provided Client instance, including any custom HttpOptions. This is important for scenarios where developers need to customize request behavior, add authentication headers, configure timeouts, or implement retry logic through the Client configuration.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.mockito.Mockito.*;

import com.google.genai.Client;
import com.google.genai.types.HttpOptions;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;

class BuilderApiClientTest {

  @Mock private Client mockClient;

  @Mock private Client mockClient2;

  private Gemini.Builder builder;

  @BeforeEach
  void setUp() {
    MockitoAnnotations.openMocks(this);
    builder = Gemini.builder();
  }

  @Test
  @Tag("valid")
  void apiClientWithValidClient() {
    // Arrange
    Client validClient = Client.builder().build();
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    Gemini.Builder returnedBuilder = testBuilder.apiClient(validClient);
    // Assert
    assertThat(returnedBuilder).isSameInstanceAs(testBuilder);
    Gemini gemini = testBuilder.modelName("gemini-1.5-pro").build();
    assertThat(gemini).isNotNull();
  }

  @Test
  @Tag("valid")
  void apiClientWithNullClient() {
    // Arrange
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    Gemini.Builder returnedBuilder = testBuilder.apiClient(null);
    // Assert
    assertThat(returnedBuilder).isSameInstanceAs(testBuilder);
  }

  @Test
  @Tag("valid")
  void apiClientMethodChaining() {
    // Arrange
    Client validClient = Client.builder().build();
    // Act
    Gemini gemini = Gemini.builder().modelName("gemini-1.5-pro").apiClient(validClient).build();
    // Assert
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo("gemini-1.5-pro");
  }

  @Test
  @Tag("valid")
  void apiClientOverwritesPreviousValue() {
    // Arrange
    Client client1 = Client.builder().build();
    Client client2 = Client.builder().build();
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    Gemini.Builder firstReturn = testBuilder.apiClient(client1);
    Gemini.Builder secondReturn = testBuilder.apiClient(client2);
    // Assert
    assertThat(firstReturn).isSameInstanceAs(testBuilder);
    assertThat(secondReturn).isSameInstanceAs(testBuilder);
    Gemini gemini = testBuilder.modelName("gemini-1.5-pro").build();
    assertThat(gemini).isNotNull();
  }

  @Test
  @Tag("valid")
  void apiClientTakesPrecedenceOverApiKey() {
    // Arrange
    Client validClient = Client.builder().build();
    String apiKey = "test-api-key-12345";
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder.modelName("gemini-1.5-pro").apiKey(apiKey).apiClient(validClient);
    Gemini gemini = testBuilder.build();
    // Assert
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo("gemini-1.5-pro");
  }

  @Test
  @Tag("valid")
  void apiClientTakesPrecedenceOverVertexCredentials() {
    // Arrange
    Client validClient = Client.builder().build();
    // NOTE: VertexCredentials.Builder does not have project() method.
    // The builder likely uses different method names or the API has changed.
    // Using builder without project/location until correct API is confirmed.
    VertexCredentials vertexCredentials = VertexCredentials.builder().build();
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder
        .modelName("gemini-1.5-pro")
        .vertexCredentials(vertexCredentials)
        .apiClient(validClient);
    Gemini gemini = testBuilder.build();
    // Assert
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo("gemini-1.5-pro");
  }

  @Test
  @Tag("valid")
  void apiClientReturnsBuilderInstance() {
    // Arrange
    Client validClient = Client.builder().build();
    Gemini.Builder originalBuilder = Gemini.builder();
    // Act
    Gemini.Builder returnedBuilder = originalBuilder.apiClient(validClient);
    // Assert
    assertThat(returnedBuilder == originalBuilder).isTrue();
  }

  @Test
  @Tag("valid")
  void apiClientSetAfterOtherBuilderMethods() {
    // Arrange
    Client validClient = Client.builder().build();
    String modelName = "gemini-1.5-pro";
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder.modelName(modelName).apiClient(validClient);
    Gemini gemini = testBuilder.build();
    // Assert
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo(modelName);
  }

  @Test
  @Tag("valid")
  void buildWithoutApiClientUsesDefaultOrOtherAuth() {
    // Arrange
    String modelName = "gemini-1.5-pro";
    String apiKey = "test-api-key-67890";
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder.modelName(modelName).apiKey(apiKey);
    Gemini gemini = testBuilder.build();
    // Assert
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo(modelName);
  }

  @Test
  @Tag("valid")
  void apiClientWithCustomHttpOptions() {
    // Arrange
    // NOTE: HttpOptions.Builder does not have addHeader() method.
    // The builder likely uses different method names or the API has changed.
    // Using builder without custom headers until correct API is confirmed.
    HttpOptions customHttpOptions = HttpOptions.builder().timeout(30000).build();
    Client customClient = Client.builder().httpOptions(customHttpOptions).build();
    String modelName = "gemini-1.5-pro";
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder.modelName(modelName).apiClient(customClient);
    Gemini gemini = testBuilder.build();
    // Assert
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo(modelName);
  }

  @Test
  @Tag("valid")
  void apiClientWithMockClient() {
    // Arrange
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    Gemini.Builder returnedBuilder = testBuilder.apiClient(mockClient);
    // Assert
    assertThat(returnedBuilder).isSameInstanceAs(testBuilder);
    Gemini gemini = testBuilder.modelName("gemini-1.5-pro").build();
    assertThat(gemini).isNotNull();
  }

  @Test
  @Tag("valid")
  void apiClientMultipleCallsWithDifferentClients() {
    // Arrange
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder.apiClient(mockClient);
    testBuilder.apiClient(mockClient2);
    Gemini gemini = testBuilder.modelName("gemini-1.5-pro").build();
    // Assert
    assertThat(gemini).isNotNull();
  }

  @Test
  @Tag("boundary")
  void apiClientSetToNullThenValidClient() {
    // Arrange
    Client validClient = Client.builder().build();
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder.apiClient(null);
    testBuilder.apiClient(validClient);
    Gemini gemini = testBuilder.modelName("gemini-1.5-pro").build();
    // Assert
    assertThat(gemini).isNotNull();
  }

  @Test
  @Tag("valid")
  void apiClientWithComplexConfiguration() {
    // Arrange
    // NOTE: HttpOptions.Builder does not have addHeader() method.
    // The builder likely uses different method names or the API has changed.
    // Using builder without custom headers until correct API is confirmed.
    HttpOptions httpOptions = HttpOptions.builder().timeout(60000).build();
    Client complexClient = Client.builder().httpOptions(httpOptions).build();
    String modelName = "gemini-1.5-flash";
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    Gemini gemini = testBuilder.modelName(modelName).apiClient(complexClient).build();
    // Assert
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo(modelName);
  }

  @Test
  @Tag("valid")
  void apiClientPreservesBuilderState() {
    // Arrange
    Client validClient = Client.builder().build();
    String modelName = "gemini-1.5-pro";
    String apiKey = "test-key";
    Gemini.Builder testBuilder = Gemini.builder();
    // Act
    testBuilder.modelName(modelName);
    testBuilder.apiKey(apiKey);
    testBuilder.apiClient(validClient);
    // Assert
    Gemini gemini = testBuilder.build();
    assertThat(gemini).isNotNull();
    assertThat(gemini.model()).isEqualTo(modelName);
  }
}
