/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test adk-java-repo using AI Type Claude AI and AI Model claude-opus-4-5-20251101

ROOST_METHOD_HASH=connect_f4c2a51502
ROOST_METHOD_SIG_HASH=connect_967ce38618

Here are your existing test cases which we found out and are not considered for test generation:

File Path: /var/tmp/Roost/RoostGPT/adk-java-repo/87694200-c46c-408c-b0b1-408cb60f3b49/source/adk-java/core/src/test/java/com/google/adk/testing/TestLlm.java
Tests:
    "@Test
public BaseLlmConnection connect(LlmRequest llmRequest) {
    llmRequests.add(llmRequest);
    return new TestLlmConnection();
}
"Scenario 1: Connect with LlmRequest containing explicit model

Details:
  TestName: connectWithExplicitModelInRequest
  Description: Verify that when an LlmRequest contains an explicit model, the connect method uses that model to extract the model ID and delegates to geminiDelegate.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance using the test constructor with a valid model name (e.g., "apigee/gemini-2.5-flash") and the mock delegate. Create an LlmRequest with an explicit model set (e.g., "apigee/vertex_ai/gemini-pro").
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest containing the extracted model ID ("gemini-pro").

Validation:
  This assertion verifies that when the LlmRequest has an explicit model, that model is used instead of the default model from ApigeeLlm. The model ID extraction should correctly parse the model string and pass only the model identifier to the delegate. This is important for allowing runtime model selection flexibility.

---

Scenario 2: Connect with LlmRequest without model falls back to default

Details:
  TestName: connectWithoutModelUsesDefaultModel
  Description: Verify that when an LlmRequest does not contain a model, the connect method falls back to the ApigeeLlm's default model.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest without a model set (model returns Optional.empty()).
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest containing the model ID "gemini-2.5-flash".

Validation:
  This assertion confirms the fallback behavior when no model is specified in the request. The default model from the ApigeeLlm instance should be used, ensuring consistent behavior when clients don't specify a model. This is critical for maintaining backward compatibility and default configurations.

---

Scenario 3: Connect with model containing provider and version

Details:
  TestName: connectWithProviderAndVersionExtractsModelId
  Description: Verify that when the model string contains provider and version components (e.g., "apigee/vertex_ai/v1beta/gemini-2.5-flash"), only the model ID is extracted and passed to the delegate.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/vertex_ai/v1/gemini-base" using the test constructor. Create an LlmRequest with model "apigee/vertex_ai/v1beta/gemini-2.5-flash".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest where the model is "gemini-2.5-flash".

Validation:
  This validates that the getModelId method correctly parses complex model strings with all components (provider, version, model_id) and extracts only the final model identifier. This ensures the delegate receives a clean model ID regardless of the full path format.

---

Scenario 4: Connect with model containing only version

Details:
  TestName: connectWithVersionOnlyExtractsModelId
  Description: Verify that when the model string contains only version and model ID (e.g., "apigee/v1/gemini-pro"), the model ID is correctly extracted.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with model "apigee/v1/gemini-pro".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest where the model is "gemini-pro".

Validation:
  This test ensures that model strings with version but without explicit provider are handled correctly. The extraction logic should identify the last component as the model ID regardless of the intermediate version component.

---

Scenario 5: Connect with simple model format

Details:
  TestName: connectWithSimpleModelFormat
  Description: Verify that when the model string is in the simplest format (e.g., "apigee/gemini-pro"), the model ID is correctly extracted.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with model "apigee/gemini-pro".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest where the model is "gemini-pro".

Validation:
  This validates the simplest model string format handling. When only the prefix and model ID are provided, the extraction should work correctly. This is the most common use case and must function reliably.

---

Scenario 6: Connect with provider only format

Details:
  TestName: connectWithProviderOnlyFormat
  Description: Verify that when the model string contains provider and model ID without version (e.g., "apigee/gemini/gemini-2.5-flash"), the model ID is correctly extracted.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with model "apigee/gemini/gemini-2.5-flash".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest where the model is "gemini-2.5-flash".

Validation:
  This test confirms that model strings with provider but without version are parsed correctly. The last component should always be identified as the model ID.

---

Scenario 7: Connect with invalid model string throws exception

Details:
  TestName: connectWithInvalidModelStringThrowsException
  Description: Verify that when an LlmRequest contains an invalid model string that doesn't start with "apigee/", an IllegalArgumentException is thrown.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with an invalid model string (e.g., "invalid/model").
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that an IllegalArgumentException is thrown with a message containing "Invalid model string".

Validation:
  This validates the error handling for invalid model strings. The method should fail fast with a clear error message when the model format is incorrect, preventing downstream errors and providing clear feedback to developers.

---

Scenario 8: Connect with model string missing model ID throws exception

Details:
  TestName: connectWithMissingModelIdThrowsException
  Description: Verify that when an LlmRequest contains a model string ending with a slash (e.g., "apigee/gemini/"), an IllegalArgumentException is thrown.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with model "apigee/gemini/".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that an IllegalArgumentException is thrown.

Validation:
  This test ensures that model strings with trailing slashes (indicating missing model ID) are rejected. This prevents passing empty model IDs to the delegate which would cause errors.

---

Scenario 9: Connect with empty model part throws exception

Details:
  TestName: connectWithEmptyModelPartThrowsException
  Description: Verify that when an LlmRequest contains only the prefix "apigee/" without any model information, an IllegalArgumentException is thrown.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with model "apigee/".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that an IllegalArgumentException is thrown.

Validation:
  This validates that completely empty model parts are rejected. The validation ensures that at minimum a model ID must be provided after the "apigee/" prefix.

---

Scenario 10: Connect returns BaseLlmConnection from delegate

Details:
  TestName: connectReturnsDelegateConnection
  Description: Verify that the connect method returns the BaseLlmConnection object returned by the geminiDelegate.connect method.

Execution:
  Arrange: Create a mock Gemini delegate that returns a specific BaseLlmConnection instance. Create an ApigeeLlm instance using the test constructor. Create a valid LlmRequest.
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that the returned BaseLlmConnection is the same instance returned by the mock delegate.

Validation:
  This confirms that the connect method properly delegates to geminiDelegate and returns its result without modification. This is essential for ensuring the connection lifecycle is properly managed by the underlying implementation.

---

Scenario 11: Connect preserves other LlmRequest properties

Details:
  TestName: connectPreservesOtherRequestProperties
  Description: Verify that when creating the new LlmRequest with the extracted model ID, all other properties from the original request are preserved.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance using the test constructor. Create an LlmRequest with various properties set (system instructions, contents, tools, etc.) and a model.
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest that has the new model ID but retains all other original properties.

Validation:
  This test ensures that the toBuilder().model().build() pattern correctly preserves all request properties except the model. This is critical for maintaining request integrity when transforming the model string.

---

Scenario 12: Connect with vertex_ai provider extracts correct model ID

Details:
  TestName: connectWithVertexAiProviderExtractsModelId
  Description: Verify that model strings with "vertex_ai" provider are correctly parsed and the model ID is extracted.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/vertex_ai/gemini-pro" using the test constructor. Create an LlmRequest without a model (to use default).
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that geminiDelegate.connect was called with an LlmRequest where the model is "gemini-pro".

Validation:
  This validates that the vertex_ai provider prefix is correctly handled during model ID extraction. The provider component should be stripped, leaving only the actual model identifier.

---

Scenario 13: Connect with invalid provider in three-component format throws exception

Details:
  TestName: connectWithInvalidProviderThrowsException
  Description: Verify that when the model string has three components but an invalid provider (not "vertex_ai" or "gemini"), an IllegalArgumentException is thrown.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with model "apigee/invalid_provider/v1/gemini-pro".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that an IllegalArgumentException is thrown.

Validation:
  This test ensures that only valid providers ("vertex_ai" or "gemini") are accepted in the three-component format. Invalid providers should be rejected to prevent misconfiguration.

---

Scenario 14: Connect with version not starting with 'v' in two-component format throws exception

Details:
  TestName: connectWithInvalidVersionFormatThrowsException
  Description: Verify that when the model string has two components where the first is neither a valid provider nor a version starting with 'v', an IllegalArgumentException is thrown.

Execution:
  Arrange: Create a mock Gemini delegate. Create an ApigeeLlm instance with model name "apigee/gemini-2.5-flash" using the test constructor. Create an LlmRequest with model "apigee/invalid/gemini-pro".
  Act: Call the connect method with the LlmRequest.
  Assert: Verify that an IllegalArgumentException is thrown.

Validation:
  This validates the version format requirement. In two-component formats, if the first component is not a recognized provider, it must be a version starting with 'v'. This ensures strict format compliance.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.junit.jupiter.api.Assertions.assertThrows;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.verify;
import static org.mockito.Mockito.when;

import com.google.adk.tools.BaseTool;
import com.google.common.collect.ImmutableList;
import com.google.common.collect.ImmutableMap;
import com.google.genai.types.Content;
import com.google.genai.types.GenerateContentConfig;
import com.google.genai.types.Part;
import java.util.Optional;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.mockito.ArgumentCaptor;

public class ApigeeLlmConnectTest {

  private Gemini mockGeminiDelegate;

  private BaseLlmConnection mockConnection;

  @BeforeEach
  void setUp() {
    mockGeminiDelegate = mock(Gemini.class);
    mockConnection = mock(BaseLlmConnection.class);
    when(mockGeminiDelegate.connect(any(LlmRequest.class))).thenReturn(mockConnection);
  }

  @Test
  @Tag("valid")
  void connectWithExplicitModelInRequest() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/vertex_ai/gemini-pro").build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-pro"));
  }

  @Test
  @Tag("valid")
  void connectWithoutModelUsesDefaultModel() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-2.5-flash"));
  }

  @Test
  @Tag("valid")
  void connectWithProviderAndVersionExtractsModelId() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/vertex_ai/v1/gemini-base", mockGeminiDelegate);
    LlmRequest request =
        LlmRequest.builder().model("apigee/vertex_ai/v1beta/gemini-2.5-flash").build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-2.5-flash"));
  }

  @Test
  @Tag("valid")
  void connectWithVersionOnlyExtractsModelId() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/v1/gemini-pro").build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-pro"));
  }

  @Test
  @Tag("valid")
  void connectWithSimpleModelFormat() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/gemini-pro").build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-pro"));
  }

  @Test
  @Tag("valid")
  void connectWithProviderOnlyFormat() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/gemini/gemini-2.5-flash").build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-2.5-flash"));
  }

  @Test
  @Tag("invalid")
  void connectWithInvalidModelStringThrowsException() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("invalid/model").build();
    IllegalArgumentException exception =
        assertThrows(IllegalArgumentException.class, () -> apigeeLlm.connect(request));
    assertThat(exception.getMessage()).contains("Invalid model string");
  }

  @Test
  @Tag("invalid")
  void connectWithMissingModelIdThrowsException() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/gemini/").build();
    assertThrows(IllegalArgumentException.class, () -> apigeeLlm.connect(request));
  }

  @Test
  @Tag("invalid")
  void connectWithEmptyModelPartThrowsException() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/").build();
    assertThrows(IllegalArgumentException.class, () -> apigeeLlm.connect(request));
  }

  @Test
  @Tag("valid")
  void connectReturnsDelegateConnection() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/gemini-pro").build();
    BaseLlmConnection result = apigeeLlm.connect(request);
    assertThat(result).isSameInstanceAs(mockConnection);
  }

  @Test
  @Tag("valid")
  void connectPreservesOtherRequestProperties() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);

    Content content = Content.builder().role("user").parts(Part.fromText("Hello")).build();

    GenerateContentConfig config = GenerateContentConfig.builder().temperature(0.7f).build();

    LlmRequest request =
        LlmRequest.builder()
            .model("apigee/gemini-pro")
            .contents(ImmutableList.of(content))
            .config(config)
            .build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    LlmRequest capturedRequest = captor.getValue();

    assertThat(capturedRequest.model()).isEqualTo(Optional.of("gemini-pro"));
    assertThat(capturedRequest.contents()).isEqualTo(ImmutableList.of(content));
    assertThat(capturedRequest.config()).isEqualTo(Optional.of(config));
  }

  @Test
  @Tag("valid")
  void connectWithVertexAiProviderExtractsModelId() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/vertex_ai/gemini-pro", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-pro"));
  }

  @Test
  @Tag("invalid")
  void connectWithInvalidProviderThrowsException() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request =
        LlmRequest.builder().model("apigee/invalid_provider/v1/gemini-pro").build();
    assertThrows(IllegalArgumentException.class, () -> apigeeLlm.connect(request));
  }

  @Test
  @Tag("invalid")
  void connectWithInvalidVersionFormatThrowsException() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/invalid/gemini-pro").build();
    assertThrows(IllegalArgumentException.class, () -> apigeeLlm.connect(request));
  }

  @Test
  @Tag("boundary")
  void connectWithModelContainingSpecialCharacters() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("apigee/gemini-2.5-flash-001").build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-2.5-flash-001"));
  }

  @Test
  @Tag("valid")
  void connectWithGeminiProviderExtractsModelId() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini/gemini-1.5-pro", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-1.5-pro"));
  }

  @Test
  @Tag("valid")
  void connectWithVertexAiAndVersionExtractsModelId() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/vertex_ai/v1beta/gemini-ultra", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model()).isEqualTo(Optional.of("gemini-ultra"));
  }

  @Test
  @Tag("boundary")
  void connectWithEmptyContentsPreservesRequest() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request =
        LlmRequest.builder().model("apigee/gemini-pro").contents(ImmutableList.of()).build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().contents()).isEmpty();
  }

  @Test
  @Tag("valid")
  void connectWithToolsPreservesRequest() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    BaseTool mockTool = mock(BaseTool.class);

    LlmRequest request =
        LlmRequest.builder()
            .model("apigee/gemini-pro")
            .tools(ImmutableMap.of("testTool", mockTool))
            .build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().tools()).containsKey("testTool");
  }

  @Test
  @Tag("invalid")
  void connectWithModelNotStartingWithApigeeThrowsException() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    LlmRequest request = LlmRequest.builder().model("gemini/gemini-pro").build();
    IllegalArgumentException exception =
        assertThrows(IllegalArgumentException.class, () -> apigeeLlm.connect(request));
    assertThat(exception.getMessage()).contains("Invalid model string");
  }

  @Test
  @Tag("boundary")
  void connectWithLongModelName() {
    ApigeeLlm apigeeLlm = new ApigeeLlm("apigee/gemini-2.5-flash", mockGeminiDelegate);
    String longModelName = "apigee/very-long-model-name-with-many-characters-12345";
    LlmRequest request = LlmRequest.builder().model(longModelName).build();
    apigeeLlm.connect(request);
    ArgumentCaptor<LlmRequest> captor = ArgumentCaptor.forClass(LlmRequest.class);
    verify(mockGeminiDelegate).connect(captor.capture());
    assertThat(captor.getValue().model())
        .isEqualTo(Optional.of("very-long-model-name-with-many-characters-12345"));
  }
}
