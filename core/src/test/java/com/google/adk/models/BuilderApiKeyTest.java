/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test zbio-dec-17-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=apiKey_6f7fe65f1f
ROOST_METHOD_SIG_HASH=apiKey_f645a7d0cc

Scenario 1: Setting a Valid API Key

Details:
  TestName: apiKeyWithValidString
  Description: This test verifies that the apiKey method correctly sets a valid API key string and returns the Builder instance for method chaining. It ensures the basic functionality of the setter method works as expected with a non-null, non-empty string value.
Execution:
  Arrange: Create a new Builder instance. Prepare a valid API key string (e.g., "test-api-key-12345").
  Act: Invoke the apiKey method on the Builder instance with the valid API key string.
  Assert: Verify that the method returns the same Builder instance (for method chaining). Verify that when build() is called with a modelName set, the resulting Gemini instance is created successfully without throwing exceptions.
Validation:
  The assertion verifies that the apiKey method follows the builder pattern by returning the Builder instance itself, enabling fluent API usage. This test is significant because it confirms the basic contract of the builder method - accepting valid input and maintaining the builder chain. The successful build() call indirectly validates that the API key was stored correctly in the Builder's internal state.

Scenario 2: Setting a Null API Key

Details:
  TestName: apiKeyWithNullValue
  Description: This test checks the behavior when a null value is passed to the apiKey method. It verifies whether the method accepts null values or if there are any null-safety mechanisms in place.
Execution:
  Arrange: Create a new Builder instance.
  Act: Invoke the apiKey method with a null value.
  Assert: Verify that the method returns the Builder instance without throwing a NullPointerException. Verify that the Builder can still be used for subsequent operations.
Validation:
  This test validates the null-handling behavior of the apiKey method. Since the method signature doesn't explicitly prevent null values and there's no @NonNull annotation visible, the method should accept null. This is important because the build() method has logic to handle cases where apiKey might be null (checking if apiKey != null before using it). This test ensures the Builder doesn't fail prematurely when null is provided.

Scenario 3: Setting an Empty String API Key

Details:
  TestName: apiKeyWithEmptyString
  Description: This test verifies the behavior when an empty string is provided as the API key. It checks whether the method accepts empty strings and how the Builder handles this edge case.
Execution:
  Arrange: Create a new Builder instance. Prepare an empty string ("").
  Act: Invoke the apiKey method with the empty string.
  Assert: Verify that the method returns the Builder instance. Verify that the Builder accepts the empty string without throwing exceptions.
Validation:
  This test is important for validating edge case handling. An empty string is technically a valid String object but represents no actual API key. The test ensures the Builder doesn't reject this input at the setter level. The actual validation of whether an empty API key is acceptable would occur during API calls, not during Builder construction. This separation of concerns is a valid design pattern.

Scenario 4: Setting API Key Multiple Times (Overwriting)

Details:
  TestName: apiKeySetMultipleTimes
  Description: This test verifies that calling the apiKey method multiple times on the same Builder instance correctly overwrites the previous value with the most recent one. It ensures the last-set-wins behavior.
Execution:
  Arrange: Create a new Builder instance. Prepare two different API key strings (e.g., "first-api-key" and "second-api-key").
  Act: Invoke the apiKey method first with "first-api-key", then invoke it again with "second-api-key" on the same Builder instance.
  Assert: Verify that each invocation returns the Builder instance. Verify that when build() is called with a modelName, the resulting Gemini instance uses the second API key (this can be indirectly verified by ensuring no exceptions are thrown and the build succeeds).
Validation:
  This test validates the mutability behavior of the Builder pattern. It's important to confirm that subsequent calls to apiKey replace the previous value rather than causing errors or unexpected behavior. This is standard builder pattern behavior where the last value set takes precedence, allowing users to modify their configuration before calling build().

Scenario 5: Method Chaining with API Key

Details:
  TestName: apiKeyMethodChaining
  Description: This test verifies that the apiKey method properly supports method chaining by returning the Builder instance, allowing multiple builder methods to be called in sequence.
Execution:
  Arrange: Create a new Builder instance. Prepare an API key string and a model name string.
  Act: Chain multiple method calls together: builder.apiKey("test-key").modelName("gemini-2.0-flash").
  Assert: Verify that the chaining executes without errors. Verify that calling build() after the chain creates a Gemini instance successfully.
Validation:
  This test validates the fluent API design of the Builder class. The @CanIgnoreReturnValue annotation indicates the return value can be ignored, but for proper builder pattern implementation, the method should return 'this' to enable chaining. This test ensures developers can write clean, readable code using the builder pattern's fluent interface style.

Scenario 6: API Key Takes Precedence Over Vertex Credentials

Details:
  TestName: apiKeyPrecedenceOverVertexCredentials
  Description: This test verifies the precedence logic documented in the Builder class, where if both apiKey and vertexCredentials are set, the apiKey takes precedence when building the Gemini instance.
Execution:
  Arrange: Create a new Builder instance. Prepare a valid API key string and a VertexCredentials object. Set a valid model name.
  Act: Call vertexCredentials() first, then call apiKey() on the same Builder instance. Invoke build() to create the Gemini instance.
  Assert: Verify that the build() method completes successfully. Verify that the resulting Gemini instance is created using the API key constructor path (indirectly verified by successful instantiation).
Validation:
  This test validates the documented precedence behavior in the Builder's build() method. According to the code, when both apiKey and vertexCredentials are set, the build() method checks apiKey first (else if (apiKey != null)) before checking vertexCredentials. This test ensures this precedence logic works correctly and that setting both doesn't cause conflicts or unexpected behavior.

Scenario 7: API Key with Whitespace Only

Details:
  TestName: apiKeyWithWhitespaceOnly
  Description: This test checks the behavior when an API key consisting only of whitespace characters is provided. It verifies whether the method accepts such input.
Execution:
  Arrange: Create a new Builder instance. Prepare a string containing only whitespace characters (e.g., "   " or "\t\n").
  Act: Invoke the apiKey method with the whitespace-only string.
  Assert: Verify that the method returns the Builder instance without throwing exceptions. Verify that the Builder accepts this input.
Validation:
  This test validates another edge case where the input is technically a non-null, non-empty string but contains no meaningful content. The apiKey method should accept this input as it performs no validation at the setter level. Any validation of the API key's actual validity would occur when the Client attempts to use it for API calls, following the principle of fail-late for better error context.

Scenario 8: API Key with Special Characters

Details:
  TestName: apiKeyWithSpecialCharacters
  Description: This test verifies that the apiKey method correctly handles API keys containing special characters, which is common in actual API keys that may contain hyphens, underscores, or other special characters.
Execution:
  Arrange: Create a new Builder instance. Prepare an API key string with various special characters (e.g., "test-api_key.123!@#").
  Act: Invoke the apiKey method with the special character string.
  Assert: Verify that the method returns the Builder instance. Verify that the Builder stores the API key exactly as provided without modification.
Validation:
  This test is important because real API keys often contain special characters like hyphens, underscores, and alphanumeric combinations. The test ensures the apiKey method doesn't perform any unwanted sanitization or transformation of the input string. It validates that the method acts as a simple setter, preserving the exact string value provided by the user.

Scenario 9: API Key with Very Long String

Details:
  TestName: apiKeyWithVeryLongString
  Description: This test verifies the behavior when an unusually long string is provided as the API key. It checks whether there are any length restrictions or performance issues with large strings.
Execution:
  Arrange: Create a new Builder instance. Generate a very long string (e.g., 10,000 characters) to simulate an edge case.
  Act: Invoke the apiKey method with the very long string.
  Assert: Verify that the method returns the Builder instance without throwing exceptions. Verify that the method completes in reasonable time without performance degradation.
Validation:
  This test validates the robustness of the apiKey method against unusually large inputs. While real API keys are typically of reasonable length, this test ensures there are no hidden buffer size limitations or performance issues. The method should handle any valid String object regardless of length, as Java Strings can theoretically hold up to Integer.MAX_VALUE characters.

Scenario 10: Building Gemini with Only API Key Set

Details:
  TestName: buildWithOnlyApiKeySet
  Description: This test verifies that a Gemini instance can be successfully built when only the apiKey and modelName are set, without setting apiClient or vertexCredentials.
Execution:
  Arrange: Create a new Builder instance. Set a valid model name and a valid API key.
  Act: Call the build() method on the Builder.
  Assert: Verify that a Gemini instance is created successfully. Verify that the instance is not null and is of type Gemini.
Validation:
  This test validates the complete workflow of using the apiKey method in the Builder pattern. It confirms that setting an API key is sufficient (along with modelName) to build a valid Gemini instance. According to the build() method logic, when apiKey is set, it creates a Gemini instance using the constructor that accepts modelName and apiKey, which internally creates a Client with the provided API key.

Scenario 11: Return Value is Same Builder Instance

Details:
  TestName: apiKeyReturnsSameBuilderInstance
  Description: This test explicitly verifies that the apiKey method returns the exact same Builder instance (not a copy or new instance), which is essential for proper builder pattern implementation.
Execution:
  Arrange: Create a new Builder instance and store a reference to it.
  Act: Invoke the apiKey method with a valid API key string and capture the returned value.
  Assert: Use reference equality (==) to verify that the returned Builder is the exact same object instance as the original Builder.
Validation:
  This test validates a critical aspect of the builder pattern implementation. The method must return 'this' (the same instance) rather than creating a new Builder instance. This ensures that all builder method calls modify the same underlying object and that method chaining works correctly. The @CanIgnoreReturnValue annotation suggests the return value can be ignored, but when used, it must be the same instance.

Scenario 12: API Key After Setting API Client

Details:
  TestName: apiKeyAfterSettingApiClient
  Description: This test verifies the behavior when apiKey is set after an explicit apiClient has already been set on the Builder. According to the precedence rules, the apiClient should take precedence.
Execution:
  Arrange: Create a new Builder instance. Create a mock or actual Client instance. Set the apiClient first, then set an apiKey.
  Act: Call build() with a valid modelName.
  Assert: Verify that the build() method completes successfully. Verify that the resulting Gemini instance is created (the explicit apiClient takes precedence according to build() logic).
Validation:
  This test validates the precedence hierarchy documented in the Builder class. When both apiClient and apiKey are set, the build() method checks for apiClient first (if (apiClient != null)) before checking apiKey. This test ensures that setting apiKey after apiClient doesn't override or interfere with the explicit client, maintaining the documented precedence order.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.mockito.Mockito.mock;

import com.google.genai.Client;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderApiKeyTest {

	private Gemini.Builder builder;

	@BeforeEach
	void setUp() {
		builder = Gemini.builder();
	}

	@Test
	@Tag("valid")
	void apiKeyWithValidString() {
		String validApiKey = "test-api-key-12345";

		Gemini.Builder result = builder.apiKey(validApiKey);

		assertThat(result).isSameInstanceAs(builder);
		Gemini gemini = builder.modelName("gemini-2.0-flash").build();
		assertNotNull(gemini);
	}

	@Test
	@Tag("boundary")
	void apiKeyWithNullValue() {
		Gemini.Builder result = builder.apiKey(null);

		assertThat(result).isSameInstanceAs(builder);
		assertThat(result).isNotNull();
	}

	@Test
	@Tag("boundary")
	void apiKeyWithEmptyString() {
		String emptyApiKey = "";

		Gemini.Builder result = builder.apiKey(emptyApiKey);

		assertThat(result).isSameInstanceAs(builder);
		assertThat(result).isNotNull();
	}

	@Test
	@Tag("valid")
	void apiKeySetMultipleTimes() {
		String firstApiKey = "first-api-key";
		String secondApiKey = "second-api-key";

		Gemini.Builder result1 = builder.apiKey(firstApiKey);
		Gemini.Builder result2 = builder.apiKey(secondApiKey);

		assertThat(result1).isSameInstanceAs(builder);
		assertThat(result2).isSameInstanceAs(builder);
		Gemini gemini = builder.modelName("gemini-2.0-flash").build();
		assertNotNull(gemini);
	}

	@Test
	@Tag("valid")
	void apiKeyMethodChaining() {
		String apiKey = "test-key";
		String modelName = "gemini-2.0-flash";

		Gemini gemini = builder.apiKey(apiKey).modelName(modelName).build();

		assertNotNull(gemini);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("valid")
	void apiKeyPrecedenceOverVertexCredentials() {
		// Note: VertexCredentials.Builder does not have project() and location() methods
		// Based on the provided information, VertexCredentials only has Optional<String>
		// project() and
		// location() as getters
		// The builder pattern should be used differently - commenting this test as it
		// requires
		// clarification on builder methods

		/*
		 * BUSINESS LOGIC COMMENT: The VertexCredentials.Builder does not expose project()
		 * and location() setter methods. Need to verify the actual builder API for
		 * VertexCredentials to properly construct instances. This test should be updated
		 * once the correct builder methods are identified.
		 */

		String apiKey = "test-api-key";

		Gemini gemini = builder.modelName("gemini-2.0-flash").apiKey(apiKey).build();

		assertNotNull(gemini);
	}

	@Test
	@Tag("boundary")
	void apiKeyWithWhitespaceOnly() {
		String whitespaceApiKey = "   \t\n   ";

		Gemini.Builder result = builder.apiKey(whitespaceApiKey);

		assertThat(result).isSameInstanceAs(builder);
		assertThat(result).isNotNull();
	}

	@Test
	@Tag("valid")
	void apiKeyWithSpecialCharacters() {
		String specialCharApiKey = "test-api_key.123!@#$%^&*()";

		Gemini.Builder result = builder.apiKey(specialCharApiKey);

		assertThat(result).isSameInstanceAs(builder);
		Gemini gemini = builder.modelName("gemini-2.0-flash").build();
		assertNotNull(gemini);
	}

	@Test
	@Tag("boundary")
	void apiKeyWithVeryLongString() {
		StringBuilder longStringBuilder = new StringBuilder();
		for (int i = 0; i < 10000; i++) {
			longStringBuilder.append("a");
		}
		String veryLongApiKey = longStringBuilder.toString();

		Gemini.Builder result = builder.apiKey(veryLongApiKey);

		assertThat(result).isSameInstanceAs(builder);
		assertThat(result).isNotNull();
	}

	@Test
	@Tag("valid")
	void buildWithOnlyApiKeySet() {
		String apiKey = "valid-api-key-123";
		String modelName = "gemini-2.0-flash";

		Gemini gemini = builder.modelName(modelName).apiKey(apiKey).build();

		assertNotNull(gemini);
		assertThat(gemini).isInstanceOf(Gemini.class);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("valid")
	void apiKeyReturnsSameBuilderInstance() {
		String apiKey = "test-api-key";

		Gemini.Builder returnedBuilder = builder.apiKey(apiKey);

		assertThat(returnedBuilder == builder).isTrue();
	}

	@Test
	@Tag("valid")
	void apiKeyAfterSettingApiClient() {
		Client mockClient = mock(Client.class);
		String apiKey = "test-api-key";
		String modelName = "gemini-2.0-flash";

		Gemini gemini = builder.modelName(modelName).apiClient(mockClient).apiKey(apiKey).build();

		assertNotNull(gemini);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("valid")
	void apiKeyWithAlphanumericString() {
		String alphanumericApiKey = "AIzaSyC1234567890abcdefghijklmnopqrstuvwxyz";

		Gemini.Builder result = builder.apiKey(alphanumericApiKey);

		assertThat(result).isSameInstanceAs(builder);
		Gemini gemini = builder.modelName("gemini-2.0-flash").build();
		assertNotNull(gemini);
	}

	@Test
	@Tag("valid")
	void apiKeyWithUnicodeCharacters() {
		String unicodeApiKey = "test-api-key-你好-مرحبا-🔑";

		Gemini.Builder result = builder.apiKey(unicodeApiKey);

		assertThat(result).isSameInstanceAs(builder);
		assertThat(result).isNotNull();
	}

	@Test
	@Tag("valid")
	void apiKeyMultipleChainedCalls() {
		String apiKey = "chained-api-key";

		Gemini gemini = builder.modelName("gemini-2.0-flash").apiKey(apiKey).build();

		assertNotNull(gemini);
	}

	@Test
	@Tag("boundary")
	void apiKeyWithSingleCharacter() {
		String singleCharApiKey = "a";

		Gemini.Builder result = builder.apiKey(singleCharApiKey);

		assertThat(result).isSameInstanceAs(builder);
		assertThat(result).isNotNull();
	}

}
