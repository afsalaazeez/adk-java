/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test zbio-dec-16-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=modelName_423c161ca3
ROOST_METHOD_SIG_HASH=modelName_2d01dc01ba

Scenario 1: Setting a Valid Model Name

Details:
  TestName: modelNameWithValidString
  Description: This test verifies that the modelName method correctly sets a valid model name string and returns the Builder instance for method chaining. It tests the basic functionality of the setter method with a typical valid input.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a valid model name string such as "gemini-2.0-flash".
  Act: Invoke the modelName method on the Builder instance with the valid model name string.
  Assert: Verify that the method returns the same Builder instance (for method chaining). Build the Gemini object and verify that the model name was set correctly by checking the model() method of the built Gemini instance.
Validation:
  The assertion verifies that the modelName method properly stores the provided model name in the Builder's internal state and returns the Builder instance to enable fluent API usage. This is critical for the builder pattern implementation, ensuring that users can chain multiple configuration methods together. The test confirms that the basic setter functionality works as expected for standard use cases.

Scenario 2: Setting Model Name with Empty String

Details:
  TestName: modelNameWithEmptyString
  Description: This test checks the behavior when an empty string is passed as the model name. It verifies whether the Builder accepts empty strings and how the built Gemini instance handles this edge case.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare an empty string "".
  Act: Invoke the modelName method with the empty string. Then call build() with an apiKey to create a Gemini instance.
  Assert: Verify that the method returns the Builder instance. Verify that the built Gemini instance has an empty string as its model name by calling model().
Validation:
  This test validates the Builder's behavior with boundary input (empty string). While the build() method requires a non-null modelName, it doesn't explicitly validate for empty strings. This test ensures that empty strings are handled consistently and that the system behavior is predictable. This is important for understanding the validation boundaries of the Builder class.

Scenario 3: Setting Model Name with Null Value

Details:
  TestName: modelNameWithNullValue
  Description: This test verifies the behavior when null is passed to the modelName method. It checks whether the Builder accepts null values and how this affects the build process.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder().
  Act: Invoke the modelName method with null as the parameter.
  Assert: Verify that the method returns the Builder instance. When attempting to build the Gemini instance, verify that a NullPointerException is thrown with the message "modelName must be set."
Validation:
  This test ensures that the Builder properly handles null input for the model name. According to the build() method implementation, a null modelName should cause a NullPointerException during the build phase. This test validates the error handling mechanism and ensures that the Builder enforces the requirement for a non-null model name, preventing the creation of invalid Gemini instances.

Scenario 4: Method Chaining with Multiple Builder Methods

Details:
  TestName: modelNameMethodChaining
  Description: This test verifies that the modelName method properly supports method chaining by returning the Builder instance, allowing multiple builder methods to be called in sequence.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a valid model name string, an API key string, and other builder parameters.
  Act: Chain multiple builder method calls together: modelName("gemini-2.0-flash").apiKey("test-api-key").build().
  Assert: Verify that the chaining works without errors and that a valid Gemini instance is created. Verify that the model name is correctly set in the built instance.
Validation:
  This test confirms that the modelName method correctly implements the builder pattern by returning 'this', enabling fluent API usage. Method chaining is a core feature of the builder pattern, improving code readability and usability. This test ensures that developers can configure multiple properties in a single, readable statement, which is essential for a good developer experience.

Scenario 5: Overwriting Previously Set Model Name

Details:
  TestName: modelNameOverwritesPreviousValue
  Description: This test verifies that calling the modelName method multiple times on the same Builder instance correctly overwrites the previous value with the new one.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare two different model name strings: "gemini-1.0-pro" and "gemini-2.0-flash".
  Act: Call modelName("gemini-1.0-pro") first, then call modelName("gemini-2.0-flash") on the same Builder instance. Build the Gemini instance with an apiKey.
  Assert: Verify that the final built Gemini instance has the model name "gemini-2.0-flash" (the last value set), not "gemini-1.0-pro".
Validation:
  This test ensures that the Builder correctly handles multiple invocations of the modelName method, with the last call taking precedence. This is important for scenarios where configuration might be applied conditionally or in multiple stages. The test validates that the Builder maintains consistent state management and that users can safely update configuration values before calling build().

Scenario 6: Setting Model Name with Special Characters

Details:
  TestName: modelNameWithSpecialCharacters
  Description: This test verifies that the modelName method can handle model names containing special characters, hyphens, dots, and numbers, which are common in model naming conventions.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a model name string with special characters such as "gemini-2.0-flash-exp-1234".
  Act: Invoke the modelName method with the special character string. Build the Gemini instance with an apiKey.
  Assert: Verify that the method returns the Builder instance. Verify that the built Gemini instance correctly stores and returns the model name with all special characters intact.
Validation:
  This test validates that the Builder doesn't perform any unwanted sanitization or transformation on the model name input. Model names often include version numbers, hyphens, and other special characters. Ensuring these are preserved exactly as provided is crucial for correct API communication, as the model name is used directly in API calls to identify the specific model variant.

Scenario 7: Setting Model Name with Whitespace

Details:
  TestName: modelNameWithWhitespace
  Description: This test checks the behavior when a model name containing leading, trailing, or internal whitespace is provided to verify if the Builder performs any trimming or validation.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare model name strings with various whitespace patterns: " gemini-2.0-flash", "gemini-2.0-flash ", and "gemini 2.0 flash".
  Act: Invoke the modelName method with each whitespace variant. Build Gemini instances with an apiKey.
  Assert: Verify that the method returns the Builder instance. Verify that the built Gemini instances preserve the whitespace exactly as provided without trimming or modification.
Validation:
  This test ensures that the modelName method does not perform implicit whitespace trimming or validation. While whitespace in model names is likely unintentional and could cause API errors, the Builder's responsibility is to store the value as provided. This test documents the current behavior and helps identify whether validation should be added at the Builder level or left to the API client level.

Scenario 8: Setting Model Name with Very Long String

Details:
  TestName: modelNameWithVeryLongString
  Description: This test verifies the Builder's behavior when provided with an extremely long model name string to check for any length limitations or performance issues.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Generate a very long string (e.g., 10000 characters) to use as a model name.
  Act: Invoke the modelName method with the very long string. Build the Gemini instance with an apiKey.
  Assert: Verify that the method returns the Builder instance. Verify that the built Gemini instance stores and returns the complete long string without truncation.
Validation:
  This test validates that the Builder can handle edge cases involving very long input strings without throwing exceptions or causing performance degradation. While such long model names are unrealistic in practice, this test ensures robustness and helps identify any hidden assumptions about input length. It also verifies that the Builder doesn't impose arbitrary length restrictions that aren't documented.

Scenario 9: Builder Returns Same Instance for Method Chaining

Details:
  TestName: modelNameReturnsSameBuilderInstance
  Description: This test explicitly verifies that the modelName method returns the exact same Builder instance (not a copy) to ensure proper method chaining behavior.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder() and store it in a variable.
  Act: Call the modelName method on the Builder instance and store the returned value in another variable.
  Assert: Verify that both variables reference the exact same object using reference equality (==) or Objects.equals().
Validation:
  This test confirms that the Builder follows the standard builder pattern convention of returning 'this' rather than creating a new Builder instance. This is crucial for method chaining efficiency and ensures that all chained method calls operate on the same Builder instance. Returning a different instance could lead to unexpected behavior where earlier configuration is lost.

Scenario 10: Setting Model Name Before and After Other Builder Methods

Details:
  TestName: modelNameOrderIndependence
  Description: This test verifies that the order in which modelName is called relative to other builder methods (apiKey, apiClient, vertexCredentials) does not affect the final result.
Execution:
  Arrange: Create two Builder instances. Prepare a model name and an API key.
  Act: On the first Builder, call modelName first, then apiKey. On the second Builder, call apiKey first, then modelName. Build both instances.
  Assert: Verify that both built Gemini instances have the same model name and function identically.
Validation:
  This test ensures that the Builder implementation is order-independent, meaning that the sequence in which configuration methods are called doesn't affect the final outcome. This is an important property of well-designed builders, as it gives users flexibility in how they construct objects and prevents subtle bugs related to method call ordering.

Scenario 11: Setting Model Name with Unicode Characters

Details:
  TestName: modelNameWithUnicodeCharacters
  Description: This test verifies that the modelName method correctly handles model names containing Unicode characters from various languages and symbol sets.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare model name strings containing Unicode characters such as "gemini-模型-2.0", "gemini-מודל", or "gemini-🚀-model".
  Act: Invoke the modelName method with the Unicode string. Build the Gemini instance with an apiKey.
  Assert: Verify that the method returns the Builder instance. Verify that the built Gemini instance correctly stores and returns the model name with all Unicode characters preserved.
Validation:
  This test validates that the Builder properly handles internationalization and Unicode support. While model names with non-ASCII characters are uncommon, ensuring proper Unicode handling is important for robustness and potential future use cases. This test confirms that the Builder doesn't perform any character encoding transformations that could corrupt the input.

Scenario 12: Model Name Persistence Across Multiple Build Calls

Details:
  TestName: modelNamePersistsAcrossMultipleBuilds
  Description: This test verifies whether the Builder can be reused to create multiple Gemini instances and whether the model name persists or needs to be reset between builds.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Set a model name and apiKey.
  Act: Call build() to create the first Gemini instance. Call build() again on the same Builder to create a second instance.
  Assert: Verify that both Gemini instances have the same model name. Verify that both instances are separate objects (not the same reference).
Validation:
  This test examines the Builder's reusability characteristics. It verifies whether the Builder maintains its state after build() is called, allowing for the creation of multiple similar instances. This is useful for understanding whether the Builder follows a reusable or single-use pattern, which affects how developers should use it in their code.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertThrows;

import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderModelNameTest {

	@Test
	@Tag("valid")
	void modelNameWithValidString() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String validModelName = "gemini-2.0-flash";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(validModelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(validModelName);
	}

	@Test
	@Tag("boundary")
	void modelNameWithEmptyString() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String emptyString = "";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(emptyString);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEmpty();
	}

	@Test
	@Tag("invalid")
	void modelNameWithNullValue() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(null);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		NullPointerException exception = assertThrows(NullPointerException.class,
				() -> builder.apiKey("test-api-key").build());
		assertThat(exception.getMessage()).contains("modelName must be set");
	}

	@Test
	@Tag("valid")
	void modelNameMethodChaining() {
		// Arrange
		String modelName = "gemini-2.0-flash";
		String apiKey = "test-api-key";
		// Act
		Gemini gemini = Gemini.builder().modelName(modelName).apiKey(apiKey).build();
		// Assert
		assertNotNull(gemini);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("valid")
	void modelNameOverwritesPreviousValue() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String firstModelName = "gemini-1.0-pro";
		String secondModelName = "gemini-2.0-flash";
		// Act
		builder.modelName(firstModelName);
		builder.modelName(secondModelName);
		Gemini gemini = builder.apiKey("test-api-key").build();
		// Assert
		assertThat(gemini.model()).isEqualTo(secondModelName);
		assertThat(gemini.model()).isNotEqualTo(firstModelName);
	}

	@Test
	@Tag("valid")
	void modelNameWithSpecialCharacters() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String specialCharModelName = "gemini-2.0-flash-exp-1234";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(specialCharModelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(specialCharModelName);
	}

	@Test
	@Tag("boundary")
	void modelNameWithWhitespace() {
		// Arrange
		Gemini.Builder builder1 = Gemini.builder();
		Gemini.Builder builder2 = Gemini.builder();
		Gemini.Builder builder3 = Gemini.builder();
		String leadingWhitespace = " gemini-2.0-flash";
		String trailingWhitespace = "gemini-2.0-flash ";
		String internalWhitespace = "gemini 2.0 flash";
		// Act
		builder1.modelName(leadingWhitespace);
		builder2.modelName(trailingWhitespace);
		builder3.modelName(internalWhitespace);
		Gemini gemini1 = builder1.apiKey("test-api-key").build();
		Gemini gemini2 = builder2.apiKey("test-api-key").build();
		Gemini gemini3 = builder3.apiKey("test-api-key").build();
		// Assert
		assertThat(gemini1.model()).isEqualTo(leadingWhitespace);
		assertThat(gemini2.model()).isEqualTo(trailingWhitespace);
		assertThat(gemini3.model()).isEqualTo(internalWhitespace);
	}

	@Test
	@Tag("boundary")
	void modelNameWithVeryLongString() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String veryLongString = "a".repeat(10000);
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(veryLongString);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(veryLongString);
		assertThat(gemini.model().length()).isEqualTo(10000);
	}

	@Test
	@Tag("valid")
	void modelNameReturnsSameBuilderInstance() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String modelName = "gemini-2.0-flash";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(modelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		assertThat(returnedBuilder == builder).isTrue();
	}

	@Test
	@Tag("valid")
	void modelNameOrderIndependence() {
		// Arrange
		String modelName = "gemini-2.0-flash";
		String apiKey = "test-api-key";
		// Act
		Gemini gemini1 = Gemini.builder().modelName(modelName).apiKey(apiKey).build();

		Gemini gemini2 = Gemini.builder().apiKey(apiKey).modelName(modelName).build();
		// Assert
		assertThat(gemini1.model()).isEqualTo(gemini2.model());
		assertThat(gemini1.model()).isEqualTo(modelName);
		assertThat(gemini2.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("valid")
	void modelNameWithUnicodeCharacters() {
		// Arrange
		Gemini.Builder builder1 = Gemini.builder();
		Gemini.Builder builder2 = Gemini.builder();
		Gemini.Builder builder3 = Gemini.builder();
		String chineseModelName = "gemini-模型-2.0";
		String hebrewModelName = "gemini-מודל";
		String emojiModelName = "gemini-🚀-model";
		// Act
		builder1.modelName(chineseModelName);
		builder2.modelName(hebrewModelName);
		builder3.modelName(emojiModelName);
		Gemini gemini1 = builder1.apiKey("test-api-key").build();
		Gemini gemini2 = builder2.apiKey("test-api-key").build();
		Gemini gemini3 = builder3.apiKey("test-api-key").build();
		// Assert
		assertThat(gemini1.model()).isEqualTo(chineseModelName);
		assertThat(gemini2.model()).isEqualTo(hebrewModelName);
		assertThat(gemini3.model()).isEqualTo(emojiModelName);
	}

	@Test
	@Tag("valid")
	void modelNamePersistsAcrossMultipleBuilds() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String modelName = "gemini-2.0-flash";
		builder.modelName(modelName).apiKey("test-api-key");
		// Act
		Gemini gemini1 = builder.build();
		Gemini gemini2 = builder.build();
		// Assert
		assertThat(gemini1.model()).isEqualTo(modelName);
		assertThat(gemini2.model()).isEqualTo(modelName);
		assertThat(gemini1).isNotSameInstanceAs(gemini2);
	}

}
