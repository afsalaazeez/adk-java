/*
 * Copyright 2025 Google LLC
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
// ********RoostGPT********
/*
Test generated by RoostGPT for test zbio-dec-16-adk using AI Type Claude AI and AI Model claude-sonnet-4-5-20250929

ROOST_METHOD_HASH=modelName_423c161ca3
ROOST_METHOD_SIG_HASH=modelName_2d01dc01ba

Scenario 1: Setting a Valid Model Name

Details:
  TestName: modelNameWithValidString
  Description: This test verifies that the modelName method correctly sets a valid model name string and returns the Builder instance for method chaining. It tests the basic functionality of the setter method with a standard model name.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a valid model name string such as "gemini-2.0-flash".
  Act: Invoke the modelName method on the Builder instance with the valid model name string.
  Assert: Verify that the method returns the same Builder instance (for method chaining). Build the Gemini object and verify that the model name was set correctly by checking the model() method on the built object.
Validation:
  The assertion verifies that the modelName method follows the builder pattern by returning the Builder instance itself, enabling fluent API usage. This test ensures that a valid model name is properly stored in the Builder and can be used to construct a Gemini instance. This is critical for the basic functionality of configuring the Gemini model.

Scenario 2: Setting Model Name with Null Value

Details:
  TestName: modelNameWithNullValue
  Description: This test checks the behavior when a null value is passed to the modelName method. It verifies whether the method accepts null values or if validation occurs during the build phase.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder().
  Act: Invoke the modelName method with a null value.
  Assert: Verify that the method returns the Builder instance. Attempt to build the Gemini object and verify that a NullPointerException is thrown with an appropriate message indicating that modelName must be set.
Validation:
  This test validates the null-safety of the Builder. According to the build() method implementation, modelName is required and must not be null. This test ensures that the system properly enforces this constraint and provides clear error messaging when the constraint is violated, preventing runtime errors in production.

Scenario 3: Setting Model Name with Empty String

Details:
  TestName: modelNameWithEmptyString
  Description: This test verifies the behavior when an empty string is provided as the model name. It checks whether the Builder accepts empty strings and how the system handles this edge case.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare an empty string "".
  Act: Invoke the modelName method with the empty string.
  Assert: Verify that the method returns the Builder instance. Build the Gemini object with appropriate credentials (apiKey or vertexCredentials) and verify that the object is created successfully with the empty string as the model name.
Validation:
  This test examines whether the Builder performs validation on the content of the model name string. Since the build() method only checks for null values, an empty string should be accepted. This test is important to document the current behavior and identify potential areas where additional validation might be needed for business logic requirements.

Scenario 4: Setting Model Name with Whitespace Only

Details:
  TestName: modelNameWithWhitespaceOnly
  Description: This test checks the behavior when a string containing only whitespace characters is passed to the modelName method.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a string with only whitespace characters such as "   " or "\t\n".
  Act: Invoke the modelName method with the whitespace-only string.
  Assert: Verify that the method returns the Builder instance. Build the Gemini object with appropriate credentials and verify that the object is created with the whitespace string as the model name.
Validation:
  This test validates that the Builder does not perform trimming or whitespace validation on the model name. It documents the current behavior where whitespace-only strings are treated as valid model names. This is important for understanding the validation boundaries and potential issues that might arise from user input.

Scenario 5: Setting Model Name Multiple Times

Details:
  TestName: modelNameSetMultipleTimes
  Description: This test verifies that calling the modelName method multiple times on the same Builder instance results in the last value being used, demonstrating the overwrite behavior.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare multiple model name strings such as "gemini-1.0", "gemini-2.0-flash", and "gemini-pro".
  Act: Invoke the modelName method multiple times with different values in sequence.
  Assert: Verify that each invocation returns the Builder instance. Build the Gemini object with appropriate credentials and verify that the model name is set to the last value provided.
Validation:
  This test confirms that the Builder follows the standard builder pattern where subsequent calls to the same setter method overwrite previous values rather than accumulating them. This is important for understanding the Builder's behavior when used in complex configuration scenarios or when values need to be conditionally updated.

Scenario 6: Method Chaining with Model Name

Details:
  TestName: modelNameMethodChaining
  Description: This test verifies that the modelName method properly supports method chaining by returning the Builder instance, allowing multiple builder methods to be called in a fluent manner.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a valid model name and an API key.
  Act: Chain the modelName method call with other Builder methods such as apiKey() in a single statement.
  Assert: Verify that the chained method calls execute successfully without errors. Build the Gemini object and verify that all configured values (model name and API key) are properly set.
Validation:
  This test validates the fluent API design of the Builder class. The @CanIgnoreReturnValue annotation indicates that the return value can be ignored, but for proper builder pattern implementation, the method should return the Builder instance. This test ensures that developers can use the Builder in a fluent, readable manner, which is a key aspect of the builder pattern's usability.

Scenario 7: Setting Model Name with Special Characters

Details:
  TestName: modelNameWithSpecialCharacters
  Description: This test checks how the modelName method handles strings containing special characters, numbers, and symbols that might be part of valid model names.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a model name string with special characters such as "gemini-2.0-flash-001" or "gemini_pro_v2.5".
  Act: Invoke the modelName method with the special character string.
  Assert: Verify that the method returns the Builder instance. Build the Gemini object with appropriate credentials and verify that the model name is stored exactly as provided, without any sanitization or modification.
Validation:
  This test ensures that the Builder does not perform any unwanted transformation or sanitization on the model name string. Model names often include version numbers, hyphens, underscores, and other special characters. This test validates that such valid model names are preserved exactly as specified by the user, which is critical for correct API communication.

Scenario 8: Setting Model Name with Very Long String

Details:
  TestName: modelNameWithVeryLongString
  Description: This test verifies the behavior when an extremely long string is provided as the model name, testing the Builder's handling of edge cases related to string length.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a very long string (e.g., 1000+ characters) as the model name.
  Act: Invoke the modelName method with the very long string.
  Assert: Verify that the method returns the Builder instance. Build the Gemini object with appropriate credentials and verify that the entire long string is stored without truncation.
Validation:
  This test examines whether there are any implicit length limitations in the Builder's handling of the model name. Since the field is a String type without explicit length validation, it should accept strings of any length. This test is important for understanding system behavior under extreme conditions and ensuring that no unexpected truncation or errors occur.

Scenario 9: Builder Instance Reusability After Setting Model Name

Details:
  TestName: builderReusabilityAfterModelName
  Description: This test verifies that a Builder instance can be reused to create multiple Gemini objects with the same or different model names.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Set a model name and appropriate credentials.
  Act: Build a first Gemini object. Then call modelName again with a different value on the same Builder instance and build a second Gemini object.
  Assert: Verify that both Gemini objects are created successfully. Verify that the first object has the first model name and the second object has the second model name.
Validation:
  This test validates that the Builder instance maintains its state correctly across multiple build operations and that changing the model name affects subsequent builds without corrupting previously built objects. This is important for scenarios where a Builder might be configured once and then used to create multiple similar objects with slight variations.

Scenario 10: Setting Model Name with Unicode Characters

Details:
  TestName: modelNameWithUnicodeCharacters
  Description: This test checks the behavior when the model name contains Unicode characters, including non-Latin scripts, emojis, or other international characters.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a model name string containing Unicode characters such as "gemini-模型-2.0" or "gemini-🚀-pro".
  Act: Invoke the modelName method with the Unicode string.
  Assert: Verify that the method returns the Builder instance. Build the Gemini object with appropriate credentials and verify that the Unicode characters are preserved correctly in the model name.
Validation:
  This test ensures that the Builder properly handles internationalization and Unicode support. While model names typically use ASCII characters, this test validates that the system doesn't break or corrupt data when Unicode characters are present. This is important for robustness and potential future internationalization requirements.

Scenario 11: Model Name Interaction with API Client

Details:
  TestName: modelNameWithExplicitApiClient
  Description: This test verifies that when both modelName and an explicit apiClient are set, the Builder correctly uses both values to construct the Gemini instance.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Create a mock or real Client instance. Prepare a valid model name.
  Act: Invoke the modelName method with the model name and the apiClient method with the Client instance.
  Assert: Build the Gemini object and verify that it is created successfully. Verify that the model name is correctly set by calling the model() method on the built object.
Validation:
  This test validates the interaction between the modelName setter and other Builder configuration methods. According to the build() method logic, when an explicit apiClient is provided, it takes precedence over apiKey and vertexCredentials, but the modelName should still be used. This test ensures that these configuration options work correctly together.

Scenario 12: Model Name Interaction with API Key

Details:
  TestName: modelNameWithApiKey
  Description: This test verifies that when both modelName and apiKey are set, the Builder correctly constructs a Gemini instance using both values.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a valid model name and a valid API key string.
  Act: Invoke the modelName method with the model name and the apiKey method with the API key.
  Assert: Build the Gemini object and verify that it is created successfully without throwing exceptions. Verify that the model name is correctly set.
Validation:
  This test validates one of the common usage patterns for the Builder where a model name and API key are provided together. This is likely the most common configuration scenario for users of the Gemini class. The test ensures that this standard workflow functions correctly and that both configuration values are properly utilized in the construction process.

Scenario 13: Model Name Interaction with Vertex Credentials

Details:
  TestName: modelNameWithVertexCredentials
  Description: This test verifies that when both modelName and vertexCredentials are set, the Builder correctly constructs a Gemini instance using both values.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder(). Prepare a valid model name and a VertexCredentials instance.
  Act: Invoke the modelName method with the model name and the vertexCredentials method with the credentials.
  Assert: Build the Gemini object and verify that it is created successfully. Verify that the model name is correctly set by calling the model() method.
Validation:
  This test validates another common usage pattern where Vertex AI credentials are used instead of a direct API key. According to the build() method logic, if no explicit apiClient or apiKey is provided, vertexCredentials will be used to construct the Client. This test ensures that the model name is correctly preserved and used in this configuration scenario.

Scenario 14: Return Value Verification

Details:
  TestName: modelNameReturnsBuilderInstance
  Description: This test explicitly verifies that the modelName method returns the exact same Builder instance that it was called on, confirming proper implementation of the builder pattern.
Execution:
  Arrange: Create a new Builder instance using Gemini.builder() and store the reference.
  Act: Invoke the modelName method with a valid model name and capture the returned value.
  Assert: Use reference equality (==) to verify that the returned Builder instance is the exact same object as the original Builder instance.
Validation:
  This test validates the fundamental contract of the builder pattern where setter methods return the builder instance itself to enable method chaining. The @CanIgnoreReturnValue annotation suggests the return value can be ignored, but it should still return the correct instance. This test ensures that the implementation correctly follows the builder pattern convention, which is essential for the fluent API design.

*/

// ********RoostGPT********

package com.google.adk.models;

import static com.google.common.truth.Truth.assertThat;
import static org.junit.jupiter.api.Assertions.assertNotNull;
import static org.junit.jupiter.api.Assertions.assertThrows;
import static org.mockito.Mockito.mock;

import com.google.genai.Client;
import org.junit.jupiter.api.*;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;

class BuilderModelNameTest {

	@Test
	@Tag("valid")
	void modelNameWithValidString() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String validModelName = "gemini-2.0-flash";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(validModelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(validModelName);
	}

	@Test
	@Tag("invalid")
	void modelNameWithNullValue() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(null);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		NullPointerException exception = assertThrows(NullPointerException.class, () -> {
			builder.apiKey("test-api-key").build();
		});
		assertThat(exception.getMessage()).contains("modelName must be set");
	}

	@Test
	@Tag("boundary")
	void modelNameWithEmptyString() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String emptyString = "";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(emptyString);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(emptyString);
	}

	@Test
	@Tag("boundary")
	void modelNameWithWhitespaceOnly() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String whitespaceString = "   ";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(whitespaceString);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(whitespaceString);
	}

	@Test
	@Tag("valid")
	void modelNameSetMultipleTimes() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String firstModelName = "gemini-1.0";
		String secondModelName = "gemini-2.0-flash";
		String thirdModelName = "gemini-pro";
		// Act
		builder.modelName(firstModelName);
		builder.modelName(secondModelName);
		Gemini.Builder returnedBuilder = builder.modelName(thirdModelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(thirdModelName);
	}

	@Test
	@Tag("valid")
	void modelNameMethodChaining() {
		// Arrange
		String modelName = "gemini-2.0-flash";
		String apiKey = "test-api-key";
		// Act
		Gemini gemini = Gemini.builder().modelName(modelName).apiKey(apiKey).build();
		// Assert
		assertNotNull(gemini);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("valid")
	void modelNameWithSpecialCharacters() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String specialCharModelName = "gemini-2.0-flash-001";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(specialCharModelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(specialCharModelName);
	}

	@Test
	@Tag("boundary")
	void modelNameWithVeryLongString() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String longModelName = "a".repeat(1000);
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(longModelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(longModelName);
		assertThat(gemini.model().length()).isEqualTo(1000);
	}

	@Test
	@Tag("valid")
	void builderReusabilityAfterModelName() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String firstModelName = "gemini-1.0";
		String secondModelName = "gemini-2.0-flash";
		String apiKey = "test-api-key";
		// Act
		builder.modelName(firstModelName).apiKey(apiKey);
		Gemini firstGemini = builder.build();

		builder.modelName(secondModelName);
		Gemini secondGemini = builder.build();
		// Assert
		assertNotNull(firstGemini);
		assertNotNull(secondGemini);
		assertThat(firstGemini.model()).isEqualTo(firstModelName);
		assertThat(secondGemini.model()).isEqualTo(secondModelName);
	}

	@Test
	@Tag("valid")
	void modelNameWithUnicodeCharacters() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String unicodeModelName = "gemini-模型-2.0";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(unicodeModelName);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.apiKey("test-api-key").build();
		assertThat(gemini.model()).isEqualTo(unicodeModelName);
	}

	@Test
	@Tag("integration")
	void modelNameWithExplicitApiClient() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		Client mockClient = mock(Client.class);
		String modelName = "gemini-2.0-flash";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(modelName).apiClient(mockClient);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.build();
		assertNotNull(gemini);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("integration")
	void modelNameWithApiKey() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String modelName = "gemini-2.0-flash";
		String apiKey = "test-api-key-12345";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(modelName).apiKey(apiKey);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.build();
		assertNotNull(gemini);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("integration")
	void modelNameWithVertexCredentials() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String modelName = "gemini-2.0-flash";
		// NOTE: VertexCredentials.Builder does not have project() and location() methods.
		// Based on the entity information, these are Optional<String> fields accessed via
		// getters.
		// The builder likely uses different method names or the business logic needs to
		// be reviewed
		// to understand how to properly construct VertexCredentials with project and
		// location.
		// For now, creating a basic VertexCredentials instance without project/location.
		VertexCredentials vertexCredentials = VertexCredentials.builder().build();
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(modelName).vertexCredentials(vertexCredentials);
		// Assert
		assertThat(returnedBuilder).isSameInstanceAs(builder);
		Gemini gemini = builder.build();
		assertNotNull(gemini);
		assertThat(gemini.model()).isEqualTo(modelName);
	}

	@Test
	@Tag("valid")
	void modelNameReturnsBuilderInstance() {
		// Arrange
		Gemini.Builder builder = Gemini.builder();
		String modelName = "gemini-2.0-flash";
		// Act
		Gemini.Builder returnedBuilder = builder.modelName(modelName);
		// Assert
		assertThat(returnedBuilder == builder).isTrue();
		assertThat(returnedBuilder).isSameInstanceAs(builder);
	}

}
